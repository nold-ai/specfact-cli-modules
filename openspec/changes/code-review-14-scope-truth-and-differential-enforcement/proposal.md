# Change: Make Code Review Scope and Differential Enforcement Truthful

## Why

`specfact code review run --scope changed` currently means worktree changes from `git diff HEAD`, not the committed pull-request delta. On a clean PR checkout it can select no committed files. Git resolution failure becomes an empty changed-line map, allowing blocking findings to be treated as legacy and producing a green exit.

The report also lacks an honest unknown/not-applicable state, conflates autofix availability with resolution, and determines introduction primarily through changed-line intersection. These are evidence-boundary defects that must be fixed before adding more review rules.

## What Changes

- Define explicit scope sources: `worktree`, `index`, `range`, `full`, or positional files for explicitly labelled non-PR inspection. Keep `changed` only as a deprecated alias for `worktree`.
- Require full base/head refs for range scope, enumerate all best merge bases, proceed only when exactly one commit exists, derive the PR delta from that unique merge base, and reject any positional-file invocation that claims PR-range assurance. Zero or multiple best merge bases is `UNKNOWN`; CR14 does not invent a virtual-base algorithm.
- Capture one stable immutable Git index snapshot, derive both its complete HEAD-plus-index tree and its full stage/flag metadata from that same snapshot, derive ordinary staged paths/statuses by diffing HEAD against the captured tree, and reject metadata-only unsafe states such as intent-to-add or unmerged entries before applicability. Never reread the live index after capture; materialize the captured tree for index analysis, and materialize range analysis from the resolved merge-base/head commit trees in isolated roots. Any index-snapshot/tree/metadata/diff/object mismatch is `UNKNOWN` rather than an incomplete selection. Under signed `tracked-regular-governed-input-v1`, bind Git object type and mode for every selected/eligible/invoked Python, test, test-support, policy, and configuration input; accept only tracked blob modes `100644`/`100755`; open every materialized path through descriptor-relative no-follow traversal; and require the same opened regular descriptor to hash to the committed blob. A `.py`/policy mode change to symlink, submodule, missing, or other unsafe type is governed impact and UNKNOWN before applicability or analysis. Bind accepted roots with content manifests, and run every analyzer in a fail-closed OS filesystem sandbox rooted in a digest-pinned analyzer runtime capsule and launched only by a signed static Linux x86_64 Bubblewrap ELF whose `PT_INTERP` and `DT_NEEDED` sets are empty, exposing only its active read-only snapshot, exact read-only config inputs, sealed capsule rootfs, and process-private writable output/temp roots. Deny access to caller, opposite-snapshot, context, policy-source, and other-analyzer evidence roots; reject snapshot assurance when the required sandbox capability is unavailable. Also reject mutation-capable options for snapshot scopes and `focus=simplify` for range assurance because its finding filter is intentionally incomplete.
- Emit immutable scope evidence and fail closed as `UNKNOWN` when Git scope cannot be resolved. Treat the closed `governed-policy-paths-v1` analyzer/test/coverage-policy set plus the authenticated project-runtime descriptor's target-tip `source_lock_paths[]`—including the optional target `resources/semgrep-rules/ai-bloat.yaml` pack—as governed range impact alongside Python; a candidate policy diff is never NOT_APPLICABLE and remains UNKNOWN until a separately trusted policy-promotion contract exists.
- Reserve producer `assurance_kind=range_candidate` for a complete governed merge-base-to-head Python-and-policy selection with matching claimed context, and effective `pr_range` for a separate protected consumer verification envelope over that immutable report. This modules change implements and tests only the producer/report side plus the signed static consumer contract; workflow-native recomputation and envelope emission are owned and proved by the separate core adoption change. Changed tests are always included, and path/facet/test/execution-level narrowing options are rejected before analysis. A complete local range without context remains `range_preview`; partial review remains worktree or explicit-files.
- Analyze the resolved merge-base and head source trees with the same pinned analyzer/config identities and classify findings as introduced, fixed, unchanged, or unknown using signed `finding-multiset-v1`: line-independent identity buckets bind analyzer, rule/kind, rename-normalized repository path, analyzer-emitted symbol, and a precisely normalized message; raw positions remain outside identity, but signed `occurrence-continuity-v1` recomputes an exact source anchor from the validated diagnostic span plus its immediately preceding/following physical lines and BOF/EOF markers; every source finding first receives canonical `source-span-v1` evidence (exact adapter coordinates when available, otherwise a deterministic whole-physical-line fallback); only an anchor with one distinct physical location per snapshot may be considered; signed `source-line-correspondence-v1` then proves continuity only when forcing that exact base/head anchor preserves the global minimum exact-line edit cost and forbidding the anchor raises it, with no move operation; duplicate analyzer emissions at that proven location retain multiplicity; exact bucket multiplicity is preserved; same-severity/blocking continuous occurrences pair deterministically; incompatible continuous outcomes are unknown; an identity-equal head occurrence at a proven different/non-corresponding anchor is introduced, alternate-optimal mappings, missing/invalid spans, or correspondence resource bounds are unknown, other unmatched head surplus is introduced, and unmatched base occurrence is fixed subject to suppression/rename overrides. A matched occurrence is unchanged only when normalized severity and blocking inputs also match—any unclassified severity transition is unknown. Signed `canonical-exact-rename-v1` ignores ambient Git rename settings: it derives raw added/deleted paths directly from the two captured trees, pairs only a unique deleted/added regular blob with the identical object ID, treats multiple same-blob candidates as ambiguous `UNKNOWN`, and binds the complete candidate/pair/ambiguity digest for independent consumer recomputation. Under conservative `rename-fix-policy-v1`, any base finding missing across such an exact rename is unknown and can never be claimed fixed; a missing base finding on an unpaired deleted path is also unknown whenever any unpaired governed addition exists, so a modified move cannot become a delete/add `fixed` claim. This change defines no heuristic similarity threshold or semantic-subject inference; normalize a head file anchor only through the canonical exact-rename map before comparison, and never use the supplied base-ref tip as the source-code differential baseline. Bind the resolved base-ref tip to an authenticated expected target commit/tree that the consumer independently verifies, freeze that current target-tip policy bundle, and select only contract-named sealed defaults from the signed installed module when an adapter's governed target configuration is absent. Pass the resulting safe per-snapshot config projections explicitly to both source snapshots so candidate head configuration cannot suppress its own findings, relative paths cannot escape into policy/worktree content, consumer repositories do not need to vendor module defaults, and already-promoted target policy cannot be ignored. Candidate policy/config diffs are separately selected and digested as governed impact, but stay shadow-only and cannot authorize themselves. `pr-range-v1` does not attempt Python closure for analyzer extensions: non-empty Pylint `init-hook`, every non-empty alias of `extension-pkg-allow-list`/`extension-pkg-whitelist`, repository/unapproved Pylint `load-plugins`, and Coverage `[run] plugins` outside the signed profile manifest are unsupported UNKNOWN before applicability/launch. Any future accepted plugin must be an exact signed-toolchain profile member shared by both sides; the initial Pylint and Coverage plugin manifests are empty. Targeted pytest/coverage receives only explicit sealed target-tip policy projections—or their generated pinned defaults—with candidate auto-discovery and coverage environment overrides disabled. Signed `pytest-selection-controls-disabled-v1` parses both the complete effective target `addopts` token stream and the complete effective configuration field/value map with the exact pinned pytest 9.0.3, controller-owned pytest-cov, and admitted `pytest-plugin-capabilities-v1` option/`addini` catalogs before collection. It rejects positional selectors and every collection/execution narrowing or override—including `-k`, `-m`, `--ignore`, `--ignore-glob`, `--deselect`, last-failed/cache selectors, `-x`/`--maxfail`, stepwise controls, collect-only, root/config/plugin overrides, and user pytest-cov source/disable/threshold/config controls—as `pytest_selection_policy_unsupported` UNKNOWN. Only catalogued non-selecting options may survive projection; controller-owned collection roots, JUnit, coverage, threshold, and output argv are appended afterward. Declarative sealed `testpaths`, `python_files`, `python_classes`, and `python_functions` remain suite identity. Every other effective field is classified as non-selecting, read/write projection, or unsupported selection/discovery/plugin/coverage control; non-empty `norecursedirs` is explicitly unsupported. Catalog/help/parser/`addini`/plugin drift or an unclassified token/field is UNKNOWN. Automatic plugin loading remains disabled. Target-tip external pytest plugins are admitted only when the authenticated project-runtime descriptor binds their exact distribution/version/payload/`pytest11` entry point plus parser and hook-capability manifests; the controller enables those exact modules explicitly and requires the observed registry/catalog equality. The initial signed hook-disposition catalog rejects every repository or admitted-plugin hook capable of changing discovery, collection cardinality, selector identity, or deselection. A controller observer records registered plugin/hook origins and pre/post collection registries; signed `pytest-outcome-observer-v1` also records every `pytest_runtest_logreport` node ID, phase, outcome, and `wasxfail` value into a process-private artifact that is sealed after exit and reconciled with collection, JUnit, process exit, and counts, so non-strict XPASS cannot appear as PASS; an unapproved plugin, repository parser extension, collection-shaping hook, registry drift, or removed raw-collected item is UNKNOWN. Signed `pytest-item-controls-disabled-v1` also observes the complete pinned Python collector decision graph before built-in filtering. Its closed `pytest-builtin-collector-decision-v1` catalog enumerates every pinned built-in collector activated for governed Python inputs. The initial catalog has two explicit members: native `_pytest.python` descriptor-safe raw module/class namespace plus MRO manifests, reconciliation against pytest's observed `getattr(obj, "__dict__")` traversal, module/class type and metaclass namespace-control identities, duplicate/imported-object ownership, name/explicit-nose eligibility, class-versus-function dispatch, static/class-method unwrapping, callable/real-function checks, fixture-marker exclusion, abstract-class exclusion, raw/final `__test__`, and `Class.collect` `hasinit`/`hasnew`; plus `_pytest.unittest` TestCase recognition/abstractness, class `__test__`, a raw name-prefixed callable manifest derived descriptor-safely from the class MRO before candidate metaclass filtering, exact `unittest.TestLoader` control identity and `getTestCaseNames` inventory, per-method `__test__`, and `runTest`/Twisted fallback. Initial `unittest-loader-controls-v1` requires the pinned prefix/pattern/sort controls and rejects candidate-modified loader state or a metaclass whose `__dir__`, `__getattribute__`, or `__getattr__` differs from the sealed default; raw-candidate/loader-inventory mismatch is UNKNOWN. The observer records every branch input, outcome, and downstream result at `pytest_collectstart` plus a try-first `pytest_pycollect_makeitem` wrapper. Initial `pytest-native-namespace-controls-v1` obtains raw module dictionaries through sealed `types.ModuleType` access and raw class/MRO mapping proxies through sealed `type` access before candidate overrides; it rejects custom module/class/metaclass `__getattribute__`, `__getattr__`, `__dir__`, or `__dict__` behavior that changes pytest's visible candidates. Any raw eligible callable missing from the observed namespace/makeitem stream is UNKNOWN. The catalog classifies every branch as `intentional_non_candidate`, `collectable`, or `unsupported_candidate_control`; an unclassified/drifted branch is UNKNOWN. A native class/callable or unittest class/method made eligible by its sealed catalog member that candidate-controlled runtime state then suppresses before `pytest_itemcollected`—including false class/method `__test__`, fixture marking, abstractness, non-real callable shape, import-ownership manipulation, custom `__init__`, or custom `__new__`—yields `pytest_item_control_unsupported` UNKNOWN. If any activated built-in collector lacks an exact catalog member/observer or its activation/implementation drifts, targeted pytest is UNKNOWN before execution. The sealed observer is the sole non-mutating exception to the collection-hook prohibition. Fixture/observer plugins without a forbidden hook remain supported. Independently, every path classified as `test_candidate` in either immutable snapshot—not only a changed path—must contribute at least one collected selector on that side; otherwise role/collection reconciliation is UNKNOWN. Initial strict range assurance uses signed `complete-pytest-suite-v1`: whenever governed runtime-measurable production `.py`, Python test, or test-support change activates the member, collect the complete selector inventory recognized by that sealed pytest policy independently at merge base and head, execute the complete applicable inventories, and reconcile removal/addition before PASS. Signed `pytest-input-role-v1` classifies each governed test-side `.py` before collection using only its immutable path plus the identical authorized target-tip pytest 9.0.3 `testpaths`/root-default and effective `python_files` policy under exact pinned `_pytest.pathlib.fnmatch_ex`: on the initial Linux profile a pattern without `/` matches the basename, while a pattern with `/` matches the full materialized path and a relative pattern is prefixed with `*/`; matching uses Python `fnmatch.fnmatch`, including `**` under that whole-string behavior. `conftest.py` and non-matching helpers below a collection root are support; an exact-matching module path is a test candidate; a governed test-facet path outside collection roots is an uncollectable test candidate, never support. File contents, `__test__`, marks, AST, and collection results cannot change the role. Any test candidate that contributes no selector on an extant side is `uncollected_test_candidate` UNKNOWN; a changed candidate or rename downgrade retains the more specific `uncollected_changed_test` reason. It defines no source-to-test mapping, related-test inference, or intent-completeness claim; optimization requires a later signed profile. Every pinned-schema Coverage write destination is rewritten into the process-private output root; the read-only snapshot and configuration mounts are never Coverage data/report targets. Pytest policy selection follows the exact pinned pytest 9.0.3 locator across its complete supported source/section set: present `pytest.toml`/`.pytest.toml` and `pytest.ini`/`.pytest.ini` are active even when empty, table-bearing `pyproject.toml`/`tox.ini`/`setup.cfg` follow in order, and a bare `pyproject.toml` is the final fallback only when no earlier source matches. The pinned path schema classifies every path-bearing field/argv token as read/source or writable output: read paths are rebased onto the corresponding immutable snapshot, while `cache_dir`, `log_file`, `--basetemp`, `--junitxml`/`--junit-xml`, `--log-file`, and every other pinned writable destination are rewritten into deterministic process-private cache/temp/output paths. The controller passes both explicit `-c` and `--rootdir` and rejects unclassified, escaping, colliding, or ineffective writable-path projection. Coverage selection likewise includes every source recognized by pinned Coverage 7.15.4, including `.coveragerc.toml`, under its recorded precedence. Strict `coverage-exclusions-disabled-v1` explicitly clears Coverage's default exclusion/partial-branch regex sets and rejects any non-empty target `[report] exclude_lines`, `exclude_also`, `partial_branches`, or `partial_also` before launch; candidate text cannot exploit a target regex to inflate coverage. Signed `runtime-measurable-production-v1` classifies tracked regular governed production `.py` as runtime-measurable and governed `.pyi` stubs as static-analysis-only. Before threshold evaluation, the normalized coverage file manifest must contain every runtime-measurable production `.py` path present in the corresponding immutable snapshot exactly once; a target policy `source`/`include`/`omit` choice that leaves such a path unmeasured yields `UNKNOWN`, never partial PASS. Signed `analyzer-input-kinds-v1` records exact eligible kinds per profile member and sub-execution. Governed `.pyi` remains in Ruff, Radon, Semgrep/semgrep-bugs when activated, ai-bloat-ast, ast-clean-code, basedpyright, Pylint, and the contracts `icontract-static-scan` eligible/invoked manifests and in a separate static-only manifest. The `contracts.icontract-static-scan` component preserves the existing contract-rule activation boundary: it is required for governed `.py`/`.pyi` only when signed `icontract-static-activation-v1` reproduces the current `_has_icontract_usage` predicate, while CrossHair 0.0.109 admits only tracked regular governed production `.py`. Activation inputs/outcome are bound and drift is UNKNOWN; this change does not broaden the AST rule to repositories without icontract usage. On a stub-only side `contracts.crosshair` is NOT_APPLICABLE with manifest-proven `no_crosshair_executable_input`; when static activation is true, the parent member derives PASS/FAIL/UNKNOWN from the completed static scan plus that non-applicable CrossHair component and can never become NOT_APPLICABLE. When activation is false, the static component records its separate manifest-proven `no_icontract_usage` NOT_APPLICABLE state; only then may both components make the parent NOT_APPLICABLE. A `.pyi`-only range makes `targeted-pytest-coverage` NOT_APPLICABLE while every exact stub-capable component remains required. Every mandatory adapter SHALL satisfy signed `governed-input-completeness-v1`: bind each side's eligible/invoked input manifests, apply only pinned option-catalog transforms that prevent a governed file from being skipped or wholly silenced, and make catalog/effective-option/manifest mismatch UNKNOWN. The initial profile always passes Ruff `--no-cache` and `--no-force-exclude` and uses a bound generated Ruff projection that clears both pinned per-file diagnostic-suppression maps (`per-file-ignores` and `extend-per-file-ignores`), `per-file-target-version`, and `namespace-packages`, normalizes Ruff `src` to the active snapshot root, while retaining one shared global `target-version` (or its pinned default) and other non-path-scoped authorized rule settings; clears Pylint `ignore`, `ignore-patterns`, `ignore-paths`, and the complete pinned no-member exemption family (`ignored-modules`, `ignored-classes`, `generated-members`, `ignore-none`, `ignore-on-opaque-inference`, mixin-name/check bypasses, `signature-mutators`, and target-added `contextmanager-decorators` aliases), uses a total, no-fallthrough Pylint 4.0.7 option-disposition catalog: finding/category filters are forced maximally inclusive (`confidence=HIGH,CONTROL_FLOW,INFERENCE,INFERENCE_FAILURE,UNDEFINED`, `errors-only=false`), input-source overrides such as `from-stdin` and recursive discovery are forced off, controller output/exit/cache controls are sealed, and only options explicitly classified as trusted rule semantics may retain target values; the generated projection's only source arguments are the exact eligible manifest; sets basedpyright `include` to the sorted exact eligible snapshot-relative file manifest and clears `exclude`/`ignore`; requires the authorized target path-scoped `strict` array to be absent or canonically empty before launch and only then emits empty `strict`; rejects target `executionEnvironments` in the initial profile; preserves global `typeCheckingMode` and diagnostic settings unchanged; and removes every effective `baselineFile` reference in each side projection while retaining the original path-scoped controls only as governed evidence; forces pinned native Semgrep suppression off for both passes with controller-owned `--disable-nosem` and effective-option verification, rejects every Semgrep rule-level `paths.include`/`paths.exclude` target-narrowing control before launch, then reconciles each accepted Semgrep pass's normalized JSON `paths.scanned` exactly against eligible inputs while retaining `paths.skipped`. Pass-level scanned union is not treated as per-rule proof; policy with rule-level narrowing is unsupported UNKNOWN. These controls preserve non-targeting analyzer rule semantics while preventing target policy from removing governed inputs; any newly accepted whole-file exclusion control requires a new signed profile and regression. Semgrep's optional AI-bloat pack is selected from the authorized target tip when present, otherwise from the exact signed module payload, and is sealed into the same explicit bundle for both snapshots; a candidate edit to that pack is governed UNKNOWN. Ruff configuration first enumerates exactly the repository-root `.ruff.toml`, `ruff.toml`, and `pyproject.toml:[tool.ruff]` sources: zero applicable sources uses explicit `--isolated`, exactly one is selected, and multiple applicable sources are `ruff_config_ambiguous` UNKNOWN before applicability/launch. The selected Ruff configuration includes a sealed, manifest-bound target-tip transitive `extend` closure so an extended policy file cannot fall outside governed impact or sandbox inputs. Radon 6.0.1 configuration discovery is disabled rather than trusted: the adapter runs from a controller-owned empty control directory with a private empty home and sanitized `RADONCFG`, receives only absolute manifest-bound snapshot inputs, and passes a canonical full-result option set that forces CC ranks A..F, MI ranks A..C, empty exclude/ignore controls, controller-owned output, and the signed profile's retained non-filtering metric options. Repository-root `radon.cfg`, `pyproject.toml:[tool.radon]`, and `setup.cfg:[radon]` remain governed target-policy evidence but are never auto-discovered by either snapshot. Basedpyright likewise seals its target-tip `extends` graph and `baselineFile` artifact; absence of repository config produces the exact contract-named per-snapshot `basedpyright-default-v1` projection instead of implicit `--project .` discovery.
- Define the closed `pr-range-v1` analyzer profile and report its identity/coverage; incomplete required evidence is `UNKNOWN` when no validated blocker already proves FAIL. The released signed module SHALL provision the exact loader/analyzer versions bound by the profile and implementation checkpoint from its signed analyzer-environment lock—never ambient bare or lower-bound resolution—including explicit `pytest==9.0.3` and `coverage==7.15.4` lock entries. Analyzer-only direct or transitive distributions SHALL NOT be copied into `module-package.yaml:pip_dependencies`; that field remains the host/controller dependency contract and cannot safely carry the isolated analyzer closure. Portable Python distribution identity binds wheel or canonical `.dist-info/RECORD` payloads and normalized entry points, not environment-specific generated launcher bytes. A signed `pr-range-v1-toolchain-lock.json` closes the complete runtime dependency DAG for each initially supported Linux x86_64 CPython 3.11–3.13 environment and binds an immutable digest-qualified HTTPS OCI Distribution locator (registry/repository@manifest digest), config plus ordered layer digests/diff IDs/media types/sizes, rootfs schema, a checkpoint-bound canonical lock projection/digest, and a fixed in-image wheelhouse whose filename/SHA-256 manifest maps every selected direct/transitive analyzer lock member to payload bytes. Every later signed lock resource must be regenerated canonically from and equal the frozen checkpoint projection/digest before package signing. A controller-owned content-addressed cache outside every checkout/capsule/sandbox is the only offline source; every cached or fetched byte is rehashed. A verified complete cache supports network-offline use, while a cache miss follows only an all-HTTPS redirect chain whose every target is signed-allowlisted before credentials are sent; the complete chain/final URL are evidence, and unavailable/mismatched acquisition is `UNKNOWN` with no tag, mutable URL, downgrade, credential-forwarding, host-runtime, or unverified-cache fallback. `toolchain.py` materializes an `analyzer-runtime-capsule-v1` rootfs from that verified OCI payload, installs the exact/hash-checked analyzer lock offline from only its fixed wheelhouse, then seals the final interpreter, stdlib, extension modules, dynamic loader/shared libraries, analyzer payloads, bootstrap, and canonical root-manifest digest. Bubblewrap uses that capsule as its root filesystem with no host runtime mounts. Python analyzers start under the capsule interpreter with `-I -S` through the sealed bootstrap; snapshot roots are passed as validated data and inserted only after interpreter startup, never through startup `PYTHONPATH`, cwd path injection, site processing, or executable `.pth` files. Before adding any snapshot or project-runtime root, signed `capsule-reserved-imports-v1` freezes the exact top-level import prefixes for the capsule interpreter/stdlib/bootstrap, `specfact_code_review` built-ins, and every Python distribution in the analyzer-runtime lock; every prefix includes all descendants. The bootstrap keeps a capsule-only finder ahead of snapshot/project paths for those prefixes and preflights both roots for top-level module, stub, regular-package, or namespace-package collisions. Any collision or reserved module origin outside the capsule is `reserved_import_collision` UNKNOWN before analyzer dispatch. For all non-reserved imports, the active snapshot remains ahead of the project-runtime layer. The SpecFact core/module controller environment is outside the locked set, may contain unrelated core dependencies, and is never used as analyzer import or native-runtime resolution. The mandatory `ai-bloat-ast` and `ast-clean-code` members are not ambient built-ins. Under `verified-installed-module-payload-v1`, `toolchain.py` obtains the already-enabled Code Review module root from loader registration, re-verifies the deterministic full-payload checksum and signature with the protected approved-key identity, enumerates and copies the complete canonical `specfact_code_review` package payload through descriptor-relative no-follow regular-file reads into the capsule, and binds the real marketplace metadata shape—module name/version plus deterministic payload checksum/signature—together with loader/origin/root and approved-key identities, every canonical payload path/type/mode/size/content digest, entry modules, sealed destination and final root-manifest digest. The original downloaded archive is not required and no archive reacquisition is attempted; installed files are a verified copy source only and are never mounted or imported by analyzers. A mode name, unverified host import path, candidate key, missing loader attestation, or missing/extra/drifted payload evidence is UNKNOWN. Direct-only pins, ambient transitive resolution, or host-site leakage cannot activate the profile.
- Provision project and test dependencies separately from the analyzer lock through an authenticated `project-runtime-layer-v1` descriptor carried by the protected target-tip context. The layer SHALL bind the authorized target repository/commit/tree, declared dependency-input paths/blob/content digests, exact resolved distributions/native payloads/DAG/markers/entry points, Python ABI/platform, immutable digest-qualified OCI payload, builder workflow/ref/run/attestation, and canonical root manifest. The same read-only layer is used for merge-base and head and is exposed only to the closed import-capable members (`basedpyright`, `pylint`, `contracts`, and `targeted-pytest-coverage`); snapshot-local project code always comes from the active immutable source tree for non-reserved imports. The layer excludes the reviewed project distribution itself and every reserved analyzer/runner/bootstrap component, so it cannot shadow pytest, pytest-cov, Coverage, pluggy, analyzer loaders, or the sealed bootstrap. The producer verifies structure and bytes but still emits only `range_candidate`; the protected consumer independently verifies target-tip ownership, dependency inputs, builder provenance, artifact digest, and the immutable report binding before `pr_range`. Missing, untrusted, candidate-controlled, mismatched, colliding, host-mounted, or per-side-different project runtime evidence is `UNKNOWN`, never an ambient fallback or NOT_APPLICABLE.
- Detect candidate-added inline analyzer/test/coverage suppression or analyzer-result control directives before finding fingerprint classification using the closed pinned `suppression-directives-v1` registry over Python comment tokens, including Ruff 0.15.12's complete pinned source-suppression grammar: inline/file-level `noqa` forms; case-sensitive `ruff:` `ignore[...]`, `file-ignore[...]`, `disable[...]`, and `enable[...]` forms with the pinned stable/preview selector grammar; and every isort action comment in both accepted prefix families (`isort:` and `ruff: isort:`) for `skip_file`, `on`, `off`, `skip`, and `split`; pinned Pylint `disable-all` and legacy `disable-msg=<rule-list>` spellings in addition to `disable`, `disable-next`, and `skip-file`; every `pyright:` source directive recognized by pinned basedpyright 1.39.10—including line-level `pyright: ignore` and bracketed `pyright: ignore[reportRule,...]`, strict/basic/standard modes, every parser-exposed boolean diagnostic setting such as `analyzeUnannotatedFunctions`, and every diagnostic-level rule/value—without guessing whether its value weakens analysis; and every source directive recognized by the pinned CrossHair parser, including special on/off forms and all `AnalysisOptionSet.directive_fields` assignments. An introduced, changed, or relocated directive is always an open blocking finding in CR14 and cannot make a baseline analyzer finding look fixed: diagnostic-suppression families emit `introduced_inline_suppression`, while all recognized basedpyright source directives and CrossHair analyzer-result controls emit `introduced_analyzer_result_control`. CR14 accepts no suppression-waiver input; authenticated exceptions remain owned by the later `governance-02-exception-management` change. Parse/normalization uncertainty is `UNKNOWN`, and CR14 does not infer arbitrary suppression reach. An unchanged registered directive is not assumed harmless when its governed Python file changes: signed `suppression-affected-edit-v1` emits required `UNKNOWN` for every required or activated analyzer mapped to an unchanged directive in any content-changed file, before aggregate verdict derivation. It binds analyzer/path, base/head blob and directive-manifest digests, and the changed-hunk digest; an identical-blob pure rename is not a content change. CR14 deliberately does not infer directive reach or add analyzer-specific unsuppressed execution; a later signed profile may replace this conservative file quarantine only with authoritative native suppression-disabled evidence. Both policies are data-bound by exactly one signed package resource, `resources/contracts/pr-range-v1-suppression-catalog.json`, whose canonical bytes/SHA-256, schema/catalog/parser versions, analyzer mappings, normalization rules, and policy constants are frozen in the implementation checkpoint and bound by the activated profile, report, package signature, and protected-consumer envelope; controller literals cannot substitute for that resource.
- Separate finding severity, lifecycle status, autofix availability, and blocking policy.
- Use `PASS`, `FAIL`, `UNKNOWN`, and `NOT_APPLICABLE` truthfully with closed aggregate precedence after differential/lifecycle classification: resolved no-impact is NOT_APPLICABLE; otherwise any valid completed blocker that remains open is FAIL; otherwise any required uncertainty is UNKNOWN; otherwise PASS. A baseline-only FAIL paired to a valid head PASS under identical policy remains per-snapshot evidence but is fixed/non-blocking and excluded from aggregate blockers. Mixed remaining FAIL/UNKNOWN retains every unknown member fact while aggregate remains FAIL. The schema reserves a nullable waiver reference, but CR14 has no waiver ingestion/verification path, always emits it unset for newly derived findings, and never changes blocking from it; signed exception behavior remains owned by `governance-02-exception-management`.
- Migrate the first-party review ledger to consume and persist schema 1.6 `assurance_status`; UNKNOWN and NOT_APPLICABLE are audit records but never passing streaks or reward events.
- Migrate every canonical merge/PR-quality instruction surface to protected-CI range/base/head/context/full usage; label a complete local range without authenticated event context `range_preview`, and retain worktree/positional commands only for local staged or simplification workflows that do not claim PR assurance.
- Publish a signed schema 1.6 consumer compatibility matrix covering all authoritative statuses, post-differential mixed FAIL/UNKNOWN precedence, legacy projections, exit behavior, accepted/rejected envelope fixtures, and the repository's exact schema-less `ledger update` input `{"run_id":"review-run-001","timestamp":"2026-03-16T00:00:00Z","score":85,"findings":[],"summary":"ok"}` plus its legacy normalization before any core consumer migration. The matrix is a static interoperability resource, not a module-side workflow verifier.

## Capabilities

### Modified Capabilities

- `review-run-command`: Resolve explicit snapshot scopes and perform deterministic base/head differential review.
- `review-finding-model`: Separate lifecycle, remediation availability, differential disposition, and policy.
- `review-cli-contracts`: Prove range selection, unknown handling, differential classifications, and analyzer coverage.

## Impact

- Planning artifacts only. No package source, tests, manifests, registry, version, signatures, prompts, or generated docs change in this commit.
- Later implementation changes the public review CLI and JSON schema additively, with a deprecation path for `--scope changed`.
- CI integrations that claim pull-request assurance must migrate to `--scope range --base-ref <base> --head-ref <head>` and require producer `scope_evidence.assurance_kind=range_candidate` plus a trusted envelope with `effective_assurance_kind=pr_range`; positional files remain valid only as `explicit_files` evidence and cannot satisfy that policy.
- Rollback: retain the legacy alias and dual-write old/new report fields during one compatibility release.

### Report compatibility and release gate

The additive authoritative field SHALL be `assurance_status` in `ReviewReport` schema `1.6`. For one compatibility release, producers SHALL also write the legacy `overall_verdict`, `ci_exit_code`, and `enforcement_mode` fields with this closed projection:

| `assurance_status` | Legacy `overall_verdict` | Non-shadow `ci_exit_code` | Shadow `ci_exit_code` |
|---|---|---:|---:|
| `PASS` | `PASS`, or `PASS_WITH_ADVISORY` when non-blocking findings remain | 0 | 0 |
| `FAIL` | `FAIL` | 1 | 0 |
| `UNKNOWN` | `FAIL` as the conservative legacy projection | 1 | 0 |
| `NOT_APPLICABLE` | `PASS_WITH_ADVISORY`, with an explicit no-governed-impact summary | 0 | 0 |

`enforcement_mode` is copied from the normalized enforcement-policy request, not from scope: `enforce` normalizes to legacy `full`; `full`, `changed`, and `shadow` serialize unchanged. `changed` remains valid for one release only with the deprecated changed/worktree path. Because the current CLI default is `changed`, an omitted enforcement option is resolved with scope awareness: range defaults to strict `full`, while the deprecated changed/worktree path retains `changed`. Explicit range plus `changed` is invalid; a non-shadow range run writes `full`, a shadow range run writes `shadow`, and `range` is never an `enforcement_mode` value. Scope truth remains in `scope_evidence.requested_scope`, `effective_scope`, and `assurance_kind`.

The new field is authoritative; dual-writing MUST NOT rewrite `UNKNOWN` or `NOT_APPLICABLE` to `PASS`. The modules repository's own staged `scripts/pre_commit_code_review.py` consumer SHALL use schema `1.6` `assurance_status` and `ci_exit_code` as authoritative, so changed-line fallback cannot turn UNKNOWN into exit zero; only reports older than `1.6` retain the existing staged-line compatibility calculation. Schema 1.6 first-party consumers, including `code review ledger update`, SHALL read `assurance_status` rather than the legacy projection. The ledger persists UNKNOWN and NOT_APPLICABLE explicitly, retains the complete canonical schema 1.6 report as `report_json` plus `report_digest` in local and Supabase run records, applies zero reward, and leaves both pass and block streak counters unchanged. Reports older than 1.6 retain their prior PASS/PASS_WITH_ADVISORY/FAIL ledger behavior. The exact schema-less ledger fixture normalizes under the existing legacy reader to `schema_version=1.0`, `reward_delta=5`, `overall_verdict=PASS`, and `ci_exit_code=0` from its score 85 and empty findings, while preserving run/timestamp/score/findings/summary; it does not gain authoritative `assurance_status`. Any schema older than `1.6` may be read only as legacy `PASS` (from `PASS` or `PASS_WITH_ADVISORY`) or `FAIL`; it can never be upgraded by inference to `UNKNOWN` or `NOT_APPLICABLE`. Schema `1.6+` with a missing or invalid `assurance_status` is invalid/unknown and cannot pass.

The first signed C14 module release SHALL advertise only the exact tested `core_compatibility: '===0.56.0'` after a pre-release runtime smoke uses the immutable core `0.56.0` tag plus recorded full commit/tree identity in a fresh environment, installs the candidate module package, loads it through that core, and validates the complete schema `1.6` consumer matrix. The strict arbitrary-equality specifier SHALL reject PEP 440 local or alternate version strings such as `0.56.0+vendor`. It SHALL NOT publish `==0.56.0`, `>=0.56.0,<1.0.0`, a wildcard, or any other compatibility expression that admits an untested core identity. If that immutable core identity is unavailable or any install/load/matrix case fails, release and compatibility metadata remain blocked. This exact-version smoke proves only the advertised module-load/schema interoperability; actual protected core PR-CI migration is explicitly downstream of the signed release. The behavior-ready implementation PR regenerates command references and public docs; the canonical post-merge publish workflow alone generates and signs registry/archive/checksum/sidecar artifacts. A separate accepted core adoption change then pins those final signed identities, authors and proves the workflow-native consumer tests, implements the protected consumer/envelope emitter, and requires the verified `pr_range` envelope; it never adopts a feature-branch package. After that core adoption is released, a later module metadata release MAY replace the exact specifier only with that newly released exact core version after the same immutable tag/commit/tree smoke passes; no future or range-wide compatibility is inferred. Module completion SHALL NOT claim those downstream runtime tests as module evidence.

## Explicit Non-Goals

- Add or change individual Ruff, Radon, Semgrep, basedpyright, pylint, contract, or AI-bloat detectors.
- Fuse Requirements verdicts into Code Review.
- Perform LLM review or claim semantic completeness.
- Infer historical pytest dependency closure.
- Infer a reviewed repository's project/test dependency closure from imports, plugins, or ambient host state; CR14 consumes only an explicit authenticated target-tip project-runtime attestation.
- Implement analyzer-policy promotion or semantic monotonicity comparison; C14 detects the closed candidate policy/config diff and reports UNKNOWN, while a separate accepted trust-epoch/policy-promotion contract is required to authorize it.
- Implement the global governance evidence graph.
- Claim hostile-candidate resistance for pytest, CrossHair, or any analyzer that executes candidate Python. CR14 isolates other evidence roots but does not make candidate-authored runtime output an independent security attestation.

## Source Tracking

<!-- source_repo: nold-ai/specfact-cli-modules -->
- **Origin**: dogfooding analysis of core PRs #665–#671
- **Flagship track**: deterministic code review and AI-bloat defense foundation
- **Planning date**: 2026-08-13

