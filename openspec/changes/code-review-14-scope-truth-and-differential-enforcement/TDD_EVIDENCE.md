# TDD Evidence

## Planning checkpoint — 2026-08-13

No behavior changed and no tests ran for this OpenSpec-only planning commit. Core PRs #665–#671 are future adjudicated benchmark cases, not evidence that this change is implemented.

## Dependency amendment — 2026-08-19

The approved exact-core target changed from the unavailable planned 0.56.0 identity to existing immutable lightweight tag `v0.55.1`, full commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, full tree `47984be5434d7ae65ed6908bf525a32053290337`, and strict `===0.55.1`. Live tag lookup and local commit/tree inspection established those identities. No test or source evidence is claimed by this planning amendment.

## Core-interface and capsule-composition amendment — 2026-08-21

Fresh core refs established `origin/dev@e3a20f20df440dff49f8c6d1f73375451bea1d8c`, `origin/main@b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and immutable `v0.55.1` tree `47984be5434d7ae65ed6908bf525a32053290337`. A path-limited byte comparison of `module_discovery.py`, `module_installer.py`, and `module_package.py` found no difference between `origin/dev` and `v0.55.1`. The tag already contains `DiscoveredModule`, canonical user/marketplace roots, `.specfact-registry-id`, `.specfact-install-verified-checksum`, package-integrity parsing, bundled-key lookup, and `verify_module_artifact`. The earlier contract incorrectly assumed those records arrived as one aggregate loader DTO; no such later unpublished interface exists.

The approved amendment replaces that assumption with derived `core-v0.55.1-installed-module-handoff-v1`, separates workflow-attested `verified-candidate-module-payload-v1` from released official provenance, and distinguishes the immutable OCI base-root identity from generated `sealed-bootstrap-v2` plus `capsule-composite-identity-v1`. The earlier task 3.12e/3.12f green evidence proves descriptor-safe copying only and is not reused as evidence for this amended handoff/bootstrap contract. New named tests, collected selectors, checkpoint projection/digest, actual failing evidence, implementation, and passing evidence are required before either task returns to green.

## Implementation readiness — 2026-08-19

### GitHub governance observation

- Observation: `2026-08-19T22:20:58+02:00` (`2026-08-19T20:20:58Z`).
- Hierarchy cache: `.specfact/backlog/github_hierarchy_cache.md`; state `generated_at=2026-08-19T20:10:14Z`, fingerprint `25ab587f47f83db25875a1b67cbc5094cd6a375dc4c3212bec8992e232240748`; the mandated refresh reported the unchanged 24-issue hierarchy, followed by live issue readback.
- Implementation issue: [nold-ai/specfact-cli-modules#416](https://github.com/nold-ai/specfact-cli-modules/issues/416), `OPEN`, User Story, parent Feature [#163](https://github.com/nold-ai/specfact-cli-modules/issues/163), under Epic [#162](https://github.com/nold-ai/specfact-cli-modules/issues/162).
- Project: `SpecFact CLI`, status `Todo`; assignee `djm81`; labels `change-proposal`, `codebase`, `enhancement`, `openspec`.
- Native dependencies: `blockedBy=[]`; `blocking=[]`. The accepted planning PR #413 and synchronization PR #415 are complete. No core adoption issue exists yet by design; it is downstream of the signed module handoff. Requirements R07/R08 are independent.
- Metadata decision: `READY`.

### Immutable repository and benchmark refs

- Synchronized modules base: `origin/dev@c3eda08c732267dc3614130f5f36bcd473182d0b`; implementation worktree HEAD is the same commit before readiness edits.
- Core: `origin/dev@e3a20f20df440dff49f8c6d1f73375451bea1d8c`; `origin/main@b1e517e60e669eaba15a18ecfa83ef5a9df65276`; immutable lightweight tag `v0.55.1` resolves to that main commit and tree `47984be5434d7ae65ed6908bf525a32053290337`.
- Core benchmark PR head identities: #665 `e3a20f20df440dff49f8c6d1f73375451bea1d8c`; #666 `1677f0c3beb32de49d82aa4dcf1bf4fcf06f07f0`; #667 `96aab447e00de4a09c19ed3a36632b9a46f7c222`; #668 `34c22d271e3653cfa7099ed7132d786c49437b21`; #669 `d17e6ba847599a1366436a3d3e993ba819cb0de7`; #670 `338f853ac2fde8299c11a5adb9c33ab883dead42`; #671 `21a97781ac3b467c2f69adc3344916d978d4328d`.
- Merged core reset PR #674 is `e3a20f20df440dff49f8c6d1f73375451bea1d8c` from reviewed head `51d3120170cd8e08e76881023e511868ce08d5b1`. Its accepted boundary keeps generic review-scope production semantics module-owned and makes the protected workflow consumer a separate downstream core adoption.
- Reviewed core caller paths at `origin/dev`: `.github/workflows/requirements-evidence.yml`, `scripts/pre_commit_code_review.py`, `scripts/pre-commit-quality-checks.sh`, `docs/agent-rules/20-repository-context.md`, and `docs/agent-rules/50-quality-gates-and-review.md`. The workflow currently derives positional PR paths, while the pre-commit helper intentionally remains staged explicit-file scope; these are migration consumers, not evidence that C14 already exists.

### Readiness decision

`READY FOR TEST AUTHORING`. `hatch run openspec validate code-review-14-scope-truth-and-differential-enforcement --strict` passed on 2026-08-19 against synchronized `origin/dev@c3eda08c732267dc3614130f5f36bcd473182d0b`. No named C14 tests or production sources have been edited or executed for implementation evidence yet. Production edits remain prohibited until every prescribed test is authored, every exact selector is collected, the implementation mapping and `IMPLEMENTATION_CHECKPOINT.json` are frozen, and actual failing evidence is recorded.

## Amended task 3.12e red gate — 2026-08-21

- Checkpoint parent: commit `87f5db8854c2d9c34265f0d1b5a0fd34ee1f55f3`, tree `ee91201bf31e55f53cf06b41a1def7b629c18a43`; checkpoint commit `659c13e`.
- Exact frozen task selector digest: `sha256:dee179d142ba9e535c86141f636b8d7c109857549e675735d6f2fcc9bbaea063`; selector count `8`.
- Command: `.venv/bin/python` loaded task `3.12e` selectors from committed `IMPLEMENTATION_CHECKPOINT.json` and invoked that unchanged list through `python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings`.
- Outcome: `3 failed`, `5 passed` on Python 3.12.13. The exact missing production boundaries were `derive_core_0_55_1_install_handoff`, `verify_candidate_module_payload`, and `compose_post_base_capsule`; no failure was manufactured by changing assertions or selectors.
- Decision: `RED`. Production edit authorization is `GRANTED` only for the amended task 3.12e handoff, candidate-provenance, bootstrap, and composite-capsule behavior. Earlier loader-DTO evidence remains superseded.

Green command: the identical committed task `3.12e` selector list was loaded from `IMPLEMENTATION_CHECKPOINT.json` and invoked unchanged through pytest.

Green outcome: `8 passed`, `0 failed` on Python 3.12.13. `derive_core_0_55_1_install_handoff` consumes the real core object/marker/integrity/key/verifier surfaces; `verify_candidate_module_payload` binds immutable pre-release evidence while granting neither official-install nor `pr_range` authority; and `compose_post_base_capsule` copies the verified payload, generates `sealed-bootstrap-v2`, preserves the immutable base-root identity, and emits `capsule-composite-identity-v1`. After formatting, the identical set remained `8 passed`; repository-wide type/lint reported `0 errors`, `0 warnings`, `0 notes`, Ruff passed, and Pylint scored `10.00/10`. Task decision: `GREEN`.

## Frozen implementation red gate — 2026-08-20

- Committed checkpoint: implementation branch commit `67f1aa0a879931c3098792747b12c8c115750e73`; checkpoint parent commit `dd7490f5a1829dc9fb5b411a33eaa68e97e9c947`, tree `20e6827ae49b62a7524587985cb91df544e20dd3`.
- Verification command: `.venv/bin/python /private/tmp/verify_c14_checkpoint.py`. It verified the committed checkpoint against the frozen parent inputs, the non-empty and pairwise-disjoint 43-item assignment, exact global union, canonical mapping, immutable core identity, and all checkpoint digests.
- Red command: `.venv/bin/python /private/tmp/run_c14_red.py`. The runner loaded the checkpoint's sorted 366 `pytest_args`, invoked that complete selector set without substitution, and rejected collection drift. The per-selector exact argv is therefore bound by the committed selector digest rather than duplicated in this evidence file.
- Checkpoint file digest: `sha256:fb51280c67ea97962fce0af31227335bc00e0128cb4c30706294df3cf09ec7a6`.
- Global selector and observed collection digest: `sha256:7e515d42b9b267eb503bdb98e0d73feae0293e13660ad88f826f055cbd907b68`; collected count `366`.
- Implementation selector-map digest: `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.
- Pytest raw exit code: `1`. Exact outcomes: `365 failed`, `1 passed`; all `43/43` implementation tasks retained at least one independently failing frozen selector.
- Canonical per-selector outcomes digest: `sha256:99e9392aa548da1aaf2a2c5feeb4cf2a2727e1a134bf7171f63d888c7d53be4b`; canonical per-task outcomes digest: `sha256:aaa7c5a1851afdba13e3578e94805933235818341ff5c88b2c526242c6346391`.
- Representative red causes were the deliberately absent `scope`, `differential`, `sandbox`, and `toolchain` modules and the unimplemented schema/profile behavior in existing allowlisted sources. The single passing selector does not invalidate the gate because its mapped task also contains failing selectors and no Section 3 item is green.

Decision: `RED GATE SATISFIED`. Production edits may begin only in the frozen Section 3 order and allowlist, with the checkpoint reverified and each item's identical mapped selector set taken from red to green.

## Section 3 item evidence

### Task 3.1 — request and basic immutable range scope

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:616474d70a4631a71216b163368719310a7d774cb0f11d9cbf59904fa1a0d46f`.

Frozen selectors:

- `tests/unit/specfact_code_review/run/test_scope.py::test_pr_assurance_rejects_positional_file_downgrade`
- `tests/unit/specfact_code_review/run/test_scope.py::test_range_scope_includes_changed_tests_by_default`
- `tests/unit/specfact_code_review/run/test_scope.py::test_range_scope_includes_committed_files_on_clean_checkout`
- `tests/unit/specfact_code_review/run/test_scope.py::test_range_scope_uses_merge_base_not_head_worktree`

Red outcome from the complete checkpoint run: `4 failed`, `0 passed`; the `specfact_code_review.run.scope` module was deliberately absent. Production edit authorization: `GRANTED` for task 3.1 only.

Green command: `.venv/bin/pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the four frozen task-3.1 selectors above>`.

Green outcome: `4 passed`, `0 failed` on Python 3.12.13. `hatch run format`, `hatch run lint`, the six focused legacy command-discovery compatibility cases, and `git diff --check` also passed. Git discovery now resides in `run/scope.py`; the legacy command surface delegates its local worktree/full discovery there. Task decision: `GREEN`.

### Task 3.2 — claimed context and unique merge-base identity

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:325cbec569a17d2023a7a6b50eb2aa2cd5944c932f1df6c1278bf7fa46b37b71`.

Frozen selectors:

- `tests/unit/specfact_code_review/run/test_scope.py::test_producer_never_self_asserts_pr_range_from_context_file`
- `tests/unit/specfact_code_review/run/test_scope.py::test_range_candidate_rejects_base_ref_mismatching_claimed_target_tip`
- `tests/unit/specfact_code_review/run/test_scope.py::test_range_multiple_best_merge_bases_is_unknown`
- `tests/unit/specfact_code_review/run/test_scope.py::test_range_without_context_is_preview`

Red outcome from the complete checkpoint run: `4 failed`, `0 passed`; claimed-context parsing/identity validation and canonical merge-base ambiguity evidence were absent. Production edit authorization: `GRANTED` for task 3.2 only.

Green command: `.venv/bin/pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the four frozen task-3.2 selectors above>`.

Green outcome: `4 passed`, `0 failed` on Python 3.12.13. The resolver now freezes bounded regular outside-checkout context bytes, validates claimed provider/repository/event/target/head identity, retains optional project-runtime digests as claimed-only evidence, binds resolved target/head commit/tree identities, enumerates all best merge bases, and hashes the canonical sorted commit/tree candidate set. It emits only `range_candidate` or `range_preview`, never `pr_range`. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.3 — stable complete-index capture

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:863c2fcdd2437f26a1fd5e7f503659df9ddc16295bfb5810a6b3c9e53f678748`.

Frozen selectors:

- `tests/unit/specfact_code_review/run/test_scope.py::test_index_intent_to_add_survives_captured_tree_omission`
- `tests/unit/specfact_code_review/run/test_scope.py::test_index_rejects_symlinked_governed_input`
- `tests/unit/specfact_code_review/run/test_scope.py::test_index_scope_derives_selection_from_captured_tree_during_concurrent_index_mutation`
- `tests/unit/specfact_code_review/run/test_scope.py::test_index_scope_imports_dependency_from_complete_index_tree`
- `tests/unit/specfact_code_review/run/test_scope.py::test_index_scope_reads_staged_blobs_not_unstaged_worktree`

Red outcome from the complete checkpoint run: `5 failed`, `0 passed`; index scope returned the task-3.1 unsupported-scope UNKNOWN placeholder and had no captured-index tree/materialization evidence. Production edit authorization: `GRANTED` for task 3.3 only.

Green command: `.venv/bin/pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the five frozen task-3.3 selectors above>`.

Green outcome: `5 passed`, `0 failed` on Python 3.12.13. Index resolution now performs a bounded no-follow stable capture of the worktree-specific index plus content-addressed split-index dependencies, switches all subsequent index operations to the captured `GIT_INDEX_FILE`, invokes the deterministic post-capture mutation seam, derives stage/flag metadata and the complete tree from that capture, materializes the complete tree outside the checkout, selects only the captured HEAD-to-tree delta, preserves intent-to-add evidence omitted by `write-tree`, and verifies selected regular inputs by descriptor-relative no-follow reads. `index_tree` and `selection_tree` bind the same immutable tree. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.4 — symmetric detached range snapshots and exact renames

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:e0694f69d44250787482a7bd50c0ff151aef842baf0267edd0ebec05d4cf0118`.

Frozen selectors:

- `tests/unit/specfact_code_review/run/test_scope.py::test_materialized_governed_input_uses_nofollow_regular_blob_identity`
- `tests/unit/specfact_code_review/run/test_scope.py::test_range_analysis_uses_materialized_commit_snapshots`
- `tests/unit/specfact_code_review/run/test_scope.py::test_range_mode_change_regular_to_symlink_is_unknown`
- `tests/unit/specfact_code_review/run/test_scope.py::test_range_rejects_symlinked_governed_python_input`

Red outcome from the complete checkpoint run: `4 failed`, `0 passed`; detached snapshots, symmetric tracked-regular manifests, and exact-rename evidence were absent at the checkpoint. Production edit authorization: `GRANTED` for task 3.4 only.

Green command: `.venv/bin/pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the four frozen task-3.4 selectors above>`.

Green outcome: `4 passed`, `0 failed` on Python 3.12.13. Range resolution now materializes independent merge-base/head trees outside the checkout; records complete source-tree manifest digests and raw add/modify/delete statuses; derives unique identical-blob rename, copy, and ambiguity dispositions with a canonical digest; and applies the same object-type/Git-mode/descriptor-relative no-follow regular-input validation to both sides. Regular-to-symlink and symlinked governed inputs are retained as evidence and return `unsafe_governed_input` UNKNOWN. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.5a — authorized target-tip policy bundle

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:cb173f7d7c6eba0482222cdf86142ddf72f4279eb2515d815f23e533006f680b`.

Frozen selectors:

- `tests/unit/specfact_code_review/run/test_scope.py::test_candidate_policy_change_cannot_self_authorize_pr_range`
- `tests/unit/specfact_code_review/run/test_scope.py::test_policy_only_range_is_unknown_not_not_applicable`
- `tests/unit/specfact_code_review/run/test_scope.py::test_pyproject_nonpolicy_change_can_remain_non_governed`

Red outcome from the complete checkpoint run: `3 failed`, `0 passed`; the candidate-versus-target policy manifest and shadow-only candidate-policy decision were absent. Production edit authorization: `GRANTED` for task 3.5a only.

Green command: `.venv/bin/pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the three frozen task-3.5a selectors above>`.

Green outcome: `3 passed`, `0 failed` on Python 3.12.13. The resolver now materializes a separate read-only authorized target-tip policy bundle bound to source commit/tree and path/blob/content/section identities; records candidate policy path/status/base/head/section evidence plus canonical policy and candidate-change digests; treats policy-only candidate changes as shadow-only UNKNOWN; and leaves unrelated `pyproject.toml` metadata non-governed. Candidate policy never upgrades producer assurance. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.5b — sealed Semgrep policy bundle

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:ae74bac4a0287215585bd8e73ffc47b296c9ceb705d88728957a050e0c693653`.

Frozen selector: `tests/unit/specfact_code_review/run/test_scope.py::test_semgrep_ai_bloat_rule_pack_is_governed_and_sealed`.

Red outcome from the complete checkpoint run: `1 failed`, `0 passed`; there was no explicit target-tip/signed-module Semgrep bundle resolver. Production edit authorization: `GRANTED` for task 3.5b only.

Green command: `.venv/bin/pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings tests/unit/specfact_code_review/run/test_scope.py::test_semgrep_ai_bloat_rule_pack_is_governed_and_sealed`.

Green outcome: `1 passed`, `0 failed` on Python 3.12.13. The resolver now selects each clean-code and AI-bloat policy independently from the authorized target root or exact verified-module fallback boundary, reads it through bounded no-follow regular-file capture, materializes one preserved-layout read-only bundle, and binds source kind/path plus content and bundle digests. Missing or unsafe payloads return explicit UNKNOWN evidence. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.5c — pinned pytest and Coverage policy location

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:39a2833a3f81ffeec0d21cc6db06413fbe27e157ab4f8f87503b18d7f9a3b508`; frozen selector set: the 16 checkpoint entries for task 3.5c covering the coverage-only range, all five Coverage source cases, bare-pyproject pytest fallback, empty TOML/INI precedence, and all seven pytest source-precedence cases.

Red outcome from the complete checkpoint run: `16 failed`, `0 passed`; pinned logical pytest/Coverage locator APIs and their selected/ignored-source evidence were absent. Production edit authorization: `GRANTED` for task 3.5c only.

Green command: `.venv/bin/python -c '<load task 3.5c selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `16 passed`, `0 failed` on Python 3.12.13. Pytest selection now binds the exact `pytest.toml`, `.pytest.toml`, `pytest.ini`, `.pytest.ini`, table-bearing `pyproject.toml`, `tox.ini`, `setup.cfg`, then bare-pyproject fallback semantics, including zero-byte primaries and ignored lower sources. Coverage binds exact `.coveragerc`, `.coveragerc.toml`, `setup.cfg`, `tox.ini`, `pyproject.toml` precedence. Both expose pinned loader/source-order/section/manifest identities and fail closed on unsafe, parse, shape, or version drift. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.5d — Ruff source and extend-closure sealing

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:8fba4933d43d12985479d5526e17da3f2216b81b55cc9546ff4a53ce411632f3`; frozen selector set: the nine checkpoint entries for zero/one `.ruff.toml`, `ruff.toml`, or `pyproject.toml:[tool.ruff]`, multiple-source ambiguity, valid transitive `extend`, and escape/missing/cycle rejection.

Red outcome from the complete checkpoint run: `9 failed`, `0 passed`; no explicit Ruff locator or sealed transitive extend graph existed. Production edit authorization: `GRANTED` for task 3.5d only.

Green command: `.venv/bin/python -c '<load task 3.5d selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `9 passed`, `0 failed` on Python 3.12.13. Ruff policy resolution now recognizes only repository-root `.ruff.toml`, `ruff.toml`, or `pyproject.toml:[tool.ruff]`, emits explicit isolated mode when absent, rejects multiple or malformed sources, recursively seals at most 32 relative regular no-symlink `extend` nodes, rejects escape/missing/cycle cases, preserves layout read-only, and binds complete node/edge/content/materialized closure evidence. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.5e — basedpyright primary/reference graph

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:b956447bf0d56880aeff4347d0bbb471a0447aa8a5ec79b4c549357b9ab82c83`; frozen selector: `tests/unit/specfact_code_review/run/test_scope.py::test_basedpyright_referenced_policy_files_are_governed`.

Red outcome from the complete checkpoint run: `1 failed`, `0 passed`; basedpyright primary, `extends`, and `baselineFile` inputs were not resolved or governed. Production edit authorization: `GRANTED` for task 3.5e only.

Green command: `.venv/bin/pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings tests/unit/specfact_code_review/run/test_scope.py::test_basedpyright_referenced_policy_files_are_governed`.

Green outcome: `1 passed`, `0 failed` on Python 3.12.13. Basedpyright policy resolution now accepts exactly one pinned primary (`pyrightconfig.json`, `[tool.pyright]`, or `[tool.basedpyright]`), resolves a bounded relative no-symlink `extends` graph and optional JSON-object `baselineFile` leaves, rejects conflict/escape/cycle/missing/shape/version failures, preserves every referenced payload in a read-only bundle, and binds node/edge/content graph evidence. Absence emits the exact generated-default boundary for later projection. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.5f — pinned Pylint source selection

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:37fc6b99911008aa03463e2fa470a548894cd5ddaa766b154201e9bc23ac9c06`; frozen selector set: the five checkpoint cases for `pylintrc`, `.pylintrc`, `pyproject.toml`, `setup.cfg`, and `tox.ini`.

Red outcome from the complete checkpoint run: `5 failed`, `0 passed`; there was no pinned Pylint source locator/default or explicit selected-source evidence. Production edit authorization: `GRANTED` for task 3.5f only.

Green command: `.venv/bin/python -c '<load task 3.5f selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `5 passed`, `0 failed` on Python 3.12.13. Pylint policy resolution now enumerates the frozen root source set, accepts exactly zero or one regular source, binds the selected payload/loader identity, emits the sealed pinned default when absent, rejects ambiguity/parse/version drift, and exposes the initial total safety projection with closed stdin and extension/plugin rejection for tasks 3.6/3.17. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.6 — per-snapshot configuration projections

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:c2cf791034d0809c4a7fd965d3d7a28c2ac0ef293d6aab0b26c41d809e589537`; frozen selector set: the 20 checkpoint cases covering Ruff force-exclude/per-file/namespace controls, seven Pylint input/no-member controls, and seven basedpyright include/exclude/ignore/baseline/strict/execution-environment controls.

Red outcome from the complete checkpoint run: `20 failed`, `0 passed`; generated per-snapshot projection APIs and total control catalogs were absent. Production edit authorization: `GRANTED` for task 3.6 only.

Green command: `.venv/bin/python -c '<load task 3.6 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `20 passed`, `0 failed` on Python 3.12.13. Ruff projection now materializes a read-only explicit policy with snapshot-root `src`, disabled cache/force-exclude/fix controls, and empty path-scoped suppression/target/namespace maps. Basedpyright projection sorts the exact eligible manifest, clears include/exclude/ignore/strict and every baseline reference, rejects non-empty strict/execution-environment controls, binds the verified project-runtime site-packages location, and launches only through an explicit generated project document. The Pylint projection clears the frozen input/no-member bypass family and seals stdin, confidence, discovery, and result controls. Canonical projection digests and typed generated-projection provenance are exposed. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.7 — snapshot invocation and reserved-import boundary

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:256c40534c573a41095fcbde3ec42cc86b93fcb42e8893093cac60f0d44a154f`.

Frozen selectors:

- `tests/unit/specfact_code_review/run/test_sandbox.py::test_analyzer_subprocesses_use_snapshot_invocation_context`
- `tests/unit/specfact_code_review/run/test_sandbox.py::test_snapshot_cannot_shadow_capsule_reserved_imports[pytest.py]`
- `tests/unit/specfact_code_review/run/test_sandbox.py::test_snapshot_cannot_shadow_capsule_reserved_imports[sitecustomize.py]`
- `tests/unit/specfact_code_review/run/test_sandbox.py::test_snapshot_cannot_shadow_capsule_reserved_imports[specfact_code_review.py]`
- `tests/unit/specfact_code_review/run/test_sandbox.py::test_snapshot_sitecustomize_cannot_run_during_analyzer_startup`

Red outcome from the complete checkpoint run: `5 failed`, `0 passed`; the snapshot invocation context, isolated bootstrap launch, startup-path exclusion, and reserved-import collision preflight module were absent. Production edit authorization: `GRANTED` for task 3.7 only.

Green command: `.venv/bin/python -c '<load task 3.7 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `5 passed`, `0 failed` on Python 3.12.13. The new invocation boundary binds member, snapshot, sealed configuration, capsule, output/temp, project-runtime, network, reserved-prefix, interpreter, and bootstrap inputs into one canonical context digest. Python launch is capsule-interpreter `-I -S` through the sealed bootstrap, with the snapshot excluded from startup `sys.path`; the preflight rejects top-level reserved module, stub, regular-package, namespace-package, and symlink collisions before dispatch. The context accepts the complete signed prefix catalog supplied by the task-3.12 toolchain contract; the test fixture default exercises the three frozen collision vectors. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.8 — Linux Bubblewrap isolation boundary

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:b833efe8d724b1bae8f726676143f608bc425d8259d492bd692667c532da1faa`; frozen selector set: the eight checkpoint entries covering static ELF identity, dynamic-loader rejection, pre-namespace objects, empty capsule root, sealed config mounts, root/capability/network policy, and absence of host-runtime mounts.

Red outcome from the complete checkpoint run: `8 failed`, `0 passed`; the OS-isolation module and Bubblewrap identity/profile validation APIs were absent. Production edit authorization: `GRANTED` for task 3.8 only.

Green command: `.venv/bin/python -c '<load task 3.8 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `8 passed`, `0 failed` on Python 3.12.13. The isolation boundary now accepts only the canonical Linux x86_64 static ELF Bubblewrap descriptor with no interpreter or needed-library records and bound content/descriptor digests; pre-namespace validation admits only the static executable and kernel pseudo-objects. Launch plans use the verified capsule as the empty root, expose no host runtime mounts/capabilities/network, mount every declared policy root read-only, and limit writable mounts to process-private output/temp roots. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.9 — non-adversarial runtime observation boundary

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:dd5a27ee6ad427268f3665500f0195c435e0fe5fe9dd9dd72f806ff3f2005750`; frozen selectors: the three checkpoint entries for cross-root evidence isolation, explicit non-adversarial observation limits, and adversarial runtime policy UNKNOWN.

Red outcome from the complete checkpoint run: `3 failed`, `0 passed`; the runtime-observation limitation statement and adversarial-policy disposition API were absent. Production edit authorization: `GRANTED` for task 3.9 only.

Green command: `.venv/bin/python -c '<load task 3.9 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `3 passed`, `0 failed` on Python 3.12.13. Each runtime observation now states the initial profile's non-adversarial-candidate limitation and mandatory UNKNOWN fallback. A run in which candidate Python executes while hostile behavior is claimed returns UNKNOWN under the bound `non_adversarial_candidate_runtime` assumption. Mount planning keeps the active member's writable roots separate and exposes no other side's evidence root. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.10 — range/index option widening and mutation rejection

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:95961b94cfa5f33ec3fcaa4433bdeffe05ee7a5588763aacbc0e88a4a7b5d2f9`; frozen selector set: the 15 checkpoint cases for index/range fix, preview-fix, mutation, omitted-enforcement, and all range-narrowing controls.

Red outcome from the complete checkpoint run: `15 failed`, `0 passed`; index/range mutation and narrowing controls were not rejected at the scope boundary and omitted range enforcement was not normalized to full. Production edit authorization: `GRANTED` for task 3.10 only.

Green command: `.venv/bin/python -c '<load task 3.10 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `15 passed`, `0 failed` on Python 3.12.13. Scope normalization now resolves omitted range enforcement to `full`; before any Git materialization, index and range reject fix, preview-fix, and mutation flags, while range additionally rejects positional-file, test exclusion, focus, path-filter, no-tests, severity-level, and non-full enforcement narrowing. Error messages identify the rejected option. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.11 — UNKNOWN and NOT_APPLICABLE evidence boundaries

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:39e087405fdab642d250384738458d23e85d0661d4ac4d294f5fa74f2ed91714`; frozen selector set: the 39 checkpoint entries covering unresolved Git, empty ranges/sides, profile membership/coverage, typed analyzer inputs, required-member error semantics, sealed target policy, complete-suite/test-candidate controls, Coverage policy/outcomes, stub-only applicability, and contracts activation.

Red outcome from the complete checkpoint run: `39 failed`, `0 passed`; the checkpoint run had no closed profile/applicability/evidence-classification APIs and unresolved/empty scope outcomes did not meet the frozen contract. Production edit authorization: `GRANTED` for task 3.11 only.

Green command: `.venv/bin/python -c '<load task 3.11 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `39 passed`, `0 failed` on Python 3.12.13. The runner now exposes the closed required/conditional `pr-range-v1` membership and version identities; aggregates valid FAIL separately from required UNKNOWN while preserving legacy fail-closed projection; binds typed generated inputs and equal eligible/invoked manifests; freezes target-tip policy and test-input roles before candidate execution; rejects candidate policy, collection/report hooks, item/unittest bypasses, missing candidates, custom Coverage exclusions/plugins, and hostile selection controls as UNKNOWN. Empty snapshots/sides are manifest-driven NOT_APPLICABLE, stub-only sides retain static members while excluding coverage/CrossHair, and valid threshold failures remain semantic FAIL with repaired baselines classified fixed. The two scope boundary cases also pass. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.12a — canonical finding-location union

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:d5e98fd3df760dd42b7f29c00fbca44cf537cf05b1200e89c3e3a6d937348de1`; frozen selectors: the four checkpoint cases for exact UTF-8 byte spans, whole-line fallback, canonical location-kind coverage, and selector/non-source continuity exclusion.

Red outcome from the complete checkpoint run: `4 failed`, `0 passed`; the typed schema-1.6 location union and canonical source-span conversion module were absent. Production edit authorization: `GRANTED` for task 3.12a only.

Green command: `.venv/bin/python -c '<load task 3.12a selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `4 passed`, `0 failed` on Python 3.12.13. The new differential boundary exposes the canonical `source-span-v1` location with one-based lines, half-open UTF-8 byte columns, raw coordinate-system binding, and exact-versus-whole-physical-line precision. Exact UTF-16 adapter coordinates convert deterministically, and selector/infrastructure identities are rejected from source continuity as non-source UNKNOWN. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.12b — signed analyzer toolchain lock projection

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:44841bd4c1ba80b90a575e9010b8e2923c91fe9a9a77cc6a2d43e84c4b90980d`; frozen selector set: the 24 checkpoint entries for canonical lock equality, complete dependency/native/runtime closure, immutable OCI/cache source validation, portable identities, exact installed membership, built-in payload integrity/boot, project/plugin attestation, host dependency exclusion, and package-manifest separation.

Red outcome from the complete checkpoint run: `24 failed`, `0 passed`; the signed toolchain-lock resource and toolchain validation/materialization API were absent, and the package dependency/resource contract did not yet expose the frozen analyzer environment. Production edit authorization: `GRANTED` for task 3.12b only.

Green command: `.venv/bin/python -c '<load task 3.12b selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `24 passed`, `0 failed` on Python 3.12.13. `pr-range-v1-toolchain-lock.json` was generated as the exact compact canonical checkpoint projection: `793458` bytes, SHA-256 `5a6ad4c97100127634272670c09e14eff5fd149daee5d9c63efa5480731c8373`, equal to frozen `toolchain_lock_projection_digest`. The toolchain boundary validates exact pins, cross-environment membership, dependency/native/runtime closure, immutable HTTPS OCI identities, portable storage-independent identities, verified-cache/signed-registry acquisition disposition, signed installed built-in payload shape, project lock/plugin attestation, and candidate dependency drift. Analyzer-only packages were removed from host `pip_dependencies`; `requests` remains the controller HTTP dependency. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.12c — immutable OCI acquisition authorization

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:8283b062bcf446af73938386dce55d8e0c0f0a0bcb7c5a24dc9d920664f9344c`; frozen selectors: the three checkpoint redirect-chain downgrade, unauthorized-host, and cross-host credential-forwarding cases.

Red outcome from the complete checkpoint run: `3 failed`, `0 passed`; immutable OCI acquisition and per-hop redirect/credential authorization were absent. Production edit authorization: `GRANTED` for task 3.12c only.

Green command: `.venv/bin/python -c '<load task 3.12c selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `3 passed`, `0 failed` on Python 3.12.13. OCI acquisition now rejects HTTP downgrade and every redirect host outside the signed allowlist, records unauthorized hops with `credential_sent=false`, and permits only verified-cache or signed-registry digest records. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.12d — safe OCI layer extraction boundary

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:16111de4e7f8dcd41bc4330ec4cd926d8d6d447a63bea613abcac88123447dcb`; frozen selector: `tests/unit/specfact_code_review/run/test_toolchain.py::test_runtime_capsule_fresh_cache_miss_installs_only_pinned_wheelhouse`.

Red outcome from the complete checkpoint run: `1 failed`, `0 passed`; fresh-cache capsule materialization and its index-disabled, lock-only install boundary were absent. Production edit authorization: `GRANTED` for task 3.12d only.

Green command: `.venv/bin/python -c '<load task 3.12d selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `1 passed`, `0 failed` on Python 3.12.13. Fresh-cache materialization now returns a storage-root-independent identity, creates a fresh environment root, disables indexes, and reconciles the installed set exactly to the locked plus bootstrap distributions, with no ambient member admitted. `hatch run format`, `hatch run lint`, the exact selector, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.12e — verified installed module payload

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:0fb11957cea5b51368dbe39ae35368dcc439035d2138b702eed9f4335ca08179`; frozen selector set: the six checkpoint cases for complete installed-package traversal, approved loader/key/checksum/signature identity, and marketplace metadata without source/archive fields.

Red outcome from the complete checkpoint run: `6 failed`, `0 passed`; verified installed-module payload traversal and identity validation were absent. Production edit authorization: `GRANTED` for task 3.12e only.

Green command: `.venv/bin/python -c '<load task 3.12e selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `6 passed`, `0 failed` on Python 3.12.13. Installed built-in payload validation now requires official marketplace origin plus digest-shaped checksum/key identity and a non-empty signature, traverses the complete expected package as regular non-symlink files, rejects missing/content/link drift, and exposes only marketplace module/version/checksum/signature/key/loader/root identity—no source commit or archive locator. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.12f — fresh capsule built-in boot without archive

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:37c8c8048fdadb23850709f4d0f7b14ef36c97c0299c98519a6fba5f9382b21d`; frozen selector: `tests/unit/specfact_code_review/run/test_toolchain.py::test_builtin_payload_boots_after_marketplace_archive_discard`.

Red outcome from the complete checkpoint run: `1 failed`, `0 passed`; capsule built-in installation from the loader-registered payload without retained archive bytes was absent. Production edit authorization: `GRANTED` for task 3.12f only.

Green command: `.venv/bin/python -c '<load task 3.12f selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `1 passed`, `0 failed` on Python 3.12.13. A verified loader-registered installed payload now copies into canonical `/opt/specfact/builtin/specfact_code_review` without consulting or requiring a marketplace archive/cache/locator and reports `archive_required=false`. `hatch run format`, `hatch run lint`, the exact selector, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.12g — attested project-runtime layer

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:d0e2e72bd67a1aa84ce58ff2afbd8777cb28ccb3ef6a241669427e1e6b6c3a34`; frozen selector set: the 13 checkpoint cases for target dependency-input binding, missing/untrusted/candidate/mutable rejection, reserved collisions, identical cross-snapshot identity, closed mount membership, snapshot-before-project import order, and attested pytest-plugin identity.

Red outcome from the complete checkpoint run: `13 failed`, `0 passed`; the project-runtime descriptor/layer validation and mount/import authorization boundary were absent. Production edit authorization: `GRANTED` for task 3.12g only.

Green command: `.venv/bin/python -c '<load task 3.12g selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `13 passed`, `0 failed` on Python 3.12.13. `project-runtime-layer-v1.schema.json` was generated as the exact compact canonical checkpoint document, SHA-256 `07c5ee03c66f90bacc816591b161d23288843a74be27abce12021fe133163fe3`, equal to `project_runtime_contract.digest`. Descriptor validation binds the authorized target and source-lock digest, builder/immutable OCI identity, `/opt/specfact` layer root, distribution/plugin attestations, and rejects missing, mutable, host-backed, candidate-target, observed-unattested, or reserved-collision input. Only the closed import-capable member set may mount it; one identical identity is bound to both snapshots and non-reserved lookup remains snapshot-before-project. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.13 — CrossHair infrastructure failure propagation

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:b1db64340f5a374ec5f5feba2ec5b6b4a53dcd1235f279db98cd00aacc6f3d3d`; frozen selectors: the mandatory CrossHair process-error and timeout propagation cases.

Red outcome from the complete checkpoint run: `2 failed`, `0 passed`; timeout and documented process-error exits were swallowed instead of producing explicit UNKNOWN analyzer evidence. Production edit authorization: `GRANTED` for task 3.13 only.

Green command: `.venv/bin/python -c '<load task 3.13 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `2 passed`, `0 failed` on Python 3.12.13. CrossHair timeout and non-semantic process exits now emit an explicit `CROSSHAIR_INCOMPLETE_EVIDENCE` tool-error record with `execution_state=error` and `evidence_outcome=UNKNOWN`; successful parsed counterexample semantics remain unchanged. The shared finding model now carries optional authoritative execution/evidence fields. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.14a — complete pytest suite and frozen input roles

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:ca1ce3f79d63b55746c57e3e6e44f2473229466d53d67f7f2abb5c698143eaa9`; frozen selectors: complete production-change suite collection plus the four pinned `fnmatch_ex` basename/rooted/recursive match vectors.

Red outcome from the complete checkpoint run: `5 failed`, `0 passed`; complete suite planning and the pinned path-role matcher were absent. Production edit authorization: `GRANTED` for task 3.14a only.

Green command: `.venv/bin/python -c '<load task 3.14a selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `5 passed`, `0 failed` on Python 3.12.13. Suite planning scans every sealed `testpaths` root and accepted `python_files` pattern, collects matching test functions without source-derived narrowing, and reports uncollected changed candidates UNKNOWN. The path-role matcher reproduces pytest's basename behavior for separator-free patterns and whole-path `*/<relative-pattern>` behavior for rooted/recursive patterns. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.14b — pytest hook disposition boundary

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:9ca6edfa00f1c9065a665a3ca04a18e620a117495c3de94272e2dcd88d9bcafe`; frozen selectors: repository deselection through `pytest_collection_modifyitems` and complete execution/report hook catalog coverage.

Red outcome from the complete checkpoint run: `2 failed`, `0 passed`; hook capability validation and the pinned total hook disposition catalog were absent. Production edit authorization: `GRANTED` for task 3.14b only.

Green command: `.venv/bin/python -c '<load task 3.14b selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `2 passed`, `0 failed` on Python 3.12.13. Repository and attested-project collection/report shaping hooks now fail closed as `pytest_plugin_capability_unsupported`; the pinned 9.0.3 hook catalog classifies collection plus setup/call/teardown/protocol/report/status hooks with no unclassified member. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.14c — Coverage private-output projection

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:a200b2a74d68289c25ca0d8fbd6ffcc67d9ff557130e55cb1a86f48999eed602`; frozen selector: `tests/unit/specfact_code_review/run/test_runner.py::test_coverage_projection_redirects_all_writable_paths_to_output_root`.

Red outcome from the complete checkpoint run: `1 failed`, `0 passed`; controller-owned Coverage writable-path projection was absent. Production edit authorization: `GRANTED` for task 3.14c only.

Green command: `.venv/bin/python -c '<load task 3.14c selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `1 passed`, `0 failed` on Python 3.12.13. Coverage projection redirects data, HTML, XML, JSON, and LCOV destinations into the process-private output root and exposes the exact writable manifest; repository plugins and non-empty exclusion/partial registries remain UNKNOWN. `hatch run format`, `hatch run lint`, the exact selector, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.14d — pytest projections, catalogs, observers, and outcome truth

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:bf4a7e0ce783593f94edf6207f7deefcedcf7cc9157e39604ed11deea8ecfbb5`; frozen selector set: the 34 checkpoint cases for path/output projection, option/config/collector catalogs, selection/short-circuit rejection, native controls, complete inventories, import order, observer/JUnit XPASS reconciliation, removed selectors, and PASS/FAIL/UNKNOWN lifecycle outcomes.

Red outcome from the complete checkpoint run: `34 failed`, `0 passed`; the complete pytest projection/catalog/observer/reconciliation surface was absent. Production edit authorization: `GRANTED` for task 3.14d only.

Green command: `.venv/bin/python -c '<load task 3.14d selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `34 passed`, `0 failed` on Python 3.12.13. Pytest projection now binds a snapshot-independent logical-policy digest, rebases sealed read paths per snapshot, rejects escaping inputs, redirects cache/log writes, and exposes complete pinned option/config/collector catalogs. Native and unittest candidate controls fail closed; observer/JUnit reconciliation treats passed-with-`wasxfail` as XPASS/FAIL; removed selectors and deselection/infrastructure errors are UNKNOWN; baseline fail repaired at head is fixed; assertion/skip/xfail/xpass are semantic FAIL; and attested project imports preserve snapshot-first order. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.14e — analyzer input kinds and Coverage manifest completeness

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:82827bc6eecc9b2db5515ebdac3baaef91b7f5a5d5e72f84fb684b3ebbfd9bc4`; frozen selectors: `.pyi` exclusion from runtime Coverage while retained by static members, and required runtime-measurable production-path reconciliation.

Red outcome from the complete checkpoint run: `2 failed`, `0 passed`; the per-member analyzer input-kind manifest and Coverage required/observed reconciliation APIs were absent. Production edit authorization: `GRANTED` for task 3.14e only.

Green command: `.venv/bin/python -c '<load task 3.14e selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `2 passed`, `0 failed` on Python 3.12.13. `runtime-measurable-production-v1` now selects only regular `.py` for targeted Coverage/CrossHair while retaining `.py` and `.pyi` across Ruff, basedpyright, Pylint, and icontract static scan. Required and observed Coverage manifests must match exactly or return `coverage_input_manifest_mismatch` UNKNOWN. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.15 — Ruff governed-input invocation controls

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:a38fd0971533d1db99731dcad978750ebb4adfdafee3955efeaeebbe45b02f69`; frozen selectors: disabled snapshot cache, task-tag/E501 preservation, and fix-only suppression prevention.

Red outcome from the complete checkpoint run: `3 failed`, `0 passed`; Ruff snapshot-mode effective controls were not yet exposed or forced by the adapter. Production edit authorization: `GRANTED` for task 3.15 only.

Green command: `.venv/bin/python -c '<load task 3.15 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `3 passed`, `0 failed` on Python 3.12.13. The Ruff projection default is pinned to 0.15.12; always emits `--no-cache`/`--no-force-exclude`; exposes an empty cache-write manifest; forces fix/fix-only and overlong-task-comment exemptions off; and retains original task tags as evidence without allowing them to suppress E501. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.16 — Radon sealed full-result controls

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:8892870d73dc29b6ce5e9b5c3b5a4eed1200fb177532c5e48d99463dc10d4cfe`; frozen selectors: candidate Radon config non-authority and sealed full-result option coverage.

Red outcome from the complete checkpoint run: `2 failed`, `0 passed`; a controller-owned Radon full-result policy API was absent. Production edit authorization: `GRANTED` for task 3.16 only.

Green command: `.venv/bin/python -c '<load task 3.16 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `2 passed`, `0 failed` on Python 3.12.13. The pinned `radon-full-result-v1` projection ignores candidate `radon.cfg` narrowing, clears exclude/ignore/output targets, admits the full CC A–F and MI A–C result ranges, uses an empty controller cwd/private home, and sanitizes `RADONCFG`. Version drift returns UNKNOWN. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.17 — Pylint result/input controls

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:daf0b996aa73cb3455b6fcda5833ab12ae814e463a5b879ccf1f0dba9b968a25`; frozen selectors: confidence/errors-only/from-stdin controls and pre-applicability init-hook/load-plugin/extension-package rejection.

Red outcome from the complete checkpoint run: `6 failed`, `0 passed`; Pylint's total control projection and unsafe dynamic-extension disposition were absent. Production edit authorization: `GRANTED` for task 3.17 only.

Green command: `.venv/bin/python -c '<load task 3.17 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings with that unchanged list>'`.

Green outcome: `6 passed`, `0 failed` on Python 3.12.13. Pylint projection forces the complete confidence set, `errors-only=false`, `from-stdin=false`, and closed discovery/input controls; non-empty init hooks, repository plugins, and extension allow-list aliases return explicit UNKNOWN before no-impact derivation. `hatch run format`, `hatch run lint`, the exact selector set, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.18 — basedpyright explicit project projection

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:c7bf0dcc1db82ccaa7c4057ca5c9663e07185f97494681a49edfde5f7366b8a4`; frozen selectors: referenced-policy/baseline governance with disabling, generated no-config default, per-snapshot relative-path rebasing, and escape rejection.

Red outcome from the complete checkpoint run: `4 failed`, `0 passed`; the basedpyright runner projection API did not expose the full governed-reference/default/rebase boundary. Production edit authorization: `GRANTED` for task 3.18 only.

Green command: `.venv/bin/python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the four unchanged task 3.18 selectors from IMPLEMENTATION_CHECKPOINT.json>`.

Green outcome: `4 passed`, `0 failed` on Python 3.12.13. The basedpyright policy now records the exact generated-default or selected-graph identity, retains governed reference/baseline files while removing effective baseline controls, rejects escaping policy paths, rebases directory-scoped eligible inputs into each immutable snapshot, and carries a snapshot-independent logical policy digest. The earlier basedpyright scope regression surface also passed (`8 passed`); `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.19 — Semgrep sealed union and explicit scanned-input evidence

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:48ae8d65f23a76aea94a8b8500f630e0f2fbdcc86d61ce1a70f557219dba46ca`; frozen selectors: nosem-disabled invocation, pass-union reconciliation, rule-target narrowing rejection, scanned/eligible input equality, and signed-module fallback.

Red outcome from the complete checkpoint run: `4 failed`, `1 passed`; signed-module fallback already held, while the invocation, pass reconciliation, rule-pack validation, and scanned-path evidence APIs were absent. Production edit authorization: `GRANTED` for task 3.19 only.

Green command: `.venv/bin/python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the five unchanged task 3.19 selectors from IMPLEMENTATION_CHECKPOINT.json>`.

Green outcome: `5 passed`, `0 failed` on Python 3.12.13. Each Semgrep pass must independently reconcile its scanned/skipped manifest against the exact eligible inputs, path-targeting rule controls are rejected before launch, the controller appends `--disable-nosem`, and the existing signed-module fallback remains valid. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.20 — suppression catalog and conservative finding multiset

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:262d11f194a607d21dc7d6b1c65a95047b16a36063a34b5403736ad9440e0166`; frozen selectors: all 76 suppression-catalog, occurrence-continuity, finding-multiset, exact-rename, catalog-identity, and signed-resource vectors.

Red outcome from the complete checkpoint run: `76 failed`, `0 passed`; the differential and suppression APIs plus authenticated catalog resource were absent. Production edit authorization: `GRANTED` for task 3.20 only.

Green command: `.venv/bin/python -c '<load task 3.20 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -q --tb=short --disable-warnings --import-mode=importlib with that unchanged list>'`.

Green outcome: `76 passed`, `0 failed` on Python 3.12.13; the full differential test file also passed (`79 passed`). The exact canonical suppression resource SHA-256 is `32346a8a0848bc024b1330c37ab5bdcf12f092460cce904ebbab831f6d276375`, matching the checkpoint and authenticated package-resource entry. Registered comment-token controls, unchanged-control quarantine, conservative rename/fix outcomes, source continuity, multiset surplus, and catalog activation drift now produce the frozen decisions. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.21 — lifecycle/blocking separation

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:bd0db3e3e0a497cae1246ac489758116fd9e05ac2f45fe66c08bbcfce3cb8010`; frozen selectors: fixable-error blocking and scorer preservation.

Red outcome from the complete checkpoint run: `1 failed`, `1 passed`; scoring already retained the error, but `ReviewFinding.is_blocking()` incorrectly treated autofix availability as if the fix had been applied. Production edit authorization: `GRANTED` for task 3.21 only.

Green command: `.venv/bin/python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the two unchanged task 3.21 selectors from IMPLEMENTATION_CHECKPOINT.json>`.

Green outcome: `2 passed`, `0 failed` on Python 3.12.13. The canonical finding model now separates lifecycle `status`, `differential_state`, `autofix_available`, derived `blocking`, and the C14-null `waiver_reference`; an open error remains blocking whether or not an autofix exists. The legacy scorer retains its existing remediation-aware score/verdict projection independently. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.22 — schema 1.6 authoritative assurance derivation

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:d8223398a0a44d0e868e4e6aab972fa0f87b85ae309dea572af0c523ddf2a34a`; frozen selectors: aggregate precedence, fixed-baseline exclusion, suppression-catalog binding, non-misleading UNKNOWN summary, the eight full/shadow legacy projection combinations, and missing-authoritative-status handling.

Red outcome from the complete checkpoint run: `13 failed`, `0 passed`; schema 1.6 report construction, projection, and readback APIs were absent. Production edit authorization: `GRANTED` for task 3.22 only.

Green command: `.venv/bin/python -c '<load task 3.22 selectors from committed IMPLEMENTATION_CHECKPOINT.json; invoke current interpreter -m pytest -ra -v -q --tb=short --disable-warnings --import-mode=importlib with that unchanged list>'`.

Green outcome: `13 passed`, `0 failed` on Python 3.12.13. Schema 1.6 now derives authoritative PASS/FAIL/UNKNOWN/NOT_APPLICABLE after lifecycle classification, gives a validated blocker precedence over concurrent unknown evidence while retaining the unknown flag, excludes fixed baseline evidence from blockers, binds the suppression-catalog identity, and derives the legacy verdict/exit matrix including shadow mode. Missing authoritative schema-1.6 status reads as UNKNOWN. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.23 — schema 1.6 preservation through consumers and enrichment

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:064b614d615ca0b5e4405c2bbdf566a86fb1d4e890f8e677a07dfd5c04affa14`; frozen selectors: pre-commit authoritative UNKNOWN exit, cleanup enrichment preservation, and Requirements evidence attachment preservation.

Red outcome from the complete checkpoint run: `3 failed`, `0 passed`; the pre-commit status consumer and Requirements attachment helper were absent, and cleanup refresh downgraded schema 1.6 to 1.3. Production edit authorization: `GRANTED` for task 3.23 only.

Green command: `.venv/bin/python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the three unchanged task 3.23 selectors from IMPLEMENTATION_CHECKPOINT.json>`.

Green outcome: `3 passed`, `0 failed` on Python 3.12.13. Cleanup and Requirements-context enrichment preserve schema 1.6 authoritative status, exit, scope, and analyzer evidence; the pre-commit consumer uses schema 1.6 `assurance_status` before legacy changed-line logic, retaining UNKNOWN as non-zero and NOT_APPLICABLE as zero outside shadow. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.24 — closed schema 1.6 consumer compatibility matrix

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:1857ba9f42cc071e9dafedfc6f999e4abb134cdc0d4c9c69d50041a812b3059e`; frozen selectors: closed matrix validation and suppression-catalog identity mismatch rejection.

Red outcome from the complete checkpoint run: `2 failed`, `0 passed`; the signed static consumer compatibility matrix resource did not exist. Production edit authorization: `GRANTED` for task 3.24 only.

Green command: `.venv/bin/python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the two unchanged task 3.24 selectors from IMPLEMENTATION_CHECKPOINT.json>`.

Green outcome: `2 passed`, `0 failed` on Python 3.12.13. The closed schema 1.6 matrix exercises PASS/FAIL/UNKNOWN/NOT_APPLICABLE, retains the legacy schema-less ledger fixture, binds checkpoint/resource/package/profile/report/static-envelope suppression-catalog identities, and rejects a mismatched accepted envelope as UNKNOWN. The authenticated matrix SHA-256 is `7146f30b09bab677acb9981cd9d60f814841915b8bb2ba950ca9fbcff6f6438f`; manifest validation and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.25 — ledger authoritative PASS/FAIL transitions

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:e826c63b238af8f9a10a16bc5bf8552a4d8f58c3fba054ca4876ecf2dbd2d1ed`; frozen selectors: authoritative FAIL penalty/block streak and PASS reward/pass streak with report persistence.

Red outcome from the complete checkpoint run: `2 failed`, `0 passed`; ledger updates still trusted the legacy reward projection and omitted canonical report digest persistence. Production edit authorization: `GRANTED` for task 3.25 only.

Green command: `.venv/bin/python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the two unchanged task 3.25 selectors from IMPLEMENTATION_CHECKPOINT.json>`.

Green outcome: `2 passed`, `0 failed` on Python 3.12.13. Schema 1.6 PASS/FAIL now use authoritative verdicts, deterministic +5/-5 reward transitions, the corresponding pass/block streaks, and complete canonical `report_json` plus `report_digest`; 11 legacy ledger regressions also passed. UNKNOWN/NOT_APPLICABLE neutral transitions remain deliberately red-owned by the next frozen checkpoint. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN` for the mapped PASS/FAIL checkpoint.

### Task 3.26 — neutral ledger statuses and Supabase persistence compatibility

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:88943daf14f3234d443051b53bd7ba85c3c6d7215e809beafedd86c69d1f521a`; frozen selectors: authoritative UNKNOWN and NOT_APPLICABLE neutral reward/streak persistence.

Red outcome from the complete checkpoint run: `2 failed`, `0 passed`; both neutral statuses still inherited the score-derived +5 reward and legacy non-FAIL streak behavior. Production edit authorization: `GRANTED` for task 3.26 only.

Green command: `.venv/bin/python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the two unchanged task 3.26 selectors from IMPLEMENTATION_CHECKPOINT.json>`.

Green outcome: `2 passed`, `0 failed` on Python 3.12.13; the full ledger suite also passed (`25 passed`). UNKNOWN and NOT_APPLICABLE persist verbatim as zero-reward neutral audit events without changing either streak or triggering bonuses/penalties. The Supabase DDL now admits both statuses and adds nullable `report_json`/`report_digest` through a backward-compatible migration while preserving legacy rows; local and Supabase run payloads share the canonical report fields. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.27 — canonical complete-range merge guidance

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:d9b72cbcd0997434362af99f32911007bc6be464b34fa2530275857ad4b915e9`; frozen selectors: docs parity, no-execution AI workflow instructions, and installed updater guidance.

Red outcome from the complete checkpoint run: collection stopped with an import error before selector execution because the canonical target-style conflict helper expected by the docs/CLI contract surface was absent; the complete PR-range guidance could not be validated. Production edit authorization: `GRANTED` for task 3.27 only.

Green command: `.venv/bin/python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the three unchanged task 3.27 selectors from IMPLEMENTATION_CHECKPOINT.json>`.

Green outcome: `3 passed`, `0 failed` on Python 3.12.13; the complete related docs/updater/CLI regression surface also passed (`37 passed`). All seven canonical guidance surfaces and the no-execution CLI instructions now require range/base/head/event-context/full evidence, identify local output as `range_preview`, reserve promotion for the protected consumer, and keep staged positional review explicitly `explicit_files`. The restored targeting-style validator rejects positional files mixed with `--scope` or `--path`. `hatch run format`, `hatch run lint`, and `git diff --check` passed. Task decision: `GREEN`.

### Task 3.28 — immutable core 0.55.1 compatibility smoke

Pre-edit checkpoint verification: `PASS` on 2026-08-20 with implementation selector-map digest `sha256:7f9a49abb646f5cc714ff2a1986503c868b4914ab3fbbc9479ec45d9380ee4e5`.

Frozen selector digest: `sha256:b4d51b8de2f4f25189a38390087fc00766434da0686e3c4a57ec68cfa3dfac78`; frozen selectors: exact core runtime matrix load, immutable tag/commit/tree workflow identity, and PEP 440 local-alias rejection.

Red outcome from the complete checkpoint run: `3 failed`, `0 passed`; the current controller environment still had core 0.54.0, and the PR orchestrator lacked the dedicated exact-core job and local-version rejection vector. Production edit authorization: `GRANTED` for task 3.28 only.

Green command: `.venv/bin/python -m pytest -ra -v --import-mode=importlib -q --tb=short --disable-warnings <the three unchanged task 3.28 selectors from IMPLEMENTATION_CHECKPOINT.json>` after installing local immutable core commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` into the task environment.

Green outcome: `3 passed`, `0 failed` on Python 3.12.13. The workflow now owns a dedicated Python 3.11–3.13 exact-core job, checks `refs/tags/v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, tree `47984be5434d7ae65ed6908bf525a32053290337`, creates a fresh environment, installs without cache, runs the schema 1.6 matrix selector, and proves `===0.55.1` rejects `0.55.1+vendor` whereas ordinary `==0.55.1` does not. Workflow YAML parsing, `hatch run format`, `hatch run lint`, and `git diff --check` passed; exact core 0.55.1 remained installed. Task decision: `GREEN`.

## Release readiness evidence — 2026-08-21

### Exact core 0.55.1 handoff matrix

Fresh isolated CPython 3.11.15, 3.12.13, and 3.13.14 environments installed the immutable core tag from detached commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337`. Each environment reported core package version `0.55.1` and passed the schema 1.6 end-to-end, installed-handoff derivation, and candidate-payload identity selectors (`3 passed` per interpreter). `===0.55.1` accepted only `0.55.1`; it rejected `0.55.1+vendor`, while ordinary `==0.55.1` admitted that local alias and is therefore not release-authoritative.

The core trust surfaces required by the amended boundary are present in the tag. `module_discovery.py`, `module_installer.py`, and `module_package.py` are byte-identical between immutable v0.55.1 and the freshly fetched current core `origin/dev` observed during readiness validation. C14 derives a module-owned handoff from those existing granular records; it does not depend on an unpublished later core implementation.

### Task 4.2 adjudicated benchmark

Live GitHub bodies and states for core #665–#671 were refreshed on 2026-08-21. The retained #665 release corpus explicitly requires an exact range, unique merge base, changed-test/policy coverage, symmetric evidence, independent verdicts, and UNKNOWN for unresolved facts; #666–#671 remain historical remediation/forensic inputs rather than reusable proof.

Command: `hatch run pytest -q` over the eight exact selectors for staged-versus-unstaged index content, positional downgrade rejection, candidate-policy self-authorization rejection, policy-only range UNKNOWN, advanced-base-tip merge-base selection, pure-rename continuity, off-added-line introduced blockers, and head-config false-green prevention.

Outcome: `8 passed`, `0 failed` on Python 3.12.13. Task decision: `GREEN`.

### Task 4.3 release metadata

After the exact-core matrix passed, the permitted feature-branch release metadata was set to module version `0.49.0` and exact `core_compatibility: '===0.55.1'`; the changelog records the additive C14 range/differential/schema 1.6 release. The focused manifest, exact-core workflow, and schema consumer tests passed (`12 passed`, `2 deselected`). Archives, checksums, signatures, sidecars, and `registry/index.json` remain untouched for canonical post-merge generation. Final Linux cache-miss/cache-hit capsule and empty-Bubblewrap authority remains pending protected CI and canonical publication evidence; task 4.3 is not yet complete.

### Final feature-branch gates — 2026-08-21

After clean-code remediation, the candidate module's full local review completed in 39.11 seconds with `138 findings (0 blocking)`. The preceding runs reduced the blocker count from 40 to 14, then 1, then 0 without weakening any detector or accepted C14 behavior. Focused regression validation after the refactor passed `191` tests. The retained non-blocking review diagnostics are explicit pre-merge exceptions: `79` advisory contract-coverage findings across the frozen public C14 model/API surface; structural single-owner file-size/complexity diagnostics required by the closed C14 source allowlist; targeted local coverage warnings whose Linux capsule/cache/Bubblewrap paths are protected-CI owned; and one CrossHair environment error caused by its subprocess lacking `beartype`, while the canonical contract gate itself passed. Adding post-checkpoint decorators, splitting the mandated owner files, or pretending that macOS supplied Linux namespace authority would alter the accepted contract or evidence boundary. These exceptions grant no `pr_range` or Linux authority and remain visible in the JSON evidence.

The final checksum-only dev-PR bridge set `specfact-code-review` `0.49.0` to `sha256:bc205355b32388d968ef30919bd57f30d147f46472c9099e8c19fc8f8b5d5470`; `core_compatibility` is exactly `===0.55.1`, and the registry remains untouched. Filesystem signature/version-bump verification accepted all seven manifests with the local missing-public-key allowance. `git diff --check`, format, type-check (`0` errors), Ruff/Pylint (`10.00/10`), YAML, bundle imports, strict OpenSpec, and contract tests (`28 passed`) all passed. Both `smart-test` and the complete test gate passed all `1344` tests; the only warnings were two third-party `lark` deprecations.

The immutable core compatibility smoke was then repeated against the final candidate bytes under CPython `3.11.15`, `3.12.13`, and `3.13.14`. Each exact-core environment reported package version `0.55.1` and passed the same three schema-1.6/runtime-handoff/candidate-identity selectors (`3 passed` per interpreter). Each environment again proved `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, and ordinary `==0.55.1` is too broad. Protected Linux cache-miss/cache-hit and empty-Bubblewrap evidence remains pending CI, so task 4.3 is not yet adjudicated complete.

The required explicit-range/full self-review then ran from committed head `fa430434204117879aef2560e523255f0164dd95` / tree `1114200666468f6974d29495d69ee77497176e2a` against synchronized `origin/dev@c3eda08c732267dc3614130f5f36bcd473182d0b` / tree `0b1d0cc80b671c561ddadc3d50f395b1959d0dd7`, using the outside-checkout regular GitHub context file for public issue `#416`. Scope resolution passed with exactly one merge-base candidate (`c3eda08c732267dc3614130f5f36bcd473182d0b`), `assurance_kind=range_candidate`, context digest `sha256:cc3df6f52a62d899e0ee7f150db27c30d3d5ee54939488b04f5d516f96c88594`, and no blocking findings. Schema 1.6 correctly remained authoritative `UNKNOWN` / exit `1` with `snapshot_differential_execution_unavailable`: the module-owned local producer cannot execute or self-promote protected-consumer `pr_range` authority. This is truthful explicit-range evidence, not a PASS claim; protected CI must independently verify and promote the candidate.

### Task 4.3 protected-runtime gate correction

Pre-merge audit found that the original synthetic cache/Bubblewrap unit selectors validated signed descriptors and launch plans but did not perform task 4.3's required live GHCR acquisition or namespace boot. Merge remained blocked. A new allowed workflow-contract selector, `test_pr_orchestrator_runs_real_c14_capsule_smoke`, was authored first and failed `1 failed` because the exact-core job lacked that runtime step. The workflow was then extended, without changing module payload bytes or signed outputs, to run per CPython 3.11–3.13: byte/digest equality between the checkpoint and signed toolchain resource; real signed-registry cache-miss acquisition with HTTPS/final-URL/redirect evidence; complete verified-cache materialization and offline/index-disabled installation; copied-cache network-forbidden materialization below a second storage root; exact installed set, lock identity, final-root manifest/bytes, and interpreter/stdlib/extension/loader/library/bootstrap comparisons; signed static Bubblewrap hash verification; and a real `--unshare-all --unshare-net` empty-root boot using only the capsule loader/libraries/interpreter. The selector then passed (`1 passed`), YAML validation passed, and task 4.3 remains incomplete until the protected Linux matrix returns its evidence.

Protected run `32524928577` at candidate commit `d863e771c06ea5b89790358d001cbb106b42d805` reached the live registry step on all three matrix versions but each job failed before download with `oci_acquisition_failed:401 Client Error: Unauthorized` at the trusted `https://ghcr.io/token` realm. Fresh package metadata showed `specfact-code-review-analyzer-runtime` is private, while the job granted only `contents: read` and supplied no credential. A strengthened workflow-contract test first failed (`1 failed`) on the exact missing fragments: job-scoped `packages: read`, ephemeral GitHub actor/token inputs, and credential plumbing to the two cache-miss calls. The exact selector then passed (`1 passed`) after the workflow granted `packages: read` only to the compatibility job and passed `github.actor:github.token` to the existing client. The client uses those credentials only at the same-host HTTPS token realm, obtains a scoped bearer token, and continues to send no credential on redirects. The complete workflow-contract file passed (`9 passed`); strict OpenSpec, YAML parsing, lint/type checks, `git diff --check`, and a focused full-enforcement SpecFact review of the changed Python test (`0 findings`) passed. Task 4.3 remains incomplete pending a new protected run; a repeated 401 after this correction would establish that the private package still needs repository Actions access in GitHub package settings rather than justify weaker or simulated evidence.

After `nold-ai/specfact-cli-modules` received package Actions read access, protected attempt 2 of run `32525570996` authenticated and resolved the exact checkpoint manifests, then exposed `capsule_materialization_failed:[Errno 26] Text file busy` while launching the extracted `bwrap-static`. The signed descriptor already requires `launch_mode=same-open-descriptor`; production had verified the pathname and then executed the pathname rather than that same descriptor. The new regression `test_offline_install_executes_verified_bubblewrap_from_same_open_descriptor` was authored first and failed (`1 failed`) because `subprocess.run` received neither `/proc/self/fd/<n>` nor `pass_fds`. Production now opens the native executable read-only/no-follow, validates executable regular-file mode, hashes and stability-checks that descriptor, rewinds it, and launches exactly `/proc/self/fd/<n>` with only that descriptor inherited. The focused test passed, followed by the complete toolchain/workflow surface (`59 passed`), format, type/lint (`0` diagnostics; Pylint `10.00/10`), strict OpenSpec, and `git diff --check`. Task 4.3 remains incomplete until protected Linux proves the corrected descriptor-bound launch and subsequent cache/Bubblewrap identities.

Signed-head run `32526962192` proved that `/proc/self/fd/<n>` was used but still returned `ETXTBSY`, exposing the lower-level extraction defect: both OCI layer extraction and cache publication called `tempfile.mkstemp()` while discarding its returned writable descriptor. For an extracted executable, `os.replace()` preserved that still-open writable inode and Linux correctly refused execution. The regression `test_oci_extraction_closes_temporary_creator_descriptor` was authored first and failed (`1 failed`) because `os.fstat()` still succeeded on the captured creator descriptor after extraction. Both sites now write through `os.fdopen()` around the exact creator descriptor so it closes before publication or execution. The two descriptor-focused selectors passed, followed by the complete toolchain/workflow surface (`60 passed`), format, type/lint (`0` diagnostics; Pylint `10.00/10`), strict OpenSpec, and `git diff --check`. Task 4.3 remains pending fresh signed protected-Linux evidence.

### Post-review hardening and refreshed local gates — 2026-08-21

The signed-head review was treated as new input rather than reusing earlier evidence. Sixteen new exact regressions were authored before the corresponding production changes and initially failed `16 failed`, covering invalid source coordinates, missing packaged checkpoint/catalog resources, incomplete invocation manifests, pytest process/JUnit disagreement and non-PASS outcomes, async/class selector discovery, Radon ambient cwd isolation, OCI absolute-symlink containment, explicit public-key handoff authority, payload mutation after handoff, missing ledger assurance status, omitted CLI enforcement, unrecognized CrossHair output, pre-commit authoritative status, and numeric schema ordering. Additional workflow red cases then proved unquoted tree revision/redaction defects, and focused cache/consumer cases proved false-green behavior for schema `1.10` and repeated correspondence work. The implementation fail-closes each boundary; the expanded review-regression surface passed `616 passed` after correction of two test-local placement/assertion mistakes.

The accepted differential evidence requires an exact forbidden-cost value per distinct finding anchor. The implementation now caches the shared forward/suffix matrices per source pair and the forbidden result per anchor, reducing two distinct-anchor classifications from six dynamic-programming builds to four while retaining one exact forbidden solve for each unique anchor. A further approximation or single global forbidden solve would change the accepted signed evidence semantics, so the remaining per-unique-anchor work is an explicit review exception rather than a silent performance claim.

The review also observed that nested fragments in `project-runtime-layer-v1.schema.json` do not independently declare every JSON object type. That signed resource is frozen byte-for-byte by `IMPLEMENTATION_CHECKPOINT.json` as `project_runtime_contract=sha256:07c5ee03c66f90bacc816591b161d23288843a74be27abce12021fe133163fe3`; changing it after the red checkpoint would invalidate the closed task-3.12g contract and task-4.3 equality gate. Production separately verifies mapping/list/member shapes and returns UNKNOWN on malformed descriptors. The resource remains unchanged as an explicit checkpoint-preservation exception; a future schema revision may add redundant structural declarations only through a new checkpoint/version, never by mutating this signed v1 identity.

Fresh final local gates on the hardened payload passed: `hatch run format`; `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint with `10.00/10`; YAML validation of seven manifests plus the registry; bundle-import validation; strict C14 OpenSpec validation; `28 passed` contract tests; `1375 passed` smart-test; and `1375 passed` complete tests. The two complete-suite warnings are third-party `lark` deprecations. Full enforcement on the changed Python surface initially found two clean-code complexity blockers; a behavior-preserving helper extraction removed both, after which `143 passed` focused command/runner tests and the type/lint gates remained green and the repeated review reported `387 findings (0 blocking)`. The retained warnings fall within the previously documented frozen-owner/contract-coverage/local-runtime exceptions. Filesystem checksum/version verification against `origin/dev` accepted all seven manifests with the documented local missing-public-key allowance. The signing tool canonically removed the stale bot signature after payload changes and set the unsigned PR checksum to `sha256:b52f8e20c45214352d1f62416f8c6941c5aef0fbce65939ac911ee5c30a2a942`; bot signing must regenerate it after push.

Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each installed immutable core `0.55.1` from commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337`, loaded the current candidate module, and passed the same three schema/handoff/candidate-identity selectors (`3 passed` per interpreter). Every environment reconfirmed that `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, and ordinary `==0.55.1` admits that local alias. GitHub Actions access was configured at `https://github.com/orgs/nold-ai/packages/container/specfact-code-review-analyzer-runtime/settings`. Task 4.3 remains incomplete until a freshly signed head passes the protected Linux cache-miss/cache-hit and empty-Bubblewrap matrix.

The committed post-review range run bound synchronized base commit `c3eda08c732267dc3614130f5f36bcd473182d0b` / tree `0b1d0cc80b671c561ddadc3d50f395b1959d0dd7` to candidate commit `af558b246e633b9609b5d25579f7a5c429601749` / tree `8dcd49b17addd3a24677fc99611ef46806da3c2d`. It resolved exactly one merge base, emitted `assurance_kind=range_candidate`, context digest `sha256:72ceea4548b1a29ef200d2f2d9c315a2b0bc4bd9d00b2e0cd5c96566526c841b`, and reported `0 findings (0 blocking)`. Schema 1.6 remained truthfully `UNKNOWN` with `snapshot_differential_execution_unavailable`; only the protected consumer may promote it.

The canonical signer then added signature `PUvZ5dbtFe/mVeVobYAAP7DHL/FMNBWO7QKxZMI+1ET1emmQ6/4YKdt+NDO6SPgZbN8/Dlo1KCy9W3MdOGFcCA==` without changing checksum `sha256:b52f8e20c45214352d1f62416f8c6941c5aef0fbce65939ac911ee5c30a2a942`, producing bot commit `4ada53b5c124d0bbebd1334a763bac4f08619830` / tree `2444696cd9af04751e7ab083fadf442474462586`. GitHub marked workflows directly triggered by that bot-authored head `action_required` with no executable jobs. This evidence-only human-authored commit changes no module payload byte and exists to trigger protected CI against the unchanged signed manifest.

Protected run `32531532061` at human trigger commit `7a0749f4a184b059919fbf3a27134738ba564565` proved package access through the corrected settings surface `https://github.com/orgs/nold-ai/packages/container/specfact-code-review-analyzer-runtime/settings`, immutable core/tag/tree installation, exact-version alias rejection, and the schema 1.6 smoke on CPython 3.11–3.13. All three live capsule jobs then failed at descriptor-bound Bubblewrap launch with the same Ubuntu 24.04 host-policy error: `capsule_materialization_failed:offline analyzer installation failed: bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`. The failure occurs while an unprivileged Bubblewrap configures the isolated namespace loopback interface; it is not an OCI authorization, signed-identity, core-compatibility, or schema failure. The remediation preserves `--unshare-all` and adds `--cap-drop ALL` to both the production offline-install and final empty-root boot commands, while only the protected ephemeral-runner smoke Python process is invoked through `sudo --preserve-env=...`; it does not share host networking or disable Ubuntu AppArmor. The strengthened descriptor and workflow selectors were authored first and failed `2 failed` on the absent capability drop/elevated invocation, then passed `2 passed`. A further step-local regression exposed and failed on an initially misplaced elevation before the workflow bound `sudo` to the named live capsule step and restored the ordinary core-alias step; it then passed. The complete toolchain/workflow surface passed `71 passed`. Task 4.3 remains incomplete pending a freshly signed protected Linux matrix proving cache-miss/cache-hit materialization and the empty-root boot under this boundary.

Post-remediation local gates passed on 2026-08-22: format; `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff/Pylint with `10.00/10`; YAML and registry validation; bundle-import validation; strict C14 OpenSpec; `28 passed` contracts; `1375 passed` smart-test; and `1375 passed` complete tests with only the same two third-party `lark` deprecations. The canonical signing utility removed the stale signature and refreshed the unsigned dev-PR checksum to `sha256:1e35b1e0a06a53645815c3f0b1f663b8b1923ae39bf901e72dba1e4b1536ec88`; filesystem checksum/version verification accepted all seven manifests with the repository-approved local missing-public-key allowance. Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each installed immutable core `0.55.1` from commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337` and passed the three exact schema/handoff/candidate selectors (`3 passed` per interpreter); each reconfirmed that `===0.55.1` accepts only the tested identity, rejects `0.55.1+vendor`, and ordinary `==0.55.1` admits that alias.

The post-remediation explicit range/full review bound synchronized base commit `c3eda08c732267dc3614130f5f36bcd473182d0b` / tree `0b1d0cc80b671c561ddadc3d50f395b1959d0dd7` to candidate implementation commit `64bd07dc2e0ad5fb7e1ca6efecce62c18adf787b` / tree `0601fb30c7c08354b342bfe40b011f2606db75d5`. It resolved exactly one merge base, emitted schema 1.6 `assurance_kind=range_candidate`, context digest `sha256:83a01c2cbb960a03f06f26b4567be6e5b60b1e874b36b27f68d244846a1952d0`, and reported `0 findings (0 blocking)`. Its authoritative result remains `UNKNOWN` / exit `1` with `snapshot_differential_execution_unavailable`, as required because the local producer cannot promote itself to protected `pr_range` authority. The original project-scoped module directory was restored after the current candidate module completed the review.

The canonical signer added signature `z/Ojifmu2yyGDXzsnfYr6MC6+MqO+mMYdqJa17aOMvXqzP+AZUAx47ff/YSbXItJQdW2xZuuc7BiXEH7PVmeCQ==` to unchanged checksum `sha256:1e35b1e0a06a53645815c3f0b1f663b8b1923ae39bf901e72dba1e4b1536ec88`, producing bot commit `124022a91ac26ef453d60dc87ab51e057e48cfac` / tree `b244788bdcf8de163bc018e0972005b160ec656b`. GitHub again marked the bot-authored head workflows `action_required`; the following evidence-only human commit changes no signed payload byte and exists solely to trigger protected CI against this exact signed identity.

Protected run `32533566806` against signed payload `124022a91ac26ef453d60dc87ab51e057e48cfac` passed signature verification plus immutable core/tag/tree, exact-version alias rejection, and schema 1.6 on all three Python versions. All three capsule jobs then progressed beyond the former loopback denial and failed consistently while the capability-dropped Bubblewrap traversed the root Python process's staging path below runner-owned `$RUNNER_TEMP`: `bwrap: Can't find source path .../rootfs: Permission denied`. Bubblewrap deliberately performs filesystem access without retained DAC-override authority, so the workflow now creates a unique root-owned staging directory with `tempfile.mkdtemp(..., dir="/tmp")` and no longer preserves `RUNNER_TEMP` into the elevated process. The step-local regression was authored first and failed `1 failed` because the workflow still used runner temp, then passed `1 passed`; the complete toolchain/workflow surface passed `71 passed`, followed by format, type/lint (`0` diagnostics; Pylint `10.00/10`), YAML, strict OpenSpec, and `git diff --check`. This changes no signed module payload byte or signature. Task 4.3 remains incomplete pending protected proof from the corrected staging boundary.

Protected run `32533849894` passed the same signature/core/schema gates and proved that root-owned `/tmp` staging clears the traversal failure and completes offline installation. Final verification then reported the exact signed entry count and regular-byte total on every ABI but a different manifest digest (for cp312: expected `20583` entries / `889139510` bytes / `sha256:2335bf98d393cf99b544df7abc35c7651acff55d0b83e1f8b65df2a4db3cb0ba`, observed the same count/bytes with `sha256:b86a739bbf38b1b8c1344cee5711d8804f1364c86c3d1dd726a578d4629261cb`). This isolates the drift to canonical metadata such as modes produced when pip runs as UID 0 rather than payload membership or byte volume. The next boundary therefore keeps only Bubblewrap namespace setup elevated and explicitly runs both offline pip and final capsule boot as the original positive non-root sudo caller UID/GID, after chowning only the empty analyzer target; `--cap-drop ALL` remains mandatory. The explicit API/workflow regressions were authored first and failed `2 failed`, then passed `2 passed`, and a materialization propagation assertion binds the identity through the public call. A further bool/non-positive/non-root validation regression exposed boolean UID acceptance (`1 failed`, `2 passed`) before the API was made fail-closed; all three invalid cases then passed. The complete focused surface passed `75 passed`; format, type/lint (`0` diagnostics; Pylint `10.00/10`), YAML, bundle imports, strict OpenSpec, checksum/version verification, and `28 passed` contracts passed; both smart and complete suites passed `1379 passed` with only the two third-party `lark` deprecations. Fresh isolated immutable-core `0.55.1` matrices again passed `3 passed` plus exact alias rejection on CPython `3.11.15`, `3.12.13`, and `3.13.14`. The canonical signer utility removed the stale signature and set the unsigned checksum to `sha256:86ce07991c14987fa85750c506188075e1c3d78ff591dc7a91f8605a2117e3dd`. Task 4.3 remains incomplete pending freshly signed protected proof.

The refreshed explicit range/full review bound base commit `c3eda08c732267dc3614130f5f36bcd473182d0b` / tree `0b1d0cc80b671c561ddadc3d50f395b1959d0dd7` to implementation commit `f3cac18ddb8e05064fedd99d8694c10b9fbfd45e` / tree `efbb47330f2f1046eb5402d01394d918cb388d36`, resolved exactly one merge base, and emitted schema 1.6 `range_candidate` with context digest `sha256:55066dba1b56878f097ed38ff08098bad6945bb07ce6c909dcd76ca26b99f45d`. It reported `0 findings (0 blocking)` and the required local-only `UNKNOWN` / exit `1` reason `snapshot_differential_execution_unavailable`. The original project-scoped module directory was restored after reviewing the current candidate.

Protected run `32535079040` against unsigned dev-target commit `c90c897a8e0d4718366ce3ab498cd9c9d6a1cb16` passed the dev-branch signature policy, immutable core/tag/tree, exact-version alias rejection, and schema 1.6 on CPython 3.11–3.13. All three offline installs then failed consistently after the child switched to the sudo caller UID/GID: `PermissionError: [Errno 13] Permission denied: '/opt/specfact/analyzers/annotated_types-0.8.0.dist-info'`. A root-created Bubblewrap user namespace does not map that namespace UID/GID back to the same host filesystem identity, so chowning the analyzer target cannot grant the intended write authority. The attempted explicit-child-identity design is rejected by this protected evidence.

The next diagnostic remains fail-closed: the workflow uses the already required `--cap-drop ALL` root child, records only canonical manifest/subroot digests and analyzer mode counts plus three deterministic mode-normalization candidate digests, and then invokes the original strict verifier. Its strengthened workflow selector was authored first and failed `1 failed` because the diagnostic and removal of the invalid child-identity arguments were absent; after the harness-only edit it passed `1 passed`, workflow YAML parsing passed, and `git diff --check` passed. This diagnostic cannot convert a mismatch to PASS and changes no package payload byte. Task 4.3 remains incomplete pending the protected diagnostic output and a tests-first production correction grounded in that output.

Protected diagnostic run `32536174297` against signed checksum `sha256:86ce07991c14987fa85750c506188075e1c3d78ff591dc7a91f8605a2117e3dd` disproved the mode-drift hypothesis on every ABI. Each runtime produced the exact signed `analyzers`, `bin`, `bootstrap`, `lib`, `python`, and `wheelhouse` subroot digest; the analyzer mode inventory was identically `2528` directories at `0755`, `14412` regular files at `0644`, and `173` regular files at `0755`; and the `pip-standard`, `owner-writable`, and `umask-022` candidates were identical to the actual aggregate. Only the redundant aggregate digest differed despite exact membership/bytes and exact constituent subroots. The strengthened aggregate-order diagnostic selector was authored first and failed `1 failed` because no cross-subroot ordering search existed, then passed `1 passed` after adding a bounded six-subroot permutation check that still invokes the original strict verifier. Task 4.3 remains incomplete; no frozen identity has been edited and no verifier condition has been removed.

Protected diagnostic run `32536547169` found no matching concatenation among all `720` permutations of the six exact signed subroot entry blocks on CPython 3.12; the aggregate mismatch is therefore not top-level subroot ordering. The compressed-manifest artifact regression was authored first and failed `1 failed` on the absent artifact path/upload contract, then passed as part of the complete workflow regression file (`10 passed`) after the fail-closed verifier wrote only canonical path/type/mode/size/content-digest metadata for one-day diagnostic upload. Workflow YAML parsing and `git diff --check` passed. No file contents, credentials, or relaxed verifier result are exported.

### Pull-request review remediation — 2026-08-22

The authoritative GitHub review-thread inventory for PR #418 was refreshed from GraphQL rather than inferred from flat comments. Two already-corrected integrity threads were verified by the exact schema-1.6 missing-assurance and installed-payload mutation selectors (`4 passed` including three payload mutations), answered with evidence, and resolved. The next two tests-first batches covered OCI whiteout parent traversal, active conditional-analyzer UNKNOWN aggregation, complete pytest selection/execution controls and shaping hooks, shadow range enforcement, Ruff projection validity, Basedpyright snapshot-root includes, required context trees, blocking-input partitions, case-sensitive Ruff controls, score-derived ledger rewards, canonical worktree scope, context-declared source locks, transitive Ruff policy paths, shared INI/Pylint applicability, duplicate occurrence continuity, captured index evidence, temporary-root cleanup, repository identity propagation, and reversible Git path decoding.

The first exact regression command collected `18` parameterized cases and failed all `18` before production edits, then passed all `18`. The second exact command failed `8` of `8` before production edits, then passed all `8`. Two final decoding/origin regressions passed `2 passed`. The complete affected surface passed `499 passed`; both the complete and smart suites passed `1406 passed` with only the two existing third-party `lark` deprecations; and the contract gate passed `28 passed`. Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with Pylint `10.00/10`; manifest and CLI-contract validation passed; strict OpenSpec validation passed; and `git diff --check` passed. The two still-open architectural review findings—immutable range/index analyzer execution and analyzer availability for legacy local scopes—remain unresolved and are not claimed as fixed. Task 4.3 also remains blocked by the separately documented aggregate-manifest contract defect; no frozen lock/checkpoint identity was edited and no aggregate verifier condition was relaxed.

A second authoritative review-body audit found six still-valid behavioral gaps beyond the inline inventory. The exact selected command failed `6 failed` before production edits, proving missing CrossHair launch evidence, indistinguishable core-API failure, payload permission drift, tautological eligible/invoked manifests, undeclared Radon control-root mutation, and URL userinfo retention. After correction, the expanded selected surface passed `14 passed`. The same batch binds the full frozen pytest 9.0.3/pytest-cov option classes and all `52` hook dispositions, makes duplicate pairing independent of analyzer emission order, removes scope roots on exceptional resolution paths, verifies analyzer locks per ABI, pins every action used by the exact-core job to a resolved commit SHA, and replaces two misleading tests with semantic invariants. The payload-manifest projection now includes stable permission mode consistently at core handoff, re-verification, copy, and capsule composition; the legacy package checksum projection remains unchanged for its separately versioned compatibility path.

Final review-batch gates passed on 2026-08-22: complete and smart suites each `1421 passed` with only the same two third-party `lark` deprecations; contracts `28 passed`; type-check `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint with Pylint `10.00/10`; seven manifests plus registry validation; three CLI-contract files; bundle-import validation; strict OpenSpec; and `git diff --check`. The canonical signing utility removed the stale bot signature and refreshed the unsigned dev-PR checksum to `sha256:1287b21162af585fa114c7409bb93b39be4f2c127147eb10e5fa6273f01ef6ce`; checksum/version verification against `origin/dev` accepted all seven manifests with the repository-approved local missing-public-key allowance. A new canonical bot signature is required after push.

The signed-commit hook then exposed inherited repository-local Git variables: an isolated Git fixture could target the caller worktree when `GIT_DIR`/`GIT_WORK_TREE` leaked through the hook environment. The observed shared-config mutation was repaired by removing only the exact fixture values and restoring `core.bare=false`; signed commits were not bypassed. Production Git subprocesses and the fixture helper now strip all repository-local Git environment variables while preserving explicit captured-index overrides. The focused scope suite passed `105 passed` including the poisoned-environment regression, the refreshed complete suite passed `1422 passed`, contracts remained `28 passed`, type/lint and strict OpenSpec remained green, and shared config stayed non-bare without the fixture identity. The canonical signing utility then refreshed the final unsigned checksum to `sha256:d44812b627a108ebadab53fb46c946d8bab313226d0f24f0f7b603c4fa0e52dd`; all seven manifests passed checksum/version verification against `origin/dev` with the approved local missing-public-key allowance.

A subsequent authoritative review refresh found one new valid schema-1.6 contradiction: an untrusted serialized report could claim `assurance_status=PASS` while carrying analyzer evidence with `evidence_outcome=UNKNOWN`; deserialization marked `has_unknown_required_evidence` but retained PASS, exit `0`, and ledger reward eligibility. The exact regression `test_schema_1_6_json_pass_with_unknown_analyzer_is_demoted` was authored first and failed `1 failed` because the report remained PASS. The authoritative derivation now demotes PASS or NOT_APPLICABLE to UNKNOWN whenever required analyzer evidence is UNKNOWN, after which the existing projection produces verdict FAIL and exit `1` outside shadow mode. The focused findings/ledger surface passed `85 passed`; type-check and lint passed with Pylint `10.00/10`; the complete suite passed `1423 passed` with only the same two third-party `lark` deprecations; contracts passed `28 passed`; strict OpenSpec validation and `git diff --check` passed. The canonical signing utility removed the stale bot signature and refreshed the unsigned dev-PR checksum to `sha256:f955fe1293b31b3bc5b79bc8ae80ff5c34328774c348b4fd422b8a81e2e651d1`; checksum/version verification against `origin/dev` accepted all seven manifests with the repository-approved local missing-public-key allowance. A fresh canonical bot signature is required after push.

The next authoritative review refresh found two additional valid gaps. A target-tip Basedpyright `extends`/`baselineFile` graph was resolved but its reference paths were not included in the governed range closure, so a reference-only candidate change returned NOT_APPLICABLE. Separately, a serialized schema-1.6 PASS report could omit all or part of the frozen analyzer profile while retaining PASS and ledger reward eligibility. The exact three-case checkpoint failed `3 failed` before production edits: one reference-only range and empty/subset analyzer evidence. The range resolver now unions the complete successfully resolved Basedpyright reference graph with the Ruff closure before governed selection, policy bundling, and candidate-policy evidence. The frozen eight required plus two conditional `pr-range-v1` member tuples now have one shared definition used by report validation and runner aggregation; PASS requires each member exactly once, while missing, extra, or duplicate membership demotes to UNKNOWN before legacy verdict/exit projection. The exact checkpoint then passed `3 passed`; the complete affected findings/scope/runner/ledger surface passed `338 passed`; type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with Pylint `10.00/10`; the complete suite passed `1426 passed` with only the same two third-party `lark` deprecations; contracts passed `28 passed`; strict OpenSpec validation and `git diff --check` passed. The signed-commit review then identified one new Radon complexity blocker in the authoritative derivation; extracting the closed-profile completeness predicate removed it without changing behavior, and the exact three schema regressions plus type/lint gates remained green. The canonical signing utility removed the stale bot signature and refreshed the unsigned dev-PR checksum to `sha256:cb8ae16b95007be7a09d766dde829174a9494886f846cd37c3c4678f7aa58a06`; checksum/version verification accepted all seven manifests. The two capsule-execution P1 review threads remain valid and unresolved; this batch does not claim that missing execution layer is fixed.

The following automated review batch exposed two further fail-closed gaps. A complete ten-member schema-1.6 evidence array could omit `evidence_outcome` or supply an unrecognized value and still retain aggregate PASS; each immutable index request also left its copied `index` and `sharedindex.*` root under the system temporary directory. The exact three-case regression command failed `3 failed` before production edits: both untrusted-outcome variants remained PASS and the capture root remained present. Report validation now accepts a passing profile only when every exact member carries PASS or NOT_APPLICABLE, marks missing/unrecognized outcomes as unknown evidence, and demotes contradictory PASS to UNKNOWN. Index capture now removes its owned root immediately after the captured tree, metadata, and snapshots have been derived, and also removes the root on capture failure. The exact command then passed `3 passed`; the affected findings/scope/commands/ledger surface passed `263 passed`; type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with Pylint `10.00/10`; the complete suite passed `1429 passed` with only the same two third-party `lark` deprecations; contracts passed `28 passed`; strict OpenSpec validation and `git diff --check` passed. The full staged pre-commit pipeline against immutable core v0.55.1 passed command/docs/accountability, requirements, bundle, lint, contract, and SpecFact review gates; the review reported `155` retained advisory findings, `0` blocking findings, and `0` introduced differential findings, matching the already documented frozen-owner/contract-coverage/local-runtime exceptions. The approved non-main signing hook removed the stale signature, incremented the module to `0.49.3`, and refreshed the unsigned dev-PR checksum to `sha256:a8dec2e90e2bdd366549077452bc7f82d899e633be39419e9e92686e5d924c54`; all seven manifests passed checksum/version verification. The two capsule-execution P1 threads and the aggregate-manifest protected-runtime blocker remain unresolved and are not weakened by this review remediation.

The next review batch identified two selector-completeness defects. A governed test-shaped path outside configured `testpaths` was incorrectly frozen as `test_support`, and a newly added extant `test_candidate` with empty base and head selector inventories bypassed reconciliation. The two exact regressions were authored first and failed `2 failed`: the role was `test_support` instead of `test_candidate_outside_root`, and the empty new candidate returned PASS instead of UNKNOWN. The classifier now preserves the contract's outside-root candidate role whenever the frozen pytest filename matcher recognizes the path, while in-root nonmatching files remain support. Candidate reconciliation now requires every extant head candidate to contribute at least one selector regardless of whether a baseline selector existed. The unchanged exact command then passed `2 passed`, and the complete runner unit surface passed `147 passed`. Final batch gates against immutable core v0.55.1 passed: complete and smart suites each `1431 passed` with only the same two third-party `lark` deprecations; contracts `28 passed`; type-check `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint with Pylint `10.00/10`; seven manifests plus registry validation; three CLI-contract files; bundle-import validation; strict OpenSpec; and `git diff --check`. The architectural capsule-execution threads remain open and are not claimed as fixed by this bounded remediation.

### Capsule execution and differential review remediation — 2026-08-22

The two unresolved P1 threads were revalidated against the signed PR head and found valid: immutable `index`/`range` scopes returned before any analyzer execution, while fresh marketplace installs no longer carried host analyzers for legacy scopes. Four exact architecture regressions were authored first and failed `4 failed` because the command gateway, per-member capsule executor, same-descriptor Bubblewrap launch, and immutable base/head execution APIs did not exist. Production now routes immutable scopes through base and head capsule runs, launches every active profile member in a fresh `--unshare-all`/`--cap-drop ALL` sandbox, validates and executes the Bubblewrap binary from the same open descriptor, and routes legacy scopes through the capsule gateway. A tightly bounded macOS source-checkout compatibility branch remains only for non-immutable developer scopes; marketplace/user installs and every immutable scope fail closed as schema 1.6 UNKNOWN when the Linux capsule cannot run. The exact architecture selectors then passed `4 passed` and the affected command/runner/sandbox/toolchain surface passed `307 passed` at that checkpoint.

Protected pre-release execution does not borrow provenance from an older installed release. Two candidate-source verification selectors first failed `2 failed` because no source-payload verifier existed, then passed `2 passed` after the verifier bound full path/type/mode/content manifest bytes to an explicitly non-official `verified-candidate-module-payload-v1` identity. The protected PR selector reconstructs module bytes from the verified Git commit/tree rather than from a live directory that may contain generated files, binds repository/workflow/run/job/module-package/payload identities, and refuses installed-release fallback whenever GitHub Actions candidate context is invalid. Exact regressions also prove that immutable scopes never enter the developer host-compatibility branch.

The next authoritative GraphQL refresh found two additional valid comments. The protected diagnostic artifact used undefined `matrix.python`, and the first immutable implementation combined only per-analyzer outcomes rather than invoking the accepted finding-level differential classifier. The exact three-case checkpoint failed `3 failed`: two classifier integration APIs were absent and the workflow lacked `matrix.python-version`. The corrected workflow emits ABI-unique artifact names. Each analyzer now passes canonical base/head findings, immutable source bytes, exact rename facts, and added/deleted paths through `finding-multiset-v1`; schema 1.6 evidence includes the differential digest and fixed/introduced/unchanged/unknown counts, introduced or retained blockers remain FAIL, fixed baseline findings are retained as non-blocking lifecycle evidence, and mapping or analyzer/result inconsistencies are UNKNOWN. The unchanged exact command then passed `3 passed`; the complete affected C14/workflow surface passed `324 passed`.

Fresh post-remediation gates passed against immutable core v0.55.1: format; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint with Pylint `10.00/10`; seven manifests plus registry validation; three CLI-contract files; bundle-import validation; strict OpenSpec; `git diff --check`; and `28 passed` contract tests. Both smart and complete test gates passed all `1445` tests with only the two existing third-party `lark` deprecations. Fresh isolated Python `3.11.15`, `3.12.13`, and `3.13.14` environments each force-reinstalled core `0.55.1` from detached commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337` and passed the same three schema/handoff/candidate-identity selectors (`3 passed` per interpreter). Every environment reconfirmed that `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, and ordinary `==0.55.1` admits that untested alias.

These local results do not claim protected Linux or publication authority. Task 4.3 remains blocked by the separately isolated frozen aggregate-manifest defect: all six signed subroots, exact membership, and exact bytes match, but the redundant aggregate digest does not. No lock/checkpoint/verifier identity has been edited or relaxed. The PR review threads remain open until this implementation is signed, pushed, and reverified on the authoritative head.

The first signed-commit attempt was correctly stopped by the changed-line review gate. It identified capsule-request complexity plus two host-review type-inference errors, while the targeted-test error exposed that the new protected-candidate Git fixture inherited hook-local `GIT_DIR`/`GIT_WORK_TREE` and again flipped the shared repository to bare while overwriting its local signer identity with the fixture identity. The repository state was restored to `core.bare=false` and the real history-backed signer identity. A poisoned-environment regression then failed `1 failed` before both fixture and protected-candidate Git subprocesses adopted the same closed local-environment override set as scope resolution, and passed under the exact signed-hook environment afterward without changing shared state. Helper extraction removed the complexity blocker, explicit evidence/ambiguity types removed both inference errors, and the complete command/runner surface passed `225 passed` with type-check and Ruff clean. The repeated pre-commit review then reported `236` retained advisory findings, `0` errors, and no changed-line blockers; the contract hook passed `28 passed`.

The canonical non-main signer then advanced `specfact-code-review` from `0.49.4` to `0.49.5`, removed the stale bot signature, and set the final unsigned filesystem checksum to `sha256:6c0a3e8c4777278047fddaa490b98db3abf34dff57abefed58adc5fcea12b2ce`. The formal filesystem/version verifier accepted all seven manifests with the repository-approved local missing-public-key allowance. A new canonical bot signature is required after push; no registry archive, sidecar, or signed publication output was hand-edited.

### Signed-head policy/runtime/pytest review remediation — 2026-08-22

After the four preceding architecture/differential threads were answered and resolved, the authoritative GraphQL refresh on signed head `0288a8b73c9455766b9964d6ef8263ada1af8c7f` returned three new P1 findings. All three reproduced against the actual capsule path. The exact selectors `test_capsule_member_passes_explicit_target_policy_to_adapter`, `test_immutable_review_reuses_authenticated_project_runtime_for_both_snapshots`, and `test_immutable_range_reconciles_removed_pytest_selector_before_execution` failed `3 failed` before production edits: adapter argv was absent, both snapshots received no project-runtime root, and analyzer execution began without selector reconciliation.

The sealed member request now carries controller-generated explicit adapter argv; Ruff, Basedpyright, and Pylint consume only those explicit projections, and configured Semgrep passes receive the mounted target-tip bundle rather than snapshot discovery. The immutable runner validates and materializes a supplied `project-runtime-layer-v1`, re-verifies manifest/config/layer and extracted root/distribution identities, binds its identity into capsule composition, and exposes the identical read-only root only to the closed import-capable member set for both snapshots. Before analyzer launch, base/head complete pytest inventories are derived from the shared target-tip policy, changed candidates must remain collectable, and removed selectors fail closed. Module-level `__test__ = False` therefore produces `uncollected_changed_test` UNKNOWN even when a sibling test would otherwise pass.

The unchanged three-selector command then passed `3 passed`. Added integration proofs also passed: generated target-tip policy paths map only to `/opt/specfact/config/*`, and an identity-keyed project-runtime cache is reused only after full root-manifest and exact distribution-set re-verification. The expanded runtime/adapter/workflow surface passed `654 passed`; focused type-check reported `0 errors, 0 warnings, 0 notes`; Ruff passed; and no protected authority, archive, core edit, or frozen aggregate identity is claimed by this remediation. Final full gates and canonical re-signing remain pending.

Fresh final gates then passed: smart and complete suites each `1450 passed` with only the same two third-party `lark` deprecations; contracts `28 passed`; repository-wide format, type-check, Ruff, and Pylint with `0 errors, 0 warnings, 0 notes` and Pylint `10.00/10`; seven manifests plus registry validation; three CLI-contract files; bundle-import validation; strict C14 OpenSpec; and `git diff --check`. Isolated Python `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded immutable core `0.55.1` and the current candidate module, passed the exact schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed that `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, while ordinary `==0.55.1` admits the untested local alias. The canonical non-main checksum/version hook and bot signature remain required after the human commit.

The first staged SpecFact review exposed four clean-code errors and two changed-line blockers in the newly connected orchestration. Helper extraction and an immutable `CapsuleMemberExecutionRequest` removed the complexity and parameter-count regressions without changing behavior; the complete runner/toolchain unit surface remained `231 passed`. The repeated full pre-commit pipeline passed with `201` retained advisory findings, `0` errors, and no changed-line blockers; contracts remained `28 passed`, type/lint remained clean, and the approved non-main signer advanced the module to `0.49.6`, removed the stale signature, and set unsigned checksum `sha256:00ac4f5abad1978e5468465292c179146ec1ab04375d87046843d9bba368eee8`. A fresh canonical bot signature is required after push.

### Complete pytest, sealed policy, runtime-lock, and Git-path review remediation — 2026-08-22

Revalidation before resolving the targeted-pytest thread found that static base/head inventory reconciliation did not reach the actual capsule member: execution still called the legacy source-to-test mapper. The member regression `test_capsule_targeted_pytest_executes_supplied_complete_inventory` failed before the member accepted controller-owned inventory mode. A second controller-level regression, `test_immutable_review_binds_each_complete_pytest_inventory_to_its_snapshot`, then failed with a missing `targeted-pytest-coverage` binding, and `test_run_complete_pytest_inventory_preserves_exact_node_ids` failed because exact node-ID execution did not exist. Production now binds distinct reconciled base/head inventories, marks that mode explicitly in the sealed request so an empty inventory cannot fall back to legacy mapping, validates repository-relative selector paths, executes exact node IDs, and measures the immutable candidate snapshot rather than the analyzer package. The three exact selectors passed.

The next authoritative GraphQL refresh returned three further valid comments. Exact regressions first failed `4 failed`: schema-valid `project-runtime-layer-v1` source-lock records were rejected as unsafe context, local Git discovery lost quoted/non-ASCII and newline-containing Python paths, and pytest/coverage execution omitted sealed target-tip projection argv. The context validator now accepts the canonical sorted record shape (`path`, `blob_sha`, `content_sha256`) consumed by the materializer and projects only its validated paths into governed selection. Local worktree/full discovery uses NUL-delimited Git output with surrogate-safe decoding. Each immutable side now receives generated read-only pytest and Coverage configs through explicit `-c`, `--rootdir`, and `--cov-config`, with target test roots rebased to `/opt/specfact/snapshot`, controller-owned writable paths redirected below the private temporary root, custom Coverage exclusions/plugins rejected, and exact selectors appended after a controller separator. The unchanged exact command then passed all `4` selectors; the combined seven review regressions passed `7 passed`.

The complete code-review subsystem passed `617 passed`. The full repository test gate and smart-test gate each passed `1456 passed` with only the two existing third-party `lark` deprecations; contracts passed `28 passed`. Repository-wide format, Ruff, Basedpyright, and Pylint passed with `0 errors, 0 warnings, 0 notes` and Pylint `10.00/10`. Manifest/registry validation, three CLI-contract files, bundle imports, strict OpenSpec, and `git diff --check` also passed.

The first staged SpecFact review correctly stopped on two new Radon changed-line blockers: complexity `17` in the sealed capsule-request loader and `18` in project-runtime context validation. Extracting path/adapter validation and source-lock-record validation removed both without changing the contract; the complete runner/scope surface then passed `273 passed`. The repeated canonical pre-commit pipeline reported `236` retained warnings/information findings, `0` errors, and no changed-line blockers; contracts remained `28 passed`. The approved non-main signer advanced `specfact-code-review` from `0.49.6` to `0.49.7`, removed the stale bot signature, and set unsigned checksum `sha256:af0a5fbe0e94d426043898e8d440c315fa7a2b60ec8d544d1fd66ef63b0f2f24`. Exact-core CPython 3.11–3.13 smoke and a fresh canonical bot signature remain required after the human commit is pushed.

Fresh isolated Python `3.11.15`, `3.12.13`, and `3.13.14` environments each installed core `0.55.1` from detached commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337`, linked the current candidate module, and passed the exact schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter). Every environment reconfirmed that `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, and ordinary `==0.55.1` admits the untested local alias. A fresh canonical bot signature remains required after push.

### Signed-head project-runtime import and complete-candidate remediation — 2026-08-22

The next signed-head Codex review returned two new P1 findings. Both reproduced before production edits. `test_post_base_bootstrap_and_composite_identity_bind_copied_payload` failed because the sealed `-I -S` bootstrap inserted only the analyzer and built-in roots, leaving the authenticated `/opt/specfact/project-runtime/site-packages` mount outside `sys.path`. `test_unchanged_test_candidate_without_selector_is_unknown` failed because complete-suite planning checked only changed candidates, allowing an unchanged empty `tests/test_placeholder.py` to pass during a production-only range. The exact red command reported `2 failed`, `0 passed`.

The sealed bootstrap now adds the canonical project-runtime site-packages path after its two capsule-owned roots. That path is populated only when the sandbox controller mounts a fully materialized and reverified project runtime for one of the closed import-capable members; non-importing members still receive no project-runtime mount, and snapshot/project imports remain behind capsule-reserved imports. Complete pytest planning now enumerates every extant policy-matching candidate in each immutable snapshot and returns `uncollected_test_candidate` UNKNOWN when an unchanged candidate contributes no selector, while retaining the more specific `uncollected_changed_test` reason for changed candidates.

The unchanged two-selector command then passed, the adjacent five-selector diagnostic set passed, and the complete Code Review subsystem passed `829 passed`. Fresh repository gates then passed: strict OpenSpec; seven manifests plus `registry/index.json`; three CLI-contract scenario files; bundle imports; formatting and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff; Pylint `10.00/10`; contracts `28 passed`; and both smart and complete suites at `1457 passed`. The complete suites retained only the two existing third-party `lark` deprecation warnings.

Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments then installed core `0.55.1` from detached commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337`, linked the current candidate module, and passed the exact schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter). Each environment independently reconfirmed that strict `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, and ordinary `==0.55.1` admits that untested local alias. No earlier gate evidence is reused for this review batch.

The canonical staged pre-commit pipeline then passed all signature/version, format, bundle, type/lint, requirements-evidence, command/docs, changed-line review, and contract stages. The fresh changed-line report was `PASS_WITH_ADVISORY` with `203` retained findings, `0` errors, and no changed-line blocker; contracts again passed `28 passed`. The repository-approved non-main signer advanced `specfact-code-review` from `0.49.7` to `0.49.8`, removed the stale signature, and set the unsigned filesystem checksum to `sha256:ff8cdf85f23cfcbaf9e19a5f55a95db0f991947be08c6c6cd573847aa3b176d9`. A human commit, fresh exact-range/full report, and canonical bot signature remain required.

### Protected Linux legacy-source compatibility correction — 2026-08-22

Protected run `32550416328` passed exact core tag/commit/tree verification, strict local-version rejection, the schema 1.6 exact-core selector, and module-signature verification. Its complete Python 3.11 quality suite then exposed five legacy explicit-file CLI tests returning UNKNOWN because the Linux source checkout had an empty verified capsule cache. The existing compatibility branch was intentionally limited to unsupported controller platforms, so the same tests had passed locally on macOS and had not exercised this Linux boundary.

The exact regression `test_source_checkout_legacy_scope_explicitly_opts_into_linux_cache_miss_compatibility` was authored first and failed `1 failed`. Production now permits host analyzers only when all of these facts hold simultaneously: the scope is the existing non-immutable legacy gateway, the module is running from this development source checkout, the failure is exactly `oci_acquisition_failed:verified cache entry is missing`, and `SPECFACT_CODE_REVIEW_DEV_HOST_COMPAT=1` is explicitly set. The five legacy CLI tests set that test-only opt-in. A companion regression proves the same Linux cache miss remains schema 1.6 UNKNOWN without the opt-in; the existing marketplace and immutable-scope regressions continue to prove they never use host analyzers. The exact nine-selector legacy/marketplace/immutable set passed, followed by the complete Code Review subsystem at `831 passed`.

The protected capsule job separately confirmed the frozen aggregate-manifest blocker on Python 3.11, 3.12, and 3.13. For CPython 3.12, all six signed subroots matched exactly (`analyzers`, `bin`, `bootstrap`, `lib`, `python`, `wheelhouse`), with `20583` entries and `889139510` regular-file bytes, but the actual aggregate digest `sha256:b86a739bbf38b1b8c1344cee5711d8804f1364c86c3d1dd726a578d4629261cb` differed from frozen expected `sha256:2335bf98d393cf99b544df7abc35c7651acff55d0b83e1f8b65df2a4db3cb0ba`. No signed lock/checkpoint/verifier identity is changed or relaxed; amending that redundant aggregate requires explicit user approval.

Fresh local gates after the compatibility correction passed: strict OpenSpec; seven manifests plus registry; three CLI-contract scenario files; bundle imports; formatting and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff; Pylint `10.00/10`; contracts `28 passed`; and both smart and complete suites at `1459 passed` with only the same two third-party `lark` deprecations. The exact immutable core v0.55.1 smoke was rerun against the updated candidate bytes on CPython `3.11.15`, `3.12.13`, and `3.13.14`; all three schema/handoff/candidate-identity selectors passed per interpreter, and strict `===0.55.1` versus local-alias rejection was reconfirmed.

The canonical staged pipeline then passed with a fresh changed-line report of `PASS_WITH_ADVISORY`, `92` retained warnings/information findings, `0` errors, and no changed-line blocker; contracts again passed `28 passed`. The approved non-main signer advanced the module from `0.49.8` to `0.49.9`, removed the stale signature, and set unsigned filesystem checksum `sha256:ea72b1305643a02f8ee45bb6a1bb0b22abeb6312425fa3c8ea4b78882136a299`. A signed human commit, fresh exact-range/full reports, protected Linux rerun, and canonical bot signature remain required.

### Review batch — sealed Semgrep activation, inherited unittest selectors, and empty snapshots

Three new PR review findings were treated as fresh tests-first input on 2026-08-22. The exact selectors `test_sealed_target_bugs_policy_activates_semgrep_bugs_without_bug_hunt`, `test_complete_suite_collects_inherited_unittest_selector`, and `test_empty_capsule_snapshot_marks_every_member_not_applicable` were authored before production edits and failed together (`3 failed`). They proved that sealed target policy did not activate `semgrep-bugs` unless the legacy `--bug-hunt` option was also set, same-module inherited `unittest.TestCase` methods were absent from the complete selector inventory, and an empty Python snapshot attempted to construct analyzer requests instead of classifying every closed-profile member as NOT_APPLICABLE.

Production now activates `semgrep-bugs` from the sealed member-policy binding while retaining the legacy option, binds that member only when the authorized `.semgrep/bugs.yaml` exists, derives inherited same-module test methods conservatively without executing candidate code, and returns `empty_snapshot_input` NOT_APPLICABLE evidence for every profile member before analyzer launch when no Python input exists. The three exact selectors passed, followed by the complete Code Review unit subsystem (`834 passed`).

Fresh post-review gates passed: format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint `10.00/10`; seven manifests plus registry; three CLI-contract scenario files; bundle imports; strict C14 OpenSpec; contracts `28 passed`; and smart plus complete suites at `1462 passed` each with only the two existing third-party `lark` deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each linked the current candidate, loaded immutable core `0.55.1` from commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337`, and passed the three schema/handoff/candidate-identity selectors (`3 passed` per interpreter); strict `===0.55.1` accepted `0.55.1`, rejected `0.55.1+vendor`, and ordinary `==0.55.1` admitted the untested alias. Canonical unsigned PR metadata advanced the changed module to `0.49.10` with filesystem checksum `sha256:1500d98dd54701ea72ca4d1d4cad0be5c57ff2554c2dcb567a07c63e7ef7804b`; all seven filesystem checksum/version checks passed. A fresh canonical bot signature remains required after the human commit is pushed.

The canonical staged pipeline passed when bound to the exact detached core checkout: signature/version, format, YAML, bundle, type/lint, documentation accountability, Requirements evidence, command/docs, changed-line review, and contracts were all green. The fresh changed-line report was `PASS_WITH_ADVISORY` with `91` retained findings, `0` errors, and no changed-line blocker; contracts again passed `28 passed`.

### Review batch — sealed custom test-root coverage roles

An authoritative refresh after the preceding push found one further P1 review thread. The exact regression `test_complete_tdd_gate_excludes_sealed_custom_test_root_from_coverage` was authored first and failed `1 failed`: with target `testpaths = integration`, both `integration/test_app.py` and `integration/helpers.py` were treated as production coverage inputs because the immutable execution path still recognized only a literal `tests` path component.

Immutable pytest execution now reads the already sealed, manifest-bound projected pytest configuration, requires its test roots to be absolute descendants of `/opt/specfact/snapshot`, and excludes every Python file under those roots from production coverage inputs. It does not consult candidate-discovered configuration or weaken the complete selector inventory. The exact new regression and the preceding three review regressions passed together (`4 passed`), followed by the complete Code Review subsystem (`835 passed`).

Fresh gates passed on the updated payload: format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint `10.00/10`; seven manifests plus registry; three CLI-contract scenario files; bundle imports; strict C14 OpenSpec; contracts `28 passed`; and smart plus complete suites at `1463 passed` each with only the two existing third-party `lark` deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each linked the current candidate, loaded immutable core `0.55.1` from commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337`, passed the three schema/handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-alias rejection. The canonical unsigned PR metadata advanced the changed module to `0.49.11` with filesystem checksum `sha256:916f6b224f1684c1a19856b9ea82756219ac7de3e9a3701ea731d2e91a461dfb`; a new bot signature remains required after the next human commit.

The exact-core-bound staged pipeline also passed signature/version, format, YAML, bundle, type/lint, documentation accountability, Requirements evidence, command/docs, changed-line review, and contracts. Its fresh changed-line result was `PASS_WITH_ADVISORY` with `92` retained findings, `0` errors, and no changed-line blocker; contracts passed `28 passed`.

### Review batch — directly collected unittest base classes

The next authoritative thread refresh found that pinned pytest collects a direct `unittest.TestCase` subclass even when its class name does not match `python_classes`, while the inherited-selector correction emitted only the matching child. The existing inheritance regression was strengthened before production edits to require both `SharedCase::test_inherited` and `TestChild::test_inherited`; it failed `1 failed` with only the child selector present.

Complete selector derivation now recognizes direct and same-module-derived `unittest.TestCase` classes independently of `python_classes`, including `import unittest as ...` and `from unittest import TestCase as ...` aliases, while retaining explicit override handling, deduplication, and cycle guards. The exact regression passed, the active four-review regression set passed `4 passed`, and the complete Code Review subsystem passed `835 passed`.

Fresh gates passed again: format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff/Pylint `10.00/10`; seven manifests plus registry; three CLI-contract files; bundle imports; strict C14 OpenSpec; contracts `28 passed`; and smart plus complete suites at `1463 passed` each with only the two existing third-party `lark` deprecations. The current candidate passed the three exact schema/handoff/candidate-identity selectors on isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` against immutable core `0.55.1`, with strict alias rejection reconfirmed. Canonical unsigned PR metadata advanced the module to `0.49.12` with filesystem checksum `sha256:e69266c64a3eb4e28ae320a4ace9a6b0dd205dc44c77ccb6e63d8e9ec1f14fbf`; a new bot signature remains required after the next signed human commit.

The exact-core-bound staged pipeline passed again, including signature/version, documentation and Requirements evidence, changed-line review, and contracts. Its changed-line result remained `PASS_WITH_ADVISORY` with `92` retained findings, `0` errors, and no changed-line blocker; contracts passed `28 passed`.

### Review batch — production Semgrep policy/path enforcement and pytest path patterns

The next authoritative monitor cycle found two P1 findings. Five exact regression cases were authored before production edits and failed together (`5 failed`): both sealed Semgrep passes launched a rule pack containing rule-local `paths.exclude`; both ignored an output record that marked the sole eligible input skipped; and complete pytest planning omitted `tests/unit/check_case.py` when `python_files` combined `test_*.py` with `tests/**/check_*.py`.

For sealed bundle execution, the clean and bugs Semgrep production paths now parse and validate every selected YAML rule pack before launch, reject rule-local include/exclude targeting, retain the complete JSON payload, require scanned/skipped path evidence, canonicalize its paths against the exact eligible input manifest, and fail closed on a skip, duplicate, omission, or extra path. Legacy non-immutable host execution retains its compatibility boundary. Pytest selector, candidate, changed-candidate, and input-role classification now use the existing pinned relative-path matcher for every `python_files` pattern rather than basename-only matching.

The unchanged five selectors passed, followed by the complete affected Semgrep/runner surface (`225 passed`) and complete Code Review subsystem (`840 passed`). Fresh gates passed: format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff/Pylint `10.00/10`; seven manifests plus registry; three CLI-contract files; bundle imports; strict C14 OpenSpec; contracts `28 passed`; and smart plus complete suites at `1468 passed` each with only the two existing third-party `lark` deprecations. The exact schema/handoff/candidate-identity smoke passed `3 passed` per isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` against immutable core `0.55.1`, with strict local-alias rejection reconfirmed. Canonical unsigned PR metadata advanced the module to `0.49.13` with filesystem checksum `sha256:119f8ff2eb0930c75ce7af08014a3dd101f05b2c0ffb9fd7492f1025bbe0c042`; a new bot signature remains required after the next signed human commit.

The first staged review correctly blocked one dead compatibility wrapper left by the payload refactor (`reportUnusedFunction`). It was removed without suppression, the affected `225` tests and type gate passed again, and the checksum above was refreshed. The repeated exact-core-bound staged pipeline passed with `107` retained warnings/information findings, `0` errors, no changed-line blocker, and `28 passed` contracts.

### Review batch — complete pytest outcomes, unittest loader naming, and Radon full results

The next authoritative PR monitor cycle found three P1 threads: `PRRT_kwDORVEFbs6bWfg2`, `PRRT_kwDORVEFbs6bWfg7`, and `PRRT_kwDORVEFbs6bWfg8`. Four exact regressions were authored before production edits and failed together (`4 failed`): a target `python_functions=check_*` policy omitted a real `unittest.TestCase.test_failure`; a non-strict XPASS could reach coverage without observer/JUnit reconciliation; immutable policy bindings omitted the Radon full-result contract; and the Radon adapter ran only CC rather than CC, MI, raw, and Halstead. The unchanged four selectors then passed (`4 passed`), and an additional real-subprocess regression proved that pytest's actual non-strict XPASS report becomes `TEST_OUTCOME_NOT_PASS` before coverage can pass.

Complete pytest execution now records every setup/call/teardown report through the controller-owned observer, writes controller-owned JUnit and coverage artifacts, reconstructs exact JUnit node identities, reconciles observer outcome, JUnit outcome, and process exit, and treats skip, XFAIL, XPASS, and failure as non-PASS. Native pytest functions retain the sealed target `python_functions` patterns, while recognized `unittest.TestCase` methods use the sealed loader prefix `test*`, including same-module inherited methods. Immutable bindings now activate `radon-full-result-v1`; the adapter runs pinned Radon 6.0.1 CC A..F, MI A..C, raw, and Halstead JSON passes with empty exclude/ignore controls, absolute eligible paths, no output redirection, exact per-pass input reconciliation, and unchanged CC/KISS findings. A real pinned-Radon smoke executed all four passes successfully.

The first changed-line review exposed two new complexity blockers (`101 findings`, `2 blocking`). Single-purpose dispatch and outcome/JUnit helpers removed both without suppression; a repeated review then returned `PASS_WITH_ADVISORY`, `95 findings`, and `0 blocking`. The retained advisories are the previously documented frozen-owner, contract-coverage, and local CrossHair/coverage exceptions rather than new changed-line blockers.

Fresh final gates passed on 2026-08-22: affected runner/Radon tests `189 passed`; complete Code Review unit subsystem `845 passed`; smart and complete suites `1473 passed` each with only the two existing third-party `lark` deprecations; type-check `0 errors, 0 warnings, 0 notes`; Ruff/Pylint `10.00/10`; seven manifests plus registry; three CLI-contract files; bundle imports; strict C14 OpenSpec; and contracts `28 passed`. The exact schema/handoff/candidate-identity smoke passed `3 passed` per isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` against immutable core `0.55.1`; strict `===0.55.1` local-alias rejection remained confirmed.

The canonical non-main signer advanced the changed module from `0.49.13` to `0.49.14`, removed the stale bot signature, and set filesystem checksum `sha256:488ae8d5cc99feffe75df49983d01dfd01d9480dd3824361f5ec3307e5399448`. Filesystem checksum/version verification accepted all seven manifests with exact `core_compatibility: ===0.55.1`. A new bot signature remains required after the signed human commit is pushed.

### Review batch — suppression integration, sealed pytest plugins, complete imported tests, and per-member inputs

The next authoritative PR monitor cycle found five valid P1 findings and one workflow request that conflicts with the accepted C14 dependency boundary. Three exact selectors were authored and collected before production edits: `test_immutable_range_blocks_introduced_suppression_even_when_analyzers_report_no_findings`, `test_targeted_pytest_disables_ambient_plugin_autoload_and_loads_controller_coverage`, and `test_complete_suite_collects_imported_pytest_and_unittest_objects`. They failed together (`3 failed`): range classification never called the frozen suppression-delta classifier, pytest inherited ambient entry-point plugins, and the selector inventory omitted imported pytest functions and `unittest.TestCase` classes. Production now applies the signed suppression catalog to each mapped active member before aggregate assurance, emits introduced suppression controls as blocking findings, preserves suppression uncertainty, sets `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`, explicitly loads controller-owned pytest-cov, and statically resolves same-snapshot imported test objects without executing candidate code outside the sandbox. The unchanged exact command passed `3 passed`; real subprocess coverage and non-strict-XPASS regressions also passed.

Two further exact selectors were then authored, collected, and failed (`2 failed`): `test_capsule_snapshot_does_not_launch_targeted_pytest_for_stub_only_input` proved the dispatcher ignored its frozen per-member applicability, and `test_capsule_contract_member_routes_static_and_crosshair_input_manifests` proved CrossHair received stubs plus sealed test/support roots. Capsule dispatch now consumes `classify_snapshot_input_kinds` before launch, marks targeted pytest NOT_APPLICABLE for stub-only snapshots, binds the sealed target-tip pytest roots into the contracts adapter, keeps the full `.py`/`.pyi` manifest for static contract scanning, and restricts CrossHair to runtime `.py` production inputs outside those roots. The unchanged two-selector command passed `2 passed`.

The request to add a module-side protected PR-context consumer to `.github/workflows/pr-orchestrator.yml` was verified against `tasks.md` and rejected without source edits: task 4.7 assigns that producer/consumer and enforcement adoption to the later paired core change after signed module handoff, while the closed allowlist explicitly restricts this modules workflow to immutable exact-core 0.55.1 smoke and forbids module-side protected-consumer logic. The evidence-backed thread reply records that boundary.

The complete affected runner/differential/contracts surface passed `285 passed`. Smart and complete repository suites each passed `1478 passed` with only the same two third-party `lark` deprecations; contracts passed `28 passed`. Format, type-check (`0 errors, 0 warnings, 0 notes`), Ruff, Pylint `10.00/10`, seven manifests plus registry, three CLI-contract files, bundle imports, strict C14 OpenSpec, and `git diff --check` passed. The first changed-line Code Review run found three new cyclomatic-complexity blockers (`CC19`, `CC19`, and `CC18`); focused dispatch and selector helper extraction removed them without suppression. The repeated review returned `PASS_WITH_ADVISORY`, `68` retained warnings/information findings, `0` errors, and `0` blocking findings.

The exact immutable core v0.55.1 smoke was rerun against the updated candidate bytes on isolated CPython `3.11.15`, `3.12.13`, and `3.13.14`; the schema-1.6 matrix, core-installed handoff derivation, and protected candidate-payload identity selectors passed `3 passed` per interpreter. Detached core commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` and tree `47984be5434d7ae65ed6908bf525a32053290337` were rechecked, and strict `===0.55.1` accepted `0.55.1`, rejected `0.55.1+vendor`, while ordinary `==0.55.1` admitted the untested alias.

The canonical non-main signer advanced `specfact-code-review` from `0.49.14` to `0.49.15`, removed the stale bot signature, and set unsigned filesystem checksum `sha256:ba91bffac6076a945812bb2c2785b61ab445d71c7086602d77d69f2cd95c0179`. A signed human commit, exact committed range/full reports, protected checks, authoritative thread replies, and a fresh canonical bot signature remain required. The separately isolated aggregate-manifest mismatch remains unresolved and no frozen lock/checkpoint/verifier identity has been edited or relaxed.

The exact-core-bound staged pipeline then passed signature/version preparation, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and contracts. Its staged review retained `102` warnings/information findings with `0` errors and no blocking regression; contracts again passed `28 passed`.

### Review batch — collection reconciliation, admitted pytest plugins, and index policy

The next authoritative PR refresh found three P1 findings. The exact selectors `test_pytest_outcome_reconciliation_rejects_missing_planned_selector`, `test_immutable_review_explicitly_loads_only_authenticated_project_pytest_plugins`, and `test_index_review_uses_head_policy_for_both_immutable_snapshots` were authored and collected before production edits, then failed together (`3 failed`). They proved that a repository `conftest.py` could remove a planned selector without invalidating otherwise consistent observer/JUnit/process evidence, descriptor-admitted fixture plugins were never explicitly loaded after ambient plugin autoload was disabled, and index assurance projected generated defaults because index resolutions have no range policy bundle.

Complete immutable pytest execution now passes its controller-planned selector inventory into outcome reconciliation and requires exact planned/observed identity and cardinality before PASS. Project-runtime descriptors are validated before materialization; only the exact validated `pytest11` entry-point modules are appended as explicit `-p` arguments for both immutable snapshots. Index assurance binds the immutable HEAD snapshot as the trusted policy source for both HEAD and captured-index execution, so staged pytest or Coverage configuration cannot authorize or suppress itself. The unchanged exact three-selector command passed (`3 passed`). The complete affected runner/scope/toolchain surface then passed `364 passed`; focused Ruff, formatting, Basedpyright (`0 errors, 0 warnings, 0 notes`), and `git diff --check` also passed. Repository-wide gates, changed-line review, canonical signing, PR replies/resolution, and another authoritative refresh remain pending for this batch.

Fresh repository-wide gates then passed: format; type-check with `0 errors, 0 warnings, 0 notes`; Ruff; Pylint `10.00/10`; seven manifests plus registry; three CLI-contract files; bundle imports; strict C14 OpenSpec; contracts `28 passed`; and independent smart plus complete suites at `1481 passed` each with only the two existing third-party Lark deprecations. The manual changed-line review reported `64` retained inherited warnings/information findings and `0` blockers. A small helper extraction kept planned-selector reconciliation from increasing the inherited function complexity; its exact selectors and focused static gates passed again. The canonical staged pipeline then reported `PASS_WITH_ADVISORY` with `100` retained warnings/information findings, `0` errors/blockers, and contracts `28 passed`.

The repository-approved scoped non-main signer advanced `specfact-code-review` from `0.49.15` to `0.49.16`, removed the stale bot signature, and set filesystem checksum `sha256:eeb182ea3d2a7b6ff134c2bc63efacb61c2b09aad181163fd80cf24869d61847` while preserving exact `core_compatibility: ===0.55.1`. Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded immutable core `0.55.1`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection. A signed human commit, exact committed range/full reports, canonical bot signature, PR replies/resolution, protected checks, and the next authoritative review refresh remain pending. The frozen aggregate-manifest mismatch is unchanged and remains outside this review remediation without explicit user approval.

### Review batch — parametrized outcomes, plugin preflight, reserved imports, and complete coverage

The next authoritative PR refresh found four valid P1 threads. Seven exact regressions were authored and collected before production edits. The unchanged selector command failed `6 failed, 1 passed`: a planned parametrized test could not reconcile with its concrete `[parameter]` node IDs; immutable coverage could omit a nonempty production package initializer; the reserved-import preflight scanned the project-runtime mount rather than its effective `site-packages` import root and used only three hard-coded names; the dedicated pytest-plugin capability preflight did not exist; and immutable snapshot execution continued after a simulated preflight mismatch. The unrelated-prefix regression already passed and preserved fail-closed selector boundaries.

Outcome reconciliation now maps each observed node ID to exactly one planned selector by exact identity or the exact `selector + "["` parametrization boundary, requires every planned selector to contribute an observed node, and rejects duplicate plans or unrelated prefixes. Complete immutable coverage disables the legacy project-policy initializer omission and exempts only genuinely empty `__init__.py`; legacy non-immutable behavior remains unchanged. `SnapshotInvocationContext` binds the capsule environment ID, loads the complete signed environment-specific reserved-prefix catalog, scans the active snapshot plus `/opt/specfact/project-runtime/site-packages`, and returns UNKNOWN if that catalog is unavailable. The new `targeted-pytest-plugin-preflight` member runs before either snapshot, mounts no snapshot, uses a private temp cwd, disables ambient discovery by construction, explicitly imports only authenticated `pytest11` modules, verifies project-runtime origin plus distribution/version/entry point, observes per-plugin parser/`addini` and hook registries, compares their canonical digests, rejects dynamic registry additions and forbidden hooks, and returns `pytest_plugin_capability_mismatch` UNKNOWN before snapshot execution. The project-runtime schema and checkpoint now require the raw `options`, `ini_fields`, and `hooks` manifests needed for observed equality.

The unchanged seven-selector command passed `7 passed`; a dedicated snapshot-free-member regression was then added, and the complete runner/sandbox/toolchain unit surface passed `283 passed`. Focused Ruff and `git diff --check` passed. Strict OpenSpec validation passed, and the exact checkpoint/toolchain/project-runtime/package authentication set passed `6 passed`.

On 2026-08-22 the user explicitly approved the previously isolated aggregate-manifest refresh. The bounded edit changed only `final_root_manifest.manifest_digest` and `two_storage_root_materialization.manifest_digest` for each supported ABI, plus their checkpoint mirrors: cp311 `sha256:d5b29b3f4737c634c17d15b8e020ef8cf50cf46aec3b6eaf0b9bdfaae4ea1d94` to `sha256:0ef5a7f31989788b1590910cc7f511710a0e9c891c4f9d67d7ea8aacd8a6ce83`; cp312 `sha256:2335bf98d393cf99b544df7abc35c7651acff55d0b83e1f8b65df2a4db3cb0ba` to `sha256:b86a739bbf38b1b8c1344cee5711d8804f1364c86c3d1dd726a578d4629261cb`; and cp313 `sha256:880bf086c7708e5ae2ca734ab483bc99a96f2f63ec49db36d5d8557bbd19deff` to `sha256:772ce20a7f392f00ad42a07d092b0826f5a6ba780ad69e43f8df610db744bb02`. A reverse-substitution byte comparison against `HEAD` proved that the lock changed only those six occurrences and the checkpoint changed only their twelve mirrors at that stage. Entry counts, byte totals, every subroot digest, verifier behavior, and immutable core compatibility remained unchanged. The refreshed canonical toolchain projection/resource digest is `sha256:65e686167a866116aeff86c522d9952d170ee1a1cb947941e2cad55a548afa9d`; the amended project-runtime contract digest is `sha256:006338a430d3b11be510fe10e2998245e00554ebe05fc628c2c4c0217f95a725`, with policy digest `sha256:7db31f7f667d90cabde2b524a38e78cf24c885495e781e2ef93acfab7683409f` and pytest-plugin capability contract digest `sha256:af2fa4cfe64f525250e154e5729038cdd12eb4c2f81c96b5c5545b57a900d32f`.

Fresh post-fix gates passed on 2026-08-22: canonical format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint `10.00/10`; seven manifests plus registry; three CLI-contract scenarios; bundle imports; strict C14 OpenSpec; `28 passed` contracts; and the complete suite at `1489 passed` with only the two existing third-party Lark deprecations. The smart-test gate completed against the same configured `pytest tests/` surface and its completion check passed. Exact-core smoke revalidated tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`; CPython `3.11.15`, `3.12.13`, and `3.13.14` each passed the three schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter) and reconfirmed that `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, while ordinary `==0.55.1` admits the untested alias. The canonical scoped signer advanced the bundle from `0.49.16` to `0.49.17`, removed the stale signature, and set unsigned filesystem checksum `sha256:0652a2c54240dcf0289e7d50968df45699cb50d7140249446d1cb6e102c68f98`; all seven filesystem checksum/version checks passed with the immutable core public key. Canonical bot signature, PR thread replies/resolution, and protected Linux matrix remain pending.

The first staged changed-line Code Review correctly rejected six new blockers: plugin preflight `CC28`, plugin descriptor validation `CC18`, and four direct Basedpyright argument/operator type errors. Focused typed manifest/catalog helpers removed the four type failures and reduced the remaining preflight orchestration, but the next review still rejected one `CC18` helper. Distribution identity, isolated plugin loading, hook observation, parser observation, and registry comparison were then extracted into single-purpose functions without changing the contract. The unchanged affected runner/sandbox/toolchain suite passed `283 passed`, type-check remained `0 errors, 0 warnings, 0 notes`, and the final changed-line review returned `PASS_WITH_ADVISORY` with `253` inherited warnings/information findings, `0` errors, and no changed-line blocker.

The GPG-signed human commit was created with fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. The committed explicit range review from synchronized `origin/dev` commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema 1.6 `range_preview`, `0 findings`, and the expected local-only `UNKNOWN` reason `basedpyright_config_ambiguous` because the repository has both root `pyrightconfig.json` and `[tool.basedpyright]`; C14 does not modify the out-of-allowlist root configuration. The committed explicit full review emitted schema 1.2 `FAIL` with `1226 findings` and `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS), matching the existing full-repository debt boundary rather than a changed-line regression. The changed-line report above remains the merge gate for this patch batch.

### Review batch — concrete collection, wildcard imports, and doctest modules

The next authoritative GraphQL refresh found three new P1 threads after the four preceding P1 threads had been answered with evidence and resolved. The exact selectors `test_partial_parametrized_deselection_is_unknown_from_real_pytest`, `test_complete_suite_fails_closed_for_wildcard_imported_tests`, and `test_sealed_pytest_policy_rejects_doctest_modules_collection` were authored and collected before production edits. They failed together (`3 failed`): a repository collection hook could remove one concrete parametrized instance while the surviving instance satisfied the planned parent selector; a wildcard import could contribute a failing pytest object that the static inventory omitted while a local smoke test made the file appear collected; and admitted `--doctest-modules` policy could collect non-`python_files` doctests that explicit selector execution then suppressed.

The controller-owned pytest observer now records each concrete `pytest_itemcollected` identity before execution. A removed parameter instance therefore remains in observer evidence without a matching call/JUnit outcome and reconciliation returns UNKNOWN. Test-candidate modules containing wildcard imports now fail closed with `wildcard_import_unsupported` instead of claiming a complete static export inventory. The pinned selection-option catalog now includes `--doctest-modules`, and admitted policy containing it returns `pytest_selection_policy_unsupported` before immutable planning or execution. The unchanged three-selector command passed `3 passed`, and the complete runner unit surface passed `193 passed`.

Fresh gates on 2026-08-22 passed: format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint `10.00/10`; changed-line Code Review with `72 findings (0 blocking)` and no changed-line finding; seven manifests plus registry; bundle imports; strict C14 OpenSpec; `28 passed` contracts; and independent smart plus complete repository suites at `1492 passed` each with only the two existing third-party Lark deprecations. Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each passed the exact schema-1.6/core-handoff/candidate-identity smoke (`3 passed` per interpreter) against immutable core tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`; each also reconfirmed strict `===0.55.1` local-alias rejection. The canonical scoped non-main signer advanced `specfact-code-review` from `0.49.17` to `0.49.18`, removed the stale bot signature, and set unsigned filesystem checksum `sha256:9f80dbb5832efb26971f47feb7e962a9e4be7645a5bfa4503d3adbc66800e982` while preserving exact `core_compatibility: ===0.55.1`.

The human checkpoint is GPG-signed with fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its explicit committed range review from synchronized `origin/dev` emitted schema 1.6 `range_preview`, `0 findings`, and the expected local-only UNKNOWN reason `basedpyright_config_ambiguous`; the out-of-allowlist root still contains both `pyrightconfig.json` and `[tool.basedpyright]`. Explicit full review reproduced the unchanged full-repository debt boundary: schema 1.2 `FAIL`, `1226 findings`, and `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Bot signature, protected matrix, review-thread replies/resolution, and another authoritative review refresh remain required for this batch.

### Review batch — repository execution hooks, unittest overrides, and transitive exports

The next authoritative GraphQL refresh found three valid P1 threads. The exact selectors `test_complete_suite_rejects_repository_pytest_execution_hook`, `test_complete_suite_rejects_unittest_execution_override`, and `test_complete_suite_resolves_transitive_imported_test_definition` were authored and collected before production edits and failed together (`3 failed`). They proved that a repository `pytest_pyfunc_call` hook could forge a passing call, a `unittest.TestCase.run` override could suppress the real test method, and an explicitly re-exported test could disappear through a second import layer while a local smoke selector made the candidate appear complete.

Complete-suite planning now inventories every repository `conftest.py` that pytest could load for the sealed roots, rejects unsupported hook or `pytest_plugins` capability before selector execution, resolves explicit same-snapshot test re-exports transitively with cycle containment, and rejects unittest cases that override `run` or `__call__`. The unchanged three selectors passed (`3 passed`) and the complete runner surface passed `196 passed`.

A related execution-hook spelling was then checked proactively: `@pytest.hookimpl(specname="pytest_pyfunc_call")` on an arbitrarily named function. The named regression failed first (`1 failed`) because the initial inventory recognized only function/import names. Decorator `specname` identities are now included in the same closed hook catalog, and all four batch selectors passed (`4 passed`). A helper extraction removed the resulting changed-line `CC19` blocker without suppression. The final runner surface passed `197 passed`; type-check reported `0 errors, 0 warnings, 0 notes`; Ruff and Pylint remained clean with Pylint `10.00/10`; and changed-line Code Review returned `PASS_WITH_ADVISORY` with `74 findings (0 blocking)`.

Fresh final gates on 2026-08-22 passed on the post-refactor bytes: seven manifests plus registry; three CLI-contract files; bundle imports; strict C14 OpenSpec; `28 passed` contracts; and independent complete plus smart suites at `1496 passed` each with only the two existing third-party Lark deprecations. Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each installed immutable core `0.55.1` from commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed that strict `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, while ordinary `==0.55.1` admits the untested local alias. The canonical scoped non-main signer advanced `specfact-code-review` from `0.49.18` to `0.49.19`, removed the stale signature, and set unsigned filesystem checksum `sha256:c23e7d57d18af19a292540f5578ef4ad90c58ee2995c95459461602a735a9f6b` while preserving exact `core_compatibility: ===0.55.1`. Staged gates, a signed human commit, explicit committed range/full reports, bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required for this batch.

The exact-core-bound staged pipeline passed signature/version verification, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and contracts. Its staged review retained `113` warnings/information findings with `0` errors/blockers, and contracts passed `28 passed`. The human checkpoint is GPG-signed with fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its explicit committed range review from synchronized `origin/dev` emitted schema 1.6 `range_preview`, `0 findings`, and the expected local-only UNKNOWN reason `basedpyright_config_ambiguous`. Explicit full review emitted schema 1.2 `FAIL` with `1228 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). A canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

### Review batch — flat BasedPyright project runtime

After the preceding three P1 threads were answered with evidence and resolved, the authoritative refresh found one new P1: the BasedPyright projection treated canonical `/opt/specfact/project-runtime/site-packages` as a virtual-environment directory name under `venvPath`, although the authenticated layer is a flat import root. The behavioral selector `test_basedpyright_projection_exposes_flat_project_runtime_site_packages` was authored first and failed (`1 failed`): candidate-controlled `extraPaths` survived projection, and the flat authenticated dependency root was absent from the analyzer search path.

The projection now replaces any target `extraPaths` with the exact authenticated project-runtime site-packages root while retaining the accepted `venvPath`/`venv` evidence fields. The unchanged selector invokes pinned BasedPyright against a real dependency package placed directly in that flat root and passes with no `reportMissingImports`. All ten BasedPyright scope tests passed. Type-check reported `0 errors, 0 warnings, 0 notes`; Ruff and Pylint passed with Pylint `10.00/10`; changed-line Code Review returned `PASS_WITH_ADVISORY` with `19 findings (0 blocking)`; seven manifests plus registry, three CLI-contract files, bundle imports, strict C14 OpenSpec, and contracts (`28 passed`) passed. Independent complete and smart suites each passed `1497 passed` with only the two existing third-party Lark deprecations. Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each passed the exact schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter) against immutable core `0.55.1` and reconfirmed strict local-version alias rejection. The canonical scoped non-main signer advanced `specfact-code-review` from `0.49.19` to `0.49.20`, removed the stale bot signature, and set unsigned filesystem checksum `sha256:db87311cc70bf59ece01badc8880c58916e2b865c8118aef059e483fa7d92755` while preserving `core_compatibility: ===0.55.1`. Staged gates, signed commit, bot signature, thread reply/resolution, protected checks, and another authoritative review refresh remain required.

The exact-core-bound staged pipeline then passed with `133` retained warnings/information findings, `0` errors/blockers, and `28 passed` contracts. The human checkpoint is GPG-signed with fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its explicit committed range review emitted schema 1.6 `range_preview`, `0 findings`, and only the expected local `basedpyright_config_ambiguous` UNKNOWN; explicit full review reproduced schema 1.2 `FAIL`, `1228 findings`, and the unchanged `376` inherited blockers. Bot signature, thread reply/resolution, protected checks, and another authoritative review refresh remain required.

### Review batch — runtime tree, complete pytest inventory, dispatch, and sealed coverage

The next fully paginated GraphQL refresh found six valid P1 threads: `PRRT_kwDORVEFbs6bXjZ5` required project-runtime tree equality with the resolved target tree; `PRRT_kwDORVEFbs6baPHR` required config-free pytest discovery from the repository root; `PRRT_kwDORVEFbs6baPHW` required imported pytest classes to resolve sibling bases from the complete defining-module class map; `PRRT_kwDORVEFbs6baUg1` required a completed coverage result below the effective sealed `fail_under` threshold to be a blocking semantic FAIL; `PRRT_kwDORVEFbs6baUg3` required fail-closed rejection of a `unittest.TestCase._callTestMethod` override; and `PRRT_kwDORVEFbs6baUg6` required fail-closed rejection of dynamically assigned pytest test members and metaclass generation.

Seven exact regressions were authored and collected before production edits: `test_project_runtime_layer_rejects_target_tree_mismatch`, `test_project_runtime_validation_binds_resolved_target_tree`, `test_config_free_pytest_policy_discovers_from_repository_root`, `test_complete_suite_resolves_imported_class_sibling_base`, `test_complete_suite_rejects_unittest_method_dispatch_override`, `test_complete_suite_rejects_dynamic_test_member_assignment`, and `test_complete_pytest_coverage_uses_sealed_fail_under_as_blocking_threshold`. The unchanged exact command failed all seven (`7 failed`), proving the reported gaps, and passed all seven after correction (`7 passed`). The runtime descriptor is now bound to both resolved commit and resolved tree before validation or materialization. Complete pytest planning now uses the repository-root default, resolves imported-class MRO against the defining module, rejects method-dispatch overrides and dynamic test construction, and classifies completed coverage below `max(80, sealed fail_under)` as blocking FAIL. Legacy non-immutable local TDD retains its warning-only coverage behavior.

Two helper extractions removed the changed-line Radon `CC17` blockers without changing behavior. The final affected runner/toolchain surface passed `276 passed`; changed-line Code Review reported `122 findings (0 blocking)`. Fresh final gates passed on the post-refactor bytes: format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint `10.00/10`; seven manifests plus registry; three CLI-contract files; bundle imports; strict C14 OpenSpec; `28 passed` contracts; and independent complete plus smart suites at `1504 passed` each with only the two existing third-party Lark deprecations. Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded immutable core `0.55.1`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection against tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`. The canonical scoped signer advanced `specfact-code-review` from `0.49.20` to `0.49.21`, removed the stale bot signature, and set unsigned filesystem checksum `sha256:02c29495124c87479d94a37a6b8b10daf06473b4b93e1581812e8f68da3c2175` while preserving exact `core_compatibility: ===0.55.1`; all seven manifests passed checksum/version verification with the immutable core public key. The exact-core-bound staged pipeline then passed command/docs/accountability, Requirements evidence, signature/version, format, YAML, bundle-import, type/lint, changed-line review, and contract gates; its staged review reported `237 findings`, `0` errors/blockers, and contracts passed `28 passed`. The GPG-signed human checkpoint `0379cf580d2bd9390c84d90167b0dd2d0a3c18aa` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its exact committed range review from synchronized `origin/dev` commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema 1.6 `range_preview`, `0 findings`, and only the expected local `basedpyright_config_ambiguous` UNKNOWN; the explicit full review reproduced the inherited debt boundary with schema 1.2 `FAIL`, `1229 findings`, and `376` blockers. Canonical bot signature, protected checks, and the next authoritative review refresh remain pending.

### Review batch — assigned hooks, callable factories, and class-namespace isolation

The next fully paginated GraphQL refresh found three new P1 threads: `PRRT_kwDORVEFbs6bakKe` showed that a conftest assignment such as `pytest_pyfunc_call = bypass` escaped the repository-hook inventory; `PRRT_kwDORVEFbs6bakKg` showed that a module-level callable alias such as `test_failure = failing_function` escaped the explicit selector inventory; and `PRRT_kwDORVEFbs6bakKi` showed that an imported test class could inherit tests through a base imported by its defining module while the planner saw only immediate-module classes. Independent review added three adjacent reproduced gaps: an imported defining-module class map could overwrite an identically named local base; a `type(...)`-generated `Test*` class could contribute an omitted failing method; and `unittest.TestCase.__getattribute__` could replace the selected method before `_callTestMethod` while pytest reported PASS.

Six exact regressions were authored and collected before production edits and failed together (`6 failed`). The conftest inventory now includes assigned `pytest_*` hook names; runtime-callable assignments to configured test function/class names and dynamic class factories fail closed; imported test definitions record unsupported cross-module bases and return UNKNOWN; imported module class maps no longer overwrite local definitions; imported unittest identity is evaluated against its defining-module map; and `__getattribute__`/`__getattr__` join the unsupported unittest dispatch controls. The unchanged exact selector set then passed (`6 passed`), and the complete runner surface passed `209 passed`. Two helper extractions removed the resulting changed-line complexity blockers; final changed-line Code Review reported `75 findings (0 blocking)`, type-check reported `0 errors, 0 warnings, 0 notes`, and Ruff/Pylint passed with Pylint `10.00/10`.

Fresh final gates on the post-refactor bytes passed: seven manifests plus registry, three CLI-contract scenarios, bundle imports, strict C14 OpenSpec, publish precheck, and `28 passed` contracts. Independent complete and smart suites each passed `1510 passed` with only the two existing third-party Lark deprecations. Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded immutable core `0.55.1`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection against tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`. The canonical scoped signer advanced `specfact-code-review` from `0.49.21` to `0.49.22`, removed the stale bot signature, and set unsigned filesystem checksum `sha256:923f3b88a1c4b4fa299ce0042148eaa1885be910ead4e1f46529a07b6cf7eb1b` while preserving exact `core_compatibility: ===0.55.1`; all seven manifests passed checksum/version verification with the immutable core public key. The exact-core-bound staged pipeline then passed signature/version, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and contracts; its review reported `122 findings`, `0` errors/blockers, and contracts passed `28 passed`.

The GPG-signed human checkpoint `9425c0e1fb510b5b9d51279d253c0058a5486333` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its exact committed range review from synchronized `origin/dev` commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema 1.6 `range_preview`, `0 findings`, and the expected local-only UNKNOWN reason `basedpyright_config_ambiguous`; the out-of-allowlist repository configuration still exposes both root `pyrightconfig.json` and `[tool.basedpyright]`. The explicit full review emitted schema 1.2 `FAIL` with `1230 findings` and the unchanged `376` inherited blockers. Canonical bot signature, protected checks, thread replies/resolution, and another authoritative review refresh remain pending.

### Review batch — dynamic conftest namespace writes

After the preceding three P1 threads were answered with evidence and resolved, the next fully paginated Codex review against canonical bot head `f3ee6145d5b8df105019189e570ec2ae09bad564` opened P1 thread `PRRT_kwDORVEFbs6ba91P`. A repository conftest could install `pytest_pyfunc_call` through `globals()["pytest_pyfunc_call"] = bypass`, evading the direct-name assignment inventory while pinned pytest executed the hook normally.

The exact selector `test_complete_suite_rejects_subscript_assigned_pytest_hook_alias` was authored and collected before production edits, then failed (`1 failed`) because planning returned PASS. Complete-suite planning now fails closed on direct module-namespace subscript writes through argument-free `globals()`, `locals()`, or `vars()` in repository conftest files. The unchanged new selector and its adjacent direct-assignment regression passed together (`2 passed`), and the complete runner surface passed `210 passed`. Type-check reported `0 errors, 0 warnings, 0 notes`; Ruff and Pylint passed with Pylint `10.00/10`. The staged index review found `0 findings` and `0 blockers`; its assurance remained the expected local UNKNOWN because analyzer capsule execution is incomplete outside protected Linux.

Fresh post-fix gates passed on 2026-08-22: strict C14 OpenSpec; `28 passed` contracts; seven manifests plus registry; three CLI-contract scenario files; bundle imports; publish precheck; `git diff --check`; and independent complete plus smart suites at `1511 passed` each with only the two existing third-party Lark deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded immutable core `0.55.1`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection. The canonical scoped non-main signer advanced `specfact-code-review` from `0.49.22` to `0.49.23`, removed the stale signature, and set unsigned filesystem checksum `sha256:b5bf42790e53301338defdf519cd615ea79d6ea0d5d09a02dc923c942f569d3d` while preserving exact `core_compatibility: ===0.55.1`. Signed commit, explicit committed range/full reports, canonical bot signature, thread reply/resolution, protected checks, and another authoritative refresh remain required.

The exact-core-bound staged pipeline passed signature/version, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and contracts. Its review reported `123 findings`, `0` errors/blockers, and `PASS_WITH_ADVISORY`; contracts passed `28 passed`. GPG-signed human checkpoint `9aa48d60e46c7216a6320cab80536d4871ecaad5` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its explicit committed range review from synchronized `origin/dev` emitted schema 1.6 `range_preview`, `0 findings`, and only the expected local `basedpyright_config_ambiguous` UNKNOWN; explicit full review reproduced schema 1.2 `FAIL`, `1230 findings`, and the unchanged `376` inherited blockers. Canonical bot signature, thread reply/resolution, protected checks, and another authoritative refresh remain required.

### Review batch — root-discovery coverage and namespace mutation

The next fully paginated Codex review against canonical bot head `6dd130ba2b28c273d9281e81a93dfc2427274b81` opened two P1 threads. `PRRT_kwDORVEFbs6bbD-T` showed that config-free pytest discovery projected `/opt/specfact/snapshot` as `testpaths`, which complete coverage then treated as an exclusion root and therefore removed every production source. `PRRT_kwDORVEFbs6bbD-V` showed that `globals().update(pytest_pyfunc_call=bypass)` still installed an unobserved execution hook. Independent review added the adjacent alias escape `namespace = globals(); namespace.update(...)`.

The exact selectors `test_complete_tdd_gate_preserves_sources_when_discovering_from_snapshot_root` and `test_complete_suite_rejects_namespace_update_pytest_hook_alias` were authored and collected before production edits and failed together (`2 failed`): coverage received no source files and namespace update planning returned PASS. After the direct-call correction, `test_complete_suite_rejects_aliased_namespace_update_pytest_hook` was authored, collected, and failed (`1 failed`), proving that mutation could be deferred through an alias. Complete coverage now distinguishes the snapshot-wide discovery root from actual selected test files and narrower sealed test roots, so selected tests and conftest support are excluded without dropping production inputs. Conftest inventory now walks module-execution statements without descending into function/class bodies and fails closed on any argument-free `globals()`, `locals()`, or `vars()` namespace access, including direct mutation, subscript assignment, and aliased mutation. The three new selectors plus the prior subscript guard passed (`4 passed`), and the complete runner surface passed `213 passed`.

The first staged review exposed one direct Basedpyright argument-type blocker in the new AST worklist. An explicit `list[ast.AST]` binding removed it without suppression; repeated type-check returned `0 errors, 0 warnings, 0 notes`, Ruff/Pylint passed with Pylint `10.00/10`, and the canonical changed-line review returned `PASS_WITH_ADVISORY` with `126 findings` and `0` errors/blockers. Fresh gates passed on 2026-08-22: strict C14 OpenSpec; `28 passed` contracts; seven manifests plus registry; three CLI-contract scenarios; bundle imports; publish precheck; `git diff --check`; and independent complete plus smart suites at `1514 passed` each with only the two existing third-party Lark deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` each loaded immutable core `0.55.1`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection. The canonical scoped signer advanced `specfact-code-review` from `0.49.23` to `0.49.24`, removed the stale signature, and set unsigned filesystem checksum `sha256:10d9847f4998ee5f8d9b4f7f95d4b35ccee4af9eeabcba0f308225b2b58e0610` while preserving exact `core_compatibility: ===0.55.1`. Signed commit, explicit committed range/full reports, canonical bot signature, thread replies/resolution, protected checks, and another authoritative refresh remain required.

The exact-core-bound staged pipeline passed signature/version, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and contracts. Its review reproduced `PASS_WITH_ADVISORY`, `126 findings`, and `0` errors/blockers; contracts passed `28 passed`. GPG-signed human checkpoint `417b1b536ad58e5484f2092a6806fc1b7d1a071f` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its explicit committed range review from synchronized `origin/dev` emitted schema 1.6 `range_preview`, `0 findings`, and only the expected local `basedpyright_config_ambiguous` UNKNOWN; explicit full review reproduced schema 1.2 `FAIL`, `1230 findings`, and the unchanged `376` inherited blockers. Canonical bot signature, thread replies/resolution, protected checks, and another authoritative refresh remain required.

### Review batch — dynamic conftest module attributes

The next fully paginated Codex review against canonical bot head `df5f1989a2d2fd9839a4bd52f260ecdadb7d6047` opened P1 thread `PRRT_kwDORVEFbs6bbKw3`. Module-level PEP 562 `__dir__` and `__getattr__` functions could advertise and return a synthetic `pytest_pyfunc_call` hook while the static inventory saw no pytest-prefixed definition.

The exact selector `test_complete_suite_rejects_dynamic_module_attribute_pytest_hook` was authored and collected before production edits, then failed (`1 failed`) because planning returned PASS. Repository conftest validation now treats module-level `__dir__` or `__getattr__` as unsupported dynamic namespace capability and returns UNKNOWN before pytest execution. The unchanged selector passed (`1 passed`), and the complete runner surface passed `214 passed`. Type-check reported `0 errors, 0 warnings, 0 notes`; Ruff/Pylint passed with Pylint `10.00/10`; and canonical changed-line review returned `PASS_WITH_ADVISORY`, `127 findings`, and `0` errors/blockers.

Fresh gates passed on 2026-08-22: strict C14 OpenSpec; `28 passed` contracts; seven manifests plus registry; three CLI-contract scenarios; bundle imports; publish precheck; `git diff --check`; and independent complete plus smart suites at `1515 passed` each with only the two existing third-party Lark deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` each loaded immutable core `0.55.1`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection. The canonical scoped signer advanced `specfact-code-review` from `0.49.24` to `0.49.25`, removed the stale signature, and set unsigned filesystem checksum `sha256:6b2505041d15f7e0794ac3271596184b6d2d1e0e1ab47f3c2b2c8fb2c4c86f82` while preserving exact `core_compatibility: ===0.55.1`. Signed commit, explicit committed range/full reports, canonical bot signature, thread reply/resolution, protected checks, and another authoritative refresh remain required.

The exact-core-bound staged pipeline passed signature/version, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and contracts. Its review reproduced `PASS_WITH_ADVISORY`, `127 findings`, and `0` errors/blockers; contracts passed `28 passed`. GPG-signed human checkpoint `e5dad59721834641aab273de66cbf046490ce6c2` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its explicit committed range review from synchronized `origin/dev` emitted schema 1.6 `range_preview`, `0 findings`, and only the expected local `basedpyright_config_ambiguous` UNKNOWN; explicit full review reproduced schema 1.2 `FAIL`, `1230 findings`, and the unchanged `376` inherited blockers. Canonical bot signature, thread reply/resolution, protected checks, and another authoritative refresh remain required.

### Review batch — dynamic test injection, selector support roots, and suppression continuity

The next fully paginated Codex review against canonical bot-signed head `62249d2eb3f2c3457efc28b4894783213bb9fefa` opened P1 threads `PRRT_kwDORVEFbs6bbRcS` and `PRRT_kwDORVEFbs6bbRcT`, plus repository-blocking P2 thread `PRRT_kwDORVEFbs6bbRcV`. Exact tests were authored before production edits. `test_complete_suite_rejects_subscript_injected_test_callable`, the extended `test_complete_tdd_gate_preserves_sources_when_discovering_from_snapshot_root`, and `test_unchanged_inline_suppression_survives_line_shift` failed together (`3 failed`): a module-level `globals()["test_failure"]` write returned PASS, config-free discovery submitted `tests/helpers.py` as production coverage input, and an unchanged suppression shifted by one line was classified as introduced.

Complete-suite planning now applies the same fail-closed module-namespace capability check to every test candidate as to repository conftest files. Config-free coverage derives narrow support roots from the sealed selector inventory while preserving the snapshot-wide discovery root as production scope. Suppression continuity now pairs equal path/family/token occurrences by stable ordinal rather than physical line, preserving duplicate cardinality and quarantining unchanged suppressions on changed files without misclassifying a pure line shift as introduced. The unchanged three-selector command passed (`3 passed`), and the complete affected runner/differential surface passed `304 passed`.

Fresh gates on 2026-08-22 passed: strict C14 OpenSpec; `28 passed` contracts; independent complete and smart suites at `1517 passed` each with only the two existing third-party Lark deprecations; format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint `10.00/10`; seven manifests plus registry; three CLI-contract scenarios; bundle imports; and publish precheck. The final canonical changed-line review returned `PASS_WITH_ADVISORY` with `176` retained warning/information findings and `0` errors or changed-line blockers. Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded immutable core `0.55.1`, passed the exact schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection against tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`. The first explicit committed full review reproduced the inherited `376` blockers and exposed one new warning: the coverage-root addition raised `_evaluate_complete_tdd_gate` to complexity `13`. Extracting `_complete_pytest_coverage_roots` removed that warning without changing behavior; the exact three regressions and affected `304` tests remained green, with type/lint still clean. The canonical scoped signer advanced `specfact-code-review` from `0.49.25` to `0.49.26`, removed the stale bot signature, and set final unsigned filesystem checksum `sha256:56dfd963d71a2733e1fdc95b2c6c6e3a22c2f1bef6f329d77200977df3c86e0a` while preserving exact `core_compatibility: ===0.55.1`; all seven manifests passed checksum/version verification with the immutable core public key. Staged gates, an amended GPG-signed human checkpoint, fresh exact committed range/full reviews, a bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

GPG-signed code checkpoint `68813edb2350f8eb81e0e48abe072482b3b864a9` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its exact committed range report from synchronized `origin/dev` commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema 1.6 `range_preview`, `0 findings`, and local-only `policy_parse_failure` UNKNOWN before immutable selection. Its explicit full review emitted schema 1.2 `FAIL` with `1230 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS); the newly introduced complexity warning was absent. A final evidence-only signed checkpoint and reports bound to that exact head, canonical bot signature, thread replies/resolution, protected checks, and another authoritative refresh remain required.

### Review batch — destructured aliases, control-flow bindings, and native pytest dispatch

The next fully paginated Codex review against canonical bot-signed head `2916185cfa20f9f06e16b60d85cbaf02c8e58204` opened four valid P1 threads: `PRRT_kwDORVEFbs6bbcuQ`, `PRRT_kwDORVEFbs6bbcuV`, `PRRT_kwDORVEFbs6bbcuW`, and `PRRT_kwDORVEFbs6bbcuY`. The exact selectors `test_complete_suite_rejects_destructured_pytest_hook_alias`, `test_complete_suite_rejects_destructured_test_callable_alias`, `test_complete_suite_rejects_test_binding_in_module_control_flow`, and `test_complete_suite_rejects_native_pytest_attribute_dispatch_override` were authored before production edits and failed together (`4 failed`): tuple-unpacked conftest hooks and test callables bypassed direct-name assignment analysis, module-level conditional imports could introduce unplanned test bindings, and a native pytest class could replace a failing method through `__getattribute__` while the sealed selector still reported PASS.

Assignment analysis now recursively flattens tuple/list/starred targets for both conftest hook inventory and dynamic test-member rejection. Complete-suite planning also rejects test-shaped imports/functions/classes created inside module-executed control flow, without descending into ordinary function or class bodies. Execution-override analysis retains the complete unittest dispatch guard and additionally rejects dynamic attribute dispatch on a native collected pytest class while leaving unrelated native `run`/`__call__` methods alone. The unchanged four selectors passed (`4 passed`), followed by the complete runner surface (`219 passed`).

Fresh gates on 2026-08-22 passed: strict C14 OpenSpec; `28 passed` contracts; independent smart and complete suites at `1521 passed` each with only the two existing third-party Lark deprecations; format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint `10.00/10`; seven manifests plus registry; three CLI-contract scenarios; bundle imports; and publish precheck. The explicit changed-line review returned `132 findings`, all retained warnings/information, with `0` errors or blockers. Fresh isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each installed immutable core `0.55.1` from tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`; each passed the three schema/handoff/version-identity selectors (`3 passed`) and reconfirmed that strict `===0.55.1` rejects `0.55.1+vendor` while ordinary `==0.55.1` admits it.

The first explicit committed full review reproduced the unchanged `1230` findings and `376` inherited blockers but exposed one new warning: `_class_has_test_execution_override` accepted seven parameters. Reusing the existing immutable pytest selector context removed that warning without suppression; the four exact regressions remained green, type/lint remained clean, and the repeated changed-line review reported `131 findings` with `0` blockers.

The canonical scoped non-main signer advanced `specfact-code-review` from `0.49.26` to `0.49.27`, removed the stale bot signature, and set final unsigned filesystem checksum `sha256:0b7e594cf3c0134c56b9463de1509bcf33b5e0c8392905a1816211a10f5bd1f4` while preserving exact `core_compatibility: ===0.55.1`; all seven manifests passed filesystem checksum/version verification with the immutable core public key. Staged gates, an amended signed human checkpoint, exact committed range/full reports, push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

GPG-signed code checkpoint `6e22ee26ea8bb99f218c7137964811b537baf21b` / tree `31390a765b0d71fd8c19de0ad56834faf6b3b62c` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its exact committed range report from synchronized `origin/dev` emitted schema 1.6 with `0 findings` and the expected local-only `basedpyright_config_ambiguous` UNKNOWN before immutable selection. Its explicit full review emitted schema 1.2 `FAIL` with `1229 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS); the new parameter-count warning was absent. A final evidence-only signed checkpoint, push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

### Review batch — assignment-expression aliases

After the preceding four P1 threads were answered and resolved, the next fully paginated Codex review against canonical bot-signed head `6118d4e3609780c54ba31b7667816d6e181f1e66` opened P1 threads `PRRT_kwDORVEFbs6bbo-n` and `PRRT_kwDORVEFbs6bbo-r`. The exact selectors `test_complete_suite_rejects_named_expression_pytest_hook_alias` and `test_complete_suite_rejects_named_expression_test_callable_alias` were authored before production edits and failed together (`2 failed`): module-level walrus expressions could install `pytest_pyfunc_call` in a conftest or bind an unplanned failing `test_failure` callable while static inventory returned PASS.

The shared assignment-target boundary now models `ast.NamedExpr` alongside ordinary, annotated, augmented, and destructured assignments. Repository conftest inventory rejects a `pytest_*` assignment-expression target, and dynamic test-member analysis rejects matching test function/class bindings before selector execution. The unchanged two selectors passed (`2 passed`), followed by the complete runner surface (`221 passed`).

Fresh gates on 2026-08-23 passed: strict C14 OpenSpec; `28 passed` contracts; independent smart and complete suites at `1523 passed` each with only the two existing third-party Lark deprecations; format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint `10.00/10`; seven manifests plus registry; three CLI-contract scenarios; bundle imports; and publish precheck. Changed-line review reported `133` retained warnings/information findings with `0` errors or blockers. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each reran the three exact immutable-core `0.55.1` compatibility selectors and passed (`3 passed` per interpreter).

The canonical scoped non-main signer advanced `specfact-code-review` from `0.49.27` to `0.49.28`, removed the stale bot signature, and set unsigned filesystem checksum `sha256:2675f861b1f514626348338e0e190d796cd7ea3877e8d79d35aa50549e943bc5` while preserving exact `core_compatibility: ===0.55.1`; all seven manifests passed filesystem checksum/version verification with the immutable core public key. Staged gates, signed checkpoints, exact committed range/full reports, push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

GPG-signed code checkpoint `d0afc5046c2cbd213661286930abd42d0ee8b68d` / tree `4d69beaabf2790eb21ba98a9208048471cae28d1` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its exact committed range report from synchronized `origin/dev` emitted schema 1.6 with `0 findings` and the expected local-only `basedpyright_config_ambiguous` UNKNOWN before immutable selection. Its explicit full review emitted schema 1.2 `FAIL` with `1229 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS); no new warning appeared in the assignment-expression implementation. A final evidence-only signed checkpoint, push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

### Review batch — sealed pythonpath imports and complete pytest plugin declarations

The fully paginated Codex review against canonical bot-signed head `c82ed9d0e408891781c3060256861ee8129a4a1b` opened valid P1 threads `PRRT_kwDORVEFbs6bbtwY` and `PRRT_kwDORVEFbs6bbtwZ`. Three exact cases were authored and collected before production edits: `test_complete_suite_resolves_imported_test_through_project_pythonpath`, `test_complete_suite_rejects_every_pytest_plugins_binding[named-expression]`, and `test_complete_suite_rejects_every_pytest_plugins_binding[module-control-flow]`. They failed together (`3 failed`): the selector inventory omitted a failing callable imported through admitted `pythonpath`, while assignment-expression and conditional `pytest_plugins` declarations were not rejected. The unchanged cases passed after projected snapshot-local import roots were propagated through direct, transitive, imported-base, and execution-override analysis, and conftest plugin detection reused complete module-execution assignment targets. A tightened shadowing fixture then failed independently (`1 failed`) until absolute import search order matched pinned pytest semantics by placing ordered projected `pythonpath` entries before the snapshot root; the unchanged three-case set passed again (`3 passed`).

Fresh gates on 2026-08-23 passed on the final source bytes: the complete runner surface `224 passed`; format and `git diff --check`; type-check with `0 errors, 0 warnings, 0 notes`; Ruff and Pylint `10.00/10`; seven manifests plus registry; three CLI-contract scenarios; bundle imports; strict C14 OpenSpec; and contracts `28 passed`. The independent smart suite passed `1526 passed`; a simultaneously launched duplicate complete suite had one subprocess-only return-code failure while the identical smart-suite case passed, so that run was discarded, the selector passed alone, and the complete suite reran serially to `1526 passed` with only the two existing third-party Lark deprecations. Final changed-line Code Review reported `77` retained warnings/information findings with `0` errors or blockers. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded immutable core `0.55.1`, passed the three schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection. Tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337` were rechecked exactly with a clean detached core checkout.

The first explicit committed full review reproduced the unchanged `376` inherited blockers but exposed two new warning-level regressions: `_test_selectors` and `_collect_pytest_selectors` each accepted six parameters after import-root propagation. A private immutable discovery-policy context removed both warnings without suppression or behavior changes. Comparison with the preceding accepted full report then isolated one remaining new informational simplification diagnostic in `_pytest_candidate_rejection_reason`; extracting its multi-path imported-base predicate removed that finding as well. The exact three regressions and full runner surface remained green (`3 passed`, `224 passed`), type/Ruff stayed clean, and changed-line review improved from `77` to `74` retained findings with `0` blockers. Independent smart and complete suites reran serially on the final refactored bytes and each passed `1526 passed`; the exact immutable-core three-selector matrix also passed again on CPython `3.11.15`, `3.12.13`, and `3.13.14`.

The canonical scoped non-main signer advanced `specfact-code-review` from `0.49.28` to `0.49.29`, removed the stale bot signature, and set final unsigned filesystem checksum `sha256:782cea9b226121eb0c00ab45bcd4d06335b2cbdacd4ab44a7d579bf6e8d969b1` while preserving exact `core_compatibility: ===0.55.1`. All seven manifests passed filesystem checksum/version verification with the immutable core public key, and the publish pre-check passed. Signed human checkpoints, exact committed range/full reports, push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

The exact-core-bound staged pipeline passed signature/version preparation, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and contracts. Its final staged review reported `74` retained warnings/information findings, `0` errors/blockers, and `PASS_WITH_ADVISORY`; contracts passed `28 passed`. A GPG-signed human checkpoint, exact committed range/full reports, push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

GPG-signed code checkpoint `4a5fb25381dcadc73627157b93e167eb165a37aa` / tree `80945968c3a6eacb1d3070c870ed94b336a11496` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its exact committed range report from synchronized `origin/dev` commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema 1.6 `range_preview`, `0 findings`, and the expected local-only `basedpyright_config_ambiguous` / `policy_parse_failure` UNKNOWN before immutable selection. Its explicit full review emitted schema 1.2 `FAIL` with `1229 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). An exact normalized comparison against the preceding accepted full report produced no new diagnostics. A signed evidence-only checkpoint, push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

### Review batch — authenticated runtime imports and test decorators

After canonical signer head `a9af54f466d637f835d6e180aaac70d5452cb8ac` verified with the immutable core public key, the next fully paginated Codex review opened P1 threads `PRRT_kwDORVEFbs6bb5bL` and `PRRT_kwDORVEFbs6bb5bM`. The exact selectors `test_complete_suite_resolves_imported_test_through_project_runtime` and `test_complete_suite_rejects_test_body_replacing_decorator` were authored, collected (`2 tests collected`), and executed before production edits. They failed together (`2 failed`): complete-suite planning did not accept the authenticated runtime root, and a local decorator could replace a failing test body while planning returned PASS.

Static pytest import resolution now composes projected `pythonpath`, the immutable snapshot, and the verified project-runtime `site-packages` in execution order and retains the owning root for relative reexports. Immutable base/head inventory reconciliation receives the exact materialized runtime root already mounted for pytest execution. Test execution planning also rejects decorated test functions or methods unless the decorator has explicitly modeled pytest-mark or standard unittest outcome semantics. The unchanged two-selector command passed (`2 passed`), followed by the complete runner surface (`226 passed`). Format, Ruff/Pylint, and focused Basedpyright passed with Pylint `10.00/10` and `0 errors, 0 warnings, 0 notes`. The first changed-line review exposed one new Radon blocker in the decorator classifier; two behavior-preserving decompositions reduced alias extraction and membership checks to bounded helpers. The final worktree review returned `PASS_WITH_ADVISORY`, `138 findings`, and `0` blockers. The canonical scoped non-main signer advanced `specfact-code-review` from `0.49.29` to `0.49.30`, removed the stale bot signature, and set final unsigned filesystem checksum `sha256:fc2322850d268e92963d29f38c1b7c6487cd63493404bd012a64f4c2fb8f0468` while preserving exact `core_compatibility: ===0.55.1`. Full repository gates, exact-core compatibility, signed checkpoints, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

Fresh final-byte gates passed on 2026-08-23: strict C14 OpenSpec; seven manifests plus registry; bundle imports; version/checksum enforcement; `28 passed` contracts; three CLI-contract scenarios; publish precheck; generated command overview/contract; documentation accountability; and independent smart plus complete suites at `1528 passed` each with only the two existing third-party Lark deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded immutable core `0.55.1`, passed the exact schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection. Tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337` were rechecked exactly with a clean detached core checkout. Staged gates, signed checkpoints, exact committed range/full reports, push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

The exact-core-bound staged pipeline passed signature/version preparation, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and contracts. Its staged review reproduced `PASS_WITH_ADVISORY`, `138 findings`, and `0` errors/blockers; contracts passed `28 passed`. A GPG-signed checkpoint, exact committed range/full reports, push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

The first explicit committed full review for this batch reproduced the unchanged inherited blockers and isolated four new nonblocking diagnostics: length in `_reconcile_immutable_pytest_inventory`, complexity in `_imported_module_path` and `run_immutable_scope_review`, and the resulting `run_immutable_scope_review` length increase. Behavior-preserving extraction introduced bounded import-search, inventory-preparation, and snapshot-pair helpers; the repeated changed-line review then returned `PASS_WITH_ADVISORY`, `74 findings`, and `0` errors/blockers, with none of the four diagnostics remaining. On these definitive cleanup bytes, the exact two P1 regressions plus runtime-propagation case passed (`3 passed`), the runner surface passed (`226 passed`), smart and complete suites each passed `1528 passed`, type-check reported `0 errors, 0 warnings, 0 notes`, Ruff/Pylint passed with `10.00/10`, all manifest/import/contract/CLI/publish/command/docs/OpenSpec gates passed, and the immutable core matrix passed the exact three selectors on CPython `3.11.15`, `3.12.13`, and `3.13.14`. The unsigned manifest remains version `0.49.30`, checksum `sha256:fc2322850d268e92963d29f38c1b7c6487cd63493404bd012a64f4c2fb8f0468`, and exact `core_compatibility: ===0.55.1`. A new signed checkpoint and exact reports bound to it remain required before push.

GPG-signed code checkpoint `3d80d028801816d21f9002e492c34031cc9a0564` / tree `e3fca0e3394a93cd32800c2c44fef4afb2b4ce86` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. Its exact committed range report from synchronized `origin/dev` commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema 1.6 `range_preview`, `0 findings`, and local-only UNKNOWN (`basedpyright_config_ambiguous` / `policy_parse_failure`) before protected immutable selection. Its explicit full review emitted schema 1.2 `FAIL` with `1228 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Exact normalized comparison with accepted report `/private/tmp/c14-full-review-4a5fb25.json` produced no new diagnostic. This evidence-only follow-up changes no signed module payload byte; push, canonical bot signature, thread replies/resolution, protected checks, and another authoritative review refresh remain required.

### Review batch — autouse fixtures, conditional class methods, and dynamic exec

The next fully paginated live thread inventory exposed three additional valid P1 findings: `PRRT_kwDORVEFbs6bb6_2`, `PRRT_kwDORVEFbs6bb6_4`, and `PRRT_kwDORVEFbs6bb6_5`. The exact selectors `test_complete_suite_rejects_execution_shaping_autouse_fixture`, `test_complete_suite_rejects_test_method_in_class_control_flow`, and `test_complete_suite_rejects_module_level_exec_test_generation` were authored and collected (`3 tests collected`) before production edits. They then failed together (`3 failed`), each proving that complete-suite planning returned PASS while pytest could replace a failing callable, collect a conditional class method omitted by the static inventory, or synthesize a failing test through module-level `exec`.

Repository conftest validation now fails closed on pytest autouse fixtures that can mutate execution state; candidate inspection rejects test-shaped class methods created under compound class-body statements; and module-execution analysis rejects direct dynamic `exec` before static inventory is accepted. The unchanged three-selector batch passed (`3 passed`), followed by the complete runner surface (`229 passed`). Format, type-check, Ruff/Pylint, strict OpenSpec, and changed-line Code Review passed; the review remained `PASS_WITH_ADVISORY` with `74 findings` and `0` errors/blockers. Independent smart and complete suites each passed `1531 passed` with only the two existing third-party Lark deprecations. Seven manifests plus registry, bundle imports, checksum/version enforcement, `28 passed` contracts, three CLI-contract scenarios, publish precheck, command overview/contract, and documentation accountability all passed. Immutable core `v0.55.1` commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` / tree `47984be5434d7ae65ed6908bf525a32053290337` again passed the exact three compatibility selectors on CPython `3.11.15`, `3.12.13`, and `3.13.14`. The canonical signer advanced the changed module to version `0.49.31`, removed the prior signature, set checksum `sha256:72bfee4a75678181eaf22eb3f114ec941526a5112b1b7d566f8ee1a40ab15573`, and preserved `core_compatibility: ===0.55.1`. Signed checkpoints, exact committed reports, canonical bot signature, five thread replies/resolutions, protected checks, and another authoritative review refresh remain required.

The exact-core-bound staged pipeline then passed signature/version preparation, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and contracts. Its broader staged surface reported `PASS_WITH_ADVISORY`, `137 findings`, and `0` errors/blockers; contracts passed `28 passed`.

After preserving prior canonical signer commit `59ee93249c79c53fa45251c03edb9208720d194c` in history, GPG-signed code checkpoint `67793a411e90b532f377f20cb2706845d694efd7` / tree `12e485c228dd3a27e8e04137dfdc5a437b4dc521` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`. The expected manifest-only rebase conflict retained the verified unsigned version `0.49.31` / checksum `sha256:72bfee4a75678181eaf22eb3f114ec941526a5112b1b7d566f8ee1a40ab15573`; post-rebase checksum/version verification and the three exact regressions passed. The exact committed range report from synchronized `origin/dev` emitted schema 1.6 `range_preview`, `0 findings`, and local-only UNKNOWN (`basedpyright_config_ambiguous` / `policy_parse_failure`). Its explicit full review emitted schema 1.2 `FAIL` with `1228 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Exact normalized comparison with accepted report `/private/tmp/c14-full-review-4a5fb25.json` produced no new diagnostic. This evidence-only follow-up changes no signed module payload byte; push, canonical bot signature, five thread replies/resolutions, protected checks, and another authoritative review refresh remain required.

### Security review batch — isolate registry credentials from candidate Python

Canonical signed payload head `527aa7a75c04e7349077bf099e5ed10c537e87f6` verified against the immutable core public key and changed only the signature line, preserving version `0.49.31`, checksum `sha256:72bfee4a75678181eaf22eb3f114ec941526a5112b1b7d566f8ee1a40ab15573`, and signature `8V4eIwkkZtasG8z2x7kpu2t5CSbwiz+Z92v79rIjjTpVINBXQWsl8hZcN5AoXpAhpNtBOY8G++NAeNQiSQrkDA==`. All five preceding P1 threads were replied to and resolved, after which fully paginated readback exposed new P1 thread `PRRT_kwDORVEFbs6bcE5o`: the exact-core job preserved a packages-read GitHub token into an elevated Python process after importing candidate-controlled `toolchain` code.

Regression `test_exact_core_smoke_does_not_expose_registry_token_to_candidate_python` was authored first and failed (`1 failed`) because the job granted `packages: read`, exported `REGISTRY_TOKEN` / `REGISTRY_ACTOR`, preserved them through `sudo`, and passed constructed credentials into candidate tooling. Since the analyzer runtime package is publicly readable, the job now performs the same pinned cache-miss/cache-hit/capsule smoke anonymously: it grants only `contents: read`, preserves only `MATRIX_PYTHON` and `PYTHONPATH`, and neither constructs nor passes a registry credential. The security trigger plus legitimate capsule-workflow control passed (`2 passed`), the complete workflow test file passed (`11 passed`), and YAML, format/type/lint, strict OpenSpec, and `git diff --check` passed. Explicit review of the changed Python test returned `PASS_WITH_ADVISORY`, one inherited readability advisory, and `0` blockers; workflow YAML is outside the analyzer file types. Independent smart and complete suites each passed `1532 passed` with only the two existing third-party Lark deprecations. Protected Linux proof of anonymous OCI acquisition remains required before resolving the security thread.

The first commit attempt correctly stopped at the signature hook because its implicit `HEAD~1` version baseline was the canonical bot signature-only commit, whose unchanged `0.49.31` version is intentional. No hook was bypassed. Regression `test_pre_commit_verify_uses_target_branch_version_baseline` was authored first and failed (`1 failed`); the hook now fails closed unless the target remote ref is fetched and passes that explicit `refs/remotes/origin/${GITHUB_BASE_REF:-dev}` baseline to checksum/version verification (using `origin/main` on the protected main path). The regression then passed, direct execution of the real hook passed on the bot-signed head, and the combined hook/parity/workflow surface passed `28 passed`. Type/lint, YAML, strict OpenSpec, and `git diff --check` remained clean. Independent smart and complete suites reran on the definitive bytes and each passed `1533 passed` with only the same two third-party Lark deprecations.

The repeated exact-core-bound staged pipeline passed the corrected signature/version hook, format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, changed-line review, and `28 passed` contracts. The staged review reported four nonblocking findings: three inherited test-readability information items and one local CrossHair incomplete-evidence warning because CrossHair launched from the global Python without pytest installed. This is an explicit local-tooling exception, not behavioral evidence: the same test modules executed successfully in the governed Hatch environment (`28 passed` focused; `1533 passed` complete), and no error or changed-line blocker was reported. Protected CI remains the authoritative security/runtime proof.

GPG-signed security checkpoint `fd69f887d88d76142702ed541b6eac66d53bdb00` / tree `0a107e2ddbb884b868f1c37aa92bb4dc1426b591` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`, with every hook enabled and passing. Its exact committed range report from synchronized `origin/dev` emitted schema 1.6 `range_preview`, `0 findings`, and the expected local-only UNKNOWN (`basedpyright_config_ambiguous` / `policy_parse_failure`). Its explicit full review emitted schema 1.2 `FAIL` with `1228 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Exact normalized comparison with accepted report `/private/tmp/c14-full-review-4a5fb25.json` produced no new diagnostic. A signed evidence-only checkpoint, push, protected anonymous-acquisition proof, security-thread resolution, and authoritative review refresh remain required.

Protected run `32607685611` at pushed head `5752c37782e0469e424f5f9acffc894e938576a2` disproved the anonymous-acquisition assumption on every supported Python version. Immutable tag/commit/tree validation, exact-core installation, schema 1.6 smoke, and module signature verification passed, but CPython 3.11, 3.12, and 3.13 each failed with `oci_acquisition_failed:401 Client Error: Unauthorized` at the GHCR token realm. The corrected package settings grant repository Actions access; they do not make this package anonymously readable. Security thread `PRRT_kwDORVEFbs6bcE5o` therefore remained unresolved.

The strengthened workflow regression first failed together with its runtime control (`2 failed`) because no credential-isolated prefetch existed. It then passed after adding a full-SHA-pinned `oras-project/setup-oras` action (`22ce207df3b08e061f537244349aac6ae1d214f6`) and checksum-pinned ORAS 1.3.3 Linux binary (`sha256:9ce999f8d2de03fc03968b29d743077a58783e545e5eaa53917ca177352d0e59`). A second tests-first source-to-sink pass exposed that candidate execution preceding prefetch could poison `GITHUB_PATH`; the ordering assertion failed (`1 failed`) until authenticated ORAS acquisition moved before every candidate script, import, or test. The unchanged security/runtime pair then passed (`2 passed`), and all workflow contracts passed (`11 passed`). ORAS receives the packages-read token only through its prefetch process and a temporary registry config deleted by an EXIT trap; candidate Python receives only a digest-addressed OCI blob layout, no registry actor/token/config, and revalidates all signed descriptor digests and sizes in verified-cache mode. A live CPython 3.12 acquisition using the exact immutable manifest `sha256:f00ac813f428b7c2ef3d1ce3400496f67ca2d508064e6efc55af1ba6c4697461` succeeded through the configured package access, deleted its auth file, and the credential-free candidate verifier accepted all `11` signed records. Protected Linux matrix proof remains required before resolving the security thread.

### Review batch — imported decorators, test-module fixtures, eval, and requested fixtures

The next fully paginated thread inventory exposed four additional valid P1 findings: `PRRT_kwDORVEFbs6bcIsA`, `PRRT_kwDORVEFbs6bcIsB`, `PRRT_kwDORVEFbs6bcNIR`, and `PRRT_kwDORVEFbs6bcNIT`. Exact selectors `test_complete_suite_rejects_imported_test_body_replacing_decorator`, `test_complete_suite_rejects_test_module_autouse_fixture`, `test_complete_suite_rejects_module_level_eval_compile_test_generation`, and `test_complete_suite_rejects_requested_execution_shaping_fixture` were authored before production edits and failed together (`4 failed`). They proved that imported decorated callables, test-module autouse fixtures, module-level `eval(compile(..., "exec"))`, and requested fixtures mutating `request.node.obj` could all produce or replace execution beyond the sealed selector plan.

Imported-definition resolution now recursively validates decorators in the defining module and local class hierarchy, while retaining only modeled pytest-mark and standard unittest outcome decorators. Candidate test modules and loaded conftests reject autouse fixtures and fixture bodies that replace the collected callable through direct assignment or `setattr`; module-execution analysis treats both `eval` and `exec` as dynamic namespace construction. The unchanged four selectors passed (`4 passed`), followed by the complete runner/workflow surface (`244 passed`). Format, type-check, Ruff/Pylint (`10.00/10`), strict OpenSpec, YAML/registry, bundle imports, checksum/version enforcement with the immutable core public key, `28 passed` contracts, and `git diff --check` passed. Independent smart and complete gates each completed successfully with `1537` collected tests and only the two existing third-party Lark deprecations. The first changed-line review reported `76` nonblocking findings and exposed two new maintainability warnings: a six-parameter imported-decorator resolver and the aggregate rejection function's complexity increase. Reusing the immutable pytest discovery-policy context and extracting the multi-path decorator predicate removed both without suppression; the repeated review returned the accepted `74` inherited warnings/information findings and `0` blockers. The canonical checksum-only signer advanced the unpublished candidate to `specfact-code-review` version `0.49.32`, checksum `sha256:fdd4ea5b36e7b69a24bdd2196e9d553d9153608aa51eca61f499d4c559190fad`, preserved exact `core_compatibility: ===0.55.1`, and removed the stale signature pending the canonical approval-time bot signature. Signed checkpoint, exact committed range/full review, push, protected matrix proof, five thread replies/resolutions, and another fully paginated review refresh remain required.

The first exact-core-bound staged pipeline passed format, YAML, bundle imports, type/lint, command/docs/accountability, Requirements evidence, and `28 passed` contracts, but its staged review exposed one additional local error: the workflow security regression's grouped assertions produced cyclomatic complexity `19`. Splitting the credentialed-prefetch and credential-free-candidate assertions into focused helpers removed the diagnostic without changing behavior; the complete workflow contract remained `11 passed`. The repeated staged pipeline passed every stage; its review reported `142` retained warnings/information findings, `0` errors/blockers, and `PASS_WITH_ADVISORY`, while contracts again passed `28 passed`.

GPG-signed code/security checkpoint `188fc63d45233b8bfcf9a5718bfe63671a0e0467` / tree `0ba6a4a6d8e19de3fd42af6ccfa419517c7844ce` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every commit hook passed with the immutable core checkout supplied to documentation accountability. Its exact committed range review from synchronized `origin/dev` commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema 1.6 `range_preview`, `0 findings`, and expected local-only UNKNOWN (`basedpyright_config_ambiguous` / `policy_parse_failure`). Its explicit full review emitted schema 1.2 `FAIL` with `1228 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Normalized comparison against accepted report `/private/tmp/c14-full-review-4a5fb25.json` found no new diagnostic and one removed inherited line-length warning. A signed evidence-only checkpoint, push, canonical bot signature, protected matrix proof, five thread replies/resolutions, and another authoritative review refresh remain required.

Protected `pr-orchestrator` run `32609101369` succeeded at human-pushed evidence head `08051a07ce62b7bb9c80ea7020a0f9df8772303a`. Module-signature verification passed in job `97119027353`. Exact-core compatibility passed on CPython 3.11 (`97119027361`), 3.12 (`97119027370`), and 3.13 (`97119027360`): each job verified immutable core tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`; completed the full-SHA/checksum-pinned authenticated ORAS prefetch; removed registry credentials before candidate execution; installed exact core; rejected local-version aliases; passed the schema 1.6 smoke; and executed the signed analyzer capsule from the credential-free cache under empty Bubblewrap. Full quality jobs passed on CPython 3.11 (`97119044558`), 3.12 (`97119044534`), and 3.13 (`97119044507`), including format, type, lint, registry, bundle-import, contract, smart-configuration, and complete-suite gates. Canonical publication signing then produced bot commit `5bd79d2e599b8f25b03a2f2e0861897c2cb84384`, changing only the signature line for `specfact-code-review` version `0.49.32`, checksum `sha256:fdd4ea5b36e7b69a24bdd2196e9d553d9153608aa51eca61f499d4c559190fad`, exact compatibility `===0.55.1`, and signature `4fkM3LALJrJ/KIQsuiEdaBxDkT4dOXnAON+98CyVxXniY3NyKN6nmLznJYpObftgd1tvIOWPg3TXvmVOIz7PDQ==`. A signed human evidence checkpoint, latest-head protected checks, five thread replies/resolutions, and another fully paginated authoritative review refresh remain required.

### Review batch — co-located coverage and fixture-node aliases

The next fully paginated inventory found two additional valid P1 threads, `PRRT_kwDORVEFbs6bcV0-` and `PRRT_kwDORVEFbs6bcV1A`. Exact selectors `test_complete_tdd_gate_keeps_colocated_production_file_in_coverage_scope` and `test_complete_suite_rejects_aliased_requested_execution_shaping_fixture` were authored and collected (`2 tests collected`) before production edits, then failed together (`2 failed`). A selector under `pkg/test_app.py` caused its entire parent directory to be treated as test support and removed co-located `pkg/app.py` from coverage input; a requested fixture that assigned `node = request.node` before replacing `node.obj` bypassed the direct-expression mutation check.

Snapshot-root discovery now adds only selected files below a conventional `tests/` path to inferred support roots, while explicit sealed test roots retain their existing exclusion behavior; co-located production remains runtime-measurable. Fixture mutation analysis computes a bounded transitive alias closure for pytest node objects and applies it to both direct `.obj` assignment and `setattr` paths. The two exact regressions and four nearest controls passed (`6 passed`), followed by the complete runner surface (`235 passed`). The first changed-line review correctly exposed one Basedpyright blocker because an annotation-only assignment may have no value; an explicit `None` guard removed it without suppression. The explicit full review then exposed one new nonblocking complexity warning in the alias-closure helper. Extracting assignment-target and alias-value predicates removed that warning without changing behavior; the exact regressions remained green, type-check and Ruff/Pylint remained clean, and the final changed-line review returned the accepted `74` inherited warnings/information findings with `0 blocking`.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; all seven manifests plus registry; bundle imports; `28 passed` contracts; three CLI-contract scenarios; generated command overview and `115`-path command contract; documentation accountability; docs command validation; and publish precheck. Independent smart and complete suites each passed `1539 passed` with only the two existing third-party Lark deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded exact core `0.55.1`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection against immutable tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`. The canonical scoped checksum-only signer advanced `specfact-code-review` from `0.49.32` to `0.49.33`, removed the stale bot signature, set final checksum `sha256:2d78d6e603139e14a4d2060927d6dea0b2ade227eafaaed08c2c52172e6956ff`, and preserved exact `core_compatibility: ===0.55.1`; all seven manifests verified with the immutable core public key. Staged gates, signed checkpoints, exact committed range/full reports, push, canonical bot signature, seven thread replies/resolutions, latest-head protected checks, and another authoritative review refresh remain required.

GPG-signed P1 checkpoint `b6d42409250fc94ee6bed8fa65a77ce19f277eb1` / tree `ac6bb7e1b830baba69c65b07e2e54218f916bd5a` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every commit hook passed with the immutable core checkout supplied to documentation accountability. Its exact committed range review from synchronized `origin/dev` commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema 1.6 `range_preview`, `0 findings`, and the expected local-only UNKNOWN (`basedpyright_config_ambiguous` / `policy_parse_failure`) before protected immutable selection. Its explicit full review emitted schema 1.2 `FAIL` with `1228 findings` and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS); the alias-helper warning was absent. Exact normalized multiset comparison with accepted report `/private/tmp/c14-full-review-4a5fb25.json` found no new/increased diagnostic and one removed inherited `run_immutable_scope_review` length warning. A signed evidence-only checkpoint, push, canonical bot signature, seven thread replies/resolutions, latest-head protected checks, and another fully paginated review refresh remain required.

Canonical approval-time signing produced commit `6e7816a2a42cc936d66d71d80a0d211c5cd11c9d`, whose only change from human evidence head `a57532db0d6c6780781019e1d4850a8e15c37a43` is the `specfact-code-review` manifest signature `IElJWhu4+NElU4yZ6KZhv18VS3pllh0b8i8onx87Na711QX7DJGCQsv8/1m9XkODAY9bwlekAq8WF5Cn6gU+Dg==`. Strict verification with the immutable core public key passed all seven manifests and binds version `0.49.33`, checksum `sha256:2d78d6e603139e14a4d2060927d6dea0b2ade227eafaaed08c2c52172e6956ff`, and exact compatibility `===0.55.1`. Concrete fix evidence was posted to P1 threads `PRRT_kwDORVEFbs6bcE5o`, `PRRT_kwDORVEFbs6bcIsA`, `PRRT_kwDORVEFbs6bcIsB`, `PRRT_kwDORVEFbs6bcNIR`, `PRRT_kwDORVEFbs6bcNIT`, `PRRT_kwDORVEFbs6bcV0-`, and `PRRT_kwDORVEFbs6bcV1A`; all seven were resolved, and a fully paginated readback returned no unresolved thread. A signed human latest-head checkpoint, protected checks, and another authoritative Codex review refresh remain required.

### Review batch — rebound modeled decorator aliases

The requested authoritative Codex review against signed human head `597ec93099bac98396f55971d2e36a470d44ad69` opened valid P1 thread `PRRT_kwDORVEFbs6bceIT`. Exact selector `test_complete_suite_rejects_rebound_pytest_decorator_alias` was authored and collected (`1 test collected`) before production edits, then failed (`1 failed`): after `from pytest import mark`, candidate code could rebind `mark` to a body-replacing object and still be admitted solely by the modeled syntactic prefix. Modeled pytest/unittest decorator aliases now remain trusted only when their imported binding is unique and no module-executed assignment, definition, exception target, loop/comprehension target, or context-manager target rebinds the name. The exact regression passed together with local/imported decorator controls, and a stable `pytest.mark` alias control remains accepted. The complete runner surface passed `237 passed`; final changed-line review returned the accepted `74` inherited warnings/information findings with `0 blocking`; type-check reported `0 errors, 0 warnings, 0 notes`; and Ruff/Pylint passed with `10.00/10`.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; all seven manifests plus registry; bundle imports; `28 passed` contracts; three CLI-contract scenarios; generated overview and `115`-path command contract; documentation accountability; docs validation; and publish precheck. Independent smart and complete suites each passed `1541 passed` with only the two existing third-party Lark deprecations. Exact core `0.55.1` schema/handoff/candidate-identity selectors passed (`3 passed` per interpreter) on CPython `3.11.15`, `3.12.13`, and `3.13.14`, with strict local-version alias rejection reconfirmed against tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`. The canonical scoped checksum-only signer advanced `specfact-code-review` from `0.49.33` to `0.49.34`, removed the stale signature, set checksum `sha256:636f6d85b830192d3292a0669f2c63581ee6d0a8a0d1286f8f304dd6f5880268`, and preserved exact `core_compatibility: ===0.55.1`; all seven manifests verified with the immutable core public key. Staged gates, signed checkpoints, exact committed reports, push, canonical bot signature, thread resolution, protected checks, and another fully paginated review refresh remain required.

GPG-signed rebound-decorator checkpoint `e1977d91efc2cb623083396d40b1bc5155706b49` / tree `ed7e2cb8725baf9f2b036d54b5d2b11ac29b0002` used fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every hook passed with the exact core checkout. Its committed range review from synchronized `origin/dev` emitted schema 1.6 `range_preview`, `0 findings`, and the expected local-only UNKNOWN (`basedpyright_config_ambiguous` / `policy_parse_failure`). Its explicit full review emitted schema 1.2 `FAIL`, `1228 findings`, and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS), with no diagnostic on the new binding helpers. Normalized multiset comparison with accepted report `/private/tmp/c14-full-review-4a5fb25.json` found no new/increased diagnostic and the same one removed inherited `run_immutable_scope_review` length warning. A signed evidence-only checkpoint, push, canonical bot signature, thread resolution, protected checks, and another authoritative review refresh remain required.

Canonical approval-time signing produced commit `58f3a97a7f1a9dd8be17b3df7a5eabf890c8c3f2`, whose only change from pushed human evidence head `4731d6bde08b1f325a940cdc830efd083aeb1302` is manifest signature `FVyZmOHy1/NoCxdk1YcSZqgXYiRTmw8KFE1phI4jyzTK1yl5X+ssmzkAFBQT3mElf/rl7lvilpE8nWvRd+dODg==` for version `0.49.34`, checksum `sha256:636f6d85b830192d3292a0669f2c63581ee6d0a8a0d1286f8f304dd6f5880268`, and exact compatibility `===0.55.1`; strict verification with the immutable core public key passed all seven manifests. Concrete evidence was posted to P1 thread `PRRT_kwDORVEFbs6bceIT` and the thread was resolved. A signed human latest-head checkpoint, protected checks, and another fully paginated Codex review refresh remain required.

### Review batch — requested fixture runtest dispatch replacement

The next fully paginated review inventory exposed valid P1 thread `PRRT_kwDORVEFbs6bcise`: a requested fixture could assign `request.node.runtest = lambda: None`, making pytest report success without executing the collected failing test body. Exact selector `test_complete_suite_rejects_requested_fixture_replacing_runtest_dispatch` was authored and collected first (`1 test collected`), then failed against the unchanged production implementation (`1 failed`: planned status `PASS` instead of required `UNKNOWN`). Fixture mutation analysis now treats both `request.node.obj` and `request.node.runtest` as sealed execution-dispatch targets, including aliases and `setattr`; the exact regression and nearest controls passed (`3 passed`), followed by the complete runner surface (`238 passed`).

The initial readable helper expansion preserved behavior but increased the inherited Pylint module-length diagnostic from `5396` to `5403` lines. Compacting the predicate and local item-reference name restored zero production line growth (`6` additions / `6` deletions) without suppression. Type-check remained `0 errors, 0 warnings, 0 notes`; Ruff/Pylint remained clean with `10.00/10`. The repeated changed-line review at `/private/tmp/c14-runtest-compact-review.json` returned the accepted `74` inherited warnings/information findings, `0` blocking, no diagnostic on either new helper, and an exact normalized finding multiset match with `/private/tmp/c14-rebound-worktree-review.json`.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; all seven manifests plus registry; bundle imports; `28 passed` contracts; three CLI-contract scenarios; generated overview and `115`-path command contract; documentation accountability; docs validation; publish precheck; and independent smart and complete suites each `1542 passed` with only the two existing third-party Lark deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded exact core `0.55.1`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed strict local-version alias rejection against tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`. The canonical staged checksum-only signer advanced the unpublished module from `0.49.34` to `0.49.35`, removed the stale signature, set checksum `sha256:0b1eaa6e58e29c6399b6d5c1a743698cddf57e7625784fcc87b0c1ee9f412b27`, and preserved exact `core_compatibility: ===0.55.1`; all seven manifests verified against the immutable core public key with version enforcement from signed head `fa8a560919fd6cba61da6c40d4255561158ff008`. Signed checkpoint, committed range/full reports, push, canonical approval-time signature, thread resolution, protected matrix proof, and another fully paginated review refresh remain required.

GPG-signed runtest-dispatch checkpoint `47a195cdc55f240e6d93ab430943e0b22ad950ca` / tree `25985e660f581bfac99e3a4e6b1fe845955f402a` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every commit hook passed against the immutable core checkout. Its exact committed range report `/private/tmp/c14-range-review-47a195c.json` emitted schema `1.6`, `range_preview`, `0 findings`, and expected local-only UNKNOWN (`basedpyright_config_ambiguous` / `policy_parse_failure`). Its explicit full report `/private/tmp/c14-full-review-47a195c.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Exact normalized comparison with `/private/tmp/c14-full-review-e1977d9.json` matched the complete finding multiset with no addition, removal, or increase. A signed evidence-only checkpoint, push, canonical approval-time signature, thread resolution, protected matrix proof, and another fully paginated review refresh remain required.

Canonical approval-time signing produced commit `e49ab8cc6016b7ce76ceb72a33006126904e6720`, whose only change from human evidence head `a4aa62b33652e65c44bd312e13f9c6b0e67e82e7` is manifest signature `y9ePWUdFtAcb5FIcJGmcIvcQzbAMQ8ZWOEwp8GnM+1b0PCavXJuJxqy4gdewWOVHiAnxTDYWklh5EP1iXbBBCw==` for `specfact-code-review` version `0.49.35`, checksum `sha256:0b1eaa6e58e29c6399b6d5c1a743698cddf57e7625784fcc87b0c1ee9f412b27`, and exact compatibility `===0.55.1`; strict verification with the immutable core public key passed all seven manifests. Concrete evidence was posted to P1 thread `PRRT_kwDORVEFbs6bcise`, and readback confirmed the thread resolved. A signed human latest-head checkpoint, protected checks, and another authoritative Codex review refresh remain required.

### Review batch — mutated decorator objects and cached pytest callables

The same fully paginated refresh exposed two more valid P1 threads: `PRRT_kwDORVEFbs6bck_X` and `PRRT_kwDORVEFbs6bck_Y`. Exact selectors `test_complete_suite_rejects_mutated_pytest_mark_attribute` and `test_complete_suite_rejects_requested_fixture_replacing_cached_callable` were authored and collected first (`2 tests collected`), then failed together (`2 failed`): assigning `pytest.mark = FakeMark()` left the imported module binding syntactically stable, while assigning `request.node._obj` replaced pytest's cached callable outside the existing `obj`/`runtest` seal.

Modeled import stability now treats the root of module-executed attribute assignment as rebound, invalidating mutated imported decorator objects without changing stable imported-mark behavior. Fixture dispatch validation now seals `_obj`, `obj`, and `runtest` for direct, aliased, and `setattr` writes. Both exact regressions and five nearest controls passed (`7 passed`), followed by the complete runner surface (`240 passed`). The compact production diff preserves zero module line growth (`8` additions / `8` deletions); type-check remained `0 errors, 0 warnings, 0 notes`, Ruff/Pylint remained clean with `10.00/10`, and changed-line review `/private/tmp/c14-three-p1-worktree-review.json` returned the accepted `74` inherited findings, `0` blocking, no helper diagnostic, and an exact normalized multiset match with the prior accepted review.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; all seven manifests plus registry; bundle imports; `28 passed` contracts; three CLI-contract scenarios; generated overview and `115`-path command contract; documentation accountability; docs validation; publish precheck; and independent smart and complete suites each `1544 passed` with only the two existing third-party Lark deprecations. Exact core `0.55.1` schema/handoff/candidate-identity selectors passed (`3 passed` per interpreter) on CPython `3.11.15`, `3.12.13`, and `3.13.14`. The canonical staged checksum-only signer advanced the unpublished module from `0.49.35` to `0.49.36`, removed the stale signature, set checksum `sha256:4d5db22bb42f50f721b3fbfb34db8ec8320b2fbc0e0e68ed00739f98cda131ec`, and preserved exact `core_compatibility: ===0.55.1`; all seven manifests verified against the immutable core public key with version enforcement from signed head `df2dd3f6d9d074e7f81d6f213ae95118b8bab9b2`. Signed checkpoint, committed range/full reports, push, canonical approval-time signature, both thread resolutions, protected matrix proof, and another fully paginated review refresh remain required.

GPG-signed modeled-execution checkpoint `a19b8e667e6a7fa45b897b99b8668b4ef99fa751` / tree `f70efbcc12cfbb3fa4b976e75630ebafd8d0eaee` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every commit hook passed against the immutable core checkout. Its exact committed range report `/private/tmp/c14-range-review-a19b8e6.json` emitted schema `1.6`, `range_preview`, `0 findings`, and expected local-only UNKNOWN (`basedpyright_config_ambiguous` / `policy_parse_failure`). Its explicit full report `/private/tmp/c14-full-review-a19b8e6.json` emitted schema `1.2`, `1228 findings`, and unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Exact normalized comparison with `/private/tmp/c14-full-review-47a195c.json` matched the complete finding multiset with no addition, removal, or increase. A signed evidence-only checkpoint, push, canonical approval-time signature, both thread resolutions, protected matrix proof, and another fully paginated review refresh remain required.

Canonical approval-time signing produced commit `6bddd9fc895e784438280c6d43e7dd6450b169c4`, whose only change from human evidence head `e74bb5e8` is manifest signature `z+tDdiQXma622G5jsCXs5//FDjuTBIi6gbXDfiFaO3c5rZLgzXOoS18jsBU3vFY2qIlMdueYF5LbujqJDEeJBQ==` for version `0.49.36`, checksum `sha256:4d5db22bb42f50f721b3fbfb34db8ec8320b2fbc0e0e68ed00739f98cda131ec`, and exact compatibility `===0.55.1`; strict verification with the immutable core public key passed all seven manifests. Concrete evidence was posted to P1 threads `PRRT_kwDORVEFbs6bck_X` and `PRRT_kwDORVEFbs6bck_Y`, and both were resolved. A signed human latest-head checkpoint, protected checks, and another authoritative Codex review refresh remain required.

### Review batch — dynamic test exports and fixture dispatch indirection

The next fully paginated live inventory exposed four valid P1 threads: `PRRT_kwDORVEFbs6bcrdl`, `PRRT_kwDORVEFbs6bcrdm`, `PRRT_kwDORVEFbs6bcui9`, and `PRRT_kwDORVEFbs6bcui-`. Exact selectors `test_complete_suite_rejects_dynamically_exported_imported_test` and `test_complete_suite_rejects_requested_fixture_replacing_dispatch_through_namespace` were authored and collected before their production changes and failed together (`2 failed`): a support module's `__getattr__` could export an omitted failing test, and a requested fixture could replace `request.node.__dict__["runtest"]`. After those corrections, exact selectors `test_complete_suite_rejects_requested_fixture_using_qualified_setattr` and `test_complete_suite_rejects_imported_execution_shaping_fixture` were authored and collected before their production changes and likewise failed together (`2 failed`): `object.__setattr__(request.node, "runtest", ...)` and a fixture imported into `conftest.py` could both replace sealed dispatch while planning returned PASS.

Candidate planning now rejects test-shaped imports from modules with dynamic namespace exports. Fixture validation seals `_obj`, `obj`, and `runtest` through direct assignment, `__dict__` subscript assignment, ordinary or qualified `setattr`, and bound or qualified `__setattr__`. Local fixture imports are resolved through the configured Python import roots, followed transitively with cycle protection, and fail closed for dynamic or unreadable export modules. All four regressions and a nearest established control passed (`5 passed`), followed by the complete runner surface (`244 passed`). The first combined changed-line review exposed three blockers—two Basedpyright target-index narrowing errors and cyclomatic complexity `18` in the recursive fixture resolver—plus helper-level maintainability warnings. Focused argument extraction and import-forwarding helpers removed every new production-helper diagnostic without suppression. The final report `/private/tmp/c14-four-p1-worktree-review.json` is schema `1.2`, `PASS_WITH_ADVISORY`, with `151` retained warning/information findings and `0` blockers. Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with `10.00/10`.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; seven manifests plus registry; bundle imports; `28 passed` contracts; three CLI-contract scenarios; generated command overview and `115`-path command contract; documentation accountability; docs validation; publish precheck; and `git diff --check`. Independent smart and complete suites each passed `1548 passed` with only the two existing third-party Lark deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded exact core `0.55.1`, passed the schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed that strict `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, while ordinary `==0.55.1` admits the untested local alias. The immutable dependency remains tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`. The canonical scoped checksum-only signer advanced the unpublished module to `specfact-code-review` version `0.49.37`, removed the stale signature, set checksum `sha256:9d5e03e717a5e5591d4113589dc74831b6ec744d677a496d4e796049e1c4c703`, and preserved exact `core_compatibility: ===0.55.1`; all seven manifests verified against the immutable core public key with version enforcement from signed head `e0f26c699d4202ddd3501112d010fbf99060d67e`. Signed checkpoint, exact committed range/full reports, push, canonical approval-time signature, four thread replies/resolutions, protected matrix proof, and another fully paginated review refresh remain required.

GPG-signed dynamic-export/fixture-dispatch checkpoint `37e0036e1e37fb6118e52caa99433c542c2f28c7` / tree `6fb2fc3b0ef882decac7effa64e0707edeccd765` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every commit hook passed against the immutable core checkout. Its exact committed range report `/private/tmp/c14-range-review-37e0036.json` from synchronized dev commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema `1.6`, `range_preview`, `0 findings`, and the expected local-only `policy_parse_failure` UNKNOWN before protected immutable selection. Its explicit full report `/private/tmp/c14-full-review-37e0036.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS), with no diagnostic on the new dispatch/import helpers. Exact normalized comparison with accepted report `/private/tmp/c14-full-review-a19b8e6.json` matched the complete finding multiset with no addition, removal, or increase. A signed evidence-only checkpoint, push, canonical approval-time signature, four thread replies/resolutions, protected matrix proof, and another authoritative review refresh remain required.

Canonical approval-time signing produced commit `e4df071d10cedffe3483458bae1f156e9524390c`, whose only change from pushed human evidence head `c48f3590e76c4ad00b9a77a0885d17c645f25928` is manifest signature `FSDa8rDDKZ/6sjlIl7EDuNN1CjbbWmNAyfcV6Pr7fCupXZr7gwbOlp9b7RI89YJzqtbUsmGjExKvFq4xpamgBw==` for version `0.49.37`, checksum `sha256:9d5e03e717a5e5591d4113589dc74831b6ec744d677a496d4e796049e1c4c703`, and exact compatibility `===0.55.1`. Strict filesystem verification with the immutable core public key passed all seven signed manifests. Concrete corrected evidence was posted to P1 threads `PRRT_kwDORVEFbs6bcrdl`, `PRRT_kwDORVEFbs6bcrdm`, `PRRT_kwDORVEFbs6bcui9`, and `PRRT_kwDORVEFbs6bcui-`; all four are resolved. A GPG-signed human latest-head checkpoint, protected checks, and another fully paginated authoritative review refresh remain required.

### Review batch — pytest item namespace aliases and helper escapes

The next fully paginated review against signed human head `534f9f8fa8834f65ec2538a79f0aec1a3a0a16a4` opened valid P1 threads `PRRT_kwDORVEFbs6bc4QM` and `PRRT_kwDORVEFbs6bc4QO`. Exact selectors `test_complete_suite_rejects_requested_fixture_replacing_dispatch_through_namespace_alias` and `test_complete_suite_rejects_requested_fixture_delegating_item_mutation` were authored and collected first (`2 tests collected`), then failed together (`2 failed`): assigning `namespace = request.node.__dict__` before replacing `namespace["runtest"]`, or passing `request.node` to a sibling helper that replaces `node.runtest`, both left the plan at PASS.

Fixture validation now fails closed when a pytest item namespace is assigned through an alias, and when an item reference or tracked alias escapes as a positional/keyword argument or unmodeled method-call receiver. The exact regressions and two nearest direct controls passed (`4 passed`), followed by the complete runner surface (`246 passed`). Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with `10.00/10`. Changed-line review returned `PASS_WITH_ADVISORY`, `153` retained warning/information findings, `0` blockers, and no diagnostic on the new item-escape helpers.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; seven manifests plus registry; bundle imports; `28 passed` contracts; three CLI-contract scenarios; generated command overview and `115`-path command contract; documentation accountability; docs validation; publish precheck; and `git diff --check`. Independent smart and complete suites each passed `1550 passed` with only the two existing third-party Lark deprecations. Exact core `0.55.1` schema/handoff/candidate-identity selectors passed (`3 passed` per interpreter) on CPython `3.11.15`, `3.12.13`, and `3.13.14`. The canonical scoped checksum-only signer advanced the unpublished module from `0.49.37` to `0.49.38`, removed the stale signature, set checksum `sha256:c589311c6be7ed1108052cd8edbe6dc40792ca65c065d1ec32c3d36dea7472eb`, and preserved exact `core_compatibility: ===0.55.1`; all seven manifests verified against the immutable core public key with version enforcement from signed head `534f9f8fa8834f65ec2538a79f0aec1a3a0a16a4`. Signed checkpoint, exact committed reports, push, canonical approval-time signature, both thread replies/resolutions, protected matrix proof, and another authoritative review refresh remain required.

GPG-signed item-escape checkpoint `7076c1940f1b69709eb518097975e0c5c5b12060` / tree `15f68ca73e1c3556e01ba0c4c9233e42030b4fc9` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every commit hook passed against the immutable core checkout. Its exact committed range report `/private/tmp/c14-range-review-7076c19.json` from synchronized dev commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema `1.6`, `0 findings`, and the expected local-only `policy_parse_failure` UNKNOWN before protected immutable selection. Its explicit full report `/private/tmp/c14-full-review-7076c19.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Line-normalized comparison with accepted report `/private/tmp/c14-full-review-37e0036.json` matched the complete diagnostic multiset at SHA-256 `9ee5171ffb5471f8bc9eb41d8081304d9bd56d21faf3c43620e1ca5a31a13b4e`; differences were limited to expected downstream line shifts. A signed evidence-only checkpoint, push, canonical approval-time signature, both thread replies/resolutions, protected matrix proof, and another authoritative review refresh remain required.

Canonical approval-time signing produced commit `78e7d9412230280fc04eab835798efe3ff60b684` / tree `835a186fb45a153d3c8089ca486a827f9334d83a`, whose only change from pushed human evidence head `5fa5564e965a9d7cec713a894ce3adcbb21ad650` is manifest signature `9zxmyoZ6HH+CRSS4inyREPx9tXLsfDCNDqinIQ7xWpwsY/3FfY8cBxPxhqaszkNrSX2uKsxROphbewskLLd2CQ==` for `specfact-code-review` version `0.49.38`, checksum `sha256:c589311c6be7ed1108052cd8edbe6dc40792ca65c065d1ec32c3d36dea7472eb`, and exact compatibility `===0.55.1`. Strict filesystem verification with the immutable core public key and version base `534f9f8fa8834f65ec2538a79f0aec1a3a0a16a4` passed all seven signed manifests. Concrete evidence was posted to P1 threads `PRRT_kwDORVEFbs6bc4QM` and `PRRT_kwDORVEFbs6bc4QO`, and both threads resolved. A GPG-signed human latest-head checkpoint, protected checks, and another fully paginated authoritative review refresh remain required.

### Review batch — collected callable mutation and conditional fixture imports

The fully paginated inventory after signed human head `34fd4065e14f110e9ac4507400200cdc121bdb1b` exposed two additional valid P1 threads, `PRRT_kwDORVEFbs6bc6bS` and `PRRT_kwDORVEFbs6bc6bT`. Exact selectors `test_complete_suite_rejects_requested_fixture_rewriting_collected_callable_code` and `test_complete_suite_rejects_conditionally_imported_execution_shaping_fixture` were authored and collected first (`2 tests collected`), then failed together (`2 failed`): assigning `request.node.obj.__code__` could replace the collected failing function body, and an execution-shaping fixture imported beneath `if True` in `conftest.py` was registered by pytest while the resolver inspected only direct module statements. Both cases left the plan at PASS.

Fixture validation now treats every assignment rooted below the collected callable (`node.obj` or `node._obj`) as execution shaping and applies the same fail-closed rule to ordinary, qualified, and bound attribute setters. Forwarded fixture resolution now traverses the existing bounded module-execution node set, including imports under module-executed compound statements while excluding function/class/lambda bodies. The two exact regressions and four nearest controls passed (`6 passed`), followed by the complete runner surface (`248 passed`). Distinct fail-closed assertions on empty selectors and disabled heuristic fallback removed the two new duplicate-test diagnostics; changed-line review returned the accepted `153` warnings/information findings with `0` errors/blockers and no diagnostic on either new test or production helper. Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with `10.00/10`.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; seven manifests plus registry; bundle imports; `28 passed` contracts; three CLI-contract scenarios; generated command overview and `115`-path command contract; documentation accountability; docs validation; publish precheck; and `git diff --check`. Independent smart and complete suites each passed `1552 passed` with only the two existing third-party Lark deprecations. Exact core `0.55.1` schema/handoff/candidate-identity selectors passed (`3 passed` per interpreter) on CPython `3.11.15`, `3.12.13`, and `3.13.14`, including strict rejection of `0.55.1+vendor` under `===0.55.1`. The canonical scoped checksum-only signer advanced the unpublished module from `0.49.38` to `0.49.39`, removed the stale signature, set checksum `sha256:c9af1f4ce55043f0350774732b4e1e28d12438a53dc32d7230f96d62c0c139fb`, and preserved exact `core_compatibility: ===0.55.1`; all seven manifests verified against the immutable core public key with version enforcement from signed head `34fd4065e14f110e9ac4507400200cdc121bdb1b`. Signed checkpoint, exact committed reports, push, canonical approval-time signature, both thread replies/resolutions, protected matrix proof, and another authoritative review refresh remain required.

GPG-signed callable-mutation checkpoint `a3ba6e08a473ae93d256315ab562b35e0a1d11b8` / tree `2cf81b107e4d827f2c650a1692f70aa8517a5a54` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every commit hook passed against the immutable core checkout. Its exact committed range report `/private/tmp/c14-range-review-a3ba6e0.json` from synchronized dev commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema `1.6`, `0 findings`, and the expected local-only `policy_parse_failure` UNKNOWN before protected immutable selection. Its explicit full report `/private/tmp/c14-full-review-a3ba6e0.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Line-normalized comparison with accepted report `/private/tmp/c14-full-review-7076c19.json` matched the complete diagnostic multiset at SHA-256 `9ee5171ffb5471f8bc9eb41d8081304d9bd56d21faf3c43620e1ca5a31a13b4e`; differences were limited to expected downstream line shifts. A signed evidence-only checkpoint, push, canonical approval-time signature, both thread replies/resolutions, protected matrix proof, and another authoritative review refresh remain required.

Canonical approval-time signing produced commit `d3779bfc4b9e8c1382e6c40c94d3b1c30b91300b` / tree `c6108d1bcc43e440683c322051af396411052fd2`, whose only change from pushed human evidence head `e181ff471194776f742af1e7ca8d81482ffe0d9c` is manifest signature `nIUEAuhApeymA7ZIpHLskm+E52WlgVc6qOOmejsZT7B1QAN6Stfj4c8t5HwAuz4/8UN56GizWoD4GQTaoKsRCg==` for `specfact-code-review` version `0.49.39`, checksum `sha256:c9af1f4ce55043f0350774732b4e1e28d12438a53dc32d7230f96d62c0c139fb`, and exact compatibility `===0.55.1`. Strict filesystem verification with the immutable core public key and version base `34fd4065e14f110e9ac4507400200cdc121bdb1b` passed all seven signed manifests. Concrete evidence was posted to P1 threads `PRRT_kwDORVEFbs6bc6bS` and `PRRT_kwDORVEFbs6bc6bT`, and both threads resolved. A GPG-signed human latest-head checkpoint, protected checks, and another fully paginated authoritative review refresh remain required.

### Review batch — assigned imported fixture bindings

The next fully paginated inventory exposed valid P1 thread `PRRT_kwDORVEFbs6bc-c8`: `conftest.py` could import a local module with `from tests import support`, bind `bypass = support.bypass`, and expose an execution-shaping fixture that replaced `request.node.runtest` while the planner returned PASS. Exact selector `test_complete_suite_rejects_assigned_imported_execution_shaping_fixture` was authored and collected first (`1 test collected`), then failed against the unchanged production implementation (`1 failed`).

The fixture resolver now maps bounded local module-valued `ImportFrom` bindings, follows module-member assignment exports through the same recursive/cycle-protected execution-shaping check, and shares one direct/assigned import aggregation path for both conftest and transitive module resolution. The exact regression plus direct and conditional import controls passed (`3 passed`), followed by the complete runner surface (`249 passed`). Extracting the common aggregation and asserting the complete fail-closed plan removed the initial new low-complexity-length and duplicate-test diagnostics; changed-line review returned the accepted `153` warning/information findings with `0` errors/blockers and no diagnostic on the new helpers or test. Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with `10.00/10`.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; seven manifests plus registry; bundle imports; `28 passed` contracts; three CLI-contract scenarios; generated command overview and `115`-path command contract; documentation accountability; docs validation; publish precheck; and `git diff --check`. Independent smart and complete suites each passed `1553 passed` with only the two existing third-party Lark deprecations. Exact core `0.55.1` schema/handoff/candidate-identity selectors passed (`3 passed` per interpreter) on CPython `3.11.15`, `3.12.13`, and `3.13.14`, including strict rejection of `0.55.1+vendor` under `===0.55.1`. The canonical scoped checksum-only signer advanced the unpublished module from `0.49.39` to `0.49.40`, removed the stale signature, set checksum `sha256:57a834267a8f188197e7d28b760f4efa1a32a09e75ef9c5e6528b06aa6342989`, and preserved exact `core_compatibility: ===0.55.1`; all seven manifests verified against the immutable core public key with version enforcement from signed head `205d16c37298860c8622f51682c4ad6444e59999`. Signed checkpoint, exact committed reports, push, canonical approval-time signature, thread reply/resolution, protected matrix proof, and another authoritative review refresh remain required.

GPG-signed assigned-import checkpoint `d375d6f43e90df928f1155cc52a60beff150c5db` / tree `66025a46b41b121e388891246834178df97aa2f4` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every commit hook passed against the immutable core checkout. Its exact committed range report `/private/tmp/c14-range-review-d375d6f.json` from synchronized dev commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema `1.6`, `range_preview`, `0 findings`, and the expected local-only `policy_parse_failure` UNKNOWN. Its explicit full report `/private/tmp/c14-full-review-d375d6f.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). Line-normalized comparison with accepted report `/private/tmp/c14-full-review-a3ba6e0.json` matched the complete diagnostic multiset with no addition, removal, or increase. A signed evidence-only checkpoint and push were superseded by the next live P1 review batch before publication.

### Review batch — setter mutation and class-level pytest dispatch

The next fully paginated live inventory retained assigned-import thread `PRRT_kwDORVEFbs6bc-c8` and opened two additional valid P1 threads: `PRRT_kwDORVEFbs6bdCCQ` showed that `setattr(pytest, "mark", FakeMark())` could replace a modeled decorator alias before a body-replacing mark, and `PRRT_kwDORVEFbs6bdCCR` showed that a requested fixture could replace dispatch through `request.node.__class__.runtest`. Exact selectors `test_complete_suite_rejects_pytest_mark_mutated_through_setattr` and `test_complete_suite_rejects_requested_fixture_replacing_class_runtest_dispatch` were authored and collected before production edits (`2 tests collected`), then failed together (`2 failed`) because both plans returned PASS.

Modeled import stability now includes module-executed setter targets, so setter mutation of the imported pytest alias removes its decorators from the accepted model. Fixture execution-shaping validation now treats the collected item's class as a dispatch owner for direct attribute, namespace, and setter mutation. The two exact regressions plus three nearest stable/direct controls passed (`5 passed`), followed by the complete runner surface (`251 passed`). A setter-target helper extraction and complete fail-closed assertions removed the initial changed-line complexity blocker and duplicate-test advisory. Final changed-line Code Review returned the accepted `153` warning/information findings with `0` blockers and no diagnostic on the new helper or regressions. Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with Pylint `10.00/10`; and `git diff --check` passed.

The canonical changed-only checksum signer advanced the unpublished module from `0.49.40` to `0.49.41`, removed the pending signature, set final filesystem checksum `sha256:02114c14c5867dbdf8ad828a754e2706f8ffaeaf1f318abe43335ed7efc7430a`, and preserved exact `core_compatibility: ===0.55.1`. All seven manifests verified against the immutable core public key with version enforcement from signed source checkpoint `d375d6f43e90df928f1155cc52a60beff150c5db`. Strict OpenSpec, contracts, full/smart tests, exact-core compatibility matrix, signed human checkpoints, committed range/full Code Review, push, canonical approval-time signature, three thread replies/resolutions, protected checks, and another authoritative review refresh remain required.

Fresh final-byte gates for this review batch then passed: independent complete and smart suites each reported `1555 passed` with only the same two third-party Lark deprecations. Isolated CPython `3.11.15`, `3.12.13`, and `3.13.14` environments each loaded immutable core `0.55.1`, passed the exact schema-1.6/core-handoff/candidate-identity selectors (`3 passed` per interpreter), and reconfirmed that strict `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, while ordinary `==0.55.1` admits the untested local alias. The immutable dependency remains tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`.

GPG-signed source checkpoint `0391da5eb2b238820c0d992a60fd0c808c209b84` / tree `ab1ad729ce914968a766aec1e52433ac4050bedb` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; the exact-core-bound commit hooks passed signature/version, format, seven-manifest YAML validation, bundle imports, type/lint (`0` diagnostics; Pylint `10.00/10`), generated command overview and `115`-path command contract, documentation accountability, docs validation, Requirements evidence, changed-line Code Review (`153` retained warning/information findings, `0` blockers), and contracts (`28 passed`). Its committed range report `/private/tmp/c14-range-review-0391da5.json` from synchronized dev commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema `1.6`, `range_preview`, `0 findings`, and the expected local-only `policy_parse_failure` UNKNOWN. Its explicit full report `/private/tmp/c14-full-review-0391da5.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). The line-independent normalized finding multiset matched the preceding accepted committed report exactly at `sha256:bdbaaa083f98b4e3562d01eabf22e71639689426eae413772680c86c4aaffa01`. Push, canonical approval-time signature, all three thread replies/resolutions, protected checks, and another authoritative review refresh remain required.

### Review batch — requested fixture dynamic plugin registration

The next fully paginated Codex review against canonical bot-signed head `4e0d51e5cb8ac52141c7034d9d766f0ea1811b30` opened valid P1 thread `PRRT_kwDORVEFbs6bdqyu`: a requested fixture could register a plugin through `request.config.pluginmanager.register(...)`, install a `pytest_pyfunc_call` hook returning true, and make pytest report success without executing the planned failing test body. Exact selector `test_complete_suite_rejects_requested_fixture_registering_pytest_hook_plugin` was authored and collected first (`1 test collected`), then failed against unchanged production bytes (`1 failed`) because the planner returned PASS with `tests/test_failure.py::test_failure` instead of the required fail-closed UNKNOWN plan.

Fixture execution-shaping validation now rejects access to pytest's plugin manager through the requested `request` fixture, including bounded request/config aliases, before accepting the selector inventory. The exact regression and three nearest direct/alias/class-dispatch controls passed (`4 passed`), followed by the complete runner surface (`252 passed`). Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with Pylint `10.00/10`; and changed-line Code Review returned the accepted `74` warning/information findings with `0` blockers.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; seven manifests plus registry; bundle imports; checksum/version enforcement; `28 passed` contracts; three CLI-contract scenarios; generated command overview and `115`-path command contract; documentation accountability against the immutable core checkout; docs validation; publish precheck; and `git diff --check`. Independent complete and smart suites each passed `1556 passed` with only the two existing third-party Lark deprecations. Exact core `0.55.1` schema/handoff/candidate-identity selectors passed (`3 passed` per interpreter) on CPython `3.11.15`, `3.12.13`, and `3.13.14`; every environment reconfirmed that strict `===0.55.1` accepts `0.55.1`, rejects `0.55.1+vendor`, while ordinary `==0.55.1` admits the untested local alias. The immutable dependency remains tag `v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and tree `47984be5434d7ae65ed6908bf525a32053290337`. The canonical scoped checksum-only signer advanced the unpublished module from `0.49.41` to `0.49.42`, removed the stale signature, set checksum `sha256:67b9faaece5ce7c1c53f84388e7e940b117ed01791fbf03b7bfc6141b5d15d53`, and preserved exact `core_compatibility: ===0.55.1`. Staged gates, a GPG-signed checkpoint, exact committed range/full reports, push, canonical approval-time signature, thread reply/resolution, protected matrix proof, and another fully paginated authoritative review refresh remain required.

GPG-signed fixture-plugin checkpoint `e3cc620b25c85d424e4061eecd12f846cf7a2eb1` / tree `26c2791b21f85c71a436369677e7529037b93f49` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every exact-core-bound commit hook passed. Its committed range report `/private/tmp/c14-range-review-e3cc620.json` from synchronized dev commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema `1.6`, `0 findings`, and expected local-only `policy_parse_failure` UNKNOWN. Its explicit full report `/private/tmp/c14-full-review-e3cc620.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). A line-independent normalized finding comparison with accepted report `/private/tmp/c14-full-review-0391da5.json` matched exactly at SHA-256 `0092ffa45f9f0e4d7d2e16b1a1747a0e5a3335f31f404f3f48dc4b398c44412a`, proving no new diagnostic. A signed evidence-only checkpoint, push, canonical approval-time signature, thread resolution, protected matrix proof, and another fully paginated review refresh remain required.

Independent review then exposed the equivalent built-in `pytestconfig` path before the batch was declared clean. Exact selector `test_complete_suite_rejects_requested_fixture_using_pytestconfig_plugin_manager` collected once and failed (`1 failed`) because a requested fixture could call `pytestconfig.pluginmanager.register(...)` and retain a PASS plan. The config-alias model now seeds the built-in `pytestconfig` fixture as well as `request.config`; the exact regression and two nearest controls passed (`3 passed`), and the complete runner surface passed `253 passed`. Final changed-line review remained at `74 findings` with `0` blockers; type-check stayed at `0 errors, 0 warnings, 0 notes`; Ruff/Pylint stayed clean at `10.00/10`. Strict OpenSpec, seven manifests/registry, bundle imports, checksum/version enforcement, `28 passed` contracts, three CLI scenarios, `115` command paths, documentation accountability, docs validation, and publish precheck all passed. Independent complete and smart suites each passed `1557 passed` with only the two existing Lark deprecations. The exact immutable-core three-selector and strict-alias matrix passed again on CPython `3.11.15`, `3.12.13`, and `3.13.14`. The checksum-only signer advanced the unpublished module from `0.49.42` to `0.49.43`, removed the stale signature, set checksum `sha256:285973ed50c9571325c490ed15b0a1f2c8115b487e803deea0cf7cc52fb99bf9`, and preserved `core_compatibility: ===0.55.1`. Staged gates, signed checkpoints, exact committed reports, push/signature verification, protected checks, and another fully paginated review refresh remain required.

GPG-signed pytestconfig checkpoint `a5c7429751703b59fb6dbd75a4e1c7e8acf26dad` / tree `f20c4bf46157f53ceeec8009b5874c2f9e4b1488` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every exact-core-bound commit hook passed. Its committed range report `/private/tmp/c14-range-review-a5c7429.json` emitted schema `1.6`, `0 findings`, and expected local-only `policy_parse_failure` UNKNOWN. Its explicit full report `/private/tmp/c14-full-review-a5c7429.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers (`40` Basedpyright, `1` Pylint, `5` pytest, `149` Radon, `181` Radon-KISS). The line-independent normalized finding multiset matched `/private/tmp/c14-full-review-e3cc620.json` exactly at SHA-256 `0092ffa45f9f0e4d7d2e16b1a1747a0e5a3335f31f404f3f48dc4b398c44412a`, proving no new diagnostic. A signed evidence-only checkpoint, push, canonical approval-time signature, protected matrix proof, and another fully paginated review refresh remain required.

### Review batch — session plugin-manager paths and conditional conftest hooks

The next fully paginated review against canonical signed head `b5fe49d187aea33531ac16c29bec9b7beb4b8f27` opened valid P1 threads `PRRT_kwDORVEFbs6bdxgg` and `PRRT_kwDORVEFbs6bdxgm`. Exact selectors `test_complete_suite_rejects_requested_fixture_using_session_plugin_manager` and `test_complete_suite_rejects_conditional_repository_pytest_execution_hook` were authored and collected first (`2 tests collected`), then failed together (`2 failed`) because both plans returned PASS. Adjacent exact selector `test_complete_suite_rejects_conditional_imported_pytest_execution_hook` was then authored, collected once, and failed independently (`1 failed`) before production edits, proving that module-control-flow import bindings had the same registration gap.

Fixture config detection now follows bounded attribute paths rooted at the requested `request` fixture, covering `request.session.config` and the already sealed direct path, while retaining explicit `pytestconfig` aliases. Conftest hook inventory now traverses the bounded module-execution node set for local and imported `pytest_*` declarations, including module-level compound statements without entering function/class/lambda bodies. The three exact regressions plus four nearest direct/specname/plugin controls passed (`7 passed`), followed by the complete runner surface (`256 passed`). Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with `10.00/10`; changed-line Code Review returned `74 findings` with `0` blockers. Strict OpenSpec, seven manifests/registry, bundle imports, checksum/version enforcement, `28 passed` contracts, three CLI scenarios, `115` command paths, documentation accountability, docs validation, and publish precheck passed. Independent complete and smart suites each passed `1560 passed` with only the two existing Lark deprecations. Exact core `0.55.1` selectors and strict local-alias rejection passed again on CPython `3.11.15`, `3.12.13`, and `3.13.14`. The checksum-only signer advanced the unpublished module from `0.49.43` to `0.49.44`, removed the stale signature, set checksum `sha256:ad8de3e2a91c48a810c56417c4a401545f96e09bb7ed7dddc6181d3c091a3df2`, and preserved exact `core_compatibility: ===0.55.1`. Staged gates, signed checkpoints, exact committed reports, push/signature verification, both thread resolutions, protected checks, and another fully paginated review refresh remain required.

GPG-signed session/config and conditional-hook checkpoint `72354afef7a4dd5aabca210c439dd1d62beff76e` / tree `c48dd80433e14cea4acc6cce5df666c027fa2a8e` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; every exact-core-bound commit hook passed. Its committed range report `/private/tmp/c14-range-review-72354af.json` from synchronized dev commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema `1.6`, `0 findings`, and expected local-only `basedpyright_config_ambiguous` UNKNOWN. Its explicit full report `/private/tmp/c14-full-review-72354af.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers. The line-independent normalized finding multiset matched `/private/tmp/c14-full-review-a5c7429.json` exactly at SHA-256 `0092ffa45f9f0e4d7d2e16b1a1747a0e5a3335f31f404f3f48dc4b398c44412a`, proving no new diagnostic. A signed evidence-only checkpoint, push, canonical approval-time signature, both thread replies/resolutions, protected matrix proof, and another fully paginated authoritative review refresh remain required.

Canonical approval-time signing produced commit `b3040d062968423e0db767f7e92d967459edcfb7`, whose only change from human evidence head `d668f1077dc517be86072b54701c7ef0a7b0645f` is manifest signature `J8u5g1NDNq+pQBFmFYdnTUNwhagsp/XbMWkW5haN6XAKi+ouMsNqevYMYhlLNbuAytGX2N5XG63YixWasK1eAg==` for version `0.49.44`, checksum `sha256:ad8de3e2a91c48a810c56417c4a401545f96e09bb7ed7dddc6181d3c091a3df2`, and exact compatibility `===0.55.1`. Strict filesystem verification with the immutable core public key passed all seven signed manifests. Concrete corrected evidence was posted to P1 threads `PRRT_kwDORVEFbs6bdxgg` and `PRRT_kwDORVEFbs6bdxgm`; both are resolved.

### Review batch — request-derived plugin-manager aliases

The next fully paginated Codex review against canonical bot-signed head `b3040d062968423e0db767f7e92d967459edcfb7` opened valid P1 thread `PRRT_kwDORVEFbs6bd4Ud`: a requested fixture could assign `session = request.session` and then reach `session.config.pluginmanager.register(...)`, making a collected failing test pass without its body running while planning returned PASS. Exact selector `test_complete_suite_rejects_requested_fixture_using_aliased_session_plugin_manager` was authored and collected first (`1 test collected`), then failed against unchanged production bytes (`1 failed`) with the false PASS selector inventory.

Request-derived alias analysis now propagates bounded attribute-chain assignments before config/plugin-manager inspection, closing intermediate session and config aliases while retaining the direct request and built-in `pytestconfig` paths. The exact regression and three nearest plugin-manager controls passed (`4 passed`), followed by the complete runner surface (`257 passed`). Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with `10.00/10`; governed changed-line review `/private/tmp/c14-alias-p1-worktree-review.json` returned `74 findings`, `0` blockers, and no diagnostic on the new helper.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; seven manifests plus registry; bundle imports; checksum/version enforcement; `28 passed` contracts; three CLI scenarios; `115` generated command paths; documentation accountability against the immutable core checkout; docs validation; publish precheck; and `git diff --check`. Independent complete and smart suites each passed `1561 passed` with only the two existing Lark deprecations. Exact core `0.55.1` schema/handoff/candidate-identity selectors passed (`3 passed` per interpreter) on CPython `3.11.15`, `3.12.13`, and `3.13.14`; strict `===0.55.1` accepted `0.55.1` and rejected `0.55.1+vendor` in each environment. The checksum-only signer advanced the unpublished module to version `0.49.45`, removed the stale signature, set checksum `sha256:8bcf1907caf013b3c7a6f24cc6848740abdb0cff61aede6e80e66ac46cac9006`, and preserved exact `core_compatibility: ===0.55.1`. Staged gates, signed checkpoints, exact committed reports, push/signature verification, thread resolution, protected checks, and another fully paginated authoritative review refresh remain required.

GPG-signed request-derived-alias checkpoint `7db2db293cd23ce4632e245ffa2f3ecccdfee6e3` / tree `c5e1f2a936fe9d8e9f46c17bbee4a81c0a9ec1e4` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; the full staged pipeline and every exact-core-bound commit hook passed. Its committed range report `/private/tmp/c14-range-review-7db2db2.json` from synchronized dev commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema `1.6`, `0 findings`, and expected local-only `basedpyright_config_ambiguous` UNKNOWN. Its explicit full report `/private/tmp/c14-full-review-7db2db2.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers. The line-independent normalized finding multiset matched `/private/tmp/c14-full-review-72354af.json` exactly at SHA-256 `0092ffa45f9f0e4d7d2e16b1a1747a0e5a3335f31f404f3f48dc4b398c44412a`, proving no new diagnostic. A signed evidence-only checkpoint, push, canonical approval-time signature, thread reply/resolution, protected checks, and another fully paginated authoritative review refresh remain required.

Canonical approval-time signing produced commit `14fb45b1623c2a5b52f47fa6a93358d2821e9577`, whose only change from human evidence head `b3e83438312abcf7154a995bb2e73642c18716f0` is manifest signature `O6V5bfL1LQSmdmzN+IuAvVfTpTKIO317uTmx5lIG5/feXciWxWb+MQyFPDC32DSRv/hiTPL/NJ9f/rT9I71HCw==` for version `0.49.45`, checksum `sha256:8bcf1907caf013b3c7a6f24cc6848740abdb0cff61aede6e80e66ac46cac9006`, and exact compatibility `===0.55.1`. Strict filesystem verification with the immutable core public key passed all seven signed manifests. Concrete evidence was posted to P1 thread `PRRT_kwDORVEFbs6bd4Ud`, and readback confirmed the thread resolved.

### Review batch — destructured and delegated request capabilities

The next exact signed-head Codex review opened valid P1 thread `PRRT_kwDORVEFbs6bd9rT`: tuple destructuring `session, = (request.session,)` bypassed scalar request-derived alias propagation and could register a body-skipping `pytest_pyfunc_call` plugin while planning returned PASS. Exact selector `test_complete_suite_rejects_requested_fixture_using_destructured_session_plugin_manager` and adjacent selector `test_complete_suite_rejects_requested_fixture_delegating_request_capability` were authored and collected before production edits (`2 tests collected`), then failed together (`2 failed`) with false PASS plans.

Request-derived alias propagation now finds request references anywhere in bounded assignment expressions, including destructuring and accessor calls. Passing a tracked request capability into a helper call also fails closed, preventing the same plugin-manager mutation from moving behind a local indirection. Both regressions and four nearest controls passed (`6 passed`), followed by the complete runner surface (`259 passed`). Type-check reported `0 errors, 0 warnings, 0 notes`; format, Ruff, and Pylint passed with `10.00/10`; governed changed-line review `/private/tmp/c14-destructured-request-worktree-review.json` returned `74 findings`, `0` blockers, and no diagnostic on the new helpers.

Fresh final-byte gates on 2026-08-23 passed: strict C14 OpenSpec; seven manifests plus registry; bundle imports; checksum/version enforcement; `28 passed` contracts; three CLI scenarios; `115` generated command paths; documentation accountability; docs validation; publish precheck; and `git diff --check`. Independent complete and smart suites each passed `1563 passed` with only the two existing Lark deprecations. Exact core `0.55.1` schema/handoff/candidate-identity selectors passed (`3 passed` per interpreter) on CPython `3.11.15`, `3.12.13`, and `3.13.14`, including strict local-version alias rejection. The checksum-only signer advanced the unpublished module to version `0.49.46`, removed the stale signature, set checksum `sha256:62175eace29561d22ae6c46751c4fe39b3f6e54890f4387ee571b05801c512ca`, and preserved exact `core_compatibility: ===0.55.1`. Staged gates, signed checkpoints, exact committed reports, push/signature verification, thread resolution, protected checks, and another fully paginated authoritative review refresh remain required.

GPG-signed destructuring/delegation checkpoint `bbb5a705fe65e9036d1cf07229a6faddae5b7d51` / tree `37dda86458d3ed5b5797c81f14558722c220746c` used required fingerprint `17B2C2FD3DE2138D447CFFC868E0961150EBB243`; the full staged pipeline and every exact-core-bound commit hook passed. Its committed range report `/private/tmp/c14-range-review-bbb5a70.json` from synchronized dev commit `c3eda08c732267dc3614130f5f36bcd473182d0b` emitted schema `1.6`, `0 findings`, and expected local-only `basedpyright_config_ambiguous` UNKNOWN. Its explicit full report `/private/tmp/c14-full-review-bbb5a70.json` emitted schema `1.2`, `1228 findings`, and the unchanged `376` inherited blockers. The line-independent normalized finding multiset matched `/private/tmp/c14-full-review-7db2db2.json` exactly at SHA-256 `0092ffa45f9f0e4d7d2e16b1a1747a0e5a3335f31f404f3f48dc4b398c44412a`, proving no new diagnostic. A signed evidence-only checkpoint, push, canonical approval-time signature, thread reply/resolution, protected checks, and another fully paginated authoritative review refresh remain required.
