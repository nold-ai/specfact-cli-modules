# Tasks: Scope Truth and Differential Enforcement

Every implementation task targets at most two hours. Tests precede code. This planning branch must remain OpenSpec-only.

## 0. Planning

- [x] 0.1 Define explicit scope sources, truthful statuses, differential analysis, and non-goals.
- [x] 0.2 Define a one-release compatibility path for `--scope changed`.
- [x] 0.3 Create no package, test, registry, version, signature, prompt, or generated-doc changes.

## Implementation acceptance gate

- [x] A.1 Before tests or implementation, resolve the linked public implementation issue from the accepted change/PR metadata and apply `docs/agent-rules/60-github-change-governance.md`. Read `.specfact/backlog/github_hierarchy_cache.md` first; when it or `.specfact/backlog/github_hierarchy_cache_state.json` is missing or older than 300 seconds, run `python scripts/sync_github_hierarchy_cache.py`, then re-read current GitHub state. Verify the issue exists; parent relationship; required labels; project assignment/state; complete blockers and blocked-by relationships; and that no linked blocker is open. If the issue is absent/ambiguous, metadata is missing/inaccessible, a blocker is unresolved, or refreshed state remains `in progress`, STOP before test or source edits and ask the user to resolve/clarify it. Never infer readiness from this planning PR alone.
- [x] A.2 Revalidate this accepted module contract against current `dev`, inspect the public core Code Review callers and the adjudicated #665–#671 benchmark at immutable refs, and confirm the merged core #674 boundary that generic review-scope semantics remain module-owned.
- [x] A.3 Record the issue URL/number, parent, labels, project/state, blocker/blocked-by state, cache `generated_at`/GitHub observation time, stop/ready decision, and reviewed core refs/paths in this change's `TDD_EVIDENCE.md`. Only a complete ready result permits task 1.1. Do not edit core from this change. A separate accepted core adoption change is required before release task 4.7, after the signed module release exists.

## 1. Failing scope tests

- [x] 1.1 Add `test_range_scope_includes_committed_files_on_clean_checkout`.
- [x] 1.2 Add `test_range_scope_uses_merge_base_not_head_worktree` and `test_range_multiple_best_merge_bases_is_unknown`; create an isolated synthetic criss-cross repository, require the equivalent of `git merge-base --all` to yield exactly one commit, and prove zero/multiple bases return UNKNOWN before diff/materialization while retaining the sorted candidate commit/tree list and canonical candidate-set digest.
- [x] 1.3 Add `test_scope_git_failure_is_unknown_and_blocks_enforcement`.
- [x] 1.4 Add `test_empty_resolved_range_is_not_applicable`; use a successfully resolved unfiltered range whose diff contains neither governed Python/test/test-support input nor governed analyzer-policy/config input, and prove rejected narrowing options cannot manufacture NOT_APPLICABLE.
- [x] 1.5 Add `test_range_scope_includes_changed_tests_by_default`.
- [x] 1.6 Add static CLI-contract red cases for required full refs, invalid combinations, deprecated changed alias, assurance/enforcement projection, and explicit positional files. Do not add Git setup to the argv-only contract harness; tasks 1.8, 1.10, 2.4, and 2.6 own stateful temporary-repository cases.
- [x] 1.7 Add `test_pr_assurance_rejects_positional_file_downgrade` while retaining local explicit-file enforcement.
- [x] 1.8 Add `test_range_analysis_uses_materialized_commit_snapshots`, `test_range_rejects_symlinked_governed_python_input`, `test_range_mode_change_regular_to_symlink_is_unknown`, `test_index_rejects_symlinked_governed_input`, and `test_materialized_governed_input_uses_nofollow_regular_blob_identity`. Bind tree-entry object type plus exact Git mode, allow only blob `100644`/`100755`, traverse every materialized component descriptor-relatively with no-follow/beneath semantics, hash the same opened regular descriptor against the Git blob, keep unsafe recognized paths governed, and cover caller-worktree mutation, symlink/gitlink/parent-link, dereferenced-target, missing/unmerged/intent-to-add, mode/content, and pre/post manifest mismatch. Future implementation owners: 3.3 and 3.4.
- [x] 1.9 Add `test_index_and_range_reject_fix_preview_and_mutation_options`.
- [x] 1.10 Add `test_index_scope_reads_staged_blobs_not_unstaged_worktree`, `test_index_scope_imports_dependency_from_complete_index_tree`, and `test_index_scope_derives_selection_from_captured_tree_during_concurrent_index_mutation`, and `test_index_intent_to_add_survives_captured_tree_omission`; use conflicting staged/unstaged selected bytes plus an imported tracked dependency with unstaged edits, and use a deterministic Git seam to mutate the live index immediately after immutable index capture. Assert that tree and canonical stage/flag metadata derive from the same captured index snapshot, ordinary paths/statuses derive from `HEAD` versus that tree, selected-path evidence and materialized bytes share its identity, later live-index state is ignored, a `git add -N` entry omitted by `write-tree` is still `unsafe_governed_input` UNKNOWN before applicability, and capture/metadata/tree/diff/object mismatch is `UNKNOWN`.
- [x] 1.11 Add `test_range_scope_omitted_enforcement_defaults_to_full` and update existing `test_review_run_instructions_prints_ai_workflow_without_running_review` to require the executable range/base/head/`--enforcement full` workflow; retain explicit range-plus-changed rejection.
- [x] 1.12 Add `test_installed_merge_quality_guidance_uses_pr_range`; prove rules updater output and the bundled skill make protected range/base/head/context/full the merge authority, label local range preview-only, and retain worktree-scoped simplification.
- [x] 1.13 Add table-driven `test_range_scope_rejects_narrowing_filters_before_analysis` for `--exclude-tests`, `--focus source|tests|docs|simplify`, `--path`, `--no-tests`, and `--level`; no narrowed result may carry `range_candidate` or an effective pr_range envelope or produce false NOT_APPLICABLE.
- [x] 1.14 Add `test_merge_quality_guidance_requires_complete_pr_range`; cover both agent-rule files, module/bundle guides, generated instructions, updater/bundled skill, and tracked skill copies. Assert protected CI guidance includes the event-derived context file and independent verification, local range is preview-only, and local pre-commit positional/simplification worktree guidance is non-PR.
- [x] 1.15 Add module-owned producer tests `test_range_candidate_rejects_base_ref_mismatching_claimed_target_tip`, `test_producer_never_self_asserts_pr_range_from_context_file`, and `test_range_without_context_is_preview`; validate the outside-checkout regular context schema and expected provider/repository/PR-or-queue/ref/event/target/head identities; reject unsafe/mismatched context; bind the producer's complete candidate report identity/digests; and prove it emits only candidate/preview, never pr_range. Do not author protected-consumer runtime tests in this modules change; the exact downstream core tests are declared only in task 4.7.

- [x] 1.16a In `tests/unit/specfact_code_review/run/test_scope.py`, add `test_policy_only_range_is_unknown_not_not_applicable`, `test_candidate_policy_change_cannot_self_authorize_pr_range`, and `test_pyproject_nonpolicy_change_can_remain_non_governed`. Bind the closed policy path/section/status/rename/deletion manifest and candidate-policy digest; prove target-tip authority, candidate shadow-only UNKNOWN, parse failure UNKNOWN, and unrelated shared-file metadata non-governed. Future implementation owner: 3.5a.
- [x] 1.16b In `tests/unit/specfact_code_review/run/test_scope.py`, add `test_coverage_config_only_range_is_governed_unknown`, `test_pytest_supported_config_sources_follow_pinned_precedence`, `test_pytest_empty_toml_overrides_lower_precedence_config`, `test_pytest_empty_ini_overrides_lower_precedence_config`, `test_pytest_bare_pyproject_is_final_fallback`, and `test_coverage_supported_config_sources_follow_pinned_precedence`. Cover the exact pinned pytest 9.0.3 and Coverage 7.15.4 source/section orders, `.coveragerc.toml`, selected plus ignored sources, zero-byte `pytest.toml`/`.pytest.toml` and `pytest.ini`/`.pytest.ini` overriding valid lower sources, bare-`pyproject.toml` final fallback, dual pytest pyproject-table ambiguity, and loader/profile drift. Future implementation owner: 3.5c.
- [x] 1.16c In `tests/unit/specfact_code_review/run/test_scope.py`, add `test_pylint_supported_config_sources_are_governed_and_explicit`, `test_pylint_repository_plugin_or_init_hook_is_unknown`, `test_pylint_extension_package_option_is_unknown_before_no_impact`, `test_pylint_ignore_options_cannot_drop_governed_input`, `test_pylint_no_member_exemptions_cannot_hide_renamed_module`, `test_pylint_confidence_filter_cannot_hide_inference_diagnostic`, `test_pylint_errors_only_cannot_hide_warning_diagnostic`, and `test_pylint_from_stdin_cannot_replace_governed_input`. Cover every pinned-loader source/section/alias, zero-or-one selection, hook/plugin/extension rejection, and the bound projection clearing `ignore`, `ignore-patterns`, `ignore-paths`, `ignored-modules`, `ignored-classes`, `generated-members`, `ignore-none`, `ignore-on-opaque-inference`, `ignore-mixin-members`, `ignored-checks-for-mixins`, `mixin-class-rgx`, `signature-mutators`, and target-added `contextmanager-decorators`, while forcing `confidence` to exactly `HIGH, CONTROL_FLOW, INFERENCE, INFERENCE_FAILURE, UNDEFINED` and `errors-only=false` so INFERENCE and WARNING regressions remain visible, forcing `from-stdin=false` and `recursive=false`, closing stdin, and reconciling the exact positional source manifest; require every pinned option to have exactly one no-fallthrough disposition, with only catalogued trusted rule semantics retaining target values; pin and parity-check `pylint-result-controls-v1`, and make catalog drift UNKNOWN. Future implementation owners: 3.5f, 3.6, and 3.17.
- [x] 1.16d In `tests/unit/specfact_code_review/run/test_scope.py`, add `test_ruff_config_zero_or_one_source_is_explicit`, `test_ruff_config_multiple_sources_is_unknown`, `test_ruff_transitive_extend_policy_is_governed_and_sealed`, `test_ruff_extend_rejects_escape_cycle_or_missing_input`, `test_ruff_force_exclude_cannot_drop_governed_input`, and `test_ruff_per_file_ignores_cannot_silence_added_or_renamed_input`, and `test_ruff_per_file_target_version_cannot_change_rules_across_rename`, and `test_ruff_namespace_packages_cannot_change_rules_across_rename`. Prove the closed repository-root `.ruff.toml`/`ruff.toml`/`pyproject.toml:[tool.ruff]` source set, explicit isolated zero-source behavior, exactly-one selection, multi-source/malformed/loader-drift ambiguity UNKNOWN before applicability/launch, complete bounded relative target-tip closure selection and digests, UNKNOWN for unsafe/cyclic/missing/over-bound graphs, effective `--no-force-exclude`, and a bound generated projection clearing every pinned `per-file-ignores`/`extend-per-file-ignores` spelling plus `per-file-target-version` and `namespace-packages`, normalizing `src` to the active snapshot root, for added and one-to-one-renamed paths while retaining one shared global target version and other non-path-scoped authorized rule settings, with matching eligible/invoked inputs. Future implementation owners: 3.5d and 3.15.
- [x] 1.16e In `tests/unit/specfact_code_review/run/test_scope.py`, add `test_basedpyright_referenced_policy_files_are_governed`, `test_basedpyright_include_exclude_cannot_drop_governed_input`, `test_basedpyright_ignore_cannot_suppress_governed_input`, and `test_basedpyright_baseline_cannot_suppress_relocated_diagnostic`, `test_basedpyright_strict_cannot_lower_severity_across_rename`, `test_basedpyright_nonempty_strict_is_unknown_before_projection`, and `test_basedpyright_strict_path_diagnostics_cannot_disappear`. Prove the bounded target-tip `extends`/`baselineFile` graph, default identity, safe relative paths, bound flattened projection with exact eligible `include`, `exclude=[]`, and `ignore=[]`; non-empty target path-scoped `strict` and non-empty target `executionEnvironments` rejected before launch; `strict=[]` emitted only after proving target strict absent/canonically empty; global checking/diagnostic settings preserved; every effective `baselineFile` removed, no positional source arguments or `--baselinefile`, baseline artifacts retained only as governed non-mounted evidence, and UNKNOWN for conflict/escape/cycle/missing/over-bound/catalog/manifest drift. Future implementation owners: 3.5e, 3.6, and 3.18.
- [x] 1.16f In `tests/unit/specfact_code_review/run/test_scope.py`, add `test_semgrep_ai_bloat_rule_pack_is_governed_and_sealed`. Prove target-tip-or-exact-signed-module clean/AI-bloat selection, source/blob/content/materialized digests, symmetric bundle use, candidate-change UNKNOWN, and fallback integrity failure UNKNOWN. Future implementation owners: 3.5b and 3.19.

## 2. Failing differential/report tests

- [x] 2.1 Add `test_introduced_blocker_off_added_line_still_blocks`.
- [x] 2.2 Add `test_unchanged_baseline_blocker_is_retained_but_not_introduced`.
- [x] 2.3 Add `test_baseline_analysis_failure_is_unknown`.
- [x] 2.4 Add `test_range_differential_uses_merge_base_snapshot_when_base_tip_advanced`.
- [x] 2.5 Add `test_pure_rename_preserves_unchanged_fingerprint`, `test_rename_facts_ignore_ambient_git_config`, `test_ambiguous_exact_blob_moves_are_unknown`, `test_modified_delete_add_cannot_claim_fixed`, `test_missing_finding_across_any_rename_is_unknown`, `test_renamed_file_fix_is_conservatively_unknown`, and `test_matching_fingerprint_severity_change_is_unknown`, `test_duplicate_finding_multiset_preserves_head_surplus_as_introduced`, `test_line_only_shift_preserves_unchanged_occurrence`, `test_identity_equal_replacement_at_different_source_anchor_is_introduced`, `test_identical_block_moved_to_different_edit_location_is_not_unchanged`, `test_alternate_optimal_line_correspondence_is_unknown`, `test_correspondence_bounds_are_unknown`, `test_ambiguous_repeated_source_anchor_cannot_pair_unchanged`, and `test_mixed_severity_duplicate_bucket_is_unknown`; define exact canonical message normalization, line-independent identity fields, occurrence-evidence digest, signed `occurrence-continuity-v1` exact spanned/adjacent-line anchor, signed `source-line-correspondence-v1` insert/delete/replace/match costs `1/1/2/0` with no moves and global/forced/forbidden costs `G/F/X`, exact 16-MiB/20,000-line/4,000,000-cell bounds, distinct physical-location counts, multiplicity counts at one proven location, severity/blocking partitions, deterministic sort/pairing, unknown continuous residual pairs, and surplus dispositions; bind canonical/raw spans, identity/anchor/line-sequence/cost/correspondence/partition evidence, continuity decisions, pairs/surpluses/digest; ensure raw spans are evidence-only; use only `canonical-exact-rename-v1`: derive raw add/delete facts from captured trees with heuristic rename detection disabled, uniquely pair identical regular-blob object IDs, make duplicate-object groups UNKNOWN, bind the complete pair/copy/ambiguity digest, canonicalize exact-pair head anchors while retaining both paths, and make a missing base finding on any deleted path UNKNOWN when an unpaired governed addition exists. Add parameterized `test_introduced_inline_suppression_blocks_before_fixed_classification`, `test_unchanged_baseline_inline_suppression_is_retained`, `test_suppression_manifest_failure_is_unknown`, `test_suppression_waiver_input_is_unsupported_in_cr14`, `test_ruff_isort_action_comment_catalog_blocks_before_fixed_classification`, `test_ruff_ignore_file_ignore_disable_enable_catalog_blocks_before_fixed_classification`, `test_pylint_disable_and_disable_next_block_before_fixed_classification`, `test_pylint_skip_file_blocks_before_fixed_classification`, `test_basedpyright_complete_source_directive_catalog_blocks_before_fixed_classification`, `test_basedpyright_pyright_ignore_comments_block_before_fixed_classification`, `test_crosshair_source_directive_catalog_blocks_before_fixed_classification`, and `test_crosshair_disabling_aliases_block_before_fixed_classification`; assert diagnostic suppressions emit `introduced_inline_suppression`, while all recognized basedpyright and CrossHair analyzer-result controls emit `introduced_analyzer_result_control`; cover every signed `suppression-directives-v1` family—including the pinned Ruff 0.15.12 inline/file-level noqa grammar; every `ruff:` `ignore[...]`, `file-ignore[...]`, `disable[...]`, and `enable[...]` shape with stable rule codes, preview rule names, whitespace/list/trailing-comma, matched and implicit-range cases; and all ten isort action forms (`isort:` and `ruff: isort:` prefixes crossed with `skip_file|on|off|skip|split`) with the pinned parser's exact whitespace/case behavior and an I001-disappearance regression; Pylint ordinary `disable=undefined-variable` and `disable-next=undefined-variable` as separate parameter values, plus `disable-all`, legacy `disable-msg=<rule-list>` with whitespace/comma normalization, and atomic whole-file `skip-file`; for both ordinary forms require a baseline run that emits `undefined-variable`, a head run whose native Pylint JSON is empty because of the introduced directive, an `introduced_inline_suppression` blocker plus UNKNOWN affected-base disposition, and a control with the directive removed that restores the native diagnostic without a synthetic suppression finding; every `pyright:` source directive in pinned basedpyright 1.39.10's parser-derived catalog, with explicit bare `pyright: ignore` and bracketed `pyright: ignore[reportRule,...]` vectors plus strict/basic/standard, `getBooleanDiagnosticRules()`, and diagnostic-level forms, and every source directive in pinned CrossHair 0.0.109's special on/off forms plus `AnalysisOptionSet.directive_fields`. The basedpyright vectors SHALL cover `analyzeUnannotatedFunctions=false` and every accepted boolean/diagnostic-level value without strength inference. The CrossHair vectors SHALL cover `enabled=0|false|n|no` case-insensitively, `analysis_kind=asserts`, `specs_complete=yes`, `max_iterations=0`, `per_condition_timeout=0`, `per_path_timeout=0`, and `max_uninteresting_iterations=1`; every recognized basedpyright and CrossHair source directive blocks, and parser/catalog drift is UNKNOWN. Also cover candidate inline justification/waiver rejection, changed/relocated directive occurrences, and a pure-rename control. Use Python `tokenize` comment tokens only; bind normalized directive/analyzer/rules-or-settings/path/line/token plus base/head manifest digests. Do not derive analyzer/rule-specific semantic subjects; never infer arbitrary scope. Require missing base findings for the affected analyzer/path to be unknown, not fixed. Prove no CLI/report/context or injected trusted flag can waive suppression findings; defer authenticated exceptions to `governance-02-exception-management`.
- [x] 2.5b In `tests/unit/specfact_code_review/run/test_differential.py` and `tests/unit/specfact_code_review/run/test_runner.py`, add parameterized `test_changed_file_with_unchanged_suppression_is_unknown` and `test_byte_identical_rename_with_unchanged_suppression_is_not_quarantined`. For every `suppression-directives-v1` analyzer family, retain one registered occurrence, change separate source bytes, and require `unchanged_suppression_on_changed_file` with analyzer/path, base/head blob, occurrence/manifest, and changed-hunk identities before aggregate verdict derivation. Prove silent analyzer output cannot PASS, removal of the last occurrence follows ordinary classification, changed/relocated occurrences remain introduced blockers, and identical-blob pure rename is not quarantined. Also add `test_suppression_catalog_resource_matches_checkpoint` and `test_suppression_catalog_drift_is_unknown_before_profile_activation`; require the exact signed `pr-range-v1-suppression-catalog.json` bytes/digest to bind both policies and every analyzer mapping. Do not infer directive reach or implement analyzer-specific unsuppressed execution. Future implementation owner: 3.20.
- [x] 2.5c In `tests/unit/specfact_code_review/test_review_tool_pip_manifest.py`, `tests/unit/specfact_code_review/run/test_findings.py`, and `tests/e2e/specfact_code_review/test_review_run_e2e.py`, add `test_module_package_authenticates_suppression_catalog_resource`, `test_report_binds_suppression_catalog_identity`, and `test_consumer_matrix_rejects_suppression_catalog_identity_mismatch`. Vary checkpoint/resource/package/profile/report/static-envelope digests one boundary at a time; require UNKNOWN/invalid before profile activation or `pr_range` compatibility acceptance, and require the valid fixture to preserve the identical canonical catalog identity. These module selectors are future implementation owners 3.20 and 3.24. The live protected-consumer selector `test_trusted_consumer_rejects_suppression_catalog_identity_mismatch` is core-owned handoff acceptance in task 4.7 and SHALL NOT enter the module mapping/checkpoint.
- [x] 2.5a In `tests/unit/specfact_code_review/run/test_findings.py`, `tests/unit/specfact_code_review/run/test_runner.py`, every allowlisted analyzer-runner unit test, and `tests/e2e/specfact_code_review/test_review_run_e2e.py`, add parameterized `test_required_profile_findings_have_canonical_location_kind`, `test_line_only_finding_uses_full_physical_line_span_fallback`, `test_exact_adapter_coordinates_convert_to_utf8_byte_half_open_span`, and `test_selector_and_non_source_locations_do_not_enter_source_continuity`. Cover Ruff, Radon, both Semgrep passes, ai-bloat-ast, ast-clean-code, basedpyright, Pylint, contracts/CrossHair, and targeted-pytest-coverage. Preserve exact endpoints where available; otherwise expand a valid one-based source line to columns `0..UTF-8 byte length` excluding the LF terminator with `precision=line`; bind raw coordinate system/conversion evidence; type selector/infrastructure locations explicitly; make invalid path/line/span invalid continuity rather than guessing. Future implementation owners: 3.12a, 3.13–3.20.
- [x] 2.6a Add `test_default_pr_range_analyzer_profile_has_closed_membership`, `test_report_exposes_mandatory_analyzer_coverage`, `test_analyzer_identity_mismatch_is_unknown`, and `test_required_analyzer_infrastructure_error_is_unknown_not_fail`. Freeze the eight required IDs plus conditional `semgrep-bugs` and `targeted-pytest-coverage`; vary member/version/policy/config identities and prove incomplete required evidence is UNKNOWN. Future implementation owner: 3.12a.
- [x] 2.6b In new `tests/unit/specfact_code_review/run/test_toolchain.py`, add `test_pr_range_rejects_unpinned_or_mismatched_loader_toolchain`, `test_toolchain_identity_is_portable_across_install_roots`, `test_signed_toolchain_lock_closes_complete_dependency_graph`, `test_locked_analyzer_environment_excludes_host_core_packages`, `test_locked_analyzer_environment_rejects_extra_or_missing_members`, `test_module_package_does_not_install_analyzer_lock_into_host`, `test_runtime_capsule_closes_interpreter_native_runtime`, `test_runtime_capsule_identity_is_portable_across_storage_roots`, `test_runtime_capsule_acquires_pinned_oci_layers_from_registry_or_verified_cache`, `test_runtime_capsule_fresh_cache_miss_installs_only_pinned_wheelhouse`, `test_checkpoint_binds_canonical_toolchain_lock_projection`, `test_runtime_capsule_rejects_redirect_downgrade_or_credential_forwarding`, and `test_runtime_capsule_rejects_movable_or_unsafe_oci_source`. Replace the existing expectation in `test_module_package_lists_all_review_tool_pip_packages`: parse normalized PEP 508 names and assert analyzer-only direct/transitive lock members are absent from host `pip_dependencies`, while the signed lock retains every required tool. For Linux x86_64 CPython 3.11–3.13, prove signed HTTPS registry/repository@manifest acquisition, config and ordered layer digest/diff-ID/media-type/size identities, fixed in-image wheelhouse filename/platform/size/SHA-256 mapping for every selected lock member, verified controller-owned cache hit plus genuine fresh cache-miss fetch-and-offline-install with package indexes disabled, every redirect hop/final target HTTPS+signed-allowlisted before per-hop credential attachment, complete redirect-chain/final-URL evidence, canonical checkpoint lock-projection/resource equality, fail-closed missing/extra/duplicate/incompatible wheel, offline miss/partial cache/redirect/downgrade/credential-forwarding/size/media/digest/unsafe-extraction cases, immutable OCI base manifest/layers, exact analyzer lock entries, DAG/markers/edges/payloads/entry points/closure, canonical capsule root/interpreter/stdlib/extension/loader/library/bootstrap manifests, wheel-or-RECORD portable identity, host resolution beside deliberately differing compatible core dependencies, and UNKNOWN for a lock member in host dependencies, missing/extra analyzer or runtime files, host leakage/mounts, bare lock entries, or drift. Future implementation owners: 3.12b–3.12f.
- [x] 2.6c Add `test_generated_analyzer_inputs_use_typed_provenance`, `test_head_config_cannot_suppress_introduced_finding`, `test_ruff_snapshot_mode_disables_cache`, `test_ruff_task_tag_cannot_hide_e501`, `test_ruff_fix_only_cannot_hide_unfixable_finding`, `test_radon_snapshot_config_cannot_filter_complexity_results`, `test_radon_uses_sealed_full_result_options`, and `test_mandatory_analyzer_eligible_and_invoked_input_manifests_match`. Bind the closed input-provenance union plus `governed-input-completeness-v1` option-catalog, eligible/invoked input, effective-control, and cache-mode digests; prove Ruff `--no-cache`/`--no-force-exclude`, Radon 6.0.1 `radon-full-result-v1` with CC A..F, MI A..C, empty exclude/ignore, private output, empty control cwd/home, sanitized `RADONCFG`, exact eligible/invoked manifests, plus empty effective `per-file-ignores`/`extend-per-file-ignores`/`per-file-target-version`/`namespace-packages`, forced `lint.pycodestyle.ignore-overlong-task-comments=false`, `fix=false`, and `fix-only=false` with the original task-tag and mutation/result-control values retained as evidence, snapshot-root Ruff `src`, and one shared Ruff target version, empty projected Pylint ignore/no-member-exemption family plus the explicit complete `HIGH, CONTROL_FLOW, INFERENCE, INFERENCE_FAILURE, UNDEFINED` confidence set under `pylint-result-controls-v1`, exact eligible projected basedpyright include plus empty exclude/ignore/baseline controls, target strict proven absent/empty before generated empty strict, preserved global checking/diagnostic settings, and rejected non-empty target strict/execution environments, and UNKNOWN for writes, ineffective transforms, manifest mismatch, or catalog drift. Future implementation owners: 3.6, 3.12a, and 3.15–3.18.
- [x] 2.6d Add `test_semgrep_uses_signed_module_fallback_without_target_config`, `test_semgrep_scanned_paths_match_every_eligible_input`, `test_semgrep_rule_target_narrowing_is_unknown`, `test_semgrep_pass_union_cannot_hide_rule_exclusion`, and `test_semgrep_disable_nosem_preserves_string_literal_finding`. Bind package/version/checksum/signature and clean/AI payload identities; retain target-only bugs conditionality; force controller-owned `--disable-nosem` on both passes and verify its pinned option/effective argv identity; parse the pinned rule schema and reject every rule-level `paths.include`/`paths.exclude` before launch; use a multi-rule regression where one rule excludes and another scans the same file; use a baseline `eval` finding and a head physical line containing `nosemgrep` inside a Python string to prove the native diagnostic remains when suppression is disabled, plus missing/overridden/ineffective flag UNKNOWN vectors; then normalize JSON `paths.scanned`/`paths.skipped` and require exact pass-level equality with eligible inputs. Make fallback, rule-target narrowing, size skip, extra/missing path, parse, or reconciliation failure UNKNOWN. Future implementation owners: 3.5b and 3.19.
- [x] 2.6e Rename/update existing `test_run_contract_check_ignores_crosshair_timeout` to `test_run_contract_check_reports_crosshair_timeout_for_mandatory_coverage`; prove adapter timeout/process error becomes explicit UNKNOWN and the runner preserves that status. Future implementation owners: 3.12a and 3.13.
- [x] 2.6f In `tests/unit/specfact_code_review/run/test_toolchain.py`, `tests/unit/specfact_code_review/run/test_sandbox.py`, and `tests/e2e/specfact_code_review/test_review_run_e2e.py`, add `test_builtin_analyzers_boot_from_signed_capsule_payload_without_controller_paths`, `test_builtin_module_payload_manifest_covers_complete_signed_package`, `test_builtin_analyzer_missing_or_drifted_payload_is_unknown`, `test_builtin_payload_boots_after_marketplace_archive_discard`, `test_builtin_payload_rejects_untrusted_install_origin_or_key`, `test_builtin_payload_derives_core_0_55_1_install_handoff`, `test_candidate_payload_identity_is_not_official_install`, and `test_post_base_bootstrap_and_composite_identity_bind_copied_payload`. Use a signed synthetic installed module root limited to core v0.55.1's real contract: `DiscoveredModule` source/package root/metadata, canonical user-or-marketplace install root, registry marker, install-verified checksum, manifest checksum/signature, bundled approved-key fingerprint, fresh artifact-verification result, and canonical descriptor-safe manifest—no invented aggregate loader DTO, source commit/tree, retained archive, archive locator, or archive handle. Separately use a protected-workflow candidate fixture binding immutable candidate commit/tree, package/payload digests, and workflow/ref/run/attempt/job; prove it cannot satisfy official-install or `pr_range` provenance. Delete the installed fixture's source archive and every archive cache entry before capsule materialization. Require the complete canonical `specfact_code_review` installed payload—not a statically inferred import subset—to be copied at `/opt/specfact/builtin/specfact_code_review`; bind every canonical installed-payload member, core handoff/key/verification identity, immutable base-root digest, generated `sealed-bootstrap-v2`, `capsule-composite-identity-v1`, and final composite-root digest; boot both modes with host/module controller paths absent; and make missing/extra/link/collision/content/import/root/bootstrap/composition drift UNKNOWN. Future implementation owners: 3.7, 3.12a, 3.12e, and 3.12f.
- [x] 2.7 Add `test_range_uses_authorized_base_tip_policy_when_target_advanced`; keep the source baseline at merge base, bind the target tip to trusted PR/CI context, apply its sealed policy to both source snapshots, and prove moved/untrusted/missing target policy is UNKNOWN.
- [x] 2.8a Add `test_pr_range_collects_complete_pytest_suite_for_production_change`, `test_complete_suite_includes_multiple_nonconventional_related_tests`, `test_targeted_pytest_runs_complete_suite_in_test_only_range`, `test_targeted_pytest_reconciles_removed_baseline_selectors`, `test_targeted_pytest_delete_only_production_runs_complete_head_suite`, `test_changed_test_not_collected_by_sealed_policy_is_unknown`, `test_empty_merge_base_input_class_is_not_applicable_for_that_side`, `test_test_role_is_frozen_before_collection`, `test_previously_collected_test_file_becoming_empty_is_unknown`, `test_candidate_test_file_disabled_by_dunder_test_is_unknown`, and `test_test_candidate_to_support_rename_is_unknown`, and `test_pytest_input_role_uses_pinned_fnmatch_ex_for_path_patterns`. Under signed `complete-pytest-suite-v1`, collect independently from the complete sealed policy `testpaths` or repository-root default with no production-derived positional narrowing. Freeze `pytest-input-role-v1` from immutable path plus shared authorized pytest `testpaths`/root-default and exact pinned `_pytest.pathlib.fnmatch_ex` before collection. Pass the same materialized absolute Path used by pytest; under initial Linux, match separator-free patterns against basename and path-bearing patterns against the full string with `*/` prefixing for relative patterns, using Python `fnmatch.fnmatch` and retaining whole-string `**` semantics. Include `python_files = tests/unit/test_*.py` with an empty `tests/unit/test_new.py`; classify `conftest.py` and non-pattern in-root helpers as support, matching modules as candidates, and governed test-facet paths outside roots as uncollectable candidates. Bind role/collection/input/node/artifact/inventory digests; execute both complete inventories; normalize only pure file renames without allowing a candidate-to-support role downgrade; make empty/`__test__ = False`/moved/renamed candidates, removed baseline selectors, other uncollected changed tests, and collection/JUnit mismatches UNKNOWN. Prove contents, AST, observed collection, source-to-test naming/import/coverage heuristics, and candidate impact maps cannot determine roles or omit tests. Future implementation owner: 3.14a.
- [x] 2.8b Add `test_pytest_projection_rebases_relative_paths_per_snapshot`, `test_pytest_projection_rejects_unbound_paths`, `test_pytest_projection_redirects_all_writable_paths_to_private_roots`, `test_sealed_pytest_policy_rejects_selection_filters`, `test_production_change_cannot_hide_failing_test_with_authorized_ignore`, `test_pytest_cache_and_short_circuit_controls_are_unknown`, and `test_pytest_selection_option_catalog_covers_pinned_help`, `test_sealed_pytest_policy_rejects_norecursedirs`, `test_pytest_configuration_control_catalog_covers_pinned_ini`, `test_every_test_candidate_must_collect_even_when_unchanged`, `test_changed_test_cannot_be_selectively_deselected_by_conftest`, `test_collection_shaping_repository_hook_is_unknown_before_collection`, `test_report_shaping_repository_hook_is_unknown_before_execution`, `test_pytest_hook_disposition_catalog_covers_execution_and_report_hooks`, `test_attested_fixture_plugin_extends_frozen_pytest_catalog`, `test_unattested_pytest_plugin_is_unknown`, `test_plugin_collection_shaping_hook_is_unknown`, `test_same_file_item_level_dunder_test_false_is_unknown`, parametrized `test_same_file_constructor_suppressed_test_class_is_unknown` covering both `__init__` and `__new__`, parametrized `test_same_file_builtin_pycollect_filter_is_unknown` covering an abstract class and fixture-marked function, `test_same_unittest_class_method_dunder_test_false_is_unknown`, `test_unittest_metaclass_dir_cannot_hide_failing_method`, parametrized `test_unittest_run_or_call_override_cannot_bypass_failing_method` covering `run` and `__call__`, parametrized `test_native_pytest_raw_namespace_cannot_hide_failing_candidate` covering a class metaclass-filtered `__dict__` and module subtype-filtered `__dict__`, and `test_pytest_builtin_collector_decision_catalog_covers_pinned_branches`. Use different merge-base/head/caller dependency bytes; classify every pinned config/addopts path as read or writable; freeze `pytest-selection-controls-disabled-v1`, `pytest-plugin-capabilities-v1`, `pytest-hook-dispositions-v1`, `pytest-item-controls-disabled-v1`, and `unittest-execution-controls-v1` from the complete pinned pytest 9.0.3, controller-owned pytest-cov, and admitted target-tip plugin parser/help/`addini` plus hook-spec catalogs; parse the complete effective argv and config-field map, aliases and `--option=value`; reject positional selectors and every collection/cache selector, execution short-circuit, discovery/plugin override, or coverage override, including the normative vectors. Rebase allowed reads, redirect controller-owned cache/log/basetemp/JUnit/coverage outputs, pass explicit side `-c`/`--rootdir` and complete-suite argv only after accepted non-selecting options, and reject unknown or unattested/unclassified plugin-added tokens, catalog drift, escape, missing inputs, collision, or ineffective projection. The production-only ignore and non-empty `norecursedirs` regressions SHALL each contain one hidden failing test and one visible passing test and require UNKNOWN before collection. The conftest regression SHALL add one failing hook-matched test beside one visible passing selector in the same file and require `pytest_collection_hook_unsupported` UNKNOWN before execution. Preflight an attested fixture-only plugin from the project-runtime layer, compose and verify its parser catalog, then reject unlisted/snapshot/user/dynamic or collection-shaping plugins. Bind plugin/hook origins, raw-collected node IDs, and pre/post registry digests; registry drift or a raw item absent from final inventory is UNKNOWN. For the same-file built-in-control regressions, use the sealed observer-only `pytest_collectstart`/try-first `pytest_pycollect_makeitem` boundary to record the matching candidate before built-in filtering, forbid result mutation, and require `pytest_item_control_unsupported` UNKNOWN while a sibling selector still passes. Freeze closed `pytest-builtin-collector-decision-v1` from every pytest 9.0.3 built-in collector activated for governed Python inputs and require every activated implementation plus every branch to have an exact observer and intentional-non-candidate, collectable, or unsupported-candidate-control disposition. Initial members SHALL cover the complete native `PyCollector.collect`/`istestclass`/`istestfunction`/`pytest_pycollect_makeitem`/`Class.collect` graph and `_pytest.unittest.pytest_pycollect_makeitem`/`UnitTestCase.collect` graph plus the bounded `_pytest.unittest.TestCaseFunction.runtest` → `unittest.TestCase.__call__` → `unittest.TestCase.run` execution path. Cover false native class/function and unittest method `__test__`; custom `__init__`/`__new__`; fixture-marked functions; abstract native/TestCase classes; non-real callables; imported-object ownership manipulation; name/nose eligibility; static/class-method unwrapping; descriptor-safe raw module/class namespace and MRO manifests; module subtype/class metaclass `__dict__`/`__getattribute__`/`__getattr__`/`__dir__` controls; raw-versus-observed-versus-makeitem reconciliation; namespace/MRO duplicate handling; descriptor-safe raw MRO name-prefix-eligible callable manifest; exact `TestLoader` prefix/pattern/sort control identities and `getTestCaseNames` inventory; inherited method origin; candidate metaclass `__dir__`/`__getattribute__`/`__getattr__`; raw-versus-loader reconciliation; and `runTest`/Twisted fallback; unchanged ordinary nonmatching names, ignored framework attributes, non-callable data, and disabled non-applicable built-in collectors. An unclassified or drifted branch is UNKNOWN. Reconcile every path-classified test candidate on both snapshots—even unchanged candidates—against collected source paths and require at least one selector; a whole-file omission by config or collection hook is `uncollected_test_candidate` UNKNOWN. The unittest execution regression SHALL prove native pytest/JUnit falsely pass when each override skips a failing selected method, then require `pytest_execution_control_unsupported` UNKNOWN from pinned pre-import base descriptor, raw MRO/instance namespace, and default lookup-control evidence immediately before dispatch; cover intermediate-base overrides, instance shadowing, canonical-base mutation, custom `__getattribute__`/`__getattr__`, and a canonical inherited control, without emulating arbitrary unittest semantics. Future implementation owners: 3.14b and 3.14d.
- [x] 2.8c Add `test_targeted_coverage_uses_sealed_target_config_and_rejects_candidate_suppression`, `test_targeted_coverage_rejects_custom_exclusion_regexes`, `test_targeted_coverage_clears_default_exclusion_registry`, `test_coverage_repository_plugin_is_unknown`, and `test_coverage_projection_redirects_all_writable_paths_to_output_root`. Cover every pinned exclusion/partial-branch/plugin/write field, explicit `--cov-config`, private output rebasing, sanitized overrides, and UNKNOWN for non-empty exclusions, plugin load, escape/collision/unclassified/ineffective transformation. Future implementation owner: 3.14c.
- [x] 2.8d Add `test_targeted_pytest_coverage_classifies_failure_vs_unknown`, `test_targeted_pytest_rejects_head_skip_xfail_xpass_and_deselection`, `test_non_strict_xpass_is_fail_despite_passing_junit_testcase`, and `test_targeted_pytest_baseline_failure_head_pass_is_fixed`. Bind exact selector/collection/`pytest-outcome-observer-v1`/JUnit/process/count/environment/artifact evidence; capture every runtest phase and `wasxfail`, prove a non-strict XPASS yields a bare passing JUnit testcase but remains FAIL from the sealed outcome artifact, classify collected head assertion/skip/xfail/xpass as FAIL, missing/duplicate/malformed phases, deselected, artifact/JUnit/process disagreement, reconciliation, or infrastructure as UNKNOWN, and exact baseline fail/head pass as fixed. Future implementation owner: 3.14d.
- [x] 2.8e Add `test_targeted_coverage_threshold_failure_is_fail_not_unknown`, `test_targeted_coverage_baseline_threshold_failure_head_pass_is_fixed`, `test_targeted_coverage_rejects_missing_runtime_measurable_production_path`, `test_stub_only_range_is_static_and_coverage_not_applicable`, `test_coverage_manifest_excludes_pyi_but_static_analyzers_include_it`, and `test_stub_only_range_excludes_crosshair_but_requires_stub_capable_static_members` and `test_stub_only_contracts_keeps_activated_static_scan_and_marks_only_crosshair_not_applicable` and `test_contract_static_activation_preserves_existing_icontract_usage_boundary`. Freeze signed `runtime-measurable-production-v1` plus `analyzer-input-kinds-v1`: only tracked regular governed production `.py` is a Coverage or `contracts.crosshair` executable input; governed `.pyi` is bound in a separate static-only manifest and remains mandatory for Ruff, Radon, activated Semgrep passes, ai-bloat-ast, ast-clean-code, basedpyright, Pylint, and `contracts.icontract-static-scan`, but is never passed to CrossHair. Require every runtime-measurable `.py` path exactly once; make `.pyi`-only targeted pytest/coverage NOT_APPLICABLE with `no_eligible_inputs_in_snapshot`; keep contracts active through its static component while only `contracts.crosshair` is NOT_APPLICABLE with `no_crosshair_executable_input`, without launching CrossHair; reject target source/include/omit gaps plus duplicate/escape/alias/identity drift as UNKNOWN, classify complete below-threshold measurement as FAIL, and baseline threshold fail/head pass as fixed. Future implementation owner: 3.14e.
- [x] 2.8f In `tests/unit/specfact_code_review/run/test_toolchain.py`, `tests/unit/specfact_code_review/run/test_sandbox.py`, `tests/unit/specfact_code_review/run/test_runner.py`, and `tests/e2e/specfact_code_review/test_review_run_e2e.py`, add `test_project_runtime_layer_binds_target_tip_dependency_inputs`, `test_targeted_pytest_imports_attested_external_dependency`, `test_project_runtime_layer_is_identical_across_snapshots`, `test_project_runtime_layer_rejects_untrusted_or_candidate_inputs`, `test_candidate_project_dependency_input_change_is_governed_unknown`, `test_project_runtime_layer_cannot_shadow_reserved_runner_components`, `test_missing_project_runtime_is_unknown_not_not_applicable`, and `test_non_importing_member_cannot_mount_project_runtime_layer`, plus `test_attested_pytest_plugin_identity_is_bound_by_project_runtime`. Parameterize `test_non_importing_member_cannot_mount_project_runtime_layer` over Ruff, Radon, both Semgrep passes, `ai-bloat-ast`, and `ast-clean-code`; these members must receive no project-layer mount and do not participate in import-precedence assertions. Use a synthetic target lock plus an attested external dependency importing Typer-like code for basedpyright, Pylint, contracts/CrossHair, and targeted pytest; bind repository/ref/commit/tree and payload identities; prove snapshot-local code wins for those authorized consumers, exact imports collect on both sides, project/runtime/native/root manifests match, and host packages are absent. Future implementation owners: 3.8, 3.12g, and 3.14d.
- [x] 2.9 Add parameterized `test_required_analyzers_structurally_empty_snapshot_is_not_applicable` for add-only and delete-only governed Python ranges; assert manifest-bound per-side NOT_APPLICABLE, required execution on every non-empty side, applicable aggregate range status, and rejection of an unrecorded adapter empty-file return.
- [x] 2.10 Add `test_run_contract_check_reports_crosshair_process_error_for_mandatory_coverage`; cover documented CrossHair process-error exit code 2 with empty parsed findings, retained exit/stderr diagnostics, UNKNOWN coverage, and a control proving parsed counterexamples remain findings.
- [x] 2.11 Add `test_basedpyright_project_rebases_relative_paths_per_snapshot`, `test_basedpyright_project_rejects_unbound_paths`, `test_basedpyright_extends_and_baseline_files_are_governed_but_disabled`, and `test_basedpyright_no_config_uses_generated_default`; use an imported dependency with different merge-base/head content; cover rejected non-empty path-scoped `strict` and `executionEnvironments`, top-level `extraPaths`, `venvPath`/`venv`, recursive `extends`, restrictive `include`, matching `exclude`, `ignore`, and `baselineFile`; include a same-path basic-mode `strict=["a.py"]` diagnostic-disappearance regression; prove governed graph/baseline identities plus per-side config/toolchain, exact eligible/project-include equality, and original/effective include/exclude/ignore/strict/execution-environment/baseline-control digests; reproduce a fixed baselined diagnostic followed by an equivalent relocated/new diagnostic and require it to remain visible; prove exact `basedpyright-default-v1` with canonical sorted eligible include array and explicit side `--project` when no config exists; reject policy-bundle/worktree/escaping/missing/cyclic/unsupported/over-bound paths as UNKNOWN.
- [x] 2.12 Add `test_analyzer_subprocesses_use_snapshot_invocation_context`, `test_snapshot_sitecustomize_cannot_run_during_analyzer_startup`, `test_snapshot_cannot_shadow_capsule_reserved_imports`, and `test_non_reserved_snapshot_import_precedes_project_runtime`. Parameterize the invocation/startup/reserved-collision tests over Ruff, Radon, Pylint, basedpyright, both Semgrep passes, CrossHair, and targeted pytest. Parameterize `test_non_reserved_snapshot_import_precedes_project_runtime` only over the members authorized to receive that layer: Pylint, basedpyright, CrossHair/contracts, and targeted pytest; Ruff, Radon, and both Semgrep passes are covered instead by task 2.8f's required no-mount regression. Assert snapshot-root OS cwd except Radon's empty controller-owned control cwd, capsule executable/environment, Python `-I -S` sealed-bootstrap argv, snapshot roots absent from startup `sys.path`, signed `capsule-reserved-imports-v1` installed before snapshot/project roots, sanitized PYTHONPATH/PYTHONHOME/user-site/startup/plugin state, process-private output/temp roots, sandbox identity, context digest, exact eligible/invoked input manifests, effective whole-file-control digest, and cache mode. Use side-effecting startup sentinels plus candidate/project top-level `.py`, `.pyi`, regular-package, and namespace-package collisions for `pytest`, `pylint`, `radon`, `crosshair`, `specfact_code_review`, and a locked transitive dependency. Require `reserved_import_collision` UNKNOWN before dispatch, prove reserved origins remain capsule-only, prove non-reserved snapshot-before-project precedence only for authorized project-layer consumers, prove no project-layer mount for every other member, and reject early candidate startup execution, missing isolation flags, bootstrap/catalog/origin drift, caller-source, or context mismatch. Future implementation owner: 3.7.
- [x] 2.13 Add `test_snapshot_sandbox_capability_and_root_policy`, `test_snapshot_context_mounts_every_sealed_analyzer_config_root`, and `test_runtime_capsule_boots_in_empty_bwrap_root_without_host_mounts`, `test_bwrap_launcher_is_static_elf_without_interp_or_needed`, `test_bwrap_pre_namespace_maps_only_static_payload_and_kernel_objects`, and `test_bwrap_launch_rejects_dynamic_or_host_loader_dependency` in new `tests/unit/specfact_code_review/run/test_sandbox.py`; prove signed static Linux x86_64 ELF architecture and empty `PT_INTERP`/`DT_NEEDED`; no-follow open, hash, and same-descriptor execution; cleared loader-injection environment; `pre-namespace-mapped-objects-v1` with only the executable and kernel pseudo-mappings; UNKNOWN for dynamic linkage, host loader/library opens, descriptor/path substitution, or unexpected mapped objects; then prove the namespace capability probe; verified capsule as complete read-only Bubblewrap rootfs; a synthetic interpreter/analyzer boot with no host `/usr`, `/lib*`, interpreter, stdlib, extension, loader/library, or controller mount; sorted manifest-bound read-only `config_roots[]` for Ruff, generated Pylint projection, basedpyright project/non-baseline reference projections, Semgrep bundle, and pytest-coverage projection inputs; the identical verified target-tip project-runtime layer only for the closed import-capable member set; read-only active snapshot; private writable output/temp/cache/home roots; closed undeclared descriptors; denied caller/opposite/context/policy-source/sibling-output/unverified-project-runtime/credential/network access; and UNKNOWN for omitted, extra, writable, identity-mismatched, incomplete, or host-runtime-dependent roots/profile.
- [x] 2.14 Add `test_import_capable_analyzer_cannot_tamper_with_other_evidence_roots`, `test_runtime_observation_declares_non_adversarial_candidate_assumption`, and `test_adversarial_runtime_policy_is_unknown` in `tests/e2e/specfact_code_review/test_review_run_e2e.py`; run synthetic pytest and CrossHair targets that attempt absolute, relative, and symlink reads/writes across sentinel roots; prove cross-root denials, controller-captured process facts, candidate-observation labelling, and post-exit artifact sealing; then prove a hostile-candidate policy cannot PASS and is UNKNOWN rather than treating digested JUnit/coverage as trusted.
- [x] 2.15 Add `test_fixable_error_remains_blocking_until_applied` and update existing `test_score_review_single_fixable_error` to expect FAIL; production `scorer.py` remains unchanged.
- [x] 2.16 Add `test_report_never_says_all_passed_with_mandatory_unknown`.
- [x] 2.17 Add table-driven `test_schema_1_6_assurance_status_legacy_projection_and_exit_matrix` for PASS/FAIL/UNKNOWN/NOT_APPLICABLE under strict and shadow modes, add `test_pre_commit_consumer_preserves_schema_1_6_unknown_exit` in `tests/unit/scripts/test_pre_commit_code_review.py`, plus `test_assurance_status_fail_precedes_unknown_with_known_blocker` plus `test_fixed_baseline_failure_is_excluded_from_aggregate_blockers`; prove aggregate blockers are derived only after differential/lifecycle classification; retain an exact baseline-FAIL/head-PASS transition as fixed per-snapshot evidence without aggregate FAIL; prove a mixed remaining valid blocker/required UNKNOWN yields aggregate FAIL, `has_unknown_required_evidence=true`, retained member diagnostics, no complete-coverage summary, and non-shadow exit 1; also cover enforce-to-full normalization, changed/worktree compatibility, and range-plus-changed rejection.
- [x] 2.18 Add `test_schema_1_6_consumer_compatibility_matrix_is_closed` in `tests/unit/specfact_code_review/run/test_findings.py`, `test_core_0_55_1_runtime_loads_schema_1_6_consumer_matrix` in `tests/e2e/specfact_code_review/test_review_run_e2e.py`, and `test_pr_orchestrator_pins_exact_core_schema_smoke` and `test_pr_orchestrator_rejects_pep440_local_core_alias` in `tests/unit/workflows/test_pr_orchestrator_signing.py`; validate the exact checked-in package resource, its four canonical status reports plus mixed valid-blocker/required-UNKNOWN precedence case, required runtime-trust/adversarial-capability/per-member observation fields, hostile-candidate-policy UNKNOWN case, permitted legacy projections, strict/shadow exits, invalid mismatch/precedence cases, the exact schema-less ledger-update fixture and its schema-1.0/reward-5/PASS/exit-0 field mapping without `assurance_status`, and canonical digest. The runtime test SHALL load the candidate module through installed immutable lightweight core tag `v0.55.1` at full commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276` and full tree `47984be5434d7ae65ed6908bf525a32053290337`, derive `verified-candidate-module-payload-v1` from immutable candidate commit/tree, package/payload digests, and protected workflow/ref/run/attempt/job, prove that candidate identity cannot satisfy official-install or `pr_range` provenance, and exercise every matrix status/projection. The workflow tests SHALL require a dedicated CPython 3.11–3.13 job with that exact tag/commit/tree, strict `===0.55.1` metadata, rejection of `==0.55.1` and `0.55.1+vendor`, no `dev`, `main`, feature-branch, or paired-ref fallback, a fresh environment, candidate package installation, and this exact e2e selector before release metadata. The existing paired-core quality job remains unchanged and cannot satisfy this gate.
- [x] 2.19 Add `test_schema_1_6_missing_assurance_status_is_unknown` and legacy-reader cases proving old PASS/FAIL cannot imply UNKNOWN/NOT_APPLICABLE.
- [x] 2.20 Add table-driven `test_ledger_authoritative_assurance_controls_rewards_and_streaks` for schema 1.6 PASS/FAIL/UNKNOWN/NOT_APPLICABLE plus legacy PASS_WITH_ADVISORY. Prove UNKNOWN/NOT_APPLICABLE persist verbatim, apply zero reward/last delta, leave both streaks unchanged, and are accepted by local/Supabase schemas. Add a no-findings UNKNOWN case proving local and Supabase `report_json` plus canonical SHA-256 `report_digest` retain scope/analyzer diagnostics; add a mixed valid-blocker/required-UNKNOWN case proving the ledger consumes aggregate FAIL while persisted report evidence retains the unknown flag/member diagnostics. Preserve the existing schema-less `update-from-stdin` fixture verbatim and assert the signed matrix maps it to legacy schema 1.0, reward delta 5, PASS/exit 0, unchanged run/timestamp/score/findings/summary, and no inferred authoritative status.
- [x] 2.21 Add `test_cleanup_enrichment_preserves_schema_1_6_assurance_status`; exercise UNKNOWN with empty findings and prove cleanup forecast refresh preserves schema, assurance, scope/analyzer evidence, legacy projection, and exit code.
- [x] 2.22 Add `test_requirements_evidence_attachment_preserves_schema_1_6_assurance_status` in `tests/unit/specfact_code_review/run/test_commands.py`; attach validated Requirements context to an UNKNOWN report and prove schema, assurance, scope/analyzer evidence, legacy projection, and exit code remain unchanged.
- [x] 2.23 Collect the exact canonical pytest node ID for every module-authored CR14 scenario in Sections 1 and 2, write each selector into `requirements-evidence.yaml`, and build the complete task-to-selector assignment for Section 3. Every such selector SHALL have exactly one owning Section 3 task; every Section 3 task SHALL have a non-empty selector set; the per-task sets SHALL be pairwise disjoint and contain no selector outside the frozen module global set. The core-adoption test names declared in task 4.7—including `test_trusted_consumer_rejects_suppression_catalog_identity_mismatch`—are handoff acceptance cases, not module-authored selectors: they SHALL NOT appear in this change's `requirements-evidence.yaml`, global selector set, or `IMPLEMENTATION_CHECKPOINT.json`. Assign a cross-cutting module selector to the final task whose change is expected to make it green; prerequisite tasks require their own independently failing selector. Rerun strict mapping validation. Do not edit production source in this task.
- [x] 2.24 Build the deterministic plan from the accepted mapping and source identity, then write and commit `openspec/changes/code-review-14-scope-truth-and-differential-enforcement/IMPLEMENTATION_CHECKPOINT.json`. Schema version `1` SHALL contain: `change_id`; `checkpoint_parent.commit_sha` and `tree_sha`; `mapping_digest`; `plan.id` and `digest`; `core_compatibility_evidence` with exact `specifier`, `version`, immutable release `tag`, full `commit_sha`, and `tree_sha`; `analyzer_input_kinds_contract` with the complete per-member and per-sub-execution `.py`/`.pyi` eligibility table, contracts component aggregation, `icontract-static-activation-v1` predicate/manifest/digest, absence reasons, canonical digest, and CrossHair executable-input prohibition; sorted global `selectors` plus `selector_digest`; sorted `implementation_items[]`, each with `task_id`, non-empty sorted `selectors`, and `selector_digest`; canonical `implementation_selector_map_digest`; `finding_multiset_contract` containing the complete canonical identity/message-normalization/occurrence-evidence/`source-span-v1` location-kind/exact-or-line-fallback conversion/`occurrence-continuity-v1` anchor/`source-line-correspondence-v1` algorithm, `G/F/X`, fixed bounds, status/distinct-location/severity-partition/pairing/surplus/bucket-digest projection plus SHA-256; sorted `frozen_input_paths` plus `frozen_input_manifest_digest`; and sorted `analyzers[]` entries with `id`, `required`, `version`, `toolchain_digest`, `policy_digest`, `config_digest`, and sorted `toolchain_components[]`; each component uses the tagged union `python_distribution | native_executable | builtin`. A Python distribution records normalized name, exact version, signed wheel SHA-256 or canonical RECORD payload-manifest digest, normalized module/entry point, sealed interpreter identity, and exact `==` spec; it excludes generated console-script path/bytes/shebang. A native executable records platform tag, exact version, executable SHA-256, format/architecture, linkage kind, verified-descriptor digest, interpreter/needed-library sets, launch mode, loader-environment policy, and pre-namespace mapped-object policy. Initial Bubblewrap SHALL be static Linux x86_64 ELF with empty interpreter/needed sets and same-descriptor execution. A builtin records its sealed mode identity and required `module-code-payload-v1` role; a mode without that payload is invalid. The checkpoint SHALL freeze `builtin_analyzer_payload_contract` with the complete canonical payload-contract schema plus SHA-256: verified-installed-module identity fields, loader registration/origin/root and protected-key identities, deterministic full-payload checksum/signature algorithm/result, canonical module/package prefix, whole-package path/type/mode/size/content-manifest algorithm, descriptor-relative no-follow/stable-read rules, canonical capsule destination, allowed entry modules/modes, interpreter/bootstrap binding, payload/root digest fields, archive-discard behavior, and producer/protected-consumer verification roles. It SHALL NOT freeze future release bytes; the final signed release/report supplies those identities. The targeted-pytest component list SHALL name pytest, pytest-cov, and direct Coverage separately. It SHALL freeze `suppression_catalog_contract` with the complete canonical logical `pr-range-v1-suppression-catalog.json` document plus SHA-256: schema/catalog/parser and canonicalization identities; every recognized family/form/tool version/parser-catalog-help-schema digest; normalized directive class; mapped required/conditional analyzer IDs; introduced-finding kind; affected-edit reason/status/evidence fields; pure-rename/removal/changed-occurrence dispositions; and the complete `suppression-directives-v1` plus `suppression-affected-edit-v1` policy constants. It SHALL also freeze `project_runtime_contract` with the complete canonical logical `project-runtime-layer-v1.schema.json` document plus digest, policy and reserved-component-catalog projections/digests, canonical mount and non-reserved snapshot-before-project precedence, allowed member IDs, exact `capsule-reserved-imports-v1` prefix catalog/collision kinds/search order/origin-audit schema and digest, exact `pytest-selection-controls-disabled-v1` pytest/pytest-cov option plus `addini` field catalogs/parser/help/alias/classification/original-canonical-effective-config/controller-argv projections and digest, target-tip/source-lock/build-attestation/OCI/root-manifest field requirements, and producer-versus-protected-consumer authority; it SHALL NOT freeze or synthesize a consumer repository's future layer bytes. It SHALL freeze `pytest_plugin_capability_contract` with the complete target descriptor entry schema, exact pytest/pytest-cov sealed identities, plugin distribution/version/payload/dependency/`pytest11` entry-point fields, option/help/`addini` catalog composition, complete pinned hook-spec disposition catalog, allowed/forbidden origin classes, explicit-load/preflight/registry/raw-item evidence, exact `pytest-item-controls-disabled-v1` collectstart/makeitem observer schema plus the complete canonical `pytest-builtin-collector-decision-v1` branch catalog and SHA-256 covering every activated built-in collector/hook implementation; initial members cover `_pytest.python` `PyCollector.collect`/`istestclass`/`istestfunction`/`pytest_pycollect_makeitem`/`Class.collect` and `_pytest.unittest` `pytest_pycollect_makeitem`/`UnitTestCase.collect`, with activation-manifest, observer, `pytest-native-namespace-controls-v1`, intentional-non-candidate/collectable/unsupported-control dispositions and native/unittest normative vectors; target-specific future plugin bytes are not frozen. It SHALL also freeze `radon_full_result_contract` with the exact Radon 6.0.1 recognized repository-source order, forbidden ambient sources, complete configuration-key disposition catalog, canonical per-command effective options, control-cwd/private-home/environment policy, eligible/invoked/output manifest schema, and SHA-256. Sorted `toolchain_environments[]` SHALL contain environment ID (OS/architecture/Python ABI) plus `environment_kind=analyzer_runtime_capsule`, `oci-distribution-acquisition-v1` with exact HTTPS registry/repository@manifest, config and ordered layer digest/diff-ID/media-type/size records, fixed in-image wheelhouse filename/platform/size/SHA-256 mapping for every selected lock member, redirect allowlist, distribution/rootfs/whiteout schemas, cache key/acquisition bounds, canonical capsule mount/interpreter/bootstrap paths, sealed final root manifest/digest covering stdlib/extensions/dynamic loader/shared libraries, sorted complete component IDs, evaluated dependency edges/markers, bootstrap allowlist, native-tool IDs, and canonical closure digest for each initially supported Linux x86_64 CPython 3.11–3.13 environment; and `toolchain_lock_projection` containing the complete canonical logical `pr-range-v1-toolchain-lock.json` document plus `toolchain_lock_projection_digest`. Validation SHALL prove the `implementation_items[].selectors` sets are pairwise disjoint, their union equals the global selector set, every global selector occurs exactly once, every member belongs to that set, every Section 3 task appears exactly once, and no item has an empty selector set. Validation SHALL reject an unavailable, movable, branch-only, or identity-mismatched exact core compatibility record, and SHALL reject ordinary equality, a PEP 440 local/alternate version alias, a lower-bound range, wildcard, or any specifier admitting an untested core identity; a bare/lower-bound component, missing direct Coverage or transitive component, duplicate component/edge, undeclared distribution inside the analyzer runtime capsule outside the explicit bootstrap allowlist, any controller/core/module host path or `/usr`/`/lib*` runtime mount in analyzer resolution, missing interpreter/stdlib/extension/loader/library/bootstrap path, missing/mutable OCI locator, checkpoint lock-projection/resource mismatch, missing/extra/duplicate/incompatible wheelhouse member, cache/acquisition/redirect/media/size/timeout/whiteout-schema ambiguity, unsafe extraction, OCI/root-manifest drift, marker ambiguity, generated-launcher identity, or mismatch between analyzer version/toolchain/environment closure digest and its component DAG; the same capsule materialized below two controller storage roots SHALL retain identical canonical in-capsule identities. Project-runtime validation SHALL reject missing target-tip/source-lock/build/artifact identities, candidate-controlled inputs, per-side drift, reviewed-project self-installation, reserved analyzer/runner/bootstrap collisions, mutable locators, host paths, and unauthorized member mounts. All digests are canonical SHA-256 values. The parent commit/tree identifies the test-and-mapping checkpoint before this evidence file is added.
Task 2.24 SHALL freeze `core_compatibility_evidence` exactly as `specifier: ===0.55.1`, `version: 0.55.1`, `tag: v0.55.1`, `commit_sha: b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and `tree_sha: 47984be5434d7ae65ed6908bf525a32053290337`; any other or movable identity invalidates the checkpoint.

- [x] 2.25 Verify the committed checkpoint against the current frozen inputs; validate the total task-to-selector assignment and `implementation_selector_map_digest`; execute the complete global selector set; confirm the expected failing outcomes; and record the exact commands, checkpoint-file digest, selector-map digest, and outcomes in `TDD_EVIDENCE.md` before any source edit. An incomplete/empty/unknown assignment, unassigned or multiply assigned selector, overlapping task set, or frozen-input/digest mismatch invalidates the checkpoint and repeats tasks 2.23–2.25. Section 3 is blocked until this task passes.

## 3. Minimal implementation

Each item consumes only its non-empty selector set frozen in `IMPLEMENTATION_CHECKPOINT.json`; the implementer SHALL NOT choose or shrink that set after task 2.25. Before each production edit, verify the checkpoint and `implementation_selector_map_digest`, record the item task ID, frozen selector set/digest, and red outcome in `TDD_EVIDENCE.md`, then run the identical set green after the item. Any required remapping invalidates the checkpoint and repeats tasks 2.23–2.25. No Section 3 item may claim green while a frozen mapped selector remains outside its per-item gate.

- [x] 3.1 Add request/status types for worktree, index, range, full, and explicit-files scope; delegate all Git discovery from `run/commands.py` to `scope.py`; select governed Python inputs, the closed `governed-policy-paths-v1` analyzer-policy path/pyproject-section set including repository-root `radon.cfg`, `pyproject.toml:[tool.radon]`, and `setup.cfg:[radon]`, and every authenticated target-tip project-runtime `source_lock_paths[]` member as governed whole-file policy input. A candidate source-lock edit is UNKNOWN and its bytes never build the layer.
- [x] 3.2 Parse/freeze the outside-checkout claimed context file, including any structurally valid project-runtime descriptor/build-attestation digest while retaining claimed-only producer authority, resolve full base/head identities, enumerate all best merge bases, require exactly one before resolving its commit/tree, validate expected target/head plus context digest, emit range_candidate or range_preview but never pr_range, and emit sorted merge-base candidate commit/tree identities plus digest, and make zero/multiple merge bases or unsafe/mismatched context UNKNOWN. Do not synthesize a virtual base.
- [x] 3.3 Capture one stable immutable index snapshot with descriptor no-follow/pre-post stability checks and bounded retry, including content-addressed split-index dependencies; run all index inspection with `GIT_INDEX_FILE` bound to that snapshot, derive both its complete tree and canonical stage/flag metadata, derive ordinary paths/statuses/rename/deletion facts by diffing `HEAD` against that tree without rereading the live index, reject intent-to-add/unmerged metadata before applicability, materialize that same tree, keep staged governed selection separate from supporting paths, reject capture/metadata/tree/diff/object/selection mismatch as `UNKNOWN`, and implement `tracked-regular-governed-input-v1` for every selected/eligible/invoked Python/test/test-support/policy/config path: bind object type/mode/blob/content, permit only blob `100644`/`100755`, descriptor-traverse parents no-follow/beneath, open/fstat/hash the same regular final descriptor, and return `unsafe_governed_input` UNKNOWN for symlink/gitlink/missing/unmerged/intent-to-add/mismatch before applicability. Build the index SnapshotInvocationContext and retain pre/post integrity.
- [x] 3.4 Materialize detached merge-base/head source roots plus object-type/Git-mode/raw-add-delete facts; implement `canonical-exact-rename-v1` directly over captured tree entries with heuristic rename detection disabled, unique identical-blob pairing, copy/ambiguity classification, and canonical evidence digest and pre/post source manifests; apply the same task-3.3 regular-blob/no-follow validation symmetrically to both snapshots; keep recognized unsafe-mode paths governed and return UNKNOWN rather than dereference/omit/fix; clean roots or report UNKNOWN.
- [x] 3.5a In `scope.py`, materialize the authorized target-tip policy bundle, bind its source commit/tree plus closed path/section/status/rename/deletion manifest and `candidate_policy_change_digest`, and make any candidate policy change or parse failure shadow-only UNKNOWN. Do not implement analyzer-specific selection here.
- [x] 3.5b In `scope.py`, build one explicit read-only Semgrep bundle for both snapshots: target-tip `.semgrep/clean_code.yaml` or exact signed-module fallback, plus target-tip `resources/semgrep-rules/ai-bloat.yaml` or exact signed-module fallback. Bind every source/path/blob/content/materialized digest; missing/untrusted/mismatched fallback is UNKNOWN.
- [x] 3.5c In `scope.py`, select pytest and Coverage logical policies only, using the complete pinned pytest 9.0.3 and Coverage 7.15.4 source/section orders including `.coveragerc.toml`; for pytest, treat present `pytest.toml`/`.pytest.toml` and `pytest.ini`/`.pytest.ini` as matching even when empty, use table-bearing pyproject/tox/setup matches next, and select a bare `pyproject.toml` only as the final fallback; manifest selected section-or-empty/fallback reason plus every ignored source; reject dual pytest pyproject tables, parse/shape failure, or loader/profile drift; emit contract-named defaults only when the locator finds no configfile. Per-snapshot projection remains task 3.14.
- [x] 3.5d In `scope.py`, enumerate only repository-root `.ruff.toml`, `ruff.toml`, and `pyproject.toml:[tool.ruff]`; use explicit isolated mode for zero applicable sources, select exactly one, and return `ruff_config_ambiguous` UNKNOWN for multiple/malformed/unsupported/loader-drift sources before applicability/launch. Then resolve the sole selected target Ruff config's complete bounded transitive `extend` closure into a preserved-layout read-only bundle; bind every node/edge/blob/content/graph digest and reject unsafe, cyclic, missing, unsupported, or over-bound graphs.
- [x] 3.5e In `scope.py`, resolve exactly one target basedpyright primary plus its bounded `extends` graph and optional `baselineFile`; govern and seal every referenced identity; reject conflicts and unsafe/cyclic/missing/over-bound input; select exact `basedpyright-default-v1` when absent. Per-snapshot projection remains task 3.6.
- [x] 3.5f In `scope.py`, enumerate the pinned Pylint target-tip sources/sections and select exactly zero or one; bind source/section/loader identities and emit the sealed default when absent; multiple, unparseable, unsupported, or loader-drift input is UNKNOWN. Safety projection and adapter launch remain tasks 3.6 and 3.17.
- [x] 3.6 Build and validate per-snapshot configuration projections, including the closed typed input-provenance union, canonical shared identity, parent/transformation identities, side-specific projected/materialized digests, and signed `governed-input-completeness-v1` option catalogs plus eligible/invoked input manifests. Generate a Ruff projection clearing `per-file-ignores`, `extend-per-file-ignores`, `per-file-target-version`, and `namespace-packages`, setting `src` to the active snapshot root, and retaining one shared global `target-version`; generate a Pylint projection clearing `ignore`, `ignore-patterns`, `ignore-paths`, `ignored-modules`, `ignored-classes`, `generated-members`, `ignore-none`, `ignore-on-opaque-inference`, `ignore-mixin-members`, `ignored-checks-for-mixins`, `mixin-class-rgx`, `signature-mutators`, target-added `contextmanager-decorators`, target `confidence`/`errors-only` filters, and input-source overrides under the pinned total result/input-control catalog, forcing the effective Pylint confidence set to exactly `HIGH, CONTROL_FLOW, INFERENCE, INFERENCE_FAILURE, UNDEFINED`, `errors-only=false`, `from-stdin=false`, and `recursive=false`, and requiring one explicit disposition for every pinned option with no retain fallback; generate each flattened basedpyright projection with `venvPath`/`venv` bound to the canonical verified project-runtime layer site-packages and `include` equal to the sorted exact eligible snapshot-relative file manifest, `exclude=[]`, `ignore=[]`, target path-scoped `strict` required absent/canonically empty before generated `strict=[]`, exact global checking/diagnostic settings preserved, non-empty target `strict`/`executionEnvironments` rejected, every effective `baselineFile` removed, and forbid positional source arguments plus `--baselinefile`; bind original/effective values and transform/catalog/manifests; reject any effective/unclassified whole-file exclusion or mismatch. For basedpyright, project the sealed `extends` graph and baseline schema or exact no-config `basedpyright-default-v1` canonical eligible-include document, rebind every snapshot-sensitive identity, and prohibit implicit `--project .` discovery.
- [x] 3.7 Build one `SnapshotInvocationContext` per side with snapshot OS cwd/import roots except a Radon-specific controller-owned empty control cwd plus absolute snapshot inputs, sorted closed `config_roots[]` binding every adapter's typed input/role/digest/read-only mount/pre-post manifest, verified analyzer runtime capsule plus applicable task-3.12g project-runtime descriptor/layer identity and member allowlist from `toolchain.py`, sanitized Python/pytest environment, pinned plugins, process-private output/temp roots, sandbox identity, and context digest. For every Python analyzer, construct capsule-interpreter `-I -S` sealed-bootstrap argv; for `ai-bloat-ast` and `ast-clean-code`, select only the verified `/opt/specfact/builtin/specfact_code_review` entry modules bound by `module-code-payload-v1` and exclude every controller/module host path; pass validated snapshot roots as bootstrap data, keep them absent from startup `sys.path`, and avoid site/`.pth` processing. Before either mutable root is inserted, load the checkpoint-frozen `capsule-reserved-imports-v1` exact prefix catalog, install a capsule-only finder for every prefix/descendant, preflight snapshot and project roots for top-level module/stub/regular-package/namespace-package collisions, and verify observed reserved origins. Return `reserved_import_collision` UNKNOWN before dispatch on any collision or origin drift. Then insert snapshot roots first plus the verified project-runtime layer second for non-reserved imports only and only for the closed import-capable member set.
- [x] 3.8 Implement `run/sandbox.py` as the only OS-isolation boundary for the initial `linux-bwrap-v1` backend: resolve only the signed static Linux x86_64 Bubblewrap ELF from canonical verified OCI-root path `/opt/specfact/bin/bwrap-static`; verify architecture plus empty `PT_INTERP`/`DT_NEEDED`; open no-follow, hash and execute the same descriptor with loader-injection variables cleared; probe and bind pre-namespace mapped objects/opens, rejecting anything beyond the executable and kernel pseudo-mappings; bind version/SHA-256/ELF-descriptor and argv/profile schemas; then capability-probe namespaces/mount/network denial; use the verified analyzer capsule as the complete rootfs; expose no host `/usr`, `/lib*`, interpreter, stdlib, extension, loader/library, or controller runtime mount; construct the deny-by-default read-only/read-write/network/file-descriptor profile; mount every and only declared config root read-only, including basedpyright non-baseline reference and Semgrep bundle roots; mount the identical verified project-runtime layer only for basedpyright, Pylint, contracts, targeted pytest/coverage, and a dedicated snapshot-free targeted-pytest plugin-capability preflight, never for other members; the preflight may import only descriptor-attested pytest entry modules and receives no source snapshot; reject any baseline artifact or unverified/host project dependency mount; emit its canonical root manifest/digest; and return UNKNOWN when the complete profile cannot be enforced. It SHALL NOT contain analyzer, Git-scope, Requirements, or policy-verdict logic.
- [x] 3.9 Route each index/range analyzer process through a fresh sandbox from `runner.py`; keep controller/context/other-side/sibling evidence outside it; capture argv plus OS-observed exit/stdout/stderr; accept only process-private outputs after exit; label candidate-executing artifacts as observations under `candidate-not-intentionally-subverting-observer-v1`; validate/seal exact bytes before reuse; reject omitted/extra/writable/config-root identity mismatches and hostile-candidate runtime policy as UNKNOWN; and prohibit unsandboxed fallback.
- [x] 3.10 Reject every range narrowing/mutation combination and normalize omitted enforcement to strict full before materialization.
- [x] 3.11 Emit UNKNOWN for unresolved scope/context or any governed candidate policy/config change; emit NOT_APPLICABLE only for a manifest-proven range with neither governed Python/test/test-support nor governed policy input, or for a structurally empty per-snapshot analyzer input.
- [x] 3.12a In `runner.py`/`findings.py`, define exact `pr-range-v1` membership and serialize per-snapshot required/conditional execution/evidence and the closed schema-1.6 `location_kind=source_span|selector|non_source` union with canonical/raw span precision/coordinate/conversion fields or typed selector/non-source identity, project-runtime descriptor/layer/provenance identities for the closed import-capable members, plus the canonical checkpoint/resource/package/profile `suppression_catalog_identity`, `governed-input-completeness-v1` option-catalog, eligible/invoked input, effective-control, cache-mode, Semgrep scanned/skipped manifests/digests, and for both built-in members the complete `verified-installed-module-payload-v1` loader/origin/key/full-payload-verification/copy manifest plus `module-code-payload-v1` payload/root identities. Preserve semantic FAIL versus infrastructure/reconciliation UNKNOWN through aggregate status and legacy projection.
- [x] 3.12b Generate `resources/contracts/pr-range-v1-toolchain-lock.json` from the frozen checkpoint for Linux x86_64 CPython 3.11–3.13 with `oci-distribution-acquisition-v1` exact HTTPS registry/repository@manifest, config plus ordered layer digest/diff-ID/media-type/size records, fixed `/opt/specfact/wheelhouse` filename/platform/size/SHA-256 mapping for every selected lock member, redirect allowlist, distribution/rootfs/whiteout schemas, cache key/acquisition bounds, canonical capsule mount/interpreter/bootstrap paths, root-manifest algorithm, canonical OCI-root `/opt/specfact/bin/bwrap-static` payload/path plus static Bubblewrap ELF/architecture/empty-`PT_INTERP`/empty-`DT_NEEDED`/descriptor-execution/pre-namespace-mapping identities, and exact normalized analyzer direct/transitive `==` entries, including pytest 9.0.3, Coverage 7.15.4, and the complete closure. Update `module-package.yaml` and its guard in the opposite direction: remove analyzer-only lock members from host `pip_dependencies`, retain only controller-imported host dependencies (including the existing HTTP client used by signed OCI acquisition), and bind the lock resource through existing signed package integrity. Task 3.20 may subsequently update the same manifest only to add the exact checkpoint-matching suppression-catalog resource identity after generating that resource; it SHALL NOT change dependencies, version, or unrelated metadata. Regenerate the resource canonically from the frozen checkpoint's `toolchain_lock_projection`, compare every field/byte and SHA-256 to `toolchain_lock_projection_digest`, reject mismatch before package signing/profile activation, and append the comparison identities/result to `TDD_EVIDENCE.md`. The lock binds OCI layers, final interpreter/stdlib/extension/loader/library root manifest, DAG/markers/edges/payloads/entry points/bootstrap/native IDs/closure; generated launcher bytes and outside-capsule storage roots are excluded.
- [x] 3.12c In new `run/toolchain.py`, implement only immutable OCI acquisition before sandbox launch: read/reverify the outside-checkout digest-keyed controller cache; on miss fetch only the signed HTTPS Distribution registry/repository@manifest with signed redirect host/path allowlist, controller-only credentials, and bounds; require every hop/final target to remain HTTPS and be authorized before attaching credentials, strip credentials across each redirect until re-authorized, bind the complete ordered redirect/status/final-URL/credential-sent chain and digest; verify manifest/config/layer digest/diff-ID/media-type/size before atomic cache publication and prove the selected artifact declares the complete locked wheelhouse payload. Cache/network/auth/redirect/downgrade/unauthorized-credential/media/size/timeout/digest failure is UNKNOWN with no alternate source. Do not extract layers or install analyzers in this task.
- [x] 3.12d In `run/toolchain.py`, safely extract the task-3.12c verified layers into a fresh controller-owned base root using the pinned OCI order/whiteout schema; reject absolute/parent traversal, device nodes, unsafe links, duplicate/colliding paths, size/count overflow, and root escape; then produce and verify the canonical base-root manifest. Do not install analyzer packages or bootstrap in this task.
- [x] 3.12e In `run/toolchain.py`, rehash and reconcile the extracted fixed wheelhouse against every selected lock member, reject missing/extra/duplicate/incompatible wheels, then install only the signed lock closure offline with package indexes disabled. Derive `core-v0.55.1-installed-module-handoff-v1` from core's existing `DiscoveredModule` source/root/metadata, canonical install-root classification, registry marker, install-verified checksum, manifest integrity, bundled approved-key fingerprint, and fresh artifact verification; under `verified-installed-module-payload-v1`, require approved official provenance and the installed module name/version/checksum/signature package identity—never an invented loader DTO, source commit/tree, or archive metadata—then re-run the deterministic full-module-payload checksum and descriptor-relatively no-follow/read/copy the complete canonical `specfact_code_review` payload into `/opt/specfact/builtin/specfact_code_review`. Accept separately tagged `verified-candidate-module-payload-v1` only for protected pre-release compatibility/materialization evidence and never as released or `pr_range` provenance. Reject unsafe/missing/extra/colliding/drifted payload members or unstable reads, require no original archive/cache/locator and perform no archive reacquisition, bind core handoff/root/key/verification/copy identities plus `module-code-payload-v1`, generate the checkpointed `sealed-bootstrap-v2`, and bind `capsule-composite-identity-v1` over the immutable base-root digest, analyzer set, copied module payload, bootstrap, native launcher, applicable project runtime, and final composite root. Expose only canonical capsule paths; never feed analyzer entries to host resolution or permit host import/executable/native-library paths.
- [x] 3.12f In `run/toolchain.py`, materialize the complete capsule below a second controller storage root and verify identical canonical OCI/base-root/final-root/interpreter/stdlib/extension/loader/library/bootstrap plus analyzer installed-set, wheel/RECORD payload, entry-point, edge, marker, native, and closure identities. Before namespace creation, prove the task-3.8 static Bubblewrap descriptor/mapped-object closure on two supported hosts; then boot the capsule interpreter, synthetic analyzer, `ai-bloat-ast`, and `ast-clean-code` from the verified module-code payload in an empty Bubblewrap namespace with controller/module host paths absent. Missing/extra runtime/analyzer path, bare pin, host leakage/mount, movable locator, acquisition/extraction/root/payload/bootstrap drift, generated-wrapper identity, loader failure, or unsupported environment is UNKNOWN before activation.
- [x] 3.12g In `run/toolchain.py`, validate and materialize only the consumer-supplied `project-runtime-layer-v1` descriptor defined by new signed `resources/contracts/project-runtime-layer-v1.schema.json`: verify authorized target repository/commit/tree and declared dependency-input blob/content identities; sorted optional `pytest_plugins[]` entries with exact distribution/version/wheel-or-RECORD/dependency/`pytest11` entry-point/parser/hook-capability identities; complete exact distribution/native DAG, wheel/RECORD/entry-point/closure, Python ABI/platform and root manifest; immutable OCI acquisition; builder workflow/ref/run/attempt/artifact/attestation; reviewed-project exclusion; and the signed reserved analyzer/runner/bootstrap catalog. Generate the schema bytes canonically from the frozen checkpoint projection, require exact byte/digest equality, bind the resource in signed package metadata, and record the comparison in `TDD_EVIDENCE.md`. Reuse the task-3.12c/d safe acquisition/extraction primitives without adding a new source file. Expose one canonical read-only layer identity and member allowlist, require identical use on merge-base/head, and return UNKNOWN for missing/untrusted/candidate-controlled/mismatched/colliding/host-backed evidence. The module ships only this schema/policy, never a repository-specific layer or dependency resolver.
- [x] 3.13 Implement `analyzer-input-kinds-v1` for the composite contracts member first: `icontract-static-scan` accepts governed `.py` and `.pyi`; `crosshair` accepts only tracked regular governed production `.py`. Preserve the current `_has_icontract_usage` activation semantics through signed `icontract-static-activation-v1`: bind its exact input manifest, predicate identity, decision, and digest; when true run the static scan over eligible `.py`/`.pyi` and retain its findings/status; when false record only that component as NOT_APPLICABLE with `no_icontract_usage`; predicate or decision drift is UNKNOWN. Do not broaden the rule to repositories without icontract usage. Standalone `.pyi` never enters CrossHair argv; when no eligible `.py` exists only the CrossHair component records NOT_APPLICABLE with empty manifests, catalog digest, and `absence_reason=no_crosshair_executable_input`. Derive the parent contracts outcome from the two component records: an icontract-activated stub-only side can PASS/FAIL/UNKNOWN but cannot be NOT_APPLICABLE; the parent is NOT_APPLICABLE only when both components have valid independent NOT_APPLICABLE evidence. When eligible input exists, run CrossHair with the verified project-runtime layer while retaining snapshot-first imports; preserve exact CrossHair source coordinates when available and otherwise emit the canonical whole-physical-line `source-span-v1` fallback; make unavailable/launch/timeout/parse and documented process-error exits (including code 2) `execution_state=error`, `evidence_outcome=UNKNOWN`; retain successful zero-finding runs as ran and parsed counterexamples as valid completed findings that may derive FAIL.
- [x] 3.14a Implement signed `complete-pytest-suite-v1`: when governed production/test/test-support change activates the member, first implement `pytest-input-role-v1` from immutable path plus the shared authorized target-tip pytest 9.0.3 `testpaths`/root-default and exact pinned `_pytest.pathlib.fnmatch_ex` operating on pytest's materialized absolute Path form. Freeze its source/policy digest and separator-free/path-bearing/relative/absolute/`**` vectors; do not substitute basename-only, `Path.match`, or a new glob implementation. Bind per-side role manifests and classifier/policy digest; never use contents, AST, `__test__`, marks, decorators, selectors, or collection outcomes as role inputs. Collect the complete selector inventory independently at merge base and head with no production-derived positional narrowing; bind collection roots, eligible test/test-support input manifests, exact node IDs/source paths, collection artifacts and inventory digests; execute each complete inventory; normalize only pure one-to-one file renames while making candidate-to-support/outside-root transitions UNKNOWN; make empty/collection-disabled candidates, removed baseline selectors, other uncollected changed tests, no-tests-collected, or collection mismatch UNKNOWN. A side is NOT_APPLICABLE only when its entire governed production/test/test-support input class is empty. Do not implement source-to-test naming/import/coverage inference or accept a candidate impact map.
- [x] 3.14b Generate separate merge-base/head pytest projections from the same sealed logical target-tip/default policy. Implement `pytest-selection-controls-disabled-v1`, `pytest-plugin-capabilities-v1`, `pytest-hook-dispositions-v1`, and `pytest-item-controls-disabled-v1` using the frozen complete pinned pytest 9.0.3, controller-owned pytest-cov, explicitly attested target-tip plugin parser/help/`addini` plus hook-spec catalogs, and closed `pytest-builtin-collector-decision-v1` activated-collector registry with native-Python and unittest decision catalogs: disable automatic loading; preflight exact descriptor entry points in a dedicated sandbox that mounts only the sealed analyzer capsule plus identical read-only project-runtime layer and neither source snapshot; compare observed distribution/origin/catalog/hook identities; reject repository/user/unlisted/dynamic plugins, conftest parser extensions, and every collection/discovery/cardinality/selector/deselection/dynamic-registration plus execution/phase-outcome/TestReport/status/exit/evidence-artifact shaping hook or wrapper; classify every pinned hook spec exactly once and allow those runtime/report capabilities only for exact signed controller observer and controller-owned pytest-cov identities; canonicalize all effective target addopts and the complete effective config field/value map, classify every field, reject non-empty `norecursedirs` and every unsupported selection/discovery/plugin/coverage field, reject positional selectors and every collection/cache selector, execution short-circuit, discovery/plugin override, and coverage override, reject unknown or unattested/unclassified plugin-added options or catalog drift, and retain only catalogued non-selecting options. Classify every remaining pinned config/addopts path as read or writable; rebase reads; redirect controller-owned cache/log/basetemp/JUnit/coverage outputs; append explicit side `-c`, `--rootdir`, complete-collection, JUnit, coverage source/config/threshold, and private-output argv after accepted target options; reject conflicts, escape/missing/unclassified/collision/ineffective projection; bind original/canonical/rejected/effective argv/config maps, option/field classifications, parser/help/`addini`/catalog/plugin/hook-capability digests, transforms, substitutions, output digests, and controller options. Reconcile every per-side `pytest-input-role-v1` test candidate—not only changed paths—against collected source paths and emit `uncollected_test_candidate` UNKNOWN for any whole-file omission.
- [x] 3.14c Generate the per-process Coverage projection passed by explicit `--cov-config`; require absent/empty target exclusion/partial-branch fields and clear pinned defaults/additions; reject all repository plugins under the initial empty manifest; rebase every data/debug/HTML/XML/JSON/LCOV/annotation/pytest-cov write into role-specific private output paths; sanitize overrides and reject escape/collision/unclassified/mismatch.
- [x] 3.14d Execute each complete `complete-pytest-suite-v1` inventory per snapshot with typed selector locations outside source occurrence pairing under the sandbox using the sealed analyzer capsule plus the identical task-3.12g verified target-tip project-runtime layer; snapshot code precedes the dependency layer for non-reserved imports while `capsule-reserved-imports-v1` keeps analyzer/runner/bootstrap prefixes capsule-only and turns any collision into pre-dispatch UNKNOWN; install the controller-owned early plugin observer as the sole manifest-bound non-mutating collection-hook exception; at `pytest_collectstart` and a try-first `pytest_pycollect_makeitem` wrapper reproduce and record every `pytest-builtin-collector-decision-v1` input/outcome before the corresponding built-in branch; bind the activated built-in collector/hook manifest; before native `PyCollector.collect`, build sealed descriptor-safe raw module/class namespace and MRO manifests, bind module subtype/class metaclass namespace-control identities, compare against pytest's observed dictionaries/makeitem stream, and reject filtered/omitted/drifted eligible callables as UNKNOWN; then classify native namespace/MRO/import ownership, name/nose eligibility, class/function dispatch, unwrap, callable/real-function, fixture, abstract, raw/final `__test__`, and `hasinit`/`hasnew` decisions; at `pytest_collectstart` for `UnitTestCase`, build the descriptor-safe raw MRO manifest of name-prefix-eligible callables before `dir(TestCase)`, validate sealed `unittest-loader-controls-v1` prefix/pattern/sort and metaclass `__dir__`/`__getattribute__`/`__getattr__`, then record/reconcile the exact `TestLoader.getTestCaseNames` inventory, resolved-object identity, per-method `__test__`, inherited origin, and `runTest`/Twisted fallback before `UnitTestCase.collect`; immediately before each `_pytest.unittest.TestCaseFunction.runtest` dispatch, implement `unittest-execution-controls-v1` with pinned pre-import `unittest.TestCase.__call__`/`run` descriptor identity, descriptor-safe raw MRO/instance namespace and default lookup-control evidence, reject overrides/shadows/redirection/base mutation with no candidate execution gap, and retain only exact canonical inherited dispatch; reject every eligible class/callable suppressed by an unsupported candidate control as `pytest_item_control_unsupported`, and reject any unclassified/catalog-drifted branch as UNKNOWN, bind every registration/hook origin plus raw `pytest_itemcollected` inventory and pre/post registry digests, reject forbidden/unclassified/late hooks or any raw item absent from the final inventory; use a conftest `pytest_runtest_makereport` hook that rewrites a failed assertion to passed plus runtime-hook/wrapper table vectors to prove rejection before execution; then record every controller-observed `pytest_runtest_logreport` phase and `wasxfail` through signed `pytest-outcome-observer-v1` into the private output root, validate/seal it after exit, derive selector outcomes, require exact collection/outcome-artifact/JUnit/process/count reconciliation, and bind environment/plugin/outcome/JUnit/artifact identities. A passed report with `wasxfail` is XPASS even when JUnit says pass. Head assertion/skip/xfail/xpass is ran/FAIL; missing/duplicate/deselected/malformed phase, outcome-artifact/JUnit/process mismatch, reconciliation, or infrastructure is error/UNKNOWN; exact baseline fail/head pass is fixed.
- [x] 3.14e Implement signed `runtime-measurable-production-v1`; normalize and bind the Coverage `.py` file manifest plus a separate governed `.pyi` static-only manifest; require every runtime-measurable production `.py` present in each immutable snapshot exactly once; prove every `.pyi` remains in each exact stub-capable member/component eligible/invoked manifest—including `contracts.icontract-static-scan`—and never enters CrossHair's; make `.pyi`-only targeted pytest/coverage NOT_APPLICABLE, keep contracts active, and make only `contracts.crosshair` NOT_APPLICABLE with its bound reason; retain absent-side evidence; and make missing/duplicate/escaping/aliased/identity-mismatched runtime paths UNKNOWN. Record complete below-threshold measurement as ran/FAIL with threshold/total/per-file/config/artifact evidence; baseline threshold fail/head pass is fixed.
- [x] 3.15 Preserve Ruff's raw start/end row/column coordinate system and convert it to canonical UTF-8-byte half-open `source-span-v1`, then update Ruff invocation only for the explicit root of the controller-resolved, preserved-layout, manifest-bound target-tip `extend` closure—or isolated mode—plus snapshot invocation context. Always pass pinned `--no-cache` and `--no-force-exclude`, consume the task-3.6 Ruff projection with empty effective `per-file-ignores`, `extend-per-file-ignores`, `per-file-target-version`, and `namespace-packages`, forced `lint.pycodestyle.ignore-overlong-task-comments=false`, `fix=false`, and `fix-only=false` with bound original task tags and control values, plus snapshot-root `src` and one shared global target version, bind original/effective map plus projection/catalog/argv and eligible/invoked manifests, reject any cache write, non-empty path-scoped map, global-version mismatch, or skipped governed input, disable source-tree discovery, keep candidate closure shadow-only, and surface any flag/catalog/closure/config injection mismatch as UNKNOWN.
- [x] 3.16 Preserve an exact Radon source range when exposed or emit the canonical whole-physical-line `source-span-v1` fallback. Implement signed `radon-full-result-v1` from the checkpointed Radon 6.0.1 loader/option catalog: enumerate repository-root `radon.cfg`, `pyproject.toml:[tool.radon]`, and `setup.cfg:[radon]` only as governed target-policy evidence; run the sealed Radon entry point from a controller-owned empty control directory with private empty `HOME`, sanitized `RADONCFG`, and only absolute manifest-bound snapshot files; force CC A..F JSON, MI A..C JSON, raw/Halstead JSON, empty exclude/ignore, `output_file=None` for captured stdout, `include_ipynb=false`, `ipynb_cells=false`, and an explicit disposition for every remaining Radon 6.0.1 configuration key; retain metric-semantic controls only when catalogued and identical on both sides. Ensure the child changes cwd/environment before importing `radon.cli`. Bind original/effective options, recognized sources, loader/catalog/argv/control-root/output and eligible/invoked manifests; return UNKNOWN for discovery, narrowing, skipped input, outside output, unclassified controls, or drift. Preserve Radon's metric formulas and do not add/change thresholds or findings.
- [x] 3.17 Preserve Pylint's raw start/end line/column coordinate system and convert exact endpoints to canonical UTF-8-byte half-open `source-span-v1`, then enumerate only the closed pinned-loader Pylint sources, reject multiple effective target-tip sources as UNKNOWN, canonicalize all `init-hook`/`load-plugins`/`extension-pkg-allow-list`/deprecated `extension-pkg-whitelist` aliases, reject every non-empty hook or extension-package option and every plugin outside the initial empty signed plugin manifest before applicability/launch, and update Pylint invocation only for the single explicit generated projection of the target-policy `--rcfile` or sealed default plus snapshot invocation context and the verified project-runtime layer for dependency imports; require empty/disabled effective `ignore`, `ignore-patterns`, `ignore-paths`, `ignored-modules`, `ignored-classes`, `generated-members`, `ignore-none`, `ignore-on-opaque-inference`, `ignore-mixin-members`, `ignored-checks-for-mixins`, `mixin-class-rgx`, `signature-mutators`, target-added `contextmanager-decorators` plus an exact complete effective `HIGH, CONTROL_FLOW, INFERENCE, INFERENCE_FAILURE, UNDEFINED` confidence set, `errors-only=false`, `from-stdin=false`, `recursive=false`, closed stdin, only exact eligible positional arguments, matching eligible/argv manifests, and total no-fallthrough pinned catalog parity; disable source/plugin/extension discovery and parameterize every supported source/alias in tests. Do not infer or seal a native extension closure.
- [x] 3.18 Preserve basedpyright's raw range and UTF-16/code-point coordinate identity and convert it against immutable source bytes to canonical UTF-8-byte half-open `source-span-v1`, then update basedpyright invocation only for per-snapshot projected project/non-baseline reference files—derived from the sealed target graph or exact `basedpyright-default-v1`—analyzer-capsule paths/context, and the identical verified project-runtime site-packages bound through projected `venvPath`/`venv`; require explicit `--project` without positional sources, exact eligible effective `include`, empty effective `exclude`/`ignore`, target strict proven absent/empty before empty effective `strict`, exact global checking/diagnostic settings preserved, rejected non-empty target strict/`executionEnvironments`, no effective `baselineFile`/`--baselinefile`, no baseline mount/read, and matching eligible/project-include manifests; disable source discovery, and surface reference/default/projection/catalog/input-manifest mismatch as UNKNOWN.
- [x] 3.19 Preserve each Semgrep JSON start/end coordinate system and convert it to canonical UTF-8-byte half-open `source-span-v1`, then update the mandatory Semgrep clean pass only to consume the explicit bundle containing the selected target-tip or signed-module-fallback clean-code payload plus exactly one selected/sealed target-tip or signed-module-fallback AI-bloat pack. Before launch, parse the pinned rule schema and reject every rule-level `paths.include`/`paths.exclude` target control as unsupported UNKNOWN. Both Semgrep passes then consume snapshot invocation context, append and verify controller-owned pinned `--disable-nosem` under `semgrep-native-suppression-disabled-v1`, retain/normalize JSON `paths.scanned` and `paths.skipped`, require pass-level scanned paths to equal the exact eligible input manifest, and make implicit/default size skip, extra/missing path, or reconciliation failure UNKNOWN. Keep the bugs pass conditional on authorized target-tip policy and do not change any non-targeting clean-code, bug, or AI-bloat rule semantics.
- [x] 3.20 Generate exact canonical `resources/contracts/pr-range-v1-suppression-catalog.json` bytes from the checkpoint's `suppression_catalog_contract`, require byte/SHA-256 equality, bind the resource in signed package metadata plus profile/report identity and the static consumer-matrix envelope schema/fixtures, and make missing/extra/noncanonical/embedded-only or checkpoint/package/profile/report/static-envelope/parser-catalog drift UNKNOWN before applicability or analyzer launch. In `differential.py`, load only that verified resource and implement the signed `suppression-directives-v1` comment-token registry/manifest comparison before `finding-multiset-v1` classification. Emit always-open blocking `introduced_inline_suppression` or `introduced_analyzer_result_control` findings according to the frozen registry family, preserve unchanged/removal evidence, reject/ignore every purported waiver input or trusted flag because CR14 defines no authenticated ingestion path, make affected missing base fingerprints unknown rather than fixed, and fail UNKNOWN on registry/token/normalization/manifest or pinned Ruff source-suppression/isort-action parser catalogs and basedpyright/CrossHair parser-derived directive-catalog/help/schema parity uncertainty. Implement signed `suppression-affected-edit-v1`: if a governed Python blob changes while retaining any unchanged registered occurrence, emit required `unchanged_suppression_on_changed_file` UNKNOWN for each mapped required/activated analyzer with blob/directive-manifest/changed-hunk evidence; exclude byte-identical pure renames, let removal of the last occurrence proceed, and keep changed/relocated occurrences as introduced blockers. Do not infer directive reach or add analyzer-specific unsuppressed execution. Then implement signed `finding-multiset-v1` with its exact canonical identity/message normalization and occurrence evidence plus signed `occurrence-continuity-v1`: consume only canonical source locations (exact adapter range or deterministic whole-line fallback); validate spans; hash exact LF-normalized spanned physical lines plus one complete adjacent line or BOF/EOF per side and relative columns; count distinct physical locations; run `source-line-correspondence-v1` over complete exact line sequences with costs `1/1/2/0`, no moves, `G/F/X`, and fixed 16-MiB/20,000-line/4,000,000-cell bounds; permit unchanged pairing only when the anchor is unique and `F=G<X` while retaining duplicate analyzer-emission multiplicity at that location; prohibit fuzzy/AST/symbol/move/nearest-line inference; make `F>G` different/introduced and alternate-optimal/non-unique/invalid/unavailable continuity unknown; apply severity/blocking partitions only after continuity; serialize identity/anchor/partition counts, continuity decisions, pairs/surpluses/digests; consume only `canonical-exact-rename-v1` unique identical-blob pairs for normalization, validate the independently recomputed candidate/pair/ambiguity digest, and implement post-match normalized-severity/blocking-input comparison plus conservative `rename-fix-policy-v1`; any base finding missing across an exact rename remains unknown, and any unmatched base finding on a deleted path remains unknown when an unpaired governed addition exists, with no threshold, ambient Git config, similarity, or semantic-subject derivation. Do not infer suppression reach or change analyzer rule semantics.
- [x] 3.21 Separate finding lifecycle, differential state, autofix availability, reserved nullable waiver reference, and derived blocking policy in the canonical model. C14 SHALL emit `waiver_reference=null` for newly derived findings and SHALL NOT accept a CLI/report/context/injected waiver input; future signed exception semantics belong to `governance-02-exception-management`.
- [x] 3.22 Add schema 1.6 authoritative status, derive the aggregate blocker set only after differential/lifecycle classification, exclude exact fixed baseline-only FAIL/head-PASS pairs while retaining their per-snapshot evidence, then apply legacy projection, normalized enforcement, and strict/shadow exit behavior.
- [x] 3.23 Preserve schema 1.6 fields through cleanup refresh and Requirements-context attachment without verdict fusion. Update the modules repository `scripts/pre_commit_code_review.py` consumer so schema 1.6+ validates and uses authoritative `assurance_status`/`ci_exit_code` before any legacy changed-line logic; UNKNOWN stays non-zero outside shadow even with no staged-line blocker, NOT_APPLICABLE remains zero, FAIL remains non-zero, and PASS remains zero. Retain the existing changed-line fallback only for reports older than 1.6; do not change its explicit-files assurance kind.
- [x] 3.24 Add and validate the closed static consumer compatibility matrix package resource, including the exact schema-less ledger-update input/legacy-normalization fixture, candidate/preview producer reports, accepted/rejected finding-multiset canonical-span/line-fallback/duplicate/line-shift/moved-block/alternate-optimal/bound/mixed-transition buckets, matching and mismatched checkpoint/resource/package/profile/report/static-envelope suppression-catalog identities, accepted/rejected project-runtime descriptors and pr_range verification-envelope fixtures, and omitted-governed-input plus zero/multiple-merge-base, merge-base/diff/selection/rename-deletion/policy-config/producer-artifact mismatch cases; bind its digest in package metadata/tests. This task validates serialization/schema fixtures only: it SHALL NOT read workflow events, run Git recomputation, emit a live envelope, or make any task-4.7 core runtime selector green.
- [x] 3.25 Update ledger model/local persistence and neutral reward/streak behavior for UNKNOWN/NOT_APPLICABLE while retaining legacy reads.
- [x] 3.26 Migrate Supabase constraints and canonical `report_json`/`report_digest` persistence; preserve legacy rows.
- [x] 3.27 Update CLI contracts and canonical agent/module/bundle/generated/updater/skill guidance so protected CI range/base/head/context/full is merge authority, local range is preview-only, and staged positional/simplification worktree guidance is non-PR.
- [x] 3.28 Add the exact-core compatibility smoke only through the existing `.github/workflows/pr-orchestrator.yml`, `tests/unit/workflows/test_pr_orchestrator_signing.py`, and `tests/e2e/specfact_code_review/test_review_run_e2e.py`. Verify—without mutating—immutable lightweight core tag `v0.55.1`, full commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and full tree `47984be5434d7ae65ed6908bf525a32053290337` already frozen by task 2.24. For each supported CPython 3.11–3.13 create a fresh controller environment, install that core and candidate module, load the module, and run the exact schema 1.6 matrix e2e selector; when the profile is activated it must materialize and boot the separate task-3.12e analyzer runtime capsule rather than validate or mount the controller's package/native runtime set, and a dependency-bearing e2e selector must import only through a task-3.12g target-tip-attested project-runtime layer. Record only run/environment/outcome evidence in `TDD_EVIDENCE.md`. Keep the existing paired-core quality job unchanged; reject moved/mismatched identity, ordinary `==0.55.1`, a local/alternate installed version such as `0.55.1+vendor`, branch fallback, install/load/profile separation failure, or matrix mismatch. This task SHALL NOT implement the protected PR-context consumer owned by task 4.7.

## 4. Release and adoption

All Section 4 exact-core references bind lightweight tag `v0.55.1`, full commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, and full tree `47984be5434d7ae65ed6908bf525a32053290337`.

- [x] 4.1 Run focused/full tests, contracts, type/lint, strict OpenSpec, and explicit-range self-review.
- [x] 4.2 Benchmark #665–#671 and seeded false-green, staged-versus-unstaged index, mutable-worktree, advanced-base-tip, positional-downgrade, pure-rename, policy-only self-authorization, and false-introduction cases.
- [ ] 4.3 After behavior and the Section 3 dependency-lock selectors pass, and only after task 3.28 passes against the checkpointed immutable core 0.55.1 identity, update public docs, command references, bundle version, changelog, and `module-package.yaml` release metadata with the exact tested `core_compatibility: '===0.55.1'`. Reject ordinary `==0.55.1`, PEP 440 local/alternate versions such as `0.55.1+vendor`, `>=0.55.1,<1.0.0`, wildcards, and every specifier that admits a core identity absent from the immutable runtime matrix. A later module metadata release may advertise a newly released exact paired-core version only after its tag/commit/tree identity passes the same matrix smoke. Verify—without first changing them here—that the canonical signed analyzer lock bytes/digest still equal the frozen checkpoint projection, the signed project-runtime schema/policy still equal the checkpointed contract digests, and the signed suppression catalog bytes/digest still equal the frozen `suppression_catalog_contract` and that the exact direct/transitive `==` entries written in task 3.12b still include pytest 9.0.3, direct Coverage 7.15.4, basedpyright 1.39.10, Pylint 4.0.7, pytest-cov/Ruff/Semgrep/Radon/CrossHair, pluggy, nodejs-wheel-binaries, z3-solver, and every other locked package, while none of those analyzer-only entries leaked into host `pip_dependencies`. Re-run both a signed-registry fresh cache-miss acquisition with per-hop HTTPS/allowlist authorization, no pre-authorization credential forwarding, bound full redirect/final-URL evidence, canonical checkpoint-lock equality, and offline/index-disabled installation from the complete fixed wheelhouse and a verified network-offline cache-hit capsule materialization below two distinct controller storage roots and a separate core/controller host for every supported Python version; boot each capsule in an empty Bubblewrap namespace with no host runtime mounts and compare exact OCI/final-root/interpreter/stdlib/extension/loader/library/bootstrap, installed-set/DAG/closure, and portable wheel/RECORD payload/entry-point identities rather than outside-root or generated launcher bytes. Do not generate or hand-edit registry archives, checksums, signatures, sidecars, or `registry/index.json` on the feature branch.
- [ ] 4.4 Re-run the complete feature-branch gates and merge the reviewed implementation PR to `dev` only when both the static matrix and immutable exact-core 0.55.1 fresh-install/load/runtime compatibility smoke are proven. This remains distinct from task 4.7 protected-consumer adoption.
- [ ] 4.5 Observe the canonical `.github/workflows/publish-modules.yml` run and review its `auto/publish-dev-<run-id>` PR.
- [ ] 4.6 Require the generated signed manifest/archive/checksum/sidecar/index, filesystem signature/version-bump verification, and full quality matrix before merging the auto-publish PR.
- [ ] 4.7 After the signed consumer compatibility matrix passes and a separate core adoption change is accepted, give core the final merged commit/tree, module version, schema 1.6 contract and compatibility-matrix digest, archive/checksum/signature, signer, workflow, and auto-publish PR identities. The paired core change SHALL first author `test_trusted_consumer_envelope_upgrades_range_candidate_to_pr_range`, `test_trusted_consumer_rejects_merge_base_mismatch`, `test_trusted_consumer_rejects_multiple_best_merge_bases`, `test_trusted_consumer_rejects_incomplete_governed_selection`, `test_trusted_consumer_rejects_diff_or_rename_manifest_mismatch`, `test_trusted_consumer_rejects_governed_object_type_or_mode_mismatch`, `test_trusted_consumer_rejects_policy_config_identity_mismatch`, `test_trusted_consumer_rejects_suppression_catalog_identity_mismatch`, `test_trusted_consumer_rejects_unapproved_producer_or_artifact_identity`, `test_trusted_consumer_accepts_target_tip_project_runtime_attestation`, and `test_trusted_consumer_rejects_candidate_or_untrusted_project_runtime` as failing core tests, then implement the core-owned protected consumer and take those exact tests green. These handoff cases SHALL NOT be mapped as module Requirements Evidence or frozen in the module checkpoint, and module completion SHALL NOT claim their runtime evidence. The core PR SHALL keep `scripts/pre_commit_code_review.py` limited to `explicit_files` but make it consume authoritative schema 1.6 status/exit fields; its separate PR-range consumer SHALL reject any report or code path that cannot consume them. Core PR CI SHALL write the canonical context file under runner temp immediately before invocation, pass full base/head refs plus `--pr-context-file`, require producer `assurance_kind=range_candidate`, independently derive the expected merge base, complete governed diff/selection/line/object-type/Git-mode/status/rename/deletion manifests, authorized target-tip policy/config selection and declared project-runtime source-lock paths/blobs, and any applicable target-tip project-runtime dependency inputs/build/artifact attestation; require the approved signed producer module/schema/profile/toolchain/suppression-catalog and workflow/job/artifact provenance; independently obtain the approved catalog digest from the signed module/profile policy, require exact checkpoint/resource/package/profile/report equality, compare every identity/digest with the immutable report; reject any omitted governed input or mismatch as UNKNOWN; emit a separate verifier-identity-bound envelope containing every compared identity/manifest/digest with `effective_assurance_kind=pr_range`, and migrate in shadow, warning, then enforce mode.
- [ ] 4.8 Only after the canonical publish workflow has shipped the signed module release and its registry/archive/checksum/signature/sidecar identities are verified, and after the separate paired core adoption handoff is implemented, green, and merged, run exactly `openspec archive code-review-14-scope-truth-and-differential-enforcement` from the repository root; never move the change manually. A feature-branch package, auto-publish PR, unpublished signed artifact, or merely accepted core plan is insufficient.

## Prohibited shortcuts

- Do not treat Git errors as an empty file/line set.
- Do not determine introduction solely from changed-line intersection.
- Do not report PASS for a content-changed governed Python file that retains an unchanged registered suppression; emit the bounded `suppression-affected-edit-v1` UNKNOWN instead.
- Do not exclude changed tests silently.
- Do not make `autofix_available` mean resolved/non-blocking.
- Do not add detector rules or AI review to this change.
- Do not defer a frozen implementation selector's only green-producing manifest edit to the release section.

## Closed implementation allowlist

CLI/request parsing:

- `packages/specfact-code-review/src/specfact_code_review/review/commands.py`.
- `packages/specfact-code-review/src/specfact_code_review/run/commands.py` only for request translation/validation, consumption of `ScopeResolution`, and the existing post-analysis Requirements-evidence attachment model-copy so it preserves schema 1.6 truth without changing Requirements verdict semantics.
- `tests/unit/specfact_code_review/review/test_commands.py` only to replace the obsolete positional-file PR instruction expectation with the protected range/base/head/context/full workflow.
- `tests/unit/specfact_code_review/run/test_commands.py` and `tests/cli-contracts/specfact-code-review-run.scenarios.yaml`.

Canonical merge-quality guidance:

- `docs/agent-rules/20-repository-context.md` and `docs/agent-rules/50-quality-gates-and-review.md` only to replace mandatory merge-review commands with protected range/base/head/context/full guidance and label the staged pre-commit helper non-PR.
- `docs/modules/code-review.md` and `docs/bundles/code-review/run.md` only to remove positional branch-delta/changed-worktree PR guidance, document the complete range contract, and retain clearly local examples.
- `packages/specfact-code-review/src/specfact_code_review/rules/updater.py` only to replace merge-quality changed/worktree guidance with the protected range/base/head/context/full command; simplification preview remains worktree-scoped.
- `packages/specfact-code-review/src/specfact_code_review/resources/skills/specfact-code-review/SKILL.md` for the same merge-quality correction.
- `skills/specfact-code-review/SKILL.md` and `.vibe/skills/specfact-code-review/SKILL.md` only as updater-generated tracked copies after the bundled source and tests pass; never hand-edit them independently.
- `tests/unit/specfact_code_review/rules/test_updater.py` and `tests/unit/docs/test_code_review_docs_parity.py` only for focused guidance/parity regressions.
- `scripts/pre_commit_code_review.py` and `tests/unit/scripts/test_pre_commit_code_review.py` only for schema 1.6+ authoritative status/exit preservation described in tasks 2.17 and 3.23; the helper remains local staged `explicit_files` evidence. `scripts/pre-commit-quality-checks.sh` and its tests remain unchanged.

Scope:

- New exactly `packages/specfact-code-review/src/specfact_code_review/run/scope.py`.
- New exactly `tests/unit/specfact_code_review/run/test_scope.py`.
- `scope.py` is the only component allowed to invoke Git for scope discovery, index materialization, and detached merge-base/head snapshot materialization; replace/delegate the old scope helpers in `run/commands.py`.

Differential enforcement:

- New exactly `packages/specfact-code-review/src/specfact_code_review/run/differential.py`; it alone owns signed `finding-multiset-v1`, `occurrence-continuity-v1`, and bounded `source-line-correspondence-v1` comparison plus the bounded signed `suppression-directives-v1` Python-comment-token manifest/delta check. It SHALL NOT implement analyzer rules or arbitrary suppression-scope inference.
- New exactly `tests/unit/specfact_code_review/run/test_differential.py`.
- New exactly `packages/specfact-code-review/src/specfact_code_review/run/sandbox.py` as the only OS filesystem/network isolation capability layer; no analyzer, Git, Requirements, or verdict logic.
- New exactly `tests/unit/specfact_code_review/run/test_sandbox.py`.
- New exactly `packages/specfact-code-review/src/specfact_code_review/run/toolchain.py`; it alone verifies the signed OCI/analyzer lock, materializes/verifies the analyzer runtime capsule, validates/materializes an explicitly attested consumer project-runtime layer without resolving dependencies, and exposes sealed analyzer/project roots plus interpreter/bootstrap/runtime/component identities. It SHALL NOT contain analyzer rules, Git scope, Requirements, or verdict logic.
- New exactly `tests/unit/specfact_code_review/run/test_toolchain.py`.
- `packages/specfact-code-review/src/specfact_code_review/run/runner.py` builds/validates snapshot invocation contexts, routes every index/range analyzer through `sandbox.py`, validates/seals process-private artifacts after exit, consumes only `toolchain.py`'s sealed analyzer environment, runs targeted pytest under that boundary with the verified project-runtime layer, collects analyzer facts, and consumes classification; `differential.py` classifies already-produced base/head findings and contains no analyzer or Requirements logic.

Analyzer configuration and failure propagation:

- `packages/specfact-code-review/src/specfact_code_review/tools/radon_runner.py` and `tests/unit/specfact_code_review/tools/test_radon_runner.py` only to preserve/normalize canonical source-span evidence and implement `radon-full-result-v1` using the sealed executable, Radon-specific empty control cwd/private home/sanitized environment, absolute per-snapshot inputs, private outputs, and invocation-context digest; Radon thresholds/metrics/findings are unchanged.
- `packages/specfact-code-review/src/specfact_code_review/tools/ruff_runner.py` and `tests/unit/specfact_code_review/tools/test_ruff_runner.py` only to preserve/normalize Ruff canonical source-span evidence, consume the sealed executable/per-snapshot cwd/environment/output/context digest, use explicit generated target-policy `--config` or `--isolated`, and enforce pinned `--no-cache`/`--no-force-exclude`, empty effective per-file-ignore maps, forced `lint.pycodestyle.ignore-overlong-task-comments=false`, `fix=false`, and `fix-only=false`, plus projection/catalog and eligible/invoked input evidence; Ruff rule semantics remain unchanged.
- `packages/specfact-code-review/src/specfact_code_review/tools/pylint_runner.py` and `tests/unit/specfact_code_review/tools/test_pylint_runner.py` only to preserve/normalize Pylint canonical source-span evidence and use the explicit generated projection of the target-policy `--rcfile` or sealed pinned-default config, clear/bind `ignore`/`ignore-patterns`/`ignore-paths`, canonicalize/reject non-empty init hooks and plugins outside the initial empty signed manifest before launch, and consume the snapshot-root cwd/import environment; repository analyzer-extension code and Python closure inference are forbidden.
- `packages/specfact-code-review/src/specfact_code_review/tools/basedpyright_runner.py` and `tests/unit/specfact_code_review/tools/test_basedpyright_runner.py` only to preserve/normalize basedpyright canonical source-span evidence and consume the sealed executable/per-snapshot cwd/environment/output/context plus projected `--project` artifact with exact eligible `include`, empty bound `exclude`/`ignore`, target strict proven absent/empty before empty bound strict, preserved global checking/diagnostic settings, rejected non-empty target strict/execution environments, and no positional sources, remove `--project .`/direct policy-bundle use, and prove eligible/project-include/imported-path/toolchain binding; projection construction and path validation remain in `scope.py`.
- `packages/specfact-code-review/src/specfact_code_review/tools/semgrep_runner.py` and `tests/unit/specfact_code_review/tools/test_semgrep_runner.py` only to preserve/normalize Semgrep canonical source-span evidence and require an explicit selected target-tip or signed-module-fallback `bundle_root` for the mandatory clean pass, retain target-tip-only conditionality for the bugs pass, select/seal the authorized target-tip or exact signed-module-fallback `resources/semgrep-rules/ai-bloat.yaml` without changing its rules, and consume the sealed executable/per-snapshot cwd/environment/output/context digest for both passes, including explicit timeout/process/parse failure propagation and exact JSON scanned/skipped-to-eligible input reconciliation; rule semantics remain unchanged. The existing `packages/specfact-code-review/.semgrep/clean_code.yaml` is the read-only canonical fallback payload and MUST NOT be edited by this change.
- `packages/specfact-code-review/src/specfact_code_review/tools/contract_runner.py` only to preserve/normalize CrossHair canonical source-span evidence and consume the snapshot invocation context and replace swallowed CrossHair unavailable/launch/timeout/parse and documented tool/process-error exits (including code 2) with explicit `execution_state=error`, `evidence_outcome=UNKNOWN`; successful zero-finding and parsed-counterexample semantics remain unchanged.
- `tests/unit/specfact_code_review/tools/test_contract_runner.py` only to replace the timeout-ignore regression with the named fail-closed timeout case and add the focused process-error-exit regression/control.

Report truth:

- New exactly `packages/specfact-code-review/src/specfact_code_review/resources/contracts/review-report-schema-1.6-consumer-matrix.json` for the closed signed producer/consumer status-projection-exit contract. This is a static schema/fixture resource, not a protected-consumer implementation seam.
- New exactly `packages/specfact-code-review/src/specfact_code_review/resources/contracts/pr-range-v1-toolchain-lock.json` for the signed complete Linux x86_64 CPython 3.11–3.13 analyzer runtime dependency DAG and payload matrix.
- New exactly `packages/specfact-code-review/src/specfact_code_review/resources/contracts/project-runtime-layer-v1.schema.json` for the signed consumer-supplied target-tip dependency-layer descriptor, reserved-component policy, allowed-member set, and producer/consumer authority contract. No repository-specific dependency layer is shipped.
- New exactly `packages/specfact-code-review/src/specfact_code_review/resources/contracts/pr-range-v1-suppression-catalog.json` for the checkpoint-derived canonical `suppression-directives-v1` registry plus `suppression-affected-edit-v1` analyzer mapping and policy constants; it is data only and contains no waiver authority.
- `packages/specfact-code-review/src/specfact_code_review/run/findings.py` remains the only report/finding model; do not create a parallel model. `fixable` must not affect `is_blocking()`.
- `packages/specfact-code-review/src/specfact_code_review/run/cleanup_evidence.py` only to preserve the incoming schema-1.6 report/status/evidence fields while refreshing cleanup evidence; cleanup algorithms and forecast semantics are out of scope.
- `tests/unit/specfact_code_review/run/test_cleanup_evidence.py` only for the schema/status preservation regression.
- `tests/unit/specfact_code_review/run/test_findings.py`, `tests/unit/specfact_code_review/run/test_runner.py`, and existing `tests/unit/specfact_code_review/run/test_scorer.py` only to update the fixable-error regression expectation; production `scorer.py` remains forbidden.
- `tests/unit/specfact_code_review/test_review_tool_pip_manifest.py` only to parse normalized PEP 508 requirement names/specifiers, validate the exact selected-lock dependency set, and prove the manifest authenticates the exact checkpoint-derived suppression-catalog path/digest; it SHALL NOT weaken membership, exact-pin, or resource-identity checks.

First-party ledger consumer:

- `packages/specfact-code-review/src/specfact_code_review/ledger/client.py` only for authoritative-status persistence and neutral reward/streak policy.
- `tests/unit/specfact_code_review/ledger/test_client.py`.
- `packages/specfact-code-review/src/specfact_code_review/resources/supabase/review_ledger_ddl.sql` only to migrate verdict constraints and add nullable `report_json`/`report_digest` columns for canonical schema 1.6 audit evidence while preserving existing rows.
- `tests/cli-contracts/specfact-code-review-ledger.scenarios.yaml` and `docs/bundles/code-review/ledger.md` only for the public ledger status contract.

End-to-end/docs/release:

- `tests/e2e/specfact_code_review/test_review_run_e2e.py`, the CLI-contract YAML, and `docs/bundles/code-review/run.md`.
- `.github/workflows/pr-orchestrator.yml` and `tests/unit/workflows/test_pr_orchestrator_signing.py` only for task 3.28's immutable exact-core 0.55.1 (`v0.55.1`, commit `b1e517e60e669eaba15a18ecfa83ef5a9df65276`, tree `47984be5434d7ae65ed6908bf525a32053290337`) fresh-install/load/schema smoke; no branch fallback or protected PR-context consumer logic.
- `openspec/changes/code-review-14-scope-truth-and-differential-enforcement/requirements-evidence.yaml` only in task 2.23 to add exact collected selectors and freeze their mapping identity.
- New exactly `openspec/changes/code-review-14-scope-truth-and-differential-enforcement/IMPLEMENTATION_CHECKPOINT.json` in task 2.24 with only the closed schema named there.
- `openspec/changes/code-review-14-scope-truth-and-differential-enforcement/TDD_EVIDENCE.md` for acceptance task A.3, task 2.25 failing evidence, and later verified green evidence.
- `packages/specfact-code-review/module-package.yaml` in tasks 3.12b/3.12g and 3.20 only to remove analyzer-only lock members from host `pip_dependencies`, retain controller-imported host dependencies, and authenticate the generated analyzer-lock, project-runtime-schema, and suppression-catalog resource digests that exactly equal the frozen checkpoint projections and contract digests; exact analyzer constraints live solely in that signed lock resource. In task 4.3 the same file plus `CHANGELOG.md` may add the required release version, compatibility metadata, and release note without moving analyzer entries back into host resolution. Root `pyproject.toml` remains outside this change; its ambient lower bounds cannot satisfy or override the signed analyzer profile.
- Generated docs/registry/signatures only after tests pass; use existing generators and never hand-edit archives.

Explicitly forbidden:

- `packages/specfact-code-review/src/specfact_code_review/run/forecast.py`, `packages/specfact-code-review/src/specfact_code_review/run/scorer.py`, and detector/rule-semantic changes under `specfact_code_review/tools/`; only the exact adapter configuration/failure plumbing above is allowed;
- the Requirements package and archived OpenSpec changes;
- any module-side protected PR/merge-queue consumer, workflow-native Git recomputation component, verification-envelope emitter, or its runtime tests; those belong only to the paired core adoption change;
- any new source/test/resource file other than the eight exact `scope.py`/`differential.py`/`sandbox.py`/`toolchain.py` source-and-test files and the four exact contract resources above (schema 1.6 consumer matrix, pr-range-v1 analyzer toolchain lock, project-runtime-layer-v1 schema, and pr-range-v1 suppression catalog).

## 5. Post-merge release-gate remediation

- [x] 5.1 Add focused failing regressions proving schema 1.6 capsule reports carry the activated suppression-catalog digest and catalog-binding drift forces UNKNOWN before a trusted report is emitted.
- [x] 5.2 Add a focused failing regression proving immutable range analyzers receive only `ScopeResolution.selected_paths` present in each complete snapshot; retain the complete snapshots for imports and the independently reconciled complete pytest inventory.
- [x] 5.3 Add a focused failing regression proving generated pytest and Coverage writable outputs resolve beneath the Bubblewrap-mounted `/opt/specfact/tmp` root.
- [x] 5.4 Strengthen the package-manifest resource test to authenticate the raw project-runtime schema bytes, then correct only the stale manifest digest.
- [x] 5.5 Implement the narrow fixes, advance the unpublished signed payload by one patch version, correct the C14 changelog release identity, and record focused plus complete verification evidence here and in `TDD_EVIDENCE.md`.
- [x] 5.6 Close post-PR review findings by binding catalog activation to the frozen checkpoint/profile and authenticated package-manifest authorities, projecting default pytest/Coverage writes beneath `/opt/specfact/tmp`, and reconciling signature evidence with the CI signing commit.
- [x] 5.7 Prove and create the projected pytest/Coverage scratch subdirectories before capsule launch so the redirected defaults have existing writable parents.
- [x] 5.8 Fail closed when authenticated package-manifest bytes are not valid UTF-8, with a focused regression proving activation returns package-binding `UNKNOWN`.
- [x] 5.9 Preserve complete head-side pytest execution for deletion-only selected Python ranges by deriving applicability from the immutable selected scope while continuing to pass analyzers only files present in each snapshot; retain empty-range and stub-only `NOT_APPLICABLE` behavior.

## 6. Final PR #420 promotion remediation

- [x] 6.1 Prove projected policy arguments account for the sandbox's zero-based request-root slot, including Semgrep bundles.
- [x] 6.2 Prove index selection includes staged Ruff extend and basedpyright reference paths from the captured index policy graph.
- [x] 6.3 Prove suppression classification tokenizes only `.py` and `.pyi` snapshot blobs.
- [x] 6.4 Prove concurrent candidate capsule composition uses distinct immutable roots.
- [x] 6.5 Prove valid Semgrep JSON may omit optional `paths.skipped` while malformed values remain rejected.
- [x] 6.6 Prove missing Requirements bundle imports become governed `RunCommandError` failures and correct the stale public schema documentation from 1.5 to 1.6.
- [x] 6.7 Capture failing-before evidence for exactly tasks 6.1-6.6 before changing production or public documentation.
- [x] 6.8 Implement only tasks 6.1-6.6, advance `specfact-code-review` by one patch version, refresh its checksum, and preserve exact core compatibility `===0.55.1`.
- [ ] 6.9 Run targeted, strict OpenSpec, formatting, typing, lint, contract, smart, complete, Code Review, manifest, signature, and publish-precheck gates; record passing evidence in `TDD_EVIDENCE.md`.
- [x] 6.10 Close only the final in-scope review corrections: preserve governed evidence for unsafe staged primary/transitive policy entries, including primary and new reference staged together, deleted prior references, unchanged unsafe entries newly governed by a staged regular config, newly referenced missing entries, and valid per-analyzer base closures when the other analyzer is invalid, before graph resolution and record both collected node IDs for the existing parametrized transitive-policy regression.
- [x] 6.11 Add focused regressions for the four final PR #420 release blockers: reject non-canonical project-runtime registry authorities before credential forwarding, bind descriptor source-lock identities to the authenticated target tree, bind the activated suppression catalog to no-governed-impact reports, and preserve the winning immutable project-runtime root during publication collisions.
- [x] 6.12 Capture failing-before evidence for exactly task 6.11, then implement only those four corrections without admitting deferred review findings.
- [ ] 6.13 Run focused, strict OpenSpec, formatting, typing, lint, contract, smart, complete, Code Review, manifest, signature, and publish-precheck gates; advance `specfact-code-review` by one patch version and record passing evidence in `TDD_EVIDENCE.md`.
- [x] 6.14 Add a focused failing regression proving that moving an otherwise identical registered directive across a source line is introduced and blocking, while the existing unrelated-line-shift and byte-identical canonical-rename controls remain unchanged.
- [x] 6.15 Replace path/family/token/ordinal-only suppression pairing with bounded exact physical-line correspondence for changed source pairs. Admit only uniquely optimal exact-line pairs as unchanged; preserve byte-identical source pairing; classify a unique physical directive line without uniquely optimal correspondence as relocated and introduced; and fail closed as `UNKNOWN` for invalid, non-unique, or resource-unavailable correspondence. Do not change the directive catalog, waiver policy, analyzer rules, aggregate runner semantics, or any other review finding.
- [ ] 6.16 Run focused, strict OpenSpec, formatting, typing, lint, contract, smart, complete, Code Review, manifest, signature, and publish-precheck gates; advance `specfact-code-review` by one patch version and record passing evidence in `TDD_EVIDENCE.md`.
- [x] 6.17 Add focused failing regressions for review findings on repeated-directive insertion pairing and aggregate forbidden-pair work, then correct only the suppression-correspondence implementation.
- [x] 6.18 Prove the repeated existing occurrences remain paired by exact physical-line correspondence, the one inserted occurrence remains introduced, and exhausted aggregate correspondence work fails closed without another edit-matrix build.
- [x] 6.19 Add a focused failing regression for shifted repeated occurrences when the aggregate budget is exhausted, then preserve exact-line candidates as `UNKNOWN` and make bounded candidate order deterministic.
- [x] 6.20 Prove only the unmatched inserted directive remains introduced while every unproven exact-line candidate is quarantined, independent of hash iteration order.
- [x] 6.21 Add focused failing regressions proving relocated-suppression findings retain the matched baseline occurrence and bounded correspondence proof through report serialization, while genuinely new suppressions retain no fabricated baseline.
- [ ] 6.22 Preserve the private base/head transition and `G/F/X` evidence through finding construction and the existing evidence-reference surface; rerun the scoped quality, signing, protected-matrix, and review gates without admitting unrelated findings.
- [x] 6.23 Add focused failing regressions proving ordinal fallback cannot bypass correspondence resource bounds and retained or `UNKNOWN` pairs do not discard their matched baseline/correspondence evidence.
- [x] 6.24 Correct only those two candidate-patch evidence paths while preserving the existing public suppression verdict and occurrence APIs.
- [x] 6.25 Add a focused failing regression proving physical-line sequences are not repeatedly derived for each distinct directive identity in one changed source pair.
- [ ] 6.26 Cache immutable physical-line metadata across suppression identities without changing correspondence verdicts, then rerun the scoped gates and approval-time signing.
- [x] 6.27 Add a focused failing regression proving a count surplus among byte-identical repeated directive lines cannot fabricate the final head ordinal as the introduced location.
- [ ] 6.28 Quarantine every non-unique repeated-line head candidate, including surplus candidates without a matched baseline occurrence, while preserving uniquely located surplus blockers.
- [x] 6.29 Add a focused failing regression proving whole-source equality is not rescanned for every distinct directive identity and quarantine finding in one source pair.
- [ ] 6.30 Reuse the same private source-pair equality cache through classification and finding construction without changing suppression verdicts or evidence.
- [x] 6.31 Add focused failing regressions proving every real sandbox launch gates on runtime pre-namespace mapped-object/open validation and mixed suppression FAIL/UNKNOWN evidence within one member is retained independently of finding order.
- [x] 6.32 Wire the existing pre-namespace validator into the Linux launch boundary and accumulate suppression outcomes with FAIL precedence plus canonical required-unknown reasons; change no analyzer, catalog, detector, policy, or unrelated sandbox semantics.
- [ ] 6.33 Perform a fresh modified-scope code review and run focused, strict OpenSpec, formatting, typing, lint, contract, smart, complete, manifest, signature, publish-precheck, and protected checks; admit only additional findings demonstrably inside tasks 6.31-6.32.
- [x] 6.34 Add focused failing regressions for PR #438 review findings: detach the successfully validated tracee, terminate a stopped tracee without subprocess polling on failure, preserve valid `NOT_APPLICABLE` provenance, and re-derive lifecycle blocking during missing-base reclassification.
- [ ] 6.35 Correct only those four states, rerun the task 6.33 gates, obtain replacement approval-time signing, and resolve every PR #438 review thread with evidence or an explicit scope rationale.
- [x] 6.36 Add focused failing regressions for PR #420 final-head analyzer findings: projected basedpyright launches contain no positional source arguments, fatal basedpyright exits cannot produce completed evidence, and Semgrep fatal exits or structured errors cannot produce completed evidence.
- [x] 6.37 Correct only those three adapter-completion states without changing analyzer rules, projected policy content, eligible input selection, or legacy non-projected invocation behavior.
- [ ] 6.38 Run the focused runner surfaces plus strict OpenSpec, format, typing, lint, contract, smart, complete, Code Review, manifest, signature, publish-precheck, and protected checks; advance `specfact-code-review` by one patch version and resolve only the corresponding PR #420 threads.
- [x] 6.39 Add a focused failing regression for PR #440 proving ordinary non-project basedpyright adapter options retain the explicitly requested positional sources.
- [x] 6.40 Restrict positional-source omission to the exact generated `--project <path>` argument contract, then repeat the scoped gates and approval-time signing without admitting another finding family.
- [x] 6.41 Add a focused failing regression for the PR #420 finding proving Ruff operational exits cannot become completed evidence even when stdout is parseable empty JSON.
- [x] 6.42 Reject Ruff exit statuses outside completed-analysis codes 0 and 1 before parsing findings, then repeat the scoped gates and approval-time signing without changing Ruff rules, policy projection, input selection, or another analyzer.
- [x] 6.43 Add a focused failing regression for the PR #442 review finding proving the complete retained Ruff stderr snippet, including its truncation marker, never exceeds the declared 4,000-character bound.
- [x] 6.44 Correct only the Ruff stderr truncation off-by-one, rerun the scoped gates, and obtain replacement approval-time signing.
- [x] 6.45 Add focused failing regressions for PR #446 proving local capsule reports preserve `worktree`, `full`, and `explicit_files` scope identity and apply changed-line enforcement without converting required `UNKNOWN` evidence to PASS.
- [x] 6.46 Thread resolved local assurance kind through command execution, apply changed-line policy to completed capsule reports, and preserve fail-closed capsule uncertainty.
- [ ] 6.47 Run focused, strict OpenSpec, format, typing, lint, contract, smart, complete, Code Review, manifest, signature, and publish-precheck gates; sign and verify the exact `specfact-code-review` 0.49.63 candidate and resolve the corresponding PR #446 threads after protected evidence passes.
- [x] 6.48 Add focused failing regressions for PR #448 proving changed-line discovery failures cannot be interpreted as an empty changed-line set while a successful clean diff retains advisory behavior.
- [x] 6.49 Fail closed when Git diff or untracked-file discovery cannot establish changed-line evidence, rerun the scoped gates for candidate `specfact-code-review` 0.49.63, and obtain replacement approval-time signing.
- [x] 6.50 Add real-repository worktree and cached regressions proving Git-quoted UTF-8 and newline-containing paths cannot bypass changed-line enforcement.
- [x] 6.51 Decode Git-quoted diff paths to exact filesystem identities, fail closed on malformed path evidence, and repeat the scoped verification and review cycle.
- [x] 6.52 Add a real-repository regression proving an unquoted tracked filename ending in whitespace cannot bypass changed-line enforcement.
- [x] 6.53 Preserve trailing filename whitespace while removing only Git's diff-header delimiter, then repeat the scoped verification, signing, and protected review cycle.
- [x] 6.54 Add failing real-repository regressions proving ambient mnemonic/no-prefix configuration and repository-local Git redirect variables cannot hide changed lines, plus a helper regression proving non-empty whitespace-only untracked path output is not absence.
- [x] 6.55 Force compatibility-safe canonical diff prefixes, sanitize the environment of every changed-line Git command, and distinguish empty untracked output from non-empty path identity without changing changed-enforcement verdict semantics.
- [ ] 6.56 Repeat the scoped verification, signing, and protected review cycle for the corrected `specfact-code-review` 0.49.63 candidate, then resolve only the addressed PR #448 threads.
- [x] 6.57 Add failing regressions proving a changed-mode unchanged blocker survives JSON and ledger reload while an unexplained failing analyzer member cannot be projected to PASS.
- [x] 6.58 Project only fully explained unchanged-blocker member failures to authoritative PASS, retain the pre-enforcement FAIL evidence and advisory findings, and rebuild the report through schema validation.
- [x] 6.59 Repeat focused, complete, strict OpenSpec, format, typing, lint, contract, manifest, signature, publish-precheck, Code Review, and protected checks for the unchanged `specfact-code-review` 0.49.63 candidate.
- [ ] 6.60 Reply to and resolve only the addressed PR #448 serialization thread after final-head protected evidence passes; leave the original PR #446 threads open until PR #448 merges.
- [x] 6.61 Add failing real-repository worktree and cached regressions proving binary attributes, text-conversion drivers, forced Git color, and pathspec-syntax filenames cannot hide changed lines or downgrade blockers to advisory evidence.
- [x] 6.62 Force raw-text, color-free, literal-pathspec Git evidence without changing changed-enforcement verdict semantics or the declared Git compatibility floor.
- [x] 6.63 Repeat focused, complete, strict OpenSpec, format, typing, lint, contract, manifest, signature, publish-precheck, Code Review, and protected checks for the unchanged `specfact-code-review` 0.49.63 candidate; record replacement protected run IDs and results before closing this task.
- [ ] 6.64 Reply to and resolve only verified PR #448 threads after the final-head protected cycle passes; leave the original PR #446 threads open until PR #448 merges.
- [x] 6.65 Add failing real-repository worktree/cached and staged-helper regressions proving hunk content cannot forge destination headers or redirect later changed lines, and unavailable or redirected cached-diff evidence cannot become an empty change set.
- [x] 6.66 Track Git file-metadata/hunk state in both changed-line parsers, force raw canonical staged diff output, scrub repository/index redirects, and fail closed on unavailable staged evidence.
- [x] 6.67 Repeat focused, complete, strict OpenSpec, format, typing, lint, contract, manifest, signature, publish-precheck, Code Review, and protected checks for the unchanged `specfact-code-review` 0.49.63 candidate.
- [ ] 6.68 Reply to and resolve only verified PR #448 threads after the final-head protected cycle passes; leave the original PR #446 threads open until PR #448 merges.
- [x] 6.69 Add a failing runtime-boundary regression proving an unsupported local capsule assurance kind cannot be serialized as scope provenance.
- [x] 6.70 Validate local assurance kinds before runtime preparation or report construction while preserving the supported `worktree`, `full`, and `explicit_files` identities.
- [x] 6.71 Add a failing real-repository regression proving an explicitly reviewed ignored/untracked file cannot lose its changed-line identity or downgrade a blocker to advisory evidence.
- [x] 6.72 Count caller-selected untracked files independently of repository ignore filtering while preserving literal path identity and fail-closed discovery.
- [x] 6.73 Repeat the scoped verification, signing, and protected review cycle for the unchanged unpublished `specfact-code-review` 0.49.63 candidate.
- [ ] 6.74 Reply with evidence for the assurance-provenance and ignored-file findings and resolve only the still-addressed PR #448 threads; leave the original PR #446 threads open until PR #448 merges.
- [x] 6.75 Add failing real-repository worktree and cached regressions proving nested-directory invocations and absolute reviewed paths cannot split Git diff identity from analyzer finding identity.
- [x] 6.76 Normalize repository-root tracked diff paths to the caller working directory before changed-line matching without weakening exact path or fail-closed evidence handling.
- [x] 6.77 Repeat the scoped verification, signing, and protected review cycle for the unchanged unpublished `specfact-code-review` 0.49.63 candidate.
- [ ] 6.78 Reply with evidence and resolve only the verified PR #448 threads after the replacement final-head cycle; leave the original PR #446 threads open until PR #448 merges.
- [x] 6.79 Add a failing real-repository regression proving cached enforcement cannot match staged line evidence against findings from partially staged worktree bytes.
- [x] 6.80 Reject cached changed-line enforcement before analyzer preparation when reviewed index/worktree comparison is unavailable or differs, emitting required UNKNOWN even when worktree-only analysis would otherwise pass.
- [ ] 6.81 Repeat the scoped verification, signing, and protected review cycle for the unchanged unpublished `specfact-code-review` 0.49.63 candidate.
- [ ] 6.82 Reply with evidence and resolve only the verified PR #448 threads after the replacement final-head cycle; leave the original PR #446 threads open until PR #448 merges.
- [x] 6.83 Add failing real-repository regressions proving Git clean filters and `assume-unchanged` cannot make mismatched raw worktree bytes pass cached enforcement.
- [x] 6.84 Freeze one immutable stage-zero index tree plus stable base-tree/caller identities with replacement objects disabled, materialize analyzer input from its raw blobs without Git filters, verify blob and filesystem identities without following raced symlinks, reject ambiguous selected symlinks, gitlinks, and extant untracked selections, bind development-host fallback analysis and working directory to the same materialization with post-analysis verification, and derive changed-line evidence only from the frozen base/index pair.
- [ ] 6.85 Repeat the scoped verification, signing, and protected review cycle for the unchanged unpublished `specfact-code-review` 0.49.63 candidate.
- [ ] 6.86 Reply with evidence and resolve only the verified PR #448 threads after the replacement final-head cycle; leave the original PR #446 threads open until PR #448 merges.
- [x] 6.87 Add a failing regression for PR #448 proving unavailable changed-line evidence preserves a completed blocker-free PASS exactly as the existing local-capsule contract requires.
- [x] 6.88 Preserve completed ordinary PASS and FAIL verdicts when changed-line discovery is unavailable without weakening cached or required-UNKNOWN behavior.
- [ ] 6.89 Repeat focused, complete, strict OpenSpec, format, typing, lint, contract, manifest, signature, publish-precheck, Code Review, and protected checks for `specfact-code-review` 0.49.64, then resolve only verified PR #448 threads; leave the original PR #446 threads open until PR #448 merges.

Explicitly deferred from this remediation: full-tree materialization bounds, edit-distance work bounds, latent unused Semgrep snapshot-probe behavior, assurance-helper input hardening, singleton source fallback, skill wording, diagnostic-upload behavior, and test-only coverage-quality findings. No detector, policy, dependency, core-consumer, or Requirements-verdict semantics may change.
