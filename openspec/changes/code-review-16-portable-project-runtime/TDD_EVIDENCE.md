# TDD evidence

Implementation begins from dev 976460c0541b685f5fae256faef4cec32fe9ba7e on 2026-09-14 (Europe/Berlin). The original customer reproduction is inaccessible. Historical runs and subsequent external execution evidence are recorded below. Record each mapped failing-before run before its production changes and passing-after evidence below.

## Discovery — failing before production changes

`hatch run pytest -q tests/unit/specfact_code_review/run/test_runtime_discovery.py` failed collection with `ModuleNotFoundError: specfact_code_review.run.runtime_discovery` (12 cases planned). No discovery production implementation existed. Output retained in DISCOVERY_RED.txt.

## Discovery passing / artifacts failing

Discovery: 12 passed in 0.10s. Runtime artifact/adapter tests failed collection before production changes: ModuleNotFoundError runtime_artifacts. See ARTIFACTS_RED.txt.

## Artifact passing / builder failing

18 discovery/artifact/adapter tests passed in 0.11s. Builder/source-copy tests fail before implementation with ModuleNotFoundError runtime_builder; see BUILDER_RED.txt. Builder versions verified against PyPI JSON on 2026-09-14: pip 26.2.1, uv 0.12.13, Hatch 1.18.0, Poetry 2.4.3.

## Worker and CLI failing before integration

Worker tests fail collection for missing portable_worker. All three CLI tests fail: missing runtime command, missing handoff options, and rejected project_config/project_runtime kwargs. See WORKER_RED.txt and CLI_RED.txt.

## Snapshot integration failing

Seven CLI/worker tests pass; automatic snapshot attachment fails before production integration with missing portable_snapshot. See SNAPSHOT_RED.txt.

## Immutable pair failing

Independent pair test fails for missing run_project_scope_pair; two other snapshot tests pass. Existing command/runner/sandbox/tool regressions: 511 passed, 14 failed. Failures identify accidental ScopeRequest option forwarding and legacy mocked runtimes entering the newly added preparation boundary. Preserve original legacy assertions and isolate their new dependency boundary.

## Runtime hardening and real external preparation

Additional failing-before artifacts cover pytest configuration precedence and execution/coverage evidence; changed local workspace source; safe internal symlinks; explicit dependency groups; customer Actions source layouts; worker PID/mount isolation; worker identity attachment; native library inventory; credential-free dependency inventory; native Git builder support; analyzer/project dependency conflicts; and dependency-only immutable ranges. Each corresponding `*_RED.txt` was captured before retaining its implementation.

On 2026-09-14, 49 portable-runtime/corpus unit tests passed in 0.31 seconds; the subsequently added worker identity and compatibility scenarios passed separately. Scope plus portable snapshot regressions passed 130 tests in 9.90 seconds. Formatting, typing, lint, manifest/YAML validation, bundle import checks, development checksum/version checks, and strict OpenSpec validation passed before the first full-suite run.

The first complete suite reported 1,972 passed and 22 failed in 75.63 seconds. Five failures were caused by the existing paired core checkout being 0.54.0, below this bundle's already-declared >=0.55.1 requirement. Fifteen Semgrep cases could not load macOS trust anchors. Two failures identified missing command documentation/generated command artifacts. Verification now uses a separate checkout of public core 0.55.4 at d5799705, an explicit certificate bundle, and regenerated documentation; rerun evidence follows.

Real non-root Ubuntu 24.04 x86-64/Python 3.12 preparation succeeded for all four exact pinned upstream repositories: Requests/pip, Hatch/hatch-test, Flask/uv, and Poetry/test. The controller used public core 0.55.4 and the signed 0.49.85 base, with this worktree's development preparation implementation. This is acquisition/bootstrap evidence, not updated signed analyzer or release acceptance. Hatch first exposed missing Git and then its newer glibc requirement; Git now runs with its own private loader/library closure, leaving the supervisor's runtime unchanged.

The dedicated worktree was moved to `/private/tmp/specfact-473-worktree` after automatic approval-service timeouts on edits outside writable roots. Branch identity and all uncommitted changes were preserved; the main dev checkout remains untouched.

## Review-driven corrections and current delivery boundary

The corrected core/certificate/documentation baseline passed 1,994 tests in 95.74 seconds; smart tests passed the same 1,994 tests and contracts passed 28 tests (1,966 deselected). Subsequent review-driven changes require fresh evidence and do not inherit those passing claims.

The active worktree is now `/private/tmp/specfact-473-implementation`, with writable Git metadata in `/private/tmp/specfact-473-git.git`, the same feature branch and base, and canonical pre-commit hooks installed. Its task-owned development virtualenv symlink is locally excluded from Git. The user checkout remains untouched.

Additional failing-before artifacts record native Python CLI handoff and fail-closed startup, in-repository parent requirements includes, verified active environment selection, exact signed-interpreter patch constraints, and complete external analyzer-roster validation. Grouped adapter/CLI/worker tests were split into corresponding module test files; historical failing transcripts retain their original filenames.

A fresh bug-hunt review exposed an invalid development-only BasedPyright command combining `--venvpath` and `--pythonpath`. That report is not passing analyzer evidence. A real BasedPyright subprocess regression now verifies the corrected `--pythonpath` invocation and dependency imports (9 focused command tests passed). The later full run found 16 failures: foreign Hatch activation, four test observers still reading parameters moved into snapshot settings, and five worktree-identity tests rejecting the task's untracked virtualenv symlink. Discovery now verifies the active environment belongs to the reviewed root; observers inspect the equivalent settings fields; local Git exclusion fixes the task environment without relaxing source identity rules. Runner/discovery verification passed 424 tests in 12.63 seconds after these corrections.

Open acceptance work remains: full updated capsule execution across all twelve Linux corpus combinations, the labelled reconstructed Hatch/native fixture, member-specific dependency closure isolation, interpreter selection across differing base/head constraints, complete preparation/cache failure cases, and published signed installation acceptance. Successful preparation of four cp312 environments does not satisfy these criteria. This change must remain open and any initial PR must remain draft until acceptance and review are complete; #472 additionally requires access to and validation of the original customer reproduction.

Latest local full run on 2026-09-14: `hatch run test` with public core 0.55.4 and the explicit CA bundle passed **2,008 tests in 81.90 seconds**, with two pre-existing lark deprecation warnings. Contract tests passed **28 tests**, with 1,980 deselected. Formatting and typing passed. Formal development verification passed with `--payload-from-filesystem --enforce-version-bump --allow-missing-public-key --version-check-base refs/remotes/origin/dev`, matching the canonical branch-aware hook; this is not a release-signature claim.

Smart-test verification subsequently passed **2,008 tests in 82.95 seconds**. The development virtualenv lacked CrossHair, causing the review tool to discover a globally installed executable using a different interpreter. Installed the capsule-pinned `crosshair-tool==0.0.109` into the task-owned virtualenv; fresh review must be rerun with that environment. Earlier advisory reports containing the CrossHair import error are not complete passing analyzer evidence.

The staged-snapshot pytest failure was isolated to `test_github_candidate_context_failure_never_uses_stale_official_payload`: it depended on the caller checkout containing `.git`. The test now constructs its own publisher-checkout fixture and preserves the original fail-closed assertions. Recorded failing transcripts are normalized only for trailing whitespace; their outcomes and diagnostics are unchanged.


### Follow-up from candidate Linux evidence (2026-09-14)

- Signed branch head `6499136806637c6c2075ea44c857e0b90a8fb301`, PR orchestrator run `34839312884`: existing capsule namespace/cold/warm/controlled-defect fixtures completed as expected on Python 3.11/3.12/3.13. Python 3.12 Requests external acceptance passed; Hatch analysis remained incomplete. Original customer reproduction remains inaccessible and unvalidated.
- The Hatch adapter incorrectly supplied global `-e` to native `env create/find`, which take a positional name. The host command reported `Unknown environment: hatch-test` and the former harness accepted exit 1 without test inventory. These were implementation defects, not evidence of customer compatibility.
- `HATCH_EXECUTION_RED.txt`, `HATCH_NATIVE_EXPORT_RED.txt`, `EXECUTABLE_INVENTORY_RED.txt`, and `OFFLINE_PLUGIN_RED.txt` record failures before fixes. Native exports now select the concrete ABI environment; artifacts retain owned scripts/native executables; host acceptance requires actual JUnit test execution; setup/collection/internal errors remain observable. Local Linux preparation selected `hatch-test.py3.12`; execution exposed missing localhost resolution in pytest-rerunfailures, addressed with private hosts configuration inside the offline namespace.
- `MEMBER_CLOSURE_RED.txt`, `MEMBER_IMPORT_DOMAIN_RED.txt`, `NATIVE_LOADER_RED.txt`, and `NATIVE_WORKER_LAUNCH_RED.txt` precede member-specific sealed import graphs and target native loader/libc isolation. Unit checks do not substitute for Linux acceptance.
- `RECONSTRUCTION_RED.txt` and `CORPUS_CONTINUATION_RED.txt` precede the labelled detached Hatch/src-layout/asyncio/requests/pyodbc fixture and continuation across failed corpus entries. Any failed entry still fails the gate.
- `VCS_VERSION_RED.txt`, `VCS_SNAPSHOT_RED.txt`, and `PYTHON_SELECTION_RED.txt` precede Git version/cache binding and signed target Python selection independent from the controller.
- Follow-up full run: 2011 passes and 15 Semgrep failures when the task's required SSL_CERT_FILE was omitted. Rerun with the established certificate bundle is required; that failed run is not passing evidence.


### Passing follow-up evidence (2026-09-14)

- Local full and smart suites: 2032 passed, two existing lark deprecation warnings, 100.91s and 99.93s respectively. Contract suite: 28 passed. Formatting, typing, lint (10.00/10), YAML, import boundaries, strict OpenSpec, planned Requirements mapping, and development checksum/version verification passed before review refinements.
- Actual local Linux x86-64/non-root/Python 3.12 component runs: pinned Hatch `hatch-test.py3.12` collected/executed all five selected tests, exit 0; labelled detached reconstruction executed its asyncio/requests/pyodbc test, exit 0. Source and artifacts remained in private task storage. These use signed baseline Python with development worker components and are **not signed release acceptance**.
- `HATCH_INSTALLER_DOMAIN_RED.txt` records the pip-backed detached failure before manager-specific environment controls were separated. Upstream Hatch's uv installer was unaffected; the reconstruction was essential to expose pip redirection.
- Fresh SpecFact bug-hunt review identified a preparation complexity regression and long review function; refactoring passed 411 focused tests. Targeted pytest instrumentation subsequently recorded 1058 passes; a fresh review is required after the fixes.


- Instrumenting the actual review subprocess (not a separately invoked test suite) revealed 30 worktree-test failures caused by inherited `SPECFACT_CODE_REVIEW_CHANGED_DIFF=cached`. `NESTED_SCOPE_RED.txt` precedes removal of that controller-only setting from child pytest environments. The controller setting remains intact, and tests may explicitly set their own scope.


- Fresh SpecFact `--bug-hunt` after nested scope isolation: exit 0, no error findings, 43.54s. Lint/type/format recheck passed (10.00/10). Warning-level review work remains before final acceptance, including explicit standard-library bootstrap contract exceptions and snapshot argument grouping. The PR remains a draft and no release acceptance is claimed.


- The first follow-up commit attempt was stopped by canonical hooks. A new VCS fixture inherited the hook's GIT_INDEX_FILE and staged its temporary app.py into the calling index. No commit was created. The fixture-only staged artifact was removed; `HOOK_INDEX_ISOLATION_RED.txt` records regression failures before sanitizing both fixture Git processes and the child pytest environment. Source changes were retained.

## PR #474 feedback and second candidate matrix

The signed candidate `b8f762551430566b9fc2f9a03eb5ca83e2f97ad9` failed all three ABI jobs in run 34845391063. All four upstream cold reviews reached analysis. Pylint could not map its bundled checker files to modules because its sealed dependency closure was exposed only by an import finder. Poetry additionally failed xdist startup because nested Python lost its private coverage destination. The reconstructed Hatch host baseline duplicated the explicitly loaded asyncio plugin under its entry-point alias. These are failures, not completed acceptance.

The follow-up introduces a real read-only member package directory, preserves pytest child-domain/coverage settings and native Hatch extra arguments, and records the host baseline's explicit `-p no:asyncio` alias suppression while retaining `-p pytest_asyncio.plugin`. The host command and justification are checked into the corpus manifest. No upstream source or pytest selection is changed.

Local Ubuntu 24.04 x86-64 non-root cp312 component observations (not signed candidate or release acceptance): Hatch 5 tests, Flask 19 tests, Poetry 7 tests with unchanged `-n logical`, and reconstructed Hatch 1 test including pyodbc all executed successfully. Pylint separately completed with exit 0 on the Hatch slice after the member-directory fix. The reconstructed native-manager host baseline now executes successfully. The preceding full local suite passed 2046 tests in 105.67 seconds (two existing lark warnings); later review fixes require a fresh suite.

PR findings have failing-before regressions in `PR_REVIEW_474_ROUND1_RED.txt`, `PR_REVIEW_474_ROUND2_RED.txt`, `SOURCE_LINK_EXCLUSIONS_RED.txt`, and the specific option/evidence transcripts. Transcript machine paths have been replaced with stable placeholders; test assertions and observed outcomes are preserved. Every `*RED.txt` is historical failing-before evidence, not a current acceptance gate. In particular, `NATIVE_RED.txt` does not claim the module remains absent. Native Linux candidate and public-release acceptance remain mandatory and incomplete.

PR follow-up verification: full local suite passed **2066 tests in 101.72 seconds**, with two existing lark warnings. The subsequent focused command/scope/descriptor/source/startup regression set passed **238 tests in 11.36 seconds**; the source-link compatibility subset passed **36 tests**. The second fresh `--bug-hunt` review completed in 43.94 seconds after refactoring the four initially reported blockers. Warning cleanup and external acceptance remain open; development checksum verification does not substitute for release signing.

## Complete signed candidate and subsequent review refinements

Signed candidate `790c39eb47b89046f6525cce9f7345f746896706`, Actions run [34850206113](https://github.com/nold-ai/specfact-cli-modules/actions/runs/34850206113), passed the customer capsule jobs on Python 3.11, 3.12 and 3.13. All four pinned upstream repositories and the reconstructed Hatch/native fixture completed applicable analysis with unchanged source. Initial collected/executed test counts were Requests 24, Hatch 5, Flask 19, Poetry 7, and reconstruction 1 per ABI. Real findings remain visible; acceptance does not require zero findings. This is signed candidate evidence, not public release acceptance.

Subsequent local refinements cover pip-tools/pylock inputs, setup.cfg Python constraints, group/extra ambiguity, complete permission-mode source identity, required member dependency closure, private analyzer source copies, index VCS context, pytest collection/internal errors, inventory argument shape, and native ELF architecture. Failing-before transcripts are `PIP_METADATA_SELECTION_RED.txt`, `MEMBER_CLOSURE_RED.txt`, `ANALYSIS_EXCLUSIONS_RED.txt`, and `PR_REVIEW_474_ROUND3_RED.txt`. Local full verification passed **2,085 tests in 102.51 seconds** with two existing lark warnings; targeted PR regressions passed 53 tests. Type checking and lint passed after the corpus additions. These refinements need a new signed candidate matrix.

The initial warm corpus requested offline reuse without independently denying host networking. `OFFLINE_CORPUS_RED.txt` records the stronger harness requirements and a controlled-report naming regression. The corrected harness passes 11 unit tests and uses administrator-provisioned Bubblewrap solely to deny external networking around warm customer commands. A child probe verifies non-root execution, a distinct network namespace and no active non-loopback interfaces. This test harness does not grant capsule or protected PR authority. `CORPUS_TRANSFER_RED.txt` covers host-interface byte counters, explicitly labelled as host-wide measurements.

Windows/macOS automatic worker handoff and platform-native test support remain outside the accepted Linux x86-64 execution scope. Metadata discovery must not be presented as cross-platform execution acceptance. The original customer reproduction remains required before closing #472.

The stronger network wrapper was exercised on the non-root Linux component host: the child ran as UID 1001 in a distinct network namespace with no active non-loopback interface. The strict member closure also prepared the reconstructed Hatch environment and executed its native-extension test successfully (pytest 9.1.1, pytest-cov 7.1.0). This remains component evidence; the updated customer-interface candidate matrix is still required. Requirements evidence passed at the repository-required `planned` maturity; it does not claim implementation-verification maturity. Narrow review exceptions are enumerated in `REVIEW_EXCEPTIONS.md`.

Final local gate pass before the next candidate push: full tests **2,087 passed in 100.79 seconds**; smart tests **2,087 passed in 101.22 seconds**; contracts **28 passed, 2,059 deselected**. Format, type-check, lint, YAML, bundle imports, development signature/version checks, strict OpenSpec, planned Requirements evidence, and canonical publish pre-check passed. Core compatibility remains intentionally `>=0.55.1,<1.0.0`; the signed candidate's minimum-core checks passed on all three ABIs.

Fresh branch-wide SpecFact review with `--enforcement changed --bug-hunt` completed at 2026-09-14 16:33:01 Europe/Berlin in **49.14 seconds**: **zero errors**, 285 warnings and 69 informational findings. Actionable new mechanical/complexity findings were fixed. Boundary-specific exceptions and unchanged baseline findings are documented in `REVIEW_EXCEPTIONS.md`; coverage limits remain explicit and conditional on Linux acceptance. The preceding review's failing stale-observation mock was fixed at its actual imported launch boundary, and the full suite then passed.

The independent offline wrapper additionally needed a private `/dev`: the Linux user namespace could not open the bind-mounted host `/dev/null`, so GitPython failed before runtime discovery. `OFFLINE_DEVICES_RED.txt` records this failure. Adding `--dev /dev` restored `git --version` inside the non-root offline namespace, and all 11 corpus harness tests passed. Complete warm preparation/attachment remains an updated signed-CI acceptance gate; the local command-level correction alone is not that acceptance.

## PR #474 review rounds 4 and 5 (2026-09-14)

`PR_REVIEW_474_ROUND4_RED.txt` records eight failures before honoring pytest's autoload-disable control, binding directory presence/modes, detecting pylock/requirements.in ambiguity, converting malformed INI to a runtime diagnostic, and protecting the offline launcher's ownership and identity. Focused validation passed 74 tests; the then-current full suite passed 2,101 tests in 103.07 seconds. The Linux UID-1001 offline wrapper executed Git successfully; overwriting the root-owned launcher raised PermissionError.

Candidate `6eda1a0c950a09417dd563371799e08b14cef856`, run [34857677581](https://github.com/nold-ai/specfact-cli-modules/actions/runs/34857677581), failed all corpus ABI jobs. The downloaded cp312 reports identify repository-relative paths being passed to `relative_to(absolute_root)` during private source mapping. `RELATIVE_SELECTION_RED.txt` records one failing and nine passing tests before the mapping fix; afterward all ten passed, including rejection of escaping selections. Quality jobs failed their required capsule prerequisite, not a separate lint/type gate. This failure remains historical evidence; a new signed candidate run is required.

`PR_REVIEW_474_ROUND5_RED.txt` records failures before adding the builder's Git exec path and enforcing selected worker dependency versions. Git's HTTP/HTTPS transport helpers now carry their native library closure, and their bytes enter cache identity. This follows Git's [exec-path contract](https://git-scm.com/docs/git) and [remote-helper contract](https://git-scm.com/docs/gitremote-helpers), accessed 2026-09-14. An actual non-root Ubuntu 24.04 x86-64 builder executed `git ls-remote https://github.com/psf/requests.git HEAD`, exit 0, returning the pinned Requests revision `dae7ef63b4df6eded86637f251fc4e3a06c3b479`. This is transport component evidence, not signed release acceptance. The focused builder, transport and dependency-domain suite passed 24 tests.

An intermediate full suite overlapped the addition of new failing-first tests and a source mutation: 3 failed, 2,104 passed. An earlier accidentally unscoped test invocation omitted the established SSL certificate bundle and also failed. Neither is final passing evidence. The subsequent stable full/smart suites and fresh review are recorded below.

Stable round-5 validation: full suite **2,109 passed in 90.20 seconds**, smart suite **2,109 passed in 101.43 seconds**, and contracts **28 passed**. Both large suites retain two existing lark deprecation warnings. Type/lint (10.00/10), format, YAML, import boundaries, strict OpenSpec, planned Requirements evidence, the generated command overview, 118 module command-contract paths, and three CLI contract scenarios passed. A final transport-identity postcondition was then checked by its focused tests.

Final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` review completed on **2026-09-14 at 17:21:15 Europe/Berlin**, in **41.62 seconds**. The initial review caught the corpus root identity's optional value type; the type refinement and two test assertion warnings were corrected. The final focused harness/pytest/transport set passed **26 tests in 0.54 seconds**. The final review has **zero errors**, with existing/boundary warnings covered by `REVIEW_EXCEPTIONS.md`. Development checksum/version verification and the canonical publish pre-check passed; core compatibility was intentionally retained. Updated signed candidate and public-release acceptance remain required.

## PR #474 review round 6 (2026-09-14)

`PR_REVIEW_474_ROUND6_RED.txt` records seven failures before excluding Git history from analyzer source copies, preserving pytest's repository-root default when `testpaths` is absent/empty, and converting Git identity probe failures into actionable runtime diagnostics. The Git source-copy regression uses a real committed repository and asserts that the analyzer mount has no `.git`, while existing build-copy tests still verify sanitized VCS state and uncommitted source modifications. Parameterized tests cover missing/empty pytest roots and failed/timed-out/unavailable Git probes. The focused set subsequently passed **23 tests**. This follow-up still requires a new signed candidate run; earlier component/candidate receipts are not evidence for the updated artifact.

Signed candidate `dcc30b46fd7477200b55b5f07292963d62012be7`, run [34861791235](https://github.com/nold-ai/specfact-cli-modules/actions/runs/34861791235), failed the updated corpus. Downloaded cp311/cp312 receipts identify two causes: the hosted runner's writable `/opt` ancestor was rejected by the offline launcher, and Hatch's pytest exited 4 while importing its legitimate `hatch.venv` package. The latter was reproduced in the real non-root Linux private-source worker: `ModuleNotFoundError: No module named 'hatch.venv'`. The common-name `venv` exclusion had removed `src/hatch/venv` from both build and analyzer copies.

`HOSTED_RUNNER_ANCESTOR_RED.txt` precedes relocating the administrator-provisioned launcher directly under `/specfact-corpus-offline`, with all ownership, ancestor and digest checks retained. Actual Linux UID-1001 execution passed the relocated wrapper's network probe and Git command; replacement remained denied. `SOURCE_VENV_PACKAGE_RED.txt` precedes recognizing actual virtual environments by `pyvenv.cfg`, retaining ordinary `venv` packages and rejecting aliases into custom-named environments. The source/copy/corpus regression set passed **55 tests**. Source and test selection were not patched in the upstream Hatch checkout.

The corrected private Linux Hatch worker executed all five selected tests successfully with unchanged `--dist worksteal`, pytest 9.1.1, coverage 7.16.1 and pytest-cov 7.1.0. Runtime descriptor identity at that component observation was `603ad4ff2fb113e1f56b9ca355dc6c0bbbf3ad83ad308fa82ab5ca6754dd46fa`. This is component evidence, not signed candidate acceptance. Both local full/smart suites then passed **2,119 tests** (87.95s/88.02s).

`ENVIRONMENT_TEST_SELECTION_RED.txt` additionally records three failures before pruning actual environments from repository-root test candidate traversal. Installed dependency tests no longer create false ambiguity for a customer's root-level test file. The focused worker/source set passed **18 tests**. Fresh preliminary SpecFact review found no errors and one new line-length advisory, which was corrected before final verification.

Final round-6 validation: full suite **2,122 passed in 87.74 seconds**, smart suite **2,122 passed in 87.53 seconds**, contracts **28 passed**. Format, type/lint (10.00/10), YAML, bundle imports, development checksum/version verification, strict OpenSpec and planned Requirements evidence passed. Fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14 18:00:01 Europe/Berlin**, in **41.66 seconds**, with **zero errors**. The narrow advisory exceptions remain documented; no current signed-candidate or public-release acceptance is claimed.

## PR #474 review round 7 (2026-09-14)

`FALLBACK_SANITIZATION_RED.txt` records three failures before routing runtime-preparation fallback through the same sanitized private source copy as successful preparation. If copying fails, all members record incomplete evidence against an empty private root; no member receives the original unsafe source. The focused snapshot suite subsequently passed **13 tests**.

`POETRY_MANAGER_INPUTS_RED.txt` records 25 failures before importing/intersecting Poetry Python constraints and rejecting explicit requirements/constraints that non-pip adapters cannot consume. Native managers no longer implicitly select exported pip requirements. Poetry PEP 440 ranges are supported; caret, tilde, union and table declarations receive an exact unsupported-constraint diagnostic and remedy instead of being silently ignored. This is an explicit remaining compatibility limit. The focused discovery, adapter, interpreter, builder and build-driver suite passed **97 tests**. Poetry permits its locking constraint to be narrower than project metadata ([Poetry 2.4 pyproject contract](https://python-poetry.org/docs/pyproject/#requires-python), accessed 2026-09-14).

`PYTEST_RECURSION_RED.txt` records 12 failures before applying native pytest default/configured `norecursedirs` during corresponding-test discovery. Explicit settings replace defaults, including an empty setting; explicit initial test roots retain native behavior. Native pytest **9.1.1** collection comparisons verified basename, path and override semantics. The focused worker suite passed **34 tests**. These results do not claim plugin-driven collection discovery or cross-platform execution acceptance.

Round-7 repository validation passed: smart suite **2,172 tests in 87.41 seconds**, full suite **2,172 tests in 88.32 seconds**, and **28 contract tests**. Both large suites retained two existing lark deprecation warnings. Format, type/lint (10.00/10), YAML, bundle imports, strict OpenSpec, planned Requirements evidence, development checksum/version validation and canonical publish pre-check passed.

The first fresh bug-hunt found a fallback evidence-map type mismatch and new complexity/naming/lambda advisories. After the type refinement, candidate-selection extraction and parameterized interpreter regression, **116 focused tests** passed and lint/type/format plus checksum/version checks passed again. The final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14 18:32:47 Europe/Berlin**, with **zero errors**, 287 warnings and 69 informational findings. The additional deferred import belongs to the already documented runner initialization boundary; other retained advisories remain covered by `REVIEW_EXCEPTIONS.md`. Independent subagent cross-reviews reported no further actionable defects. Updated signed-candidate and public-release acceptance remain required.

## PR #474 review round 8 (2026-09-14)

`PYTEST_PATH_TYPES_RED.txt` records **42 failures and 70 passes** before validating consumed pytest `pythonpath` and `testpaths` shapes from supported TOML/INI sources. Invalid values now name the configuration and option in a `ProjectRuntimeError`; ordinary review retains the sanitized static fallback. The discovery/snapshot regression suite then passed **112 tests**.

`PYTEST_DESELECTION_RED.txt` records **5 failures and 7 passes** before reconciling the observed inventory with pytest's final selected items. Native `-k`, `--deselect`, and plugin selection no longer create false unexecuted findings; interrupted selected tests remain required. Focused observer/worker tests passed **46 tests**. An independent private environment with pytest **9.1.1**, pytest-xdist **3.8.0**, and pytest-cov **7.1.0** ran the actual `-n 2 -k keep` selection: two selected tests passed, the excluded failing test did not execute, coverage was recorded, and production observation validation passed. Distributed deselection evidence is explicitly controller-observed rather than a claim of complete worker deselection inventories.

`IMMUTABLE_PROJECT_LINKS_RED.txt` records **8 failures and 2 passes** before portable range/index materialization preserved Git symlink blobs. Regular files are written first, then links are reconstructed with original targets and validated before policy/configuration readers run. Escaping or excluded-source aliases fail closed, temporary roots are cleaned on failure, and existing governed-Python symlink restrictions remain enforced. The scope/VCS/builder suite passed **162 tests**.

An additional local CLI fallback probe in the task's emulated Linux container reached the correct malformed-configuration diagnostic, but the supervisor rejected the host Rosetta wrapper before static analyzer execution (`pre_namespace_host_object`). This probe is **not passing analysis or acceptance evidence**. The real Linux x86-64 hosted matrix remains authoritative; no emulation guard was weakened.

`EMPTY_XDIST_INVENTORY_RED.txt` records one failure before retaining the controller-observed label even when distributed collection selects zero items. The final observer/worker suite passed **47 tests**.

Signed candidate `a413f700855e081aa0edec8dea35e3a90be41d7d`, run [34869521909](https://github.com/nold-ai/specfact-cli-modules/actions/runs/34869521909), completed **all three customer corpus ABI jobs**. `CANDIDATE_A413_EVIDENCE.json` retains all **15 passing repository/ABI acceptance records**, exact commands, runtime/worker identities, selected and executed tests, outcome records, versions, timings, host-interface transfer counters and report hashes. All applicable analyzers ran; every offline preparation and explicit warm attachment independently verified denied networking and reused its cold runtime identity. Every controlled copy recorded an actual failing test. The recorded merge checkout `34db09df344d7d76cd764df7c2b05ebdee40fc1c` has parents dev `976460c0` and candidate `a413f700`; the GitHub compare API verified an identical source tree to the candidate. The signed baseline installation is 0.49.85 and source execution is candidate mode, not public 0.50.0 acceptance. These receipts precede the round-8 follow-up code; a new signed matrix remains required.

Round-8 local gates: smart suite **2,235 passed in 89.83 seconds**, full suite **2,235 passed in 90.63 seconds**, and **28 contract tests passed**. Formatting, type/lint (10.00/10), YAML, bundle imports, development checksum/version validation, strict OpenSpec and planned Requirements evidence passed. Final snapshot simplification removed added complexity and length findings; **169 focused tests passed in 11.28 seconds** after that refinement. The final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14 19:10:30 Europe/Berlin**, with **zero errors**, 287 warnings and 69 informational findings. The required observation schema and bootstrap-hook exceptions are explicit in `REVIEW_EXCEPTIONS.md`; unchanged baseline advisories remain distinguished from regressions. Independent cross-reviews found no additional actionable defects.

## Round-9 PR findings and cached invocation cross-review

`STAGED_VCS_TREE_RED.txt` records four failures before binding the staged tree separately from the captured HEAD commit. `CACHED_COMMAND_VCS_RED.txt` records three additional failures through the actual cached changed-enforcement capture path, including a HEAD move whose tree remains unchanged. Both snapshot types now carry the captured commit and tree into cache identity and the private build index. The focused cached/VCS suite passed **67 tests** (352 unrelated tests deselected), including existing unborn-branch cached-analysis behavior.

`OPTIONAL_GIT_RED.txt` records three failures before allowing package-index builds without Git or unused HTTP helpers. Available capabilities remain fingerprinted; an actual missing HTTP-helper invocation reports its installation remedy and exits 127. The focused Git/builder suite passed **26 tests**. `PYTEST_TABLE_RED.txt` records **31 failures** before validating pytest table shapes; the discovery/fallback suite passed **143 tests**, including false and empty-array values and retained independent static analysis.

`INDEX_CONTEXT_CLEANUP_RED.txt` records three failures in the **pre-existing** downstream index-context cleanup gap. Snapshot ownership now covers changed-path, policy, and manifest discovery exceptions. The combined scope/VCS/portable/builder suite passed **188 tests** before the additional cached-command cases. This is narrow hardening requested during review, not a newly introduced regression. Independent review found no defect in the optional-Git and pytest-table changes and caught the separate cached snapshot path before publication. Final combined gates and a fresh signed candidate matrix remain required for this round.

Final round-9 local gates: smart suite **2,278 passed in 91.89 seconds**, full suite **2,278 passed in 93.51 seconds**, and **28 contract tests passed**. Final cached-capture simplification and property contract passed **70 affected tests in 6.34 seconds**; capture complexity remains at its prior value. Fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14 19:40:23 Europe/Berlin** with **zero errors, 287 warnings and 69 informational findings**, after fixing both new warnings. Remaining baseline/boundary dispositions are unchanged in `REVIEW_EXCEPTIONS.md`; local runtime-builder coverage improved to 73.3% but is not represented as meeting 80%. Formatting, typing/lint (10.00/10), YAML, imports, strict OpenSpec, planned Requirements evidence, development integrity/version checks and publish pre-check passed. This evidence does not replace the required updated Linux candidate and public signed-release acceptance.

## Round-10 isolation and metadata review

`BOUND_GIT_CLOSURE_RED.txt` records five failures before exporting only the captured commit/tree/tag object closure. `GIT_GRAFTS_RED.txt` records an unbound graft failure; `GIT_EXPORT_ERRORS_RED.txt` records two unhandled process failures; `FROZEN_GIT_CONTEXT_RED.txt` records three failures before using frozen plan tags/shallow boundaries in both builder paths. Private Git metadata is initialized fresh, native Git plumbing writes only the explicit closure, and its exact object inventory is checked. Replacement refs, grafts, source-local configuration, unbound refs/objects and implicit host transports are excluded. Selected ancestry, staged objects, annotated/lightweight tags and shallow boundaries remain available. The stable VCS/builder/portable subset passed **63 tests in 4.34 seconds**. A separate intermediate combined run encountered the concurrent policy tests while they were still RED; it is not passing evidence.

`ANALYZER_ORIGIN_RED.txt` records **seven failures** before pinning analyzer-owned imports ahead of ordinary lookup and target site initialization. Real isolated Python subprocesses cover source collisions, `.pth` imports, preexisting wrong-origin modules, and project-owned import behavior. Module, namespace, symlink and missing origins are checked against the resolved member mount. Bootstrap/launcher tests passed **24 tests**. This governs import origin; it does not claim to sandbox arbitrary malicious Python startup code inside one interpreter.

`CONSUMED_METADATA_TABLES_RED.txt` records **90 failures** before validating only the pyproject/Hatch tables consumed by discovery, including explicit manager selection. The discovery/fallback suite passed **233 tests**. `UNCHANGED_POLICY_MANIFEST_RED.txt` records **10 failures** for the pre-existing policy-manifest omission. Both manifests now retain the base/head policy closure without widening selected paths or changing clean-index NOT_APPLICABLE semantics; **151 scope tests passed**. Independent review found no actionable metadata or bootstrap defects. Final combined gates, signed candidate acceptance and public release acceptance remain required.

Independent VCS review additionally verified SHA-1 and SHA-256 closed exports and reproduced a missing-origin race after input verification. `BOUND_GIT_ORIGIN_RED.txt` records two failures before source copying and frozen Git export were separated: a nonempty bound origin now must remain available before acquisition. The final VCS/builder/portable subset passed **67 tests in 4.57 seconds**, and an independent eight-test regression pass found no additional issue. The first combined smart/full invocation was interrupted for this confirmed fix and is not final passing evidence.

Final round-10 functional gates: smart suite **2,403 passed in 97.75 seconds**, full suite **2,403 passed in 99.47 seconds**, and **28 contract tests passed**. The final scope/bootstrap refactors passed **175 affected tests in 12.14 seconds**; the private Git fixture refactor passed **27 tests in 6.01 seconds**. Formatting, typing/lint (10.00/10), YAML, imports, strict OpenSpec, planned Requirements evidence, development checksum/version verification and publish pre-check passed. The scope refactor reduces `_materialized_index_context_from_capture` to 65 lines; its low-complexity informational advisory reflects that reduction, not a new control-flow regression. The startup `validate_loaded` contract exception is explicit in `REVIEW_EXCEPTIONS.md`. Local parent coverage for bootstrap remains 61.7%, not evidence of complete child-process coverage.

The preceding signed round-9 candidate `29c167bce4cdd1f35eac9ede0aceddaa620b63bd` completed every required check in [run 34876434736](https://github.com/nold-ai/specfact-cli-modules/actions/runs/34876434736). Independent receipt validation passed all 15 repository/ABI combinations, including 168 host tests, 168 cold and 168 warm capsule test calls, and 15 controlled failing tests. Tested merge `6155d90e2ceac5d2b3fb7add0e2a4ad3795a948a` has the expected dev/candidate parents and the same tree as that candidate. Recorded offline probes, source-unchanged checks and runtime reuse passed. These historical receipts execute candidate source against signed baseline 0.49.85; they do not establish round-10 or public 0.50.0 acceptance.

Fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14T18:26:40.508764Z** (UTC), with **zero errors, 287 warnings and 68 informational findings**. New fixture warnings were removed; retained baseline/boundary dispositions remain explicit. Updated signed-candidate and public signed-installation acceptance remain required.

## Round-11 review and native pytest identity hardening

The narrow round-10 hook emitted one targeted-pytest reconciliation error despite **1,417 passing tests and process exit 0**. Retained observer/JUnit evidence identified one pre-existing parser defect: `::` inside a parameter ID was treated as a structural class/name separator. `JUNIT_PARAMETER_IDENTITIES_RED.txt` records a real six-case function/class fixture whose tests pass while the old adapter returns UNKNOWN. The corrected adapter partitions the parameter suffix before structural parsing; **226 focused pytest/JUnit/TDD/coverage tests passed in 8.84 seconds**, and the original 1,417-case evidence reconciles PASS unchanged. The hook's initial passing exit alone is not represented as complete evidence.

`PYTHON_VERSION_TRIGGER_RED.txt` records two failures before `.python-version` alone triggered ordinary portable preparation. Actual runner-path tests now cover supported-pin preparation, unsupported-pin diagnostics retaining static members, and unchanged metadata-free source behavior; **23 tests passed**.

`PYTEST_QUOTED_PATHS_RED.txt` records six failures before importing quoted pytest paths/patterns with native argument boundaries. The fix follows the installed pytest **9.1.1** `Config._getini_ini` paths/args contract: `shlex.split` for string values, unchanged list boundaries. Real pytest configuration parsing confirms full/source-only selection and source-root behavior; malformed quotes name the file and option. The discovery/snapshot/worker suite passed **276 tests in 0.69 seconds**. Independent review found no introduced defect. Final combined gates and updated signed acceptance remain required.

`PTH_IMPORT_STATE_RED.txt` records **seven failures** before checking protected import state around executable `.pth` initialization. Member lookup is restored to first precedence after legitimate additive hooks; removed/mutated protected machinery and paths outside approved roots fail explicitly. Real subprocess regressions retain executable editable finders, namespace path hooks and rebased path-only workspace imports; **34 bootstrap/launch tests passed**. Project-Python preserves native startup. These checks cover startup integrity and are not a sandbox for arbitrary malicious Python inside one interpreter.

Round-11 combined gates passed **2,423 smart tests in 121.07 seconds** and **2,423 full tests in 123.54 seconds**, with the two existing lark deprecation warnings. Formatting, typing/lint (10.00/10), YAML, bundle imports, strict OpenSpec and planned Requirements evidence passed. The fresh combined review at 2026-09-14T18:56:23Z reported **zero errors, 287 warnings and 68 informational findings**, with no new advisory regressions.

The completed CodeRabbit round then identified an additional consumed manager signal. `UV_TABLE_RED.txt` records **eight failures** before validating `tool.uv` as a table, including explicit manager selection. Adding that table to the existing validator passed **244 discovery/snapshot tests in 1.55 seconds**. These focused tests follow the full-suite run above. Diagnostic regex assertions now escape complete expected messages literally. Host-specific temporary prefixes in the three requested historical transcripts and four round-11 transcripts were replaced with stable placeholders; failure/result lines and evidence content remain intact. Final signed-candidate acceptance remains required.

Final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` at **2026-09-14T18:58:37Z** completed with **zero errors, 287 warnings and 68 informational findings**. The existing baseline/startup dispositions remain unchanged. Final format, typing/lint, strict OpenSpec, development checksum/version validation and canonical publish pre-check passed after the uv-table addition. Public review completion on signed `6b733865` covered the previous round; this follow-up requires its own signed CI and completed review round.

## Round-12 native ownership correction

Signed candidate `6b733865cbefdcfb7633b7390779c3b0eb8f969e`, [run 34881298236](https://github.com/nold-ai/specfact-cli-modules/actions/runs/34881298236), failed the Poetry corpus entry on all three ABIs. Runtime preparation and all seven Poetry tests passed, but CrossHair contracts analysis reported `project_worker_analyzer_origin_mismatch:build` and remained UNKNOWN. `NATIVE_6B733_FAILURE.json` preserves the tested merge, exact commands, worker identities and report hashes. Quality jobs failed the capsule prerequisite; this is a genuine native acceptance failure, not cancellation.

The cause is our import-ownership mapping: installed CrossHair **0.0.109** metadata declares `build` and `wheelhouse` despite not recording corresponding installed payload. Its separately declared `doc` namespace does own the present `doc/source/conf.py` and remains admitted. Trusting these declarations seals Poetry's separate `build` import incorrectly. The pending correction derives analyzer-owned names from the distribution's recorded, present import payload and retains strict origin checks.

A separate synthetic review confirmed that mixed analyzer/project portions of one namespace are not currently composed. No such graph was identified in the signed baseline member closures. This remains an explicit support limitation, with origin mismatch and incomplete evidence; it is not misclassified as a version conflict and is not resolved by the stale-metadata fix.

`ANALYZER_IMPORT_OWNERSHIP_RED.txt` records **24 failures and five passes** before deriving names from present distribution-recorded Python and native-extension files. Tests cover missing/stale declarations, ordinary modules/packages/namespaces, native suffixes independent of the controller ABI, absent payload and another distribution's `build` directory. Missing file inventory fails explicitly. The corrected suite passed **29 tests in 0.84 seconds**; the combined domain/bootstrap/builder suite passed **77 tests in 0.77 seconds**. Direct inspection of installed CrossHair 0.0.109 confirms `build` and `wheelhouse` are excluded, while actual `_crosshair_tracers`, `crosshair` and `doc` payload remains admitted. Strict analyzer-origin checks are unchanged. This is local component evidence, not a successful native Poetry/capsule run.

Independent reuse checks confirmed that automatic cache keys include `runtime_domains.py`, and normal explicit attachment binds the composite worker identity containing the verified module payload and final capsule root. Both stale automatic preparation and old explicit descriptors therefore invalidate across the packaged fix; no new descriptor field was required. The earlier round-11 local commit `10783c75` passed canonical hooks with zero review errors and **28 contract tests**; it was held from push when the native failure arrived.

Round-12 combined gates passed **2,455 smart tests in 112.47 seconds** and **2,455 full tests in 119.60 seconds**, with the two existing lark warnings. Formatting, typing/lint (10.00/10), YAML, bundle imports, strict OpenSpec and planned Requirements evidence passed. The corrected import-ownership implementation still requires a fresh signed Linux corpus; the previous Poetry failure is not relabelled as acceptance.

Final ownership type-boundary correction passed **77 affected tests in 1.86 seconds**. Fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14T19:21:49Z** with **zero errors, 287 warnings and 68 informational findings**. The stricter metadata path type finding and redundant test-name advisory were removed; retained baseline/startup dispositions are unchanged. This local review does not supersede the outstanding native Poetry acceptance requirement.

## Round-13 range symmetry and parsed path containment

`PYTEST_PATH_CONTAINMENT_RED.txt` records **12 failures and four passes** before containment checked the same shell-parsed pytest `testpaths` and `pythonpath` values consumed during execution. Single/double quoted parent traversal, absolute paths and escaping links now fail before preparation. Explicit review source roots no longer bypass validation of pytest’s own Python paths. Valid spaced INI values and native lists retain their recorded representation. The discovery suite passed **237 tests in 1.30 seconds**; combined discovery/worker tests passed **277 tests in 0.66 seconds**.

`PORTABLE_RANGE_SELECTION_RED.txt` records **six failures and two passes** before range-wide selection determined whether both snapshots expand for a metadata-only change. The regression uses actual snapshot copying, dispatch/applicability, range classification and report construction; only runtime artifact preparation/lookup and analyzer execution are substituted. Deleted and added Python files now preserve the absent side, empty selections stay empty, and metadata-only selections retain symmetric analysis. Both prepared execution and missing-metadata preparation failure are covered. The snapshot/worker suite passed **71 tests in 0.35 seconds**. Independent review passed another 16 containment and nine range regressions and found no additional actionable defect.

Round-13 combined gates passed **2,479 smart tests in 98.28 seconds** and **2,479 full tests in 99.90 seconds**, retaining two existing lark deprecation warnings. Formatting, typing/lint (10.00/10), YAML, bundle imports, strict OpenSpec, planned Requirements evidence, development checksum/version checks and canonical publish pre-check passed. The range fixture’s empty-tuple typing was corrected before these final runs. Both new RED transcripts use stable workspace/temporary placeholders without changing failure evidence.

CodeRabbit review 5202051605 on signed `6c3738ef` raised an outside-diff exception-handling claim. Independent review confirmed it is already covered: `ProjectRuntimeError` derives from `ValueError`, caught by both preparation boundaries; existing preparation-failure regressions retain static findings and UNKNOWN dependent evidence. No redundant exception clause was added. Updated signed-candidate and public-release acceptance remain required for this follow-up.

Final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14T19:48:13Z**, with **zero errors, 287 warnings and 68 informational findings**. No new advisory category or source defect was introduced; the documented baseline/startup and local parent-coverage dispositions remain in force, conditional on native acceptance. This report is local evidence and does not replace the required signed Linux matrix.

## Round-14 native import precedence and unborn repositories

`NATIVE_IMPORT_PRECEDENCE_RED.txt` records **four failures and 14 passes** before discovery stopped inventing runtime import roots. A real wheel installed through offline pip contains generated package output distinct from the raw `src` tree: native pytest selected the installed output, while the prior capsule bootstrap selected raw source under both prepend and importlib modes. Missing path declarations now produce empty import overrides; explicit review roots, pytest paths and editable hooks retain their behavior. Discovery/bootstrap/worker tests passed **324 cases in 5.20 seconds**, including six native Python current-directory/script cases and flat source-only pytest. No analyzer file-selection or bootstrap production logic changed.

`UNBORN_VCS_CONTEXT_RED.txt` records **three failures and 11 passes** before distinguishing an unborn selected branch from invalid Git state. Fresh, staged and orphan repositories now support discovery, CLI inspection and private copying without VCS context; a first commit invalidates the old binding. Default HEAD absence must be independently confirmed through a symbolic branch and exact missing-ref result. Explicit missing revisions/trees, malformed or detached HEAD, corrupt refs/configuration and execution failures retain errors. The hardened Git subprocess environment and bounded export logic remain unchanged. VCS/builder/source-binding tests passed **63 cases in 6.25 seconds**. An intermediate combined run encountered the other agent’s then-failing import-path regressions and is not final passing evidence.

Independent review of both patches passed **16 targeted regressions in 1.85 seconds** and found no additional actionable defect. Combined repository gates and updated signed native acceptance remain required. Registry publication and manifest signing remain owned by the existing automatic GitHub CI workflows, as the user reconfirmed.

Round-14 combined gates passed **2,512 smart tests in 118.94 seconds** and **2,512 full tests in 122.07 seconds**, retaining two existing lark deprecation warnings. Formatting, typing/lint (10.00/10), YAML, bundle imports, strict OpenSpec and planned Requirements evidence passed. The native-import transcript retains failure outcomes with stable workspace, temporary-directory and test-environment placeholders. Updated signed native acceptance remains required.

The first fresh round-14 bug-hunt reported five new test-code advisories: fixture branch nesting, a generic-name substring, two empty-sequence comparisons and a long wheel-metadata literal. These were corrected without changing production behavior or assertions. The final affected discovery/VCS/bootstrap/worker suite passed **365 tests in 12.71 seconds**; formatting and typing/lint passed again. Native acceptance remains tied to a new signed candidate, not the still-running prior source revision.

Final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14T20:18:09Z**, with **zero errors, 287 warnings and 68 informational findings**. All five new advisories were removed; retained baseline/startup and local coverage dispositions are unchanged. Development checksum/version verification and canonical publish pre-check passed. Automatic CI signing and updated native acceptance follow the push.

## Round-15 dispatch integrity and sectionless native inventory

`STARTUP_DISPATCH_RED.txt` records **12 failures and two passes** before checking the runpy namespace, function code and code-loading primitives used by analyzer dispatch. `STARTUP_EXIT_RED.txt` records **two failures** before startup rejection used reserved exit 78. Captured entry functions and built-in observer paths are preserved across target hooks; ordinary additive editable hooks remain supported. The bootstrap/domain/selection subset passed **99 tests in 6.24 seconds**. This is startup integrity, not a general sandbox against arbitrary code in the same interpreter, consistent with the [Python 3.13 runpy contract](https://docs.python.org/3.13/library/runpy.html), accessed 2026-09-14.

The runner audit reproduced a related false-clean result: a rejected hook wrote valid `[]` JSON, and pylint parsed it despite startup failure. `PYLINT_STARTUP_RED.txt` records **one failing regression** before rejecting reserved exit 78 ahead of JSON interpretation. The resulting tool error preserves the startup diagnostic instead of claiming completed analysis.

`SECTIONLESS_ELF_RED.txt` records **three failures and 16 passes** before program-header dependency discovery for valid sectionless ELF64 x86-64 objects. The bounded fallback reads PT_DYNAMIC/DT_NEEDED through a file-backed PT_LOAD string table, requires termination and rejects malformed ranges. The existing section route is retained. Native/builder/source/model tests passed **45 cases**; a synthetic fixture's terminator mutation offset was corrected from 192 to 193 during validation. The [ELF gABI 4.2 dynamic section](https://gabi.xinuos.com/v42/elf/08-dynamic.html) and [program headers](https://gabi.xinuos.com/v42/elf/07-pheader.html) were checked on 2026-09-14. Synthetic byte parsing on macOS is not Linux execution evidence; the final signed native matrix remains required.

The first combined smart run passed 2,533 tests but failed 15 Semgrep fixtures because the local command omitted the configured CA bundle (`ca-certs: empty trust anchors`). Repeating with the existing certifi `SSL_CERT_FILE` passed **2,548 smart tests in 126.94 seconds**. This intermediate pass precedes the final startup-origin/loader follow-ups. No production certificate behavior was changed.

CodeRabbit review 5202637906 correctly identified stale paired-core command documentation at core dev `45776bf0ee64e0a9cef07ee5d3c324114d8ac44f`. The existing accountability checker checks package ownership and command roots; its success is not full subcommand/option parity evidence. Task 4.7 retains the exact-revision documentation and registration follow-up. The separate Requirements fixture's approved commit/tree must remain intact; public module signing and publication remain the canonical GitHub CI workflow's responsibility.

The full intermediate suite passed **2,548 tests in 122.29 seconds**, with the same two existing lark deprecations. Independent ELF review found no actionable defect; 666 additional synthetic truncation/extreme-field probes produced no unexpected exceptions. Startup review then confirmed two residual paths: preloaded protected imports raised ImportError with an ordinary findings exit, and loader filename methods could redirect dispatch. `STARTUP_LOADER_RED.txt` records **three failures** before mapping origin errors to startup exit 78 and fingerprinting source/sourceless loader class namespaces through their inheritance chain. The final bootstrap/domain/selection subset passed **102 tests in 6.98 seconds**. These bounded checks do not claim to authenticate arbitrary hook output or sandbox project code sharing the interpreter. Final combined lint/review and the signed native matrix remain required.

The first fresh round-15 bug-hunt at **2026-09-14T20:49:25Z** reported zero errors and seven additional test-code advisories, including previously untouched pylint-test duplication newly included in this review surface. The tests now parameterize the same four field-normalization cases, use precise names and empty-sequence assertions, and split long hook literals. All assertions and actual hook behavior are preserved. The combined bootstrap/native/pylint subset passed **103 tests** after this cleanup. Parent-process bootstrap coverage is 50.8%; real child-process regressions are reported separately, and this local metric does not establish Linux acceptance.

Final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14T20:54:22.923830Z**, with **zero errors, 287 warnings and 68 informational findings**. All new advisories were removed; retained baseline/startup and local parent-coverage dispositions remain explicit. The normalized pylint tests passed all **15 cases** after reducing helper arguments. Formatting, typing/lint (10.00/10), YAML, imports, strict OpenSpec, planned Requirements evidence, development checksum/version verification and canonical publish pre-check passed. Automatic CI signing, updated native acceptance and core #728 remain pending after the push.

## Round-16 native full-review discovery

Codex finding 4009588049 identified that full review converted configured pytest roots into explicit selectors. `PYTEST_NATIVE_DISCOVERY_RED.txt` records **12 failures and 39 passes** before delegating full-review discovery to pytest. Real pytest **9.1.1**, the actual target Observer and pytest-cov reproduce missing/partially missing paths, glob expansion and positional `addopts`; supplied positional selectors changed native collection or exited with usage error. The new full-only empty selector list preserves pytest discovery, warnings and fallback. Targeted explicit, source-only and mixed selection remains unchanged, and required collection/execution/coverage evidence still rejects empty runs while retaining genuine failures. The affected worker/snapshot/pytest suite passed **95 tests in 6.90 seconds**. This is local subprocess evidence, not Linux capsule acceptance.

The [pytest testpaths reference](https://docs.pytest.org/en/stable/reference/reference.html#confval-testpaths), accessed 2026-09-14, confirms that configured discovery applies when no command-line directories, files or test identifiers are supplied. Full signed-candidate and public-installation acceptance remain pending for the resulting revision.

Round-16 independent review found no additional actionable defect and passed **95 affected tests in 4.82 seconds**. Combined gates passed **2,562 smart tests in 155.88 seconds** and **2,562 full tests in 153.68 seconds**, retaining two existing lark deprecation warnings. Formatting, typing/lint (10.00/10), YAML, bundle imports, strict OpenSpec, planned Requirements evidence, development checksum/version verification and canonical publish pre-check passed. Fresh branch review and automatic CI signing follow these gates; native acceptance remains bound to the resulting signed revision.

Final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14T21:23:29.597335Z**, with **zero errors, 287 warnings and 68 informational findings**. No new advisories were introduced; all reported coverage percentages match the preceding review. Existing documented baseline/startup and local parent-coverage dispositions remain in force. Automatic CI signing and the resulting signed native corpus remain required after push.

## Round-17 setup failures retain incomplete execution

`PYTEST_SETUP_EXECUTION_RED.txt` records **one failure and four passes** before mixed setup failures stopped counting as completed test-body execution. The regression runs real pytest 9.1.1 with pytest-cov and the target Observer, then passes its retained observations through `run_portable_pytest` and the capsule member-response mapper. A setup failure alongside a healthy test now retains its genuine failed-phase finding and produces UNKNOWN/error evidence naming the unexecuted test and fixture-setup remedy. A real failed call, or teardown failure after an observed call, remains FAIL with completed call evidence; intentional native setup skips and xfails retain their existing policy. The affected discovery/worker/snapshot/target-pytest suites passed **100 tests in 4.19 seconds**, and Ruff checks and formatting passed. Combined repository gates and the final signed Linux acceptance matrix remain required.

Independent review found no additional defect and separately passed all **five real-pytest phase regressions in 1.14 seconds**. Combined smart and full suites each passed **2,567 tests**, retaining two existing lark deprecation warnings. Smart duration was **126.10 seconds**; full duration was **121.24 seconds**. Formatting, typing/lint (10.00/10), YAML, bundle imports, strict OpenSpec, planned Requirements evidence, development checksum/version verification against dev and canonical publish pre-check passed. The unpublished reserved minor version remains 0.50.0; GitHub CI owns signatures and registry publication.

The first round-17 fresh bug-hunt reported a new complexity blocker (CC 21) in the phase-regression test. A test-only refactor separates incomplete-setup and member-response assertions; the test now has CC 7 and its new assertion helpers have CC 7 and CC 8. Actual subprocess observations and all outcome assertions remain intact, with the failed-phase assertion strengthened to one exact test identity and message. The final affected suite passed **100 tests in 4.49 seconds**; Ruff checks/formatting and targeted pylint (10.00/10) passed. Production behavior did not change during this cleanup. A fresh staged bug-hunt remains required.

Final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14T21:51:39.623486Z**, with **zero errors, 287 warnings and 68 informational findings**. The new test-complexity finding was removed; no new advisory remains. The documented baseline/startup and local parent-coverage dispositions remain explicit. Automatic CI signing and final native acceptance follow the validated push.

### 2026-09-15: Pylint namespace source-root fidelity

The cp312 signed-candidate Poetry receipt retained six E0401/E0611 findings at `tests/utils/test_extras.py:7,9,10`. Its runtime inventory contains poetry-core and an editable `poetry.pth` bound to `/opt/specfact/snapshot/src`; Poetry and poetry-core are both project-owned. A private probe using exact pinned Poetry source demonstrated that Pylint instead cached `poetry` as `src/poetry/poetry.py`. Native Python resolved the namespace correctly. This is distinct from the mixed analyzer/project namespace limitation recorded above.

Specification preceded regression tests and production edits. `PYLINT_NAMESPACE_ROOTS_RED.txt` records **4 failed, 5 passed** on the unchanged implementation. Three real-Pylint namespace cases (editable, explicitly declared, overlapping snapshot/editable roots) produced the spurious import findings; the missing-import case additionally retained those false positives. Native repository/CLI precedence and no-root controls already passed.

The new target-only Pylint dispatcher derives defaults exclusively from already validated snapshot entries in the attached worker's `sys.path`. Deeper roots precede ancestors because Pylint chooses the first matching root. It creates an ordinary PyLinter and sets the fallback before native Run configuration and CLI parsing; explicit empty/native source-roots values therefore remain authoritative. It does not insert import paths, overlay guessed raw source on built packages, suppress import findings, or change the supervisor environment. The startup dispatch fixture now contains the new trusted dispatcher stub, retaining all existing tampering assertions.

GREEN command: `SPECFACT_CLI_REPO=/private/tmp/specfact-473-core hatch run python -m pytest -q tests/unit/specfact_code_review/run/test_target_pylint.py tests/unit/specfact_code_review/run/test_target_bootstrap.py tests/unit/specfact_code_review/tools/test_pylint_runner.py`: **91 passed in 7.32 s**. The added parallel control runs real Pylint `--jobs=2`; its synthetic runtime lacks the built interpreter launcher, so the harness explicitly supplies the host multiprocessing executable. This proves ordinary PyLinter serialization and roots preservation, not the native capsule subprocess launcher. Formatting/Ruff and Pylint checks passed; focused typing reported zero errors/warnings.

Host proof uses macOS Python 3.14.7, Pylint 4.0.8, Astroid 4.0.4 and pytest 9.1.1. The correction still requires the Linux candidate/public-installation corpus; local proof does not confer release acceptance. Pylint's documented implicit-namespace contract requires source-roots to avoid incorrect package names: https://pylint.readthedocs.io/en/stable/user_guide/usage/run.html#with-implicit-namespace-packages (Pylint 4.0.8 documentation, accessed 2026-09-15).

The corpus guard followed a separate specification → tests → RED → implementation loop. `PYLINT_CORPUS_IMPORT_GUARD_RED.txt` records **12 failed, 3 passed** before the guard: all cold/warm E0401/E0611 combinations at the three verified Poetry import lines were silently accepted. The manifest now records those module/location pairs, and both cold and warm report acceptance check them. Three controls preserve other files, other lines and unrelated diagnostic classes; no report findings are removed.

Combined dispatcher, bootstrap, Pylint parser and corpus guard suite: **125 passed in 7.05 s** (`tests/unit/test_external_capsule_corpus.py` plus the three focused files above). Bundle import boundaries, repository YAML validation, strict OpenSpec and changed-file Ruff/format checks passed. Targeted BasedPyright reported **0 errors, 0 warnings**. Production files plus the new worker tests scored **10.00/10** in Pylint. Expanding local Pylint to the two existing test modules reported five pre-existing protected-access warnings on unchanged lines (bootstrap line 294; corpus lines 195, 207, 211, 229); no new warning was introduced. New dispatcher complexity is at most 7. Independent review of the dispatcher found no production findings and independently passed 83 bootstrap/launch/worker tests.


Round-18 combined validation passed: smart and full suites each ran **2,592 tests**, with only the two existing lark deprecations. Durations were **107.31 seconds** and **108.90 seconds**, respectively. Independent corpus-guard review passed 34 tests and applied the stronger guard to both retained cp312 Poetry reports: each correctly rejected the six known import-resolution errors, while in-memory controls retaining eleven unrelated findings were accepted. The guard fails the corpus before writing acceptance and does not suppress report findings. The unpublished bundle remains 0.50.0 (registry baseline 0.49.85); its checksum was updated through canonical tooling, with signatures still owned by GitHub CI. Fresh review and new signed Linux corpus acceptance remain required.

### Pylint dispatcher cache identity follow-up

The planned Requirements adapter maps `scenario_id` to the canonical source requirement slug rather than individual scenario headings. The six namespace cases now use `pylint-preserves-attached-namespace-source-roots`; their granular behavior remains explicit in the intents and observables.

A separate spec-first cache regression showed that changing only `target_pylint.py` left the existing cache directory reusable with otherwise fixed project, environment, Git and worker identities. `PYLINT_DISPATCH_CACHE_RED.txt` records the genuine failure: offline preparation did not raise the required cache-miss diagnostic after the dispatcher bytes changed. The test uses private copies of actual helper sources and the interpreter lock, exercises real cache-key calculation and selection, and stubs artifact loading only; it acquires no dependencies. The dispatcher now participates in the existing builder digest alongside the other trusted target helpers. An unchanged repeated call reuses the same cache; changed dispatcher bytes produce a distinct identity and offline miss.

The previously reported smart and full suites each passed **2,592 tests before this cache-key addition**. Those prior results do not cover the new cache regression or final mapping correction; the final combined gates are coordinated separately.

Cache follow-up GREEN: **146 passed in 7.99 s** across runtime-builder, target-Pylint, target-bootstrap, Pylint-parser and corpus tests. The canonical staged planned Requirements adapter passed **1 source, 0 failed, 0 skipped**. Ruff/format, strict OpenSpec, diff checks and targeted BasedPyright (**0 errors, 0 warnings**) passed; the new test has complexity 6. Local Pylint retained only two existing protected-access warnings on unchanged runtime-builder line 144 and test line 41. Final signing, fresh review, complete suites and Linux candidate replay remain root integration gates.

Final cache follow-up validation: the complete suite passed **2,593 tests in 107.11 seconds**, with only the two existing lark deprecations. Configured lint passed (1,326 files already formatted; BasedPyright zero errors/warnings/notes; Ruff passed; Pylint 10.00/10). Repository YAML and bundle import-boundary checks passed, and all **28 contract tests** passed. The normal development signature/checksum and version-bump gate verified all seven manifests; publish preconditions passed with the existing `>=0.55.1,<1.0.0` compatibility intentionally retained. The current public registry remains 0.49.85; the unsigned 0.50.0 checksum was refreshed after the cache change, with publisher signatures and registry publication delegated to GitHub CI. Independent read-only review of the complete staged production patch found no defects.

The final smart suite also passed **2,593 tests in 111.10 seconds**, with the same two existing lark deprecations.

Fresh branch-wide SpecFact `--enforcement changed --bug-hunt` completed at **2026-09-14T23:07:40.175982Z**, exit 0, with **zero errors, 288 warnings and 69 info**. Relative to the preceding review, existing warnings are unchanged apart from source-line shifts and parent-process coverage: runtime_builder improves from 73.3% to 74.5%; target_bootstrap changes from 50.8% to 49.2% as its new dispatch branch executes in isolated subprocesses. Explicit narrow coverage exception: new `target_pylint.py:1` reports 0% in the parent coverage collector, while all ten real-Pylint subprocess controls execute it (editable/declared/overlapping roots, repository and CLI precedence including empty roots, genuine missing imports, built-root exclusion and parallel jobs). This measurement must not be represented as absent test execution or as native Linux acceptance; the isolated worker deliberately does not inherit host coverage injection. The existing startup/parent-coverage dispositions continue to apply. The one new informational length advisory is `test_runtime_builder.py:201`: retain its ordered unchanged/cache-reuse/changed-helper phases and offline assertions together, since extracting single-use setup would hide the causal cache regression without simplifying production. Independent review found no defect in that test or dispatcher. The new Linux signed-candidate corpus remains required.

### PR #475: native pip package entrypoints

After the specification was extended, `SETUP_CFG_INSTALL_RED.txt` recorded **5 failed, 3 passed** before changing the adapter. A tool-only `setup.cfg` previously appended an unbuildable `.` requirement; directory-shaped metadata markers were also wrongly admitted. The adapter now follows pip 26.2.1's native installable-directory predicate: a regular `pyproject.toml` or `setup.py` file permits root installation, while `setup.cfg` alone does not. Tool-only pyproject files remain eligible because native pip delegates their build metadata decisions. Requirements, constraints, extras and configuration discovery remain unchanged.

GREEN: **257 adapter/discovery tests passed in 1.33 seconds**, including an actual offline pip dry run against configuration-only requirements and immutable source assertions. Independent replay passed the same 257 tests in 0.68 seconds and found no defects. Formatting, Ruff, diff and actual AI-bloat checks passed. Native pip source and the official local-project installation contract support this distinction: https://pip.pypa.io/en/stable/cli/pip_install/ (pip 26.2.1, accessed 2026-09-15). Pushed validation and resolution of comment 4010452407 remain pending combined review fixes.

### PR #475: targeted pytest file and glob paths

The canonical scenario preceded `TARGETED_TESTPATHS_RED.txt`, which records **9 failed, 3 passed**: eight real native-pytest selection variants and an escaping expanded symlink failed before the correction. Targeted discovery now follows pytest 9.1.1's recursive glob expansion, admits individual file roots, and falls back to the repository only when all configured expansions are empty. Matched roots retain containment and excluded-environment ancestry. Explicit source/test mixtures, filename patterns, ambiguity checks and full-review native collection remain unchanged.

GREEN: **68 tests passed in 5.87 seconds**, including actual native collection, test-body calls and coverage in all eight source-only variants. Independent replay passed **68 tests in 5.90 seconds** with no findings. Formatting, Ruff, targeted typing (zero errors/warnings), and diff checks passed; new helper complexity is at most four. The existing candidate walker complexity and an unchanged protected-access test warning remain documented baseline. Comment 4010452417 remains open until the combined validated update is pushed.

### PR #475: controller log and artifact trust boundaries

The security review identified controller reopening of a builder-owned staging log. After the canonical scenario, `BUILD_LOG_RED.txt` recorded **10 failures** before remediation, using disposable sentinels only. The controller now creates an exclusive private regular log outside builder-writable mounts before execution, directs subprocess stdout/stderr to its existing descriptor, and never reopens or copies the former staging log. Native manager output and timeout partial output flow through the descriptor; public errors do not contain captured output. Launch and capture-file-creation failure controls were added. The twelve focused log tests passed; independent replay passed **12 tests in 0.96 seconds** and found no security defect. Full preparation cleanup is being checked separately before finalization.

An independent adjacent audit demonstrated another pre-sealing escape using only temporary simulated-host files: a builder-created `artifact/native` symlink redirected native-library copies and permission changes before the final manifest rejected it. The canonical scenario preceded `BUILD_ARTIFACT_BOUNDARY_RED.txt` (**7 failed, 1 passed**). A metadata-only topology validator now runs immediately after builder exit, inside safe failure-log handling, before inventory reads or native enrichment. It rejects root/nested symlinks and special nodes without reading payload bytes; final sealing remains unchanged. The new boundary plus existing artifact tests passed **17 tests in 0.23 seconds**, with typing and formatting clean. A separate reviewer found no defects and confirmed actual native-copy sentinel preservation, ordinary sealing/offline reuse, and early rejection of root, inventory, native-directory and nested links and FIFO nodes.

The combined logging, builder, driver, VCS, artifact-boundary and artifact scope passed **100 tests in 6.18 seconds** before the final downstream-failure log-cleanup follow-up. These host tests prove descriptor and path handling with safe local subprocesses; they do not substitute for a fresh Linux namespace corpus. Security comment 4010477055 remains open until the combined validated signed update is pushed.

Root review caught premature success-log deletion before downstream preparation finished. `BUILD_LOG_DOWNSTREAM_RED.txt` records **four actual failures** for native inventory, sealing, publication and final descriptor validation. The trusted log now remains available through all downstream processing, descriptor validation and staging cleanup; only a fully successful preparation removes it. Failures preserve the original actionable diagnostic and append the private log path. The final focused integration passed **104 tests in 6.39 seconds**; formatting, Ruff and diff checks passed.

### Independently verified namespace correction in native candidate e729fa3

Run [34907939755](https://github.com/nold-ai/specfact-cli-modules/actions/runs/34907939755) passed all three native ABI jobs for `e729fa3adec76b5ed152d140eae28842ff5a1b71`. Independent artifact checks confirmed tested merge `055eb4481dc53f4723564cd6022d48e89397caad` has the expected dev/candidate parents and identical tree `6af17c726016aa525d236c5ebd8d04bad392906b`. Provider ZIP digests matched for artifacts 10373772032, 10374191496 and 10374022590. Across all fifteen combinations, the receipts contain **168 host calls, 168 cold capsule calls, 168 warm capsule calls and 15 genuinely failing controlled-defect calls**. Applicable analyzers ran without UNKNOWN evidence; offline probes and runtime reuse identities matched.

Each of the six Poetry cold/warm reports across all three ABIs now has **zero** of the six previous Pylint namespace import errors, while eleven unrelated findings remain. This directly verifies the source-root correction rather than suppressing arbitrary findings. These are historical component receipts: `e729fa3` does not contain the subsequent setup.cfg, targeted-path and security fixes. Candidate installation uses public baseline 0.49.85 and an explicit candidate overlay, so this is not public signed 0.50.0 acceptance. The detached-Hatch fixture remains labelled as reconstructed, and source immutability is supported by retained harness receipts rather than an independent retained-checkout replay. A fresh final candidate matrix remains required.

Final combined full suite passed **2,637 tests in 111.57 seconds**, with only the two existing lark deprecations. Configured lint passed (1,328 files already formatted, BasedPyright zero errors/warnings/notes, Ruff passed and Pylint 10.00/10). YAML, bundle import boundaries, strict OpenSpec, the staged planned Requirements gate (one source, no failures/skips), checksum/version checks for all seven manifests and publish preconditions passed. Core compatibility remains intentionally `>=0.55.1,<1.0.0`; the public registry still reports 0.49.85. The existing final-cleanup review found no defects, and the artifact-boundary fix received independent read-only approval.

The same pre-nested-fix tree passed the smart suite with **2,637 tests in 110.20 seconds** and the **28 contract tests**. Two late P1 comments on superseded PR #474 remain applicable to #475: nested Python resets caller environment and working directory, and a repeated temporary-filesystem mount hides private caller files. These compatibility fixes and their fresh evidence remain pending; the preceding full/smart results do not cover them. Read-only call-graph review confirmed all supervisor member launches use the sanitized first-entry command, while the generated Python wrapper is invoked only within target workers.

### Late PR #474 findings inherited by #475: nested Python caller context

The canonical scenario and design preceded focused regressions. `NESTED_PYTHON_CONTEXT_RED.txt` records **10 failures and one passing first-entry isolation control** on the unchanged implementation. Four failures execute real children using the emitted launcher environment/cwd policy and demonstrate lost application values, restored removed HOME, reset cwd and missing relative data; six expose the missing immutable-domain API. This policy replay is not a Linux bubblewrap execution claim.

The nested generated Python wrapper now directly enters trusted native Python startup in the existing target namespace. First-entry workers still unconditionally establish mount/process/network isolation, mask supervisor paths, clear inherited environment and select snapshot cwd. A read-only bind of an existing trusted member file identifies the current graph by filesystem identity, independent of child environment variables; all five member domains are retained without new analyzer mounts. The existing explicit diagnostics for Python options disabling attachment remain.

Focused GREEN: **99 passed in 8.84 seconds** across launcher, bootstrap, startup and real Pylint controls. This includes **five actual generated-wrapper/exec/native-startup subprocess cases**, one per member domain, with `env={}`, private cwd, relative files and attached project/member imports. Those tests use private copies of actual trusted helpers with relocated filesystem constants; they exercise real Python execution but do not prove Linux namespace mounts.

The reconstructed Hatch test slice adds six real subprocess tests for inherited/explicit/minimal/empty environments, relative helper execution, and caller-owned temporary files in both default TMPDIR and explicit `/tmp`. Empty-environment dependency imports include Requests and pyodbc; the deliberately uninstalled detached source package is explicitly added by the caller, matching native behavior. Five unchanged stdlib test bodies passed on native Python **3.11.15** and **3.12.13** in a private supporting harness. The full reconstructed fixture, Python 3.13 and actual cold/offline-warm capsule execution remain required CI evidence. Neither reconstruction nor local probes validate the inaccessible original customer reproduction. Comments 4010422180 and 4010422190 remain open until the combined validated signed update is pushed.

The final focused scope expanded actual generated-wrapper execution to **ten cases** (empty and explicitly overlaid environments across five domains) and passed **104 tests in 10.48 seconds**. Targeted typing reported zero errors/warnings; Ruff, formatting and Pylint (10.00/10) passed, with new production helper complexity at most four. Independent read-only review found no actionable defects in the complete nested-launch and fixture diff; independent replay is coordinated after the combined suite to avoid shared evidence-file races. Checked-in RED transcripts preserve failure content with trailing whitespace removed for repository diff hygiene.

Final combined full-suite validation passed **2,658 tests in 115.87 seconds**, with only the two existing lark deprecations. Independent nested-scope replay passed **104 tests in 10.52 seconds**, including all ten actual wrapper/native-startup cases, and found no defects. Full lint passed (1,328 formatted files, typing zero errors/warnings/notes, Ruff clean, Pylint 10.00/10); YAML, import boundaries and strict OpenSpec passed. The staged planned Requirements gate passed one source with no failures/skips. Canonical unsigned checksum/version checks verified all seven manifests and publish preconditions passed with intentionally unchanged core compatibility. A fresh remote read still reports dev `976460c0541b685f5fae256faef4cec32fe9ba7e` and registry 0.49.85; 0.50.0 remains unpublished and CI owns its publisher signature.

The smart suite also passed **2,658 tests in 114.60 seconds**; all **28 contract tests** passed. Fresh SpecFact bug-hunt review at **2026-09-15T00:34:09.608536Z** correctly blocked finalization with a temporary-file-wrapper type mismatch and a Pylint fatal error in the artifact-boundary test. It also reported new nesting/length and explicit subprocess-check advisories. These findings are being remediated before signing; this failed review is not passing delivery evidence. Startup contract and explicit launcher/test-fixture informational dispositions are narrowly documented in `REVIEW_EXCEPTIONS.md`, preserving dependency-free trusted startup and auditable isolation arguments.

Review cleanup preserves behavior: the existing controller-owned stream is passed as its actual `IO[str]` file object, and the unchanged builder-input filename tuple moved to a named module constant. Log lifecycle and acquisition boundaries remain unchanged; **104 focused builder/log tests passed in 7.52 seconds**. Artifact traversal now separates immediate directory validation from the traversal stack, retaining metadata-only no-follow checks; **17 artifact tests passed in 0.90 seconds**. Canonical nesting checks report no findings for either validator helper or the mutation fixture.

The Pylint failure was independently reproduced with **Pylint 4.0.8 / Astroid 4.0.4**: its pathlib checker treated a captured callable named `read_text` as an attribute expression and accessed `Name.attrname`. Renaming that test-only alias to `original_read_text` preserves its exact callable and guard while avoiding the checker defect. A combined before-run also reproduced six false import-group warnings in unchanged `test_runtime_artifacts.py`; the final combined invocation returns **exit 0 and an empty findings array**, with that file unmodified. The newly exposed encoding advisory and explicit subprocess-check forwarding were corrected. No analyzer suppression was added. Root read-only review of the mechanical production refinements found no defects; final combined suites and fresh review follow.

After the review cleanup, final full and smart suites each passed **2,658 tests**, in **116.56 seconds** and **115.98 seconds** respectively, with only the two existing lark deprecations. Final configured lint again passed with zero typing errors/warnings/notes and Pylint 10.00/10. YAML, import boundaries, strict OpenSpec, staged planned Requirements and canonical checksum/version/publish preconditions passed. The publisher signature remains delegated to CI, and fresh Linux acceptance is still required for this final source.

Final fresh branch-wide SpecFact `--enforcement changed --bug-hunt` at **2026-09-15T00:44:35.884497Z** returned **PASS_WITH_ADVISORY, exit 0, zero errors and zero blocking findings**, with **289 warnings and 70 informational findings**. Both prior blockers, the three new nesting/length warnings, subprocess-check warning and six false import-group warnings are absent. The remaining new startup-contract and native-wrapper informational advisories have exact dispositions in `REVIEW_EXCEPTIONS.md`; they do not waive missing execution or security evidence. Parent-coverage changes relative to the published candidate are: portable_worker.py: 78.9% to 79.6%; runtime_artifacts.py: 78.7% to 67.4%; runtime_build_driver.py: 51.4% to 49.1%; runtime_builder.py: 74.5% to 75.6%; target_bootstrap.py: 49.2% to 49.7%. The documented conditional Linux/isolated-process coverage boundary remains; these percentages are not represented as meeting the local 80% threshold. All 28 final contract tests also passed in 0.97 seconds. CI signing, a fresh signed candidate corpus and public release acceptance remain pending.

### PR #475 finding 4011119813: controller capture and runtime hardlinks

Specification and planned verification cases 473-7-13/14 were written before the regressions. Actual hardlink RED: 6 failed, 1 passing copied-manager-file control (`BUILD_HARDLINK_RED.txt`). Actual process-descriptor RED: 1 failed, 1 Linux-only test skipped on macOS (`BUILD_PIPE_RED.txt`). No native Linux exploit is claimed from these local results. Linux cross-bind-mount semantics constrain the review's exact hardlink mechanism; regular inherited diagnostic descriptors still require a controller-only capture boundary. The fix therefore uses pipe output from the builder plus independent-file validation at build, seal and attachment boundaries.

Independent review found a select/read race if another reader drains the pipe after readiness. A deterministic transient `BlockingIOError` regression failed before the follow-up (`BUILD_PIPE_READINESS_RED.txt`: 1 failed). The controller now sets its pipe reader nonblocking and retries through the same absolute deadline. Independent final review found no remaining defects. Focused artifact validation passed 24 tests; expanded capture checks preserve 2 MiB binary output, early EOF timeout, write-failure cleanup, and partial logs. The Linux process-descriptor test is explicitly skipped on this macOS host and remains required in Linux CI.

Commands for this follow-up (from the isolated delivery worktree, using the paired core environment): `hatch run format`, `hatch run lint` (includes typing), `hatch run yaml-lint`, `hatch run check-bundle-imports`, strict OpenSpec validation, `hatch run contract-test`, `hatch run test`, `hatch run smart-test`, and fresh explicit-file SpecFact review with `--enforcement changed --bug-hunt --json`. Local checksum validation compares against `origin/dev`; publisher signatures are obtained only through canonical GitHub CI. No Linux cross-mount hardlink exploit or original customer reproduction is claimed from local tests.

Full follow-up suite: **2,670 passed, 1 Linux-only skip, 2 existing lark deprecation warnings in 115.77 s**. Contracts: **28 passed**. Independent reviewers found no remaining defects after the nonblocking pipe follow-up. Public signed and updated Linux candidate acceptance remain pending.

Smart-test replay: **2,670 passed, 1 Linux-only skip, 2 existing lark deprecation warnings in 116.12 s**. RED transcripts preserve actual failures, with temporary path and trailing-whitespace normalization only.

Final fresh bug-hunt review at 2026-09-15T01:59:46.244985Z: PASS_WITH_ADVISORY, exit 0. Review completed with 4 findings (1 blocking). The Linux-only skip remains explicit testing-error evidence; the existing descriptor-validator private access and explicit command/fixture structure are narrowly dispositioned in REVIEW_EXCEPTIONS.md. Introduced test-name/fixture warnings were corrected and the 28 applicable focused tests passed again (1 Linux-only skip).

### PR #475 findings 4011304439 / 4011304448

Safe preparation reasons: actual final scoped RED **10 failed, 32 passed, 1 Linux-only skip** (`PREPARATION_DIAGNOSTIC_RED.txt`). The wrapper now preserves a stable ASCII controller code and exact signed numeric builder status, with its original exception cause and private log path. It does not publish arbitrary exception details or perform new log operations. An earlier exploratory local test set expected extra private diagnostic writes; that extra behavior was removed before production because the reviewer-requested safe public code is sufficient. Only the final scoped RED is release evidence. Independent review found no defects in the minimal formatter.

Nested PYTHONPATH: actual RED **20 failed** in real native-versus-attached wrapper cases (`NESTED_PYTHONPATH_RED.txt`); caller-only imports and omitted/empty environment values failed. Focused passing evidence and updated Linux corpus remain required. Temporary paths and trailing whitespace in RED transcripts are normalized only; failures are retained verbatim otherwise.

Independent review identified a callback introduced by the final caller-path insertion: a project `.pth` could replace `sys.path` with a list subclass whose slice setter ran after member validation. The actual nested-wrapper regression failed before correction (`NESTED_PATH_CONTAINER_RED.txt`: **1 failed**). Capturing the trusted list primitive and insertion slice before project hooks avoids invoking the subclass callback. Root review confirmed the final operation uses those captured values. Final focused launch/bootstrap validation passed **115 tests in 17.48 seconds**, with typing, Ruff, formatting and Pylint (10.00/10) clean. The safe-diagnostic scope passed **42 tests**, with the one Linux-only process-descriptor test skipped on macOS. The reconstructed Hatch slice now checks a caller-only relative import in a child and grandchild; local native and attached wrapper checks passed, while the updated Linux corpus remains required.

Final combined R3 gates: full and smart suites each passed **2,697 tests**, with one Linux-only process-descriptor skip and two existing lark deprecation warnings (**124.65 s / 125.63 s**). All **28 contracts** passed. Formatting, typing, lint, YAML, bundle import boundaries and strict OpenSpec passed. Canonical checksum/version verification passed all seven manifests against unchanged dev `976460c0541b685f5fae256faef4cec32fe9ba7e`; the registry still reports 0.49.85, leaving planned minor 0.50.0 unpublished. GitHub CI remains the publisher-signature authority. Local tests do not establish current Linux or public signed installation acceptance.

Fresh review initially invalidated its snapshot because the evidence document changed during analysis; this is discarded as UNKNOWN. The unchanged-tree replay at 2026-09-15T02:41:39.811705Z returned PASS_WITH_ADVISORY and exposed one new line-length warning, corrected by splitting the diagnostic string without changing its value. Remaining startup-contract, private-validator-access and explicit-fixture informational findings have existing exact dispositions in REVIEW_EXCEPTIONS.md. The Linux-only skip remains visible as a testing error pending real Linux validation. A final fresh review follows the formatting correction.


## Release PR477 pytest evidence corrections (2026-09-15)

Review findings4019944740 and4019944751 were independently reproduced: mixed passing/skipped/XFAIL/XPASS tests and unrelated/low source coverage could return PASS. Specification scenarios preceded new tests and implementation. Adapter RED:17failed4passed; native policy RED:10failed3passed. Empty-reason XPASS additionally failed one native regression. Independent review then reproduced nested pytest-root misclassification and production coverage suppression under broad discovery roots:3adapter failures and3native-root failures were recorded before their fixes. The five RELEASE_*_RED.txt files preserve these runs.

The correction records native pytest-cov's resolved threshold, pytest root and expected-failure marker presence; it preserves raw nodeids and selection. The adapter emits blocking non-pass outcomes and reuses per-source coverage checks with the80percent floor, higher effective native thresholds and no customer-wide initializer omission. Discovery roots do not establish production exclusions. Nested nodeids resolve against the contained native root. The existing discovery harness now uses its simulated snapshot as cwd, matching the real worker.

Focused results:22adapter outcomes/coverage tests passed before root fixes;64adapter/worker tests passed. Final root/adapter/discovery tests:51passed. Native policy/root/empty-XPASS tests:17passed. An initial full run passed2730tests with1Linux-onlyskip but failed3old assertions because failure messages gained an unnecessary word; the original failure wording was restored. A test helper return annotation fixes an independently detected typing error. Final combined gates and current Linux acceptance remain required.

Version0.50.1 preserves CI-produced immutable0.50.0 artifacts. Canonical GitHub CI will provide publisher signatures and updated registry artifacts. Findings remain unresolved pending signed pushed fixes and Linux acceptance; public installation and the original customer reproduction are not claimed.


Final local validation: 2,739 full-suite tests passed, with one Linux-only /proc skip and two existing lark deprecations, in 134.65 seconds. Formatting, typing (zero errors/warnings), lint (10.00/10), YAML/manifests, import boundaries and strict OpenSpec passed. The earlier 28-contract run passed; current contract/smart selection and fresh SpecFact review are recorded in the follow-up PR. A second independent read-only review found no remaining issues in the corrected coverage and outcome paths. Linux corpus evidence is still required before resolving PR findings.

Final review after helper extraction: `specfact code review run --enforcement changed --bug-hunt --json` over the three changed runtime sources and three pytest test modules completed at 2026-09-15T21:01:01Z, exit 0, PASS_WITH_ADVISORY. It retained 106 findings (91 warnings, 14 informational, one error), including the documented deferred-import/target-hook boundaries and unchanged legacy runner findings. The error records the local Linux-only /proc test skip; this is not error-free review or Linux acceptance. The new complexity, unused-argument and missing public contract findings were corrected, and CrossHair was rerun in the dedicated environment. Report: `/private/tmp/specfact-477-review-final.json`. Smart suite: 2,739 passed, one platform skip, two existing deprecations (134.63 seconds); contracts: 28 passed. After the behavior-preserving helper extraction, 110 focused cases and lint/type checks passed. Required Linux validation remains outstanding.

## PR #478 nested pytest outcome paths (2026-09-15 Europe/Berlin)

Finding 4020270168 is valid: non-passing outcomes under a nested pytest root emitted the raw root-relative filename, so later snapshot filtering could lose the finding. A scenario was added before four regression cases. Actual RED: **4 failed, 25 deselected**, preserved in `PR478_PYTEST_PATH_RED.txt` and `/private/tmp/specfact-478-path-red.log`; two cases assert nested relative/absolute root attribution, and two assert containment before attribution.

The adapter now resolves outcome files to snapshot-relative paths using the same contained native pytest-root resolver as source coverage. Raw observed node identifiers and the diagnostic test identity remain unchanged. Outside roots and escaping node paths return incomplete evidence instead of an incorrectly attributed test finding. No discovery-root production exemption was introduced.

Focused command: `hatch run pytest -q tests/unit/specfact_code_review/run/test_portable_pytest_evidence.py tests/unit/specfact_code_review/run/test_portable_pytest_discovery.py`. Final local result: **55 passed in 7.22 seconds** (`/private/tmp/specfact-478-path-green.log`). An intermediate implementation run failed because the existing test adapter replaces `Path` with a callable; using the equivalent `Path(".").resolve()` retained the harness and fixed that incompatibility. Ruff formatting/checks passed. This local macOS Python 3.14 result is not Linux capsule or public signed acceptance; combined gates and independent review remain required.

Follow-up complete local lint includes formatting of all 1,331 files, zero type errors/warnings, Ruff clean and Pylint 10.00/10. Strict OpenSpec validation passed. Module signing and combined release gates are owned by the parent delivery task.

Parent verification: independent diff review found no additional defect; 114 combined adapter/native-policy/discovery/worker cases passed in 8.68 seconds. Fresh bug-hunt review completed at 2026-09-15T21:21:52.324760Z with PASS_WITH_ADVISORY and exit 0 (94 retained findings, one local-platform-skip blocker). The sole changed-line warning is the existing documented deferred runner import boundary. Report `/private/tmp/specfact-478-path-review.json`. No Linux acceptance is inferred from this local review.

## PR #478 native pytest startup failure (2026-09-15 Europe/Berlin)

Finding 4020397305 is valid. Native pytest 9.1.1 with `--specfact-controlled-invalid-option` exits with usage code 4 before session finish, recording `pytest_root: null` and no test records. Replaying this real observation through the capsule request handler previously raised a root-resolution TypeError and returned the unknown member envelope without `target_execution`. The startup scenario preceded the regressions and production changes. Actual final RED: **6 failed, 29 deselected** (`PR478_PYTEST_STARTUP_RED.txt`, `/private/tmp/specfact-478-startup-red.log`). An initial test-only envelope harness incorrectly replaced Path.cwd; correcting that harness before production edits yielded the recorded failure on member identity instead.

The adapter now validates observations before resolving outcome paths. Invalid executions with no outcome records return their diagnostic without inventing a pytest root. Usage exit 4 receives an actionable configuration/collection diagnostic referring to `target_execution`; native no-tests exit 5 retains its existing incomplete-execution semantics. Otherwise usable observations with explicit null, empty or non-string roots return `project_pytest_root_missing_or_invalid`. The capsule envelope test asserts that member identity, UNKNOWN/error status and the full original target execution record survive.

Focused command: `hatch run pytest -q tests/unit/specfact_code_review/run/test_portable_pytest_evidence.py tests/unit/specfact_code_review/run/test_portable_worker.py tests/unit/specfact_code_review/run/test_portable_pytest_discovery.py`. Passing result: **103 passed in 7.37 seconds** (`/private/tmp/specfact-478-startup-green.log`). This macOS Python 3.14 result is not Linux capsule or public signed acceptance. Independent review, combined release gates and CI signatures remain required.

Parent startup validation: 120 combined adapter/native/discovery/worker tests passed in 9.10 seconds. An independent read-only reviewer found no additional defects in the full startup correction and surrounding capsule envelope. Fresh bug-hunt review at 2026-09-15T21:45:21.681036Z returned PASS_WITH_ADVISORY, exit 0, with 95 retained findings and the documented Linux-only skip blocker. The new informational long-test finding has an explicit integration-proof disposition in REVIEW_EXCEPTIONS.md; no new production complexity regression was found. Report `/private/tmp/specfact-478-startup-review.json`. The prior path-only full/smart suites passed 2,743 tests each (147.38/149.80 seconds); they precede this startup correction and do not replace its forthcoming current-head Linux validation.

### 2026-09-16 — PR478 installed source coverage RED

Specification extended before tests and implementation to preserve native installed-package imports while requiring metadata ownership and byte-equivalent coverage attribution. Strict OpenSpec validation passed. At unchanged production HEAD `efe7303efb32ac31379906a38e3f960ec4e7a744`, `.venv/bin/python -m pytest tests/unit/specfact_code_review/run/test_installed_coverage.py -q` produced **11 failed, 2 passed** in 1.45s (log `/private/tmp/specfact-478-installed-coverage-red.log`). The native wheel-style installed-package pytest execution passed its actual test but snapshot-only measurement omitted its production source; the attribution assertion failed. A separate actual failing test retained native exit1. No collection/acquisition errors substitute for these behavioral failures. The test helper represents existing no-bridge behavior only when the new module is absent.


### 2026-09-16 — PR478 installed source attribution local GREEN

`PR478_INSTALLED_COVERAGE_RED.txt` preserves the initial unmodified-production failure output. Independent review then reconstructed a flat-layout regular installation: real native pytest passed while Coverage enumerated the untouched snapshot copy at zero executed lines and measured the installed copy at100%. `PR478_INSTALLED_FLAT_RED.txt` records that additional failing-first case. The correction distinguishes actual executed snapshot lines from unexecuted enumeration and replaces only evaluation aliases; raw coverage remains unchanged. These are reconstructed fixtures, not the inaccessible original customer reproduction.

The controller now reads sealed local-origin distribution RECORD ownership without importing customer packages, authorizes only owned installed coverage directories, and verifies exact source/installed bytes before and after execution. Generated or transformed selected source, ambiguous ownership or source paths, malformed RECORD data and unsafe directory ownership remain actionable incomplete evidence. Native built-package imports, failure exits, low coverage, and actual snapshot execution precedence remain intact. Snapshot discovery and owned-directory verification are cached within one preparation, avoiding repeated repository/package walks.

Local focused command: `.venv/bin/python -m pytest tests/unit/specfact_code_review/run/test_installed_coverage.py tests/unit/specfact_code_review/run/test_portable_worker.py tests/unit/specfact_code_review/run/test_target_pytest.py -q` — **72 passed** in0.74s; log `/private/tmp/specfact-478-installed-coverage-focused.log`. Real native tests include installed imports with a build-generated initializer, a failing assertion, namespace ownership, relative and absolute measured paths, stale source and installed files, duplicate ownership/source paths, malformed hashes and escaped RECORD paths, controller-to-worker coverage arguments, raw report retention, and the final flat-layout source coverage evaluator.

`hatch run format`, `hatch run lint` (including type checking, zero errors/warnings and pylint10.00), and strict OpenSpec validation passed. The Requirements compatibility adapter with the repository Requirements/code-review/codebase source roots, `--base-ref origin/dev --required-maturity planned`, passed with one source; implementation maturity remains explicitly not-yet-available. Its JSON and summary are `/private/tmp/specfact-478-installed-requirements.json` and `/private/tmp/specfact-478-installed-requirements.md`. Native Linux corpus and signed publication acceptance are still required; local results do not establish them.


### 2026-09-16 — PR478 native Coverage alias provenance

Independent review reproduced a real false PASS when native `[tool.coverage.paths]` renamed measured installed paths into the snapshot: one selected byte-identical sibling authorized package measurement, while a different installed/source file lost its mismatch diagnostic after aliasing. `PR478_INSTALLED_ALIAS_RED.txt` records the new native failing-before assertion (one failed,24 deselected); the underlying two pytest tests actually passed. The controller now retains exact installed candidate paths, and the worker records their mappings through the installed Coverage implementation's own alias engine. No coverage pattern language is reimplemented or removed. Mismatched source remains incomplete when the native alias maps that installed candidate to its snapshot path; unrelated alias groups do not reject genuine snapshot execution. Valid byte-identical mappings recheck content before accepting either original or aliased rows.

Focused validation now passes79 tests, including actual aliased native coverage, unchanged-byte acceptance, unrelated patterns, positive snapshot rows after source/installed mutation, and a missing native alias API that preserves collected test outcomes/root metadata. Metadata parsing imports no customer code; the two package-internal controller APIs use the controller's existing trusted icontract dependency for output invariants. The target worker imports no controller contract dependency. New complexity, parameter-count, test shadowing and unnecessary local-import findings were refactored; all new helper/test functions are below radon gradeC. Format/type/lint are passing. Full native Linux and public signed corpus evidence remains outstanding.

The final origin probe also matches pytest-cov's native `topdir` context. A real Coverage alias test failed before this correction when customer code left the working directory elsewhere; `PR478_INSTALLED_ALIAS_CWD_RED.txt` preserves that failure. The worker now uses the native controller's topdir inside a restoring standard-library context manager, so exact mapping and caller-directory restoration both pass. Final focused validation is **80 passed**; no new helper/test function exceeds radon grade B. The request assembler is separate from `run_portable_pytest`, preserving that existing function's baseline cyclomatic complexity of 11, and the Observer keeps related origin state together rather than adding independent attributes.


### 2026-09-16 — complete measured-origin receipts and isolated native probes

Full and smart suites exposed test-only Coverage global cache contamination: the synthetic probe constructed an uninitialized Coverage object in the same parent process across temporary roots. Both actual runs recorded2780 passed,1 failed,1 Linux-only skip. The probe now runs in a fresh subprocess, initializes through the public `get_data` API under pytest-cov's top directory, then changes to the customer directory before invoking the production receipt helper. It checks restoration inside that child. Production never clears or overrides native Coverage globals.

Independent review then identified a package-renaming variant of the native alias issue. The actual reconstruction executed installed `capsule_owned/math.py` while reviewing different bytes at `src/original/math.py`; an identical selected sibling enabled package measurement. Native Coverage rewrote the installed path into the unmatched source path, whose empty suffix-candidate set previously allowed a false PASS. `PR478_INSTALLED_RENAMED_RED.txt` preserves the failing-first native regression (1 failed,32 deselected). Controller planning now retains every RECORD-owned Python origin in measured directories, once, and the worker records the native relocation of that complete origin set. A relocated destination without an independently verified source correspondence remains incomplete; missing receipts cannot authenticate an executed snapshot row. No package-renaming correspondence is guessed, no customer path configuration is removed, and the raw coverage report remains untouched. Broader final validation and Linux corpus remain pending.

Latest focused validation after the complete-origin guard: **82 passed** in1.54s. Formatting, repository lint/type checks, strict OpenSpec validation, YAML lint, Requirements planned gate, and whitespace checks pass. This replaces neither pending final broad suites nor native Linux acceptance.


## PR478 native development environment alignment (2026-09-16)

Native workflow35068293754 failed preparation with `project_analyzer_dependency_incompatible:pylint:isort; selected=9.0.1, required=!=5.13,<9,>=5`. The actual error and developer package inventory are retained in `PR478_NATIVE_ENVIRONMENT_FAILURE.txt` and `PR478_NATIVE_ENVIRONMENT_PACKAGES.txt`. Native development packages selected Pylint4.0.8 and basedpyright1.40.1; the signed cp311/cp312/cp313 toolchain declares Pylint4.0.7 and basedpyright1.39.10. Pylint4.0.7 itself declares isort<9. No hidden resolver constraint is added.

The new spec preceded three failing cases in `PR478_DEV_ALIGNMENT_RED.txt`. The regression reads the repository declarations and signed toolchain and exercises production compatibility validation against the recorded native versions; it also retains rejection of the original incompatible versions. After explicitly declaring pylint==4.0.7 and basedpyright==1.39.10 in this repository's Hatch default environment, all six compatibility tests pass. The raw inventory remains evidence; tests keep their two observed versions inline so normal OpenSpec archival cannot break them. Customer declarations and production compatibility checks are unchanged.

## Native index activation follow-up (2026-09-16)

Native run [35070709935](https://github.com/nold-ai/specfact-cli-modules/actions/runs/35070709935) successfully prepared the reviewed project runtime (identity `sha256:a37e1bba96fabbbf7155fb672b886ddc40a84db1b77a2175d5ae67ad8e18d300`) with the declared analyzer pins. Original signature, formatting, YAML, import, lint and Requirements hooks passed, but review remained UNKNOWN: index snapshot discovery lost verified Hatch activation and an independent signed Semgrep member returned one structured error. The unchanged hooks correctly failed before contract execution; no successful receipt or delivery commit is claimed.

The actual `hatch run python` checkout/index probe reproduced checkout `default` versus `project_environment_ambiguous:default,hatch-test` in the real `_cached_analysis_snapshot` path. The specification preceded regression tests; `PR478_INDEX_ACTIVATION_RED.txt` retains two failing behaviors and three passing controls. Controller-owned snapshot repository context now supplies only activation verification; snapshot metadata, dependency input hashes and source identity remain independent. Explicit project configuration wins, and removed or unrelated activation cannot select a different environment silently. The same real Hatch probe now resolves `default` in both checkout and index.

Activation follow-up GREEN: 318 focused discovery, snapshot, VCS and attachment tests passed in 6.57 seconds, including both index and immutable revision inputs. The independently authored Semgrep diagnostic scenario preceded five real failing assertions (`PR478_SEMGREP_DIAGNOSTICS_RED.txt`), then 63 focused runner cases passed. The bounded formatter remains unpublished; the native hook executes the official signed 0.50.0 analyzer payload, so this change alone does not reveal that earlier run's lost raw error or establish its cause.

Final local activation/diagnostic candidate validation: full suite 2,798 passed, one required Linux `/proc` test skipped on macOS, two pre-existing lark warnings, 129.89 seconds; smart suite the same outcomes, 150.66 seconds. Type and repository lint passed. The fresh follow-up five-file bug-hunt report `/private/tmp/specfact-478-followup-final-review.json` is PASS_WITH_ADVISORY with 31 AST-compared baseline warnings and zero errors, explicitly scoped in REVIEW_EXCEPTIONS.md. The earlier complete delivery-surface report retains the required Linux skip and cannot authorize delivery. Native unchanged-hook execution and final candidate/public signed corpus acceptance remain outstanding.

## Native analysis follow-up (2026-09-16)

[Run35073276747](https://github.com/nold-ai/specfact-cli-modules/actions/runs/35073276747) confirms automatic indexed runtime attachment now passes; Pylint and contracts complete. The hook still fails on Semgrep, basedpyright and actual target tests. Its separate diagnostic-only official Semgrep1.144 replay returned a structured syntax error at test_installed_coverage.py147: `match case:` reported `case was unexpected`. The parameter is renamed to `scenario`, retaining all six attribution cases and assertions; this accommodates the pinned parser without skipping the file, changing customer source or accepting parser errors. The raw replay is retained in the native artifact with source/configuration/capsule hashes and explicit startup differences; it is not recovered original stdout or acceptance.

The same native observation records 99 failed phases caused by unavailable Git and 15 Semgrep fixture failures; a fixture's retained error names missing `uname` in the project Semgrep1.177 native executable. Basedpyright returned empty JSON and its cause is not established. These remain actual failures; no native hook receipt authorizes a delivery commit.

## Native tools and implicit Hatch default (2026-09-16)

The native run's real Git and uname failures above motivated explicit target capabilities, with specification before tests. `PR478_NATIVE_TOOLS_RED.txt` retains ten actual failures (five discovery behaviors and five absent capture APIs). Subsequent precise RED evidence covers a Git helper-only transitive library, missing local Git service helpers, and a builder-owned `bin` file collision before installation. The controller now captures fixed supported tools and all program/helper ELF dependencies before building, hashes every captured/generated file into cache and inventory, and installs only after project native-library inventory. The disposable builder cannot access the capture; an integration regression modifies system fixture bytes during building and confirms the original capture is installed. Destination collisions fail before any tool write.

The independent Hatch default follow-up retains three actual failures in `PR478_IMPLICIT_HATCH_DEFAULT_RED.txt`; its 253 focused cases passed before integration. A real Hatch1.18 default export/activation probe now agrees across live and immutable snapshot discovery. The builtin default is recognized only within verified activation; explicit selection and snapshot dependency identity remain intact.

Native-tools focused discovery/builder/native/snapshot validation: 314 passed in 1.99 seconds. Typing reports zero errors/warnings; new helper and test lint score10.00; every new helper/test complexity is at most9. `PR478_NATIVE_TOOLS_LINUX_SMOKE.json` records non-root UID1001 Ubuntu Linux execution with network disabled and reviewed source read-only. All ten commands passed: uname, sed, Git initialization/configuration, a real sed clean-filter changing SAFE to BLOCKED, commit, and depth-one file://clone using captured upload-pack. The capture contains50 files totaling38,128,554 bytes (36.36 MiB), including intentionally duplicated Git service executable bytes; no transfer or signed-capsule success is inferred. The smoke uses executable temporary storage inside its disposable container and a fixed Python interpreter link; it does not weaken any capsule boundary.

`PR478_SEALED_SHELL_AUDIT.json` separately records static verification of all13 signed OCI layer hashes/sizes and final /bin/sh→dash composition, with no later replacement/deletion. Actual Git-filter success inside the signed capsule remains a required native hook/corpus check. Both smoke and static audit are labeled evidence, not protected review or release acceptance. The old signed worker can consume these descriptor additions and existing runtime-bin launchers without changing its code.

The Node follow-up reproduced the actual distribution-name mismatch using BasedPyright1.39.10 wheel metadata and the production graph→mount→import finder path. `PR478_NODE_DOMAIN_RED.txt` retains two failures with two passing controls; separate evidence describes the wheel and observed native versions. The corrected catalog identifies nodejs-wheel-binaries, preserving the sealed Node entry or emitting an explicit version incompatibility. This repository now declares24.16.0 openly alongside its BasedPyright pin. The compatibility regression checks those declarations against all three signed ABI inventories and keeps the observed24.19.0 rejected. `PR478_NODE_POLICY_CACHE_RED.txt` additionally records genuine stale warm-cache reuse after changing only runtime_compatibility.py; that controller policy is now fingerprinted before lookup. Raw baseline native diagnostic confirmation remains separate from these verified causes and the final updated-snapshot gate.

Combined focused follow-up validation passed402 cases in14.29seconds, including all three ABI declaration checks and policy-cache invalidation. Fresh combined developer bug-hunt after test cleanup reports PASS_WITH_ADVISORY with215warnings,55info and zero errors; exact existing-surface and conditional coverage dispositions are in REVIEW_EXCEPTIONS.md. Full type/lint/YAML/import and planned Requirements checks passed. The canonical dev-branch payload/version gate passed all7 manifests using origin/dev and its documented optional-public-key policy; this is not CI publisher-signature verification of the unsigned candidate. Native Linux unchanged-hook and public signed acceptance are still outstanding.

Final full and smart suites each passed2841 tests with one required Linux/proc skip onmacOS and two pre-existing lark warnings (143.45s full;142.78s smart). The full command forwarded `--cov=specfact_code_review.run.runtime_builder --cov=specfact_code_review.run.target_pytest --cov-report=json:/private/tmp/specfact-478-combined-full-coverage.json` and retained the configured80% threshold. The scoped aggregate86.9674% passed; the raw data was copied to `/private/tmp/specfact-478-combined-full.coverage` before smart ran. Builder measures92.4444% with all introduced executable lines covered. Target pytest measures79.8851%; native-child-only mapping/startup lines remain outside parent data and are not rounded up to80%. Thirteen new validator boundary cases pass without production changes or manufactured RED evidence. Remaining coverage qualifications are stated precisely in REVIEW_EXCEPTIONS.md.

## Native follow-up after exact snapshot run35081982712 (2026-09-16)

The exact `d5b0c610d24b179a82b7acb5dac9c15aa1681dcc` candidate prepared successfully and its source tree remained unchanged, but the real unchanged hooks failed. Target pytest recorded877 passing calls and24 failed calls, with no collection or internal errors. Nine activation tests found that sanitized VCS lookup discarded the declared Git path; fifteen Semgrep fixture tests advanced past the earlier missing uname to a missing public certificate trust bundle. BasedPyright now ran successfully as a tool and reported the genuine missing import `specfact_cli.registry`, rather than the previous empty-JSON startup error. These are retained failures, not acceptance.

The repository's Hatch dependencies omitted core despite a separate editable developer bootstrap. The new specification preceded `PR478_CORE_DECLARATION_RED.txt` (one genuine declaration failure) and the exact native diagnostic in `PR478_CORE_NATIVE_RED.json`. The published `specfact-cli==0.55.4` wheel was downloaded without installing it into the developer environment and imported from its extracted bytes in an isolated Python invocation. `PR478_PUBLISHED_CORE_API.json` records the hash, size and actual registry/public-key API checks. The repository now explicitly declares that immutable version; all11 focused compatibility cases pass. The unchanged optional developer bootstrap may still use its matching-version editable checkout; no arbitrary host environment is copied into targets.

`PR478_ATTACHED_VCS_GIT_RED.txt` and its companion evidence retain seven real failures before verified attached-Git selection and69 focused passing cases after the correction. The equivalent six-branch if/elif coverage-fixture setup preserves all existing assertions while avoiding an independently reproduced Semgrep1.144 parser combination defect: tree-sitter rejects an empty-string argument and Pfff cannot parse match, leaving a misleading first parser error. This is fixture compatibility only; arbitrary customer syntax with the same engine limitation still yields explicit incomplete analysis rather than a false PASS.

Signed public trust follows the same test-first sequence: `PR478_PUBLIC_TRUST_RED.txt` retains nine actual failures before capture/cache/inventory/default-path integration; the final focused trust suite passes11 cases, and six affected builder/capability suites pass91 cases with one existing Linux/proc skip on macOS. Synthetic builder fixtures explicitly replace the new capture boundary rather than pretending to contain a real signed capsule. Production capture validates a contained regular public PEM, rejects private material, and precedes untrusted building; the installed bytes and cache identity remain controller-owned. `PR478_PUBLIC_TRUST_NATIVE_PROBE.json` separately reproduces missing CA startup failure and successful exact-engine initialization after public CA installation in an offline Linux container. This is diagnostic evidence, not updated signed-capsule acceptance. Final integrated native hooks remain outstanding.

The final integrated local follow-up review `/private/tmp/specfact-478-native-followup-final-review.json` is PASS_WITH_ADVISORY with215warnings,57informational findings and zero errors. Two introduced nested fixture setups were refactored without changing their cases/assertions; both affected files pass102 tests in11.19seconds, with targeted typing and Ruff passing. Independent read-only review found no remaining refactor defect. Existing measured coverage advisories and narrow boundary/readability dispositions remain in REVIEW_EXCEPTIONS.md. Full formatting, typing, lint, YAML, import and strict OpenSpec checks passed; all7 manifest payload/version checks passed under the documented optional-public-key developer policy. This remains local validation, not a successful native hook or signed release.

The recorded PR478 RED text captures have trailing whitespace normalized only for repository diff hygiene; failure diagnostics, assertions and outcomes are unchanged.

Final native-follow-up full and smart suites each pass2867 tests with one Linux/proc skip on macOS and two pre-existing lark warnings (138.69s full;136.88s smart). The full invocation preserves the configured80% threshold and exports scoped runtime_builder/target_pytest coverage to `/private/tmp/specfact-478-native-followup-coverage.json`, with raw data retained as `/private/tmp/specfact-478-native-followup.coverage`. This two-module aggregate is87.0646766%; builder measures92.5438596%, with every introduced executable line covered. Target pytest remains79.8850575%; native child mapping/startup lines158/198 are not counted in parent coverage. These are measured local results; unchanged Linux hooks and final signed external corpus acceptance remain mandatory.

## Completed native review exposes two fixture prerequisites (2026-09-16)

Run35090737789 completed its original unchanged review within the existing timeout (Block2 284.0seconds). Runtime attachment, BasedPyright, Pylint and contracts passed. Actual pytest collected1011tests, with1010passing calls and one failed call, without skips or collection/internal errors. The failed call attempted to write `.git/info/grafts` after template-free Git initialization omitted `.git/info`. Separate sealed Semgrep replay retained a syntax error on the valid keyword-only lambda in the builder logging fixture. `PR478_NATIVE_FIXTURE_FAILURES.json` binds these real failures to the verified artifact and source tree. The default-off timing diagnostic correctly declined replay because an original report existed; no timing-policy change follows.

The graft fixture scenario precedes `PR478_TEMPLATE_FREE_GRAFTS_RED.txt`: ordinary Git templates passed while explicit `git init --template=` reproduced the same missing-parent failure. The fixture now creates only its required metadata parent before writing grafts. All identity, two-commit exported ancestry and absent exported graft assertions remain unchanged in both parameter cases. The full VCS test file passes56tests in8.90seconds; targeted typing reports zero errors/warnings and Ruff checks pass. No production Git isolation or source identity behavior changed.

`PR478_LOGGING_PARSER_EVIDENCE.json` records the exact official Semgrep1.144.0 full-file parser failure and equivalent named-function passing result (error_file_count1→0; Pfff also passes). The tree-sitter-specific Common.Impossible failure remains recorded. The replacement retains the original keyword-only inputs, returned argv expression and every logging assertion, with AST comparison evidence. Its focused file passes27tests with one pre-existing Linux/proc-only skip on macOS, targeted typing zero errors/warnings and Ruff passing. This is pinned-engine fixture compatibility; unsupported valid customer syntax continues to produce incomplete analysis, and final unchanged Linux hooks remain required.

Final fixture follow-up full and smart suites each pass2868tests with the same one Linux/proc-only skip on macOS and two pre-existing lark warnings (135.88s full;134.27s smart). Scoped manual `/private/tmp/specfact-478-native-fixtures-final-review.json` passes with two informational fixture-structure suggestions and zero warnings/errors. The explicit attached-Git and builder fixtures retain distinct isolation/resource assertions; their setup remains intentionally readable. An earlier scoped attempt omitted the standard source PYTHONPATH and produced a CrossHair import warning; the corrected canonical environment resolved it without source or gate changes. Strict OpenSpec and planned Requirements pass. Previous production line-coverage measurements remain applicable because this follow-up changes only tests and evidence. The completed but failed native receipt cannot authorize delivery; the new exact tree still requires unchanged Linux hooks and signed release acceptance.

## Native coverage activation and reviewer evidence (2026-09-16)

`PR478_NATIVE_COVERAGE_FAILURE.json` binds the latest real signed0.50.0 regression to native run35093180778, source459e5ba6 and its verified artifact. All1012 selected tests passed every recorded phase, while the injected valued `--cov` changed the configured source scope and activated the repository's otherwise dormant aggregate80% gate. The native aggregate was46.88243064729194%, so the unchanged hook correctly remained failed. An independent full configured-source measurement also failed the unchanged80% policy at47.69127470559879% despite2868 passing tests (one platform skip). Widening test selection or lowering the threshold is not the correction.

The three coverage-fidelity scenarios were added before tests. `PR478_REVIEWER_COVERAGE_RECONSTRUCTED_RED.txt` retains six failures from a harness reconstructing the old argument injection; these are not six end-to-end old-worker reproductions. `PR478_TARGET_COVERAGE_WIRING_RED.txt` separately proves the actual production observer appended overriding arguments. The real native artifact above supplies the original execution evidence. `PR478_COVERAGE_LIFECYCLE_RED.txt` records two subsequent behavioral failures: missing coverage raised an uncaught NoDataError, and collect-only wrongly claimed an applied aggregate failure. `PR478_COVERAGE_BLOCKED_PRELOAD_RED.txt` records an explicitly blocked plugin being instrumented and a trusted preload breaking a strict customer warning policy. `PR478_COVERAGE_EFFECTIVE_SCOPE_RED.txt` proves missing effective CLI scope metadata. Corresponding production corrections followed each failing record.

The new target-only helper uses pytest-cov's lifecycle and worker transfer. It leaves an active native collector untouched, exports private JSON separately, and labels native versus reviewer measurement. Its separate reviewer collector does not create a native aggregate-report gate; the unchanged controller still enforces max(80,effective threshold) for each reviewed source. Configured source/include/omit exclusions are preserved. The fixed preload carries pytest's assertion-free `PYTEST_DONT_REWRITE` marker so strict warning policy is preserved without changing customer assertion rewriting. Supported native API versions are explicit; missing data or unsupported contracts remain incomplete.

Combined focused validation passes125tests in5.11seconds across target lifecycle/options/bootstrap/observer/controller suites. This includes real reviewer and native `-n2`, lost worker coverage receipt, configured subprocess collection, customer cwd changes, no-cov/no-cov-on-fail, native report write errors, collect-only, custom config/reset/source omission, byte-identical customer LCOV output, and both directions of aggregate-versus-per-file policy. The options suite introduced no production defect; its initial import/metadata expectation corrections were harness errors and are not claimed as RED proof. Bootstrap evidence retains7genuine failures plus1control, and controller evidence retains its21-failure and8-failure sequences. Ruff and targeted typing pass; new helper complexity is at most9. Independent review found no remaining lifecycle, bootstrap or controller findings.

The repository explicitly declares pytest-xdist>=3.8.0 for these mandatory distributed tests; normal Hatch resolution installed3.8.0. PyPI3.8.0 declares Python>=3.9, including the supported3.11–3.13 classifiers (accessed2026-09-16). It is not falsely attributed to the existing signed analyzer lock. Final Linux ABI execution remains pending.

Governance review supersedes the earlier assumption that a successful remote hook receipt is required before a normal developer commit. All original host pre-commit hooks remain mandatory. After their success, a normally signed developer commit can enter the canonical scratch CI signing workflow, import exact signed manifest bytes through another normal signed commit, and exercise the exact candidate observer in PR-to-dev CI. Failed immutable0.50.0 receipts are regression evidence only and authorize neither a bypass nor release acceptance. The configured thresholds, native exit statuses and final signed Linux/corpus gates remain unchanged. Broad gates and a fresh bug-hunt for this new candidate are still pending.

Follow-up measurement strengthens the new helper through18 in-process boundary cases using real Coverage data and a real reviewer CovPlugin, not synthesized line evidence. Scoped helper coverage is97.04% under the unchanged80% gate; three unmeasured statements remain explicit. Existing native child tests are retained. A separate disposable environment executes seven unchanged harness assertions against actual pytest-cov7.0.0, pytest9.1.1, Coverage7.16.1 and xdist3.8.0, including both distributed modes, missing receipts and configured child collection; all pass. `PR478_COVERAGE_PLUGIN_COMPATIBILITY.json` retains exact commands, helper identity, streams and receipts. These local measurements do not replace signed Linux acceptance.

The first consolidated full run exposed two existing nested-launch fixtures whose synthetic builtin inventory omitted the newly mandatory helper. Adding the actual helper file preserves all original assertions; the affected launcher/bootstrap suites pass130 integration cases. Eight additional direct-loader cases exercise original production code through real files and restore module/cache state. They cover27/28 loader statements (96.4286%); the defensive missing-spec-loader branch remains unmeasured. Existing real child-startup cases retain independent import-domain proof.

The additional four-module coverage measurement remains a recorded failure:75.3571% aggregate versus unchanged80, including legacy bootstrap39.2157%, helper97.0414%, observer84.0426% and builder92.5439%. After direct-loader tests, separately measured bootstrap improves to53.3333% while the introduced loader function reaches96.4286%; the whole legacy module still does not satisfy80 and is not presented as passing. These optional scoped measurements are distinct from ordinary full-test commands and from the earlier whole-project47.6913% measurement. No threshold or customer option changed.

The corrected ORIGINAL full and smart commands each pass2971tests with one genuine Linux/proc-only skip and two pre-existing lark warnings (148.72s full;155.97s smart). Their output is retained outside the checkout as `/private/tmp/specfact-478-coverage-final-full.log` and `-smart.log`. One interim manual review correctly rejected concurrent documentation/evidence changes with `worktree_snapshot_changed_during_analysis`; that report is failed evidence, not a code finding or acceptance. All source and evidence paths are now frozen for the final fresh review and original normal commit hooks. Their subsequent results are recorded outside the frozen checkout and in PR evidence before any release claim.


### PR478 single-file installed module follow-up

Review finding4028556680 is valid: the package-directory guard rejected a uniquely RECORD-owned, byte-identical `standalone.py` installed directly in site-packages. The scenario was specified before tests. `PR478_SINGLE_MODULE_RED.txt` records two genuine failures (root and src-layout) and four preservation controls before changing the bridge; the root case reports `coverage_directory_ownership_unavailable`, while the src case reports `ambiguous_snapshot_source`. A separate native Coverage probe confirms that its module selector measures the imported installed file without measuring an imported unrelated module, avoiding the entire site-packages directory.

The bridge separates authenticated file identity from optional package-directory measurement. It retains a native module selector only for a unique, valid top-level module and includes its exact path in origin receipts. It preserves duplicate-source and ownership rejection, RECORD hashes, source-byte equality, unchanged raw coverage and post-execution identity checks. The expanded bridge suite passes55 tests, including real imports for both source layouts and source/installed mutations after execution. Observer/collector wiring retains explicit native/configured scope and is validated separately before the follow-up is pushed. No finding resolution or current-head acceptance is claimed from these local results.

The preceding signed head38402b96 was produced through all original source and signature-only commit hooks, exact canonical CI signing35123796656 and strict seven-manifest verification. Its final full/smart suites each pass2971 tests; the final affected review reports zero findings after the full review's documented advisories. Current candidate run35124077143 has passed cold/warm fixtures and the modules repository on all three supported Python ABIs and is still running the pinned external corpus. Its results belong to38402b96, not this uncommitted follow-up.

The separate request/observer/collector wiring records11 genuine failing regressions and two passing native/configured-scope controls before implementation in `PR478_SINGLE_MODULE_WIRING_RED.txt`; the final affected three-file suite passes128 tests. Real native subprocess evidence measures the installed module and excludes an imported foreign module in reviewer-default mode, while configured and native app-only sources remain unchanged. Root/src bridge tests and post-execution mutation controls separately pass55 tests. Both focused groups pass typing and Ruff with no introduced Pylint warnings; independent review and final integrated gates follow before commit.

Independent review reproduced one native selector ambiguity before commit: Coverage interprets a module name as a same-named working-directory folder when that folder exists. `PR478_SINGLE_MODULE_COLLISION_RED.txt` retains one actual failing diagnostic assertion and five real native/configured-scope controls. Reviewer-default collection now reports `project_pytest_coverage_module_selector_ambiguous` with a native source_pkgs remedy; it does not claim this collision is supported, expand the measured directory, change native configuration, or discard passing test observations. All32 helper tests pass after the correction; the request validator was split by responsibility to remove its introduced complexity warning. The earlier fresh review finished with advisory status before these edits; its former CC13 warning is not waived. Final review and integration gates remain required.


### Current candidate controlled-corpus fixture correction

`PR478_CORPUS_CONTROL_RED.json` retains actual candidate35124077143 failure on signed head38402b96. All30 cold/warm executions across15 repository/ABI combinations finish with pytest PASS and no UNKNOWN evidence. Exactly nine controlled-defect reviews (Requests, Hatch and Flask on all three ABIs) retain the deliberate failing test and static type finding but lack coverage for the injected root file. The old fixture never imported or called its wrong-return function. Hatch and Flask also restrict measurement to native packages, while Requests' omit-only configuration does not enumerate unexecuted root files. The overall corpus and dependent quality jobs fail; these are not acceptance results.

The controlled-corpus scenario preceded the correction. `PR478_CONTROLLED_PACKAGE_RED.txt` records13 actual failures and34 passing controls: generated native pytest fails the real-function assertion under both source_pkgs and omit-only configurations; unsafe destinations and invalid/missing pinned metadata are rejected by the new contract. The generated test now imports and calls the wrong-return function at an explicit pinned package path before asserting the integer result, retaining the intended runtime and static failures. Only the identified disposable copy is modified. Native coverage filters/options and the strict no-UNKNOWN, test-failure and type-failure validators are unchanged. Exclusive writes and contained ordinary parent directories prevent overwriting existing paths or following checkout symlinks.

The final focused corpus suite passes47 tests, including actual executed function lines1/2 under both unchanged coverage configurations and unchanged original source identity. Ruff, typing and diff checks pass with no introduced Pylint warnings; new production complexity is at most9. These local generated-fixture results do not establish final pinned Linux acceptance. New RED transcript formatting removes only trailing whitespace; the original actual output remains under `/private/tmp`. All source, specifications and evidence are frozen together for independent review and final integrated verification before a new signed push.
