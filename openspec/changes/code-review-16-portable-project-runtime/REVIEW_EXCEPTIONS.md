# Review disposition for portable runtime boundaries

These exceptions apply only to the named findings and execution boundaries in #473. They do not waive runtime failures, missing evidence, security findings, or new complexity regressions. The final fresh report and test outcome are recorded in TDD_EVIDENCE.md.

| Finding and location | Disposition and supporting boundary |
| --- | --- |
| `MISSING_ICONTRACT` in `runtime_build_driver.py`, `target_bootstrap.py`, `target_launch.py`, `target_pytest.py`, and `scripts/external_capsule_corpus.py` | These entry points run before project dependencies are installed, inside the isolated interpreter startup path, or in the standalone customer harness. This includes `AnalyzerFinder.validate_loaded`, which checks analyzer origins before target site initialization, and `target_launch.execution_domain`, which validates the inherited read-only member identity before project startup. Importing icontract would add a controller dependency to the target startup path. Keep explicit input checks, fail-closed exits, and subprocess/corpus tests. Ordinary supervisor runtime APIs retain contracts. |
| `C0415` and `R0401` for runtime/runner integration in `portable_worker.py`, `portable_snapshot.py`, `runtime_interpreter.py`, `runtime_commands.py`, `_review_utils.py`, `scope.py`, `target_pytest.py`, and `runner.py` | Deferred imports avoid partially initialized runner symbols and keep worker-only code out of controller startup. Python resolves these imports at invocation after module initialization. Pytest is imported only after its selected worker environment is established. Scope discovery is deferred to avoid the VCS/scope initialization cycle. CLI, snapshot, alternate-ABI, and full-suite tests exercise these boundaries. This is not permission for unrelated deferred imports; unnecessary imports in new tests were moved to module scope. |
| `W0212` in `runtime_builder._build` | Reuse `sandbox._verified_bubblewrap_descriptor` to inherit the existing descriptor-bound launcher verification. Duplicating that security-sensitive primitive solely to avoid the private-name warning would create a second validator. The dependency remains within the review bundle. |
| `W0718` in `sitecustomize.py` | Python can ignore startup-hook failures and continue. Catching `BaseException` here deliberately terminates with exit 78, preventing unconfigured project execution. The fail-closed startup regression covers this behavior. |
| `kiss.nesting.warning` in `runner._run_local_capsule_context` | The extra lexical level is an `ExitStack` owning index/source resources. Retain deterministic cleanup across static, portable, and cached paths. No new unbounded iteration or environment-selection branch is exempted. |
| `kiss.parameter-count.warning` in `external_capsule_corpus.review` | Four execution inputs plus two named options preserve explicit descriptor attachment and distinct cold/warm/controlled evidence names. A regression proves the controlled report cannot overwrite the untouched report. The optional artifact name is intentional and is not inferred from arbitrary source filenames. |
| `ai-bloat.loc-vs-complexity` in `target_pytest.main` | This startup function assembles the explicit observation schema and native pytest invocation. The additional deselection fields retain selected-versus-deselected evidence and label controller-only visibility under xdist. They are required evidence, not redundant control flow; preserve the explicit schema and actual native selection regressions. |
| `banned-generic-public-names` for test names containing `metadata` | The generic-name pattern matches the substring `data` inside the precise term `metadata`. Preserve descriptive regression names; these are pytest tests, not customer APIs named `data` or `process`. |
| `dry.duplicate-function-shape` in distinct source/configuration and environment-isolation regressions | The shape detector ignores the differing configuration literals, environment variables, and expected outcomes. Separate cases preserve failure attribution for environment leaks, path escapes, manager ambiguity, extras, and permission classes. Shared setup is already factored where it conveys the same contract. |

## Nested launcher informational dispositions

- `ai-bloat.loc-vs-complexity` in `target_launch.target_command`: retain the explicit bubblewrap argument vector. Mount order, descriptor paths and environment-clearing order define the isolation boundary; a compact generated mapping would make that boundary harder to audit. The function adds a single read-only domain bind and does not introduce duplicate resolution logic. First-entry isolation and all five nested-domain subprocess cases pass.
- `ai-bloat.loc-vs-complexity` in `test_target_launch._native_nested_wrapper`: retain the ordered creation of private descriptor, installed member/project modules, relocated copies of actual trusted helpers, domain identity and executable wrapper. The ten real child tests rely on the complete startup chain. Splitting this one fixture into single-use factories would obscure which production bytes are executed without reducing behavior or duplication.

## Existing review surface

The branch-wide report also analyzes pre-existing large modules and tests in `runner.py`, `scope.py`, and `commands.py`. Existing module-length, selector-complexity, fixture-parameter, collection-internals, and duplicate-test-shape findings are outside this runtime change when their implementation is unchanged from dev `976460c0541b685f5fae256faef4cec32fe9ba7e`. This exception requires comparing the actual diff; it does not apply merely because a file existed before the change. New portable implementation is split into dedicated discovery, builder, inventory, source, native, domain, and worker modules.

Parent-process coverage does not measure isolated Linux builder/startup subprocesses. If a targeted local review reports reduced coverage for these boundaries, the relevant proof is the focused failure-path tests plus the non-root Linux corpus. Do not describe parent coverage as complete subprocess coverage or substitute a successful bootstrap for completed analysis.

## Local coverage boundary in the final branch review

The local branch review runs on macOS and does not merge coverage from the independent Linux customer CLI or isolated child interpreters. Its measured percentages are retained, not relabelled as meeting the 80% threshold: `portable_worker.py` 71.4%, `runtime_artifacts.py` 78.7%, `runtime_build_driver.py` 51.4%, `runtime_builder.py` 52.9%, `runtime_commands.py` 50.0%, `runtime_interpreter.py` 69.2%, `sitecustomize.py` 0.0%, `target_bootstrap.py` 67.8%, and `target_pytest.py` 75.0%. The older `_review_utils.py` and `sandbox.py` also remain below the local threshold.

For this Linux-only capsule change, accept these local coverage warnings conditional on the full Linux candidate and public-release corpus gates: those execute real CLI preparation, native-manager installation, descriptor loading/attachment, worker bootstrap and test observation that local mocks cannot establish. Focused unit tests retain independent checks for malformed/corrupt descriptors, source escapes, stale observations, incompatible interpreters and missing native libraries. This does not establish exhaustive failure-branch coverage; any actual incomplete analyzer result still blocks acceptance. The final release must not rely on this exception if the Linux corpus is incomplete.

## Controller pipe capture follow-up

- `TEST_OUTCOME_NOT_PASS` in the local explicit-file review is caused by the single `test_builder_proc_reopen_cannot_access_regular_log_inode` Linux `/proc` test being skipped on macOS. Keep that actual incomplete platform evidence visible. This is a local validation limitation only: the Linux CI full suite and new candidate corpus must pass before resolution or integration. Do not waive a failed, skipped or missing Linux run or call the local review error-free.
- `ai-bloat.loc-vs-complexity` in `runtime_builder.builder_command`: keep the explicit ordered namespace, mount and descriptor argv, as for `target_launch.target_command` above. No change to its command semantics was made in the pipe-capture follow-up.
- `ai-bloat.loc-vs-complexity` in `test_runtime_builder_logging._builder`: this fixture explicitly assembles source, the capsule handle, a verified-launcher substitution, a real child command and capture tracking. Preserve their visible sequence for the real subprocess security regressions; extracting single-use setup functions would obscure the exact trusted/untrusted boundary without removing behavior.


## Portable pytest release correction

The deferred coverage-policy import in `portable_worker.run_portable_pytest` reuses the supervisor's established coverage evaluator only when executing analysis. Test selection and runtime discovery can import `portable_worker` without initializing the large runner and its analyzer integrations. Keep this scoped deferred import rather than duplicate coverage policy or make discovery import the analyzer stack. `Observer.pytest_sessionfinish` is covered by the existing target-worker contract exception: importing icontract into the customer interpreter would add supervisor dependencies; native subprocess tests prove its evidence fields and precedence.

## Native pytest usage-error proof

`ai-bloat.loc-vs-complexity` in `test_native_usage_error_retains_capsule_member_and_target_execution`: retain the explicit native subprocess, captured observation and capsule-envelope replay in one integration regression. Most length is the isolated observer script and boundary-specific assertions; these establish that a real usage error preserves member identity and the exact target evidence. Extracting single-use setup would hide the sequence without removing behavior. Six failing-before cases and the combined 120-case passing run cover this correction.


## Installed coverage attribution integration

- `W0212` at `target_pytest._native_coverage_relocations`: the worker calls the installed Coverage object's `_make_aliases` to use exactly the same path-mapping semantics as pytest-cov's native combine step. This is a deliberately bounded native API dependency, not a reimplementation or coverage-policy override. `test_native_coverage_alias_cannot_credit_transformed_source` exercises the actual native rewrite; identical and unrelated alias cases pass. Missing/incompatible method interfaces produce a named origin diagnostic while preserving test records and pytest root. This exception does not permit inferred aliases or treating unavailable origin evidence as proof.
- The existing `C0415`/`R0401` runner-cycle disposition applies to the same deferred `portable_worker.run_portable_pytest -> runner` edge. Pylint may attach `R0401` to the new `test_installed_coverage.py` batch-analysis anchor; that test imports the established modules at module scope and introduces no new production import edge. The bridge evaluator is passed into its helper, avoiding an additional deferred import.
- The existing macOS-only `TEST_OUTCOME_NOT_PASS` disposition remains tied exclusively to `test_builder_proc_reopen_cannot_access_regular_log_inode`. An explicit-file run may attach the aggregate outcome to the first selected file, now `installed_coverage.py`. Keep that genuine Linux-only skip visible; this does not waive any installed-coverage regression, native Linux test, or external capsule acceptance.

## Bounded Semgrep diagnostic follow-up baseline

The fresh follow-up review also scans unchanged Semgrep adapter/test bodies. AST comparison against signed local baseline `efe7303efb32ac31379906a38e3f960ec4e7a744` confirms the following flagged functions are unchanged; only the bounded formatter helpers, its completion-error branch and three new diagnostic test functions changed. Retain these narrowly scoped existing findings rather than broaden this runtime-failure fix into unrelated adapter API/test refactoring:

- `semgrep_runner.py`: existing `R0916` in `reconcile_passes`; existing `MISSING_ICONTRACT` on `reconcile_scanned_paths`, `validate_rule_pack`, `reconcile_passes`, and `build_snapshot_invocation`. No new public entry point is introduced by this formatter change.
- `test_semgrep_runner.py`: existing fixture `_stub_semgrep_on_path` is requested by pytest's `usefixtures`, so the AST private-call detector does not see its use; existing duplicate empty-input test shapes and `C1803` empty-list assertions remain distinct pre-existing cases; existing local scope/runner imports and repeated local `shutil` imports produce `C0415`, `W0621`, and `W0404`. These bodies are unchanged from that baseline.

New diagnostic line-length/import warnings and new activation-discovery complexity were corrected. Existing deferred imports in `portable_snapshot.py` retain the earlier runner-cycle disposition. This exception does not waive nonempty Semgrep errors, malformed observations, missing runtime attachment or incomplete native acceptance.

## Combined PR478 native-tools and Node review (2026-09-16)

The fresh combined developer review `/private/tmp/specfact-478-combined-final-review.json` reports PASS_WITH_ADVISORY, 215 warnings and55 informational findings, with zero errors. Introduced test complexity, fixture shadowing/unused-private-name, empty-map assertions, generic naming and duplicate wrapper-shape findings were corrected; no native-tools production helper findings remain. Independent review compared production findings with signed HEAD efe7303efb32ac31379906a38e3f960ec4e7a744:96 of104 refer to AST-identical enclosing functions. The remaining production findings are the already-scoped deferred import, runner module length, Coverage private native API and target startup contracts above, plus the measured coverage warnings below. This is not a blanket exemption for a changed file.

The targeted parent-process review measured runtime_builder.py67.1% and target_pytest.py71.8%, below80%; its deleted temporary data alone could not establish new-branch execution. The subsequently retained full-suite JSON (`/private/tmp/specfact-478-combined-full-coverage.json`) measures runtime_builder.py92.4444% with every introduced executable line covered, and target_pytest.py79.8851%, still below the individual80% threshold. Thirteen additional boundary cases cover the installed-coverage candidate validator, including malformed inputs and a real symlink escape. Parent coverage still does not observe native alias mapping line158 or startup candidate assignment line198; native child controls remain separate evidence, with no invented parent percentage credit. The unchanged aggregate80% gate passes at86.9674% for these two measured modules. This is not whole-repository coverage or completed native acceptance. Native unchanged hooks and the signed external corpus remain mandatory.

Retain the informational length suggestion for test_runtime_builder._assert_policy_change_invalidates_reuse: the shared fixture visibly arranges the real helper bytes and cache identity, verifies initial miss then warm reuse, modifies exactly one policy file and verifies a renewed miss. Two parametrized policies reuse this sequence without duplicating assertions. The unchanged banned-generic-public-names finding on test_builder_provides_vcs_metadata_tool_without_host_configuration at test_runtime_builder.py86 is outside the new tests; it is not waived for new names.

## Native follow-up fixture review (2026-09-16)

The new informational `ai-bloat.loc-vs-complexity` finding on `test_runtime_vcs.attached_vcs_git_fixture` is retained narrowly: its real executable captures sanitized environment values before forwarding to actual Git, and its descriptor, builtin inode/context and empty system search path establish independently asserted trust boundaries. Keeping that explicit setup makes the provenance control auditable. Existing `_builder` and `_assert_policy_change_invalidates_reuse` readability dispositions above still apply after their explicit synthetic public-trust mocks. The new nested scenario setup warnings were corrected with early-return fixture helpers without dropping scenarios or assertions.

The first new native-follow-up parent review measured runtime_builder.py at69.3%; that selected-test measurement remains a warning, not a claim about full-suite coverage or unchanged lines. The final selected-test review measures target_pytest.py at75.9% as well. Earlier full measurements precede the public-trust/VCS follow-up; updated full coverage and actual native acceptance remain required.

The completed follow-up full suite replaces that uncertainty with retained line-level data: scoped aggregate87.0646766%, runtime_builder92.5438596%, target_pytest79.8850575%. All introduced builder execution lines are covered. Target pytest's new lines158/198 execute in native subprocess controls but are absent from parent coverage; its individual result remains below80% and is not rounded up. Full and smart each pass2867 tests, with the genuine Linux/proc skip still visible on macOS. Neither this qualified coverage disposition nor local test success substitutes for Linux capsule acceptance.

### Target coverage lifecycle boundary (2026-09-16)

`run/target_coverage.py` defers Coverage and pytest-cov imports until after the verified pytest-observe target bootstrap. Its fixed module is also loaded during child startup and must remain importable with only the standard library; moving these imports to module scope would violate the tested startup boundary. This is the same narrow C0415/target-entry contract rationale already applied to target_pytest, not an allowance to import customer plugins in the supervisor. Public hook/configuration functions cannot depend on supervisor icontract in the isolated target dependency graph. Real subprocess tests exercise their input/evidence contracts instead.

The helper's `_should_report` override and one native `_should_report` observation use the explicitly version-checked pytest-cov7.0.0/7.1.0 lifecycle contract. The override applies only to a separate reviewer collector; native plugin methods/options are never replaced. The single protected-access warning is retained under this bounded compatibility rationale, with real native/reviewer xdist, subprocess, report-preservation and incomplete-worker tests. Unknown versions receive an actionable incomplete diagnostic. No general warning, threshold or test-outcome exemption follows.

The newly included `target_bootstrap.py` parent coverage is measured, not labelled unchanged or sufficient: full four-module tracing initially records39.2157%; direct real-file loader tests then measure53.3333% for the whole legacy file and27/28 statements (96.4286%) in the introduced loader. Real first-entry/sitecustomize/nested-exec controls execute copied bootstrap code under isolated interpreter paths, which parent-file coverage does not count. The call site is exercised by those real startup controls. This narrowly qualified boundary evidence supports the developer warning disposition, not a waiver of native customer coverage findings or final Linux acceptance. The new target_coverage helper itself measures97.0414% with real parent-process Coverage boundary tests; observer84.0426% and builder92.5439% meet80 in the retained full scoped measurement.
